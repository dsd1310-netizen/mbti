# 🔮 나풀이 — 사주 × MBTI 복합 해석 앱 개발 및 결과 보고서

> **작성일**: 2026년 7월 23일
> **최종 업데이트**: 2026년 8월 7일 — **📋 계획안 전체 정합성 재검토(신규 코드 작성 없음).** 실제 코드(`api/tsconfig.json`/`escapeHtml`/`callGeminiJsonApi`/`calcSipsin`/`baseKeyId`/`DEEP_MODELS`/채팅·닮은인물·PDF섹션선택 등)를 소스와 직접 대조한 결과 **문서 서술과 코드 간 실질적 불일치는 없음**을 확인. 다만 문서 자체의 오기 3건 발견·수정: ① 7-AC/7-AD가 "아직 배포 전"이라 기록했으나, `git status`로 커밋 `b51e69a`가 이미 `origin/main`에 push돼 있고 로컬 `npm run build` 산출물이 프로덕션 번들(`index-Ds3ykOW3.js`, 콘텐츠 해시 완전 일치)과 동일함을 직접 대조해 **실제로는 이미 배포 완료 상태였음**을 확정(7-AC-4/7-AD-5에 정정 반영). ② 7-AC 섹션 헤더("아직 배포 전")와 7-AC-4 본문("배포 완료")이 서로 모순되던 것도 정정. ③ 이 문서 최상단이 "남은 미해결은 절기 데이터뿐"이라 단언했지만, 실제로는 그 직후 세션(7-AD, 아래 8월 6일 항목의 마지막 줄)에서 발견·수정된 🔴 "심화해석 실패가 성공으로 영구 캐시되는 버그" 등 5건이 이 요약에 반영되지 않고 있었음 — 아래에 그대로 두되 이번 줄에서 명시적으로 짚어둠. **코드 레벨 신규 발견(문서에 없던 것)**: (1) localStorage 캐시 키가 19개로 늘었는데 만료/정리 로직이 전혀 없어 여러 인물·시각을 시도한 사용자는 저장공간이 무한정 누적될 수 있음 — **같은 세션에서 `setCachedItem()` 헬퍼로 즉시 수정 완료**(8번 표 참고, `tsc`/`build` 클린), (2) `src/App.tsx`(4,410줄)·`geminiApi.ts`(1,425줄)가 계속 비대해지고 있던 것, (3) `mbti-delta-red.vercel.app` 하드코딩이 정확히 3곳(`App.tsx:1445,1612`, `geminiApi.ts:40`) 존재하던 것. **(2)·(3)과 함께 "오늘" 탭 콘텐츠 구조 차별화(8-2번 잔여 과제)·개별 결과 이미지 공유 기능까지 총 4건을 이어서 구현 완료 — 상세: 섹션 7-AE.** `geminiApi.ts`는 5개 모듈로, `App.tsx`는 타입/헬퍼/컴포넌트 분리로 4,309줄까지 축소(단, App.tsx의 렌더 트리 자체를 하위 컴포넌트로 쪼개는 더 큰 리팩터링은 시각적 검증 수단 없이는 위험해 범위 밖으로 남김). 이 세션은 Playwright를 쓰지 않아 실제 화면 동작 검증은 다음 세션 과제로 남음(7-AE-5 참고). (아래는 8월 6일까지의 기존 기록) **✅ (7차) 신규 기능 3종 구현 완료(AI 후속질문/나와 닮은 인물/PDF 섹션 선택) — 상세: 섹션 7-AC. `AskUserQuestion`으로 배치·지속성·인물 범위·PDF 체크 단위 등을 기능별로 여러 차례 확정한 뒤 구현, Playwright 4개 시나리오 PASS. 사용자 확인 후 배포 완료.** (6차) 로딩 속도 개선: 실측 결과 Gemini "사고형" 모델이 짧은 프롬프트에도 답변보다 훨씬 많은 사고 토큰을 써서 15.8초가 걸리던 것을 `thinkingConfig.thinkingBudget: 512`로 3.6초까지 단축(배포 완료). 이어서 "심화해석"은 개선폭이 작았던 문제를 추가로 파고들어, 긴 프롬프트에서 `gemini-3.5-flash-lite`가 품질 저하 없이 5.9초 만에 끝나는 것을 확인해 심화해석 6종 전용 `DEEP_MODELS` 순서를 신설(짧은 콘텐츠의 기본 모델 순서는 그대로 유지, 배포 완료) — 상세: 섹션 7-AB. 같은 날 이어서 "나머지도 진행" 요청으로 8-1(궁합 이력·알림 시간·재시도 버튼)·8-2(타로 카드 비주얼·연속 방문 스트릭) 전부 구현 완료(섹션 7-AA, Playwright 4개 시나리오 PASS). 이제 계획안에 남은 미해결은 절기 천문 데이터 확보(1930년대/2031년 이후, 데이터 소스 없이는 진행 불가)뿐.** 그 앞서 **2026-07-23부터 이어져 온 최우선 🔴 버그("해석" 버튼 등 AI 기능 실패)도 이 날 완전히 해결됨.** 진짜 원인은 Vercel이 `api/gemini.ts`를 빌드할 때 루트 `tsconfig.json`(Vite용, `module: ESNext`)을 잘못 물려받아 ESM/CJS가 꼬여 핸들러 실행 전 모듈 로드 단계에서 매번 크래시(`FUNCTION_INVOCATION_FAILED`)하던 것 — `api/tsconfig.json`(module: commonjs) 추가로 해결, 배포 후 프로덕션 엔드포인트에 실제 앱과 동일한 요청을 `curl`로 보내 HTTP 200 정상 응답까지 직접 확인함. 상세: 섹션 7-Z. 남은 건 사용자가 실제 브라우저에서 강력 새로고침 후 최종 체감 확인하는 것뿐. (아래는 이 세션 안에서 있었던 조사 순서 그대로의 상세 기록 — 7-W-5 마무리 검증 완료(타로 헤더 키워드 칩 위치 Playwright 확인 + 커밋) + 전면 재검토(api/, utils/, data/, App.tsx 전체와 실제 Gemini API 호출 테스트)로 신규 버그 4건 발견·수정: (1) `api/gemini.ts` 자체 에러 응답을 클라이언트가 못 읽던 버그, (2) "재시도 불가" 오류가 실제로는 12회 재시도되던 구조적 버그, (3) `keyword` 없는 구버전 캐시 시 나풀이/트랜짓 칩 노출 버그(문서상 "undefined" 증상 기재가 부정확했음도 함께 정정), (4) model 화이트리스트 검증 순서. 상세: 섹션 7-X. "해석" 버튼 API 오류(최우선 미해결)의 확정 원인은 아니지만 관련 가능성 있는 두 버그를 고쳤으니 다음 재현 시 에러 메시지 확인부터 할 것. **이어서 같은 날(2026-08-06) "나머지도 작업해줘" 요청에 따라 섹션 8에 남아있던 항목 중 코드로 해결 가능한 것 전부 처리**: 네이티브 앱 AI 실패 가능성(API_BASE 분기 + Origin 허용리스트, 실기기 검증은 미완) 수정, X-Forwarded-For 우회 우려는 Vercel 공식 문서로 근거 없음을 확인해 문서만 정정, 궁합 상대방 폼·오늘의 트랜짓에 근사치 경고 문구 추가, GEMINI_API_KEY 죽은 코드 주석 보강 — 상세: 섹션 7-Y. 남은 미해결은 절기 천문 데이터 확보가 선행 조건인 2건(1930년대/2031년 이후)과 최우선 미확정 버그뿐. 이전 업데이트(2026년 8월 5일, 앱스토어 출시 준비 완료 — 계정삭제·개인정보처리방침·재미고지·Gemini 키 서버 프록시(+오남용 방지 보강)·Capacitor+아이콘/스플래시, 신규 코드 정밀 재검토로 버그 5건 수정, "오늘의 나풀이/타로/트랜짓"을 새 "오늘" 탭으로 통합, 6개 평면 탭을 [오늘/사주(+서브탭 4개)/별자리] 2단 구조로 재정리, 타로를 메이저 22장에서 78장 풀덱으로 확장, Firebase Admin SDK 기반 IP별 하루 250회 요청 제한 + 개발자 우회 키(모바일 URL 방식 포함) 추가, 네이티브 앱 전용 매일 알림(오전 9시 로컬 알림) 추가(웹 푸시는 7-S-4에 향후 계획만 기록), 궁합 탭에 실제 두 사람 생년월일 기반 정밀 궁합(일지 관계·일간 오행 상생상극·오행 보완) 추가, 최초 실행 시에만 노출되는 온보딩 화면 추가, 결과 화면 3회 조회 시 네이티브 앱스토어 인앱 리뷰 유도 추가. 섹션 7-O~7-V 참고. **2026-08-05 전체 재검토로 🔴 이슈 1건(네이티브 앱에서 AI 기능 전체 실패 가능성) + 🟡 1건 + 🟢 3건 신규 발견, 전부 미해결 — 섹션 8 표 참고. 이후 사용자가 "해석" 버튼 클릭 시 API 오류로 결과가 안 나오는 현상을 실제로 재현·보고함(원인 미확인, 섹션 8 표 최상단) — 다음 세션 최우선 작업은 이 실사용 버그의 재현·원인 파악. 추가로 "오늘" 탭 콘텐츠가 빈약/지루하다는 피드백을 받아, 그중 콘텐츠 깊이/차별화(입력 데이터 확장 + 톤 분리 + 키워드 칩)는 섹션 7-W에서 구현 완료. 비주얼(타로 카드 이미지 등)·습관화(스트릭 등)는 섹션 8-2에 남은 과제로 대기 중. 7-W 배포 직후 한정으로 구버전 캐시에 keyword 필드가 없어 "undefined" 칩이 뜰 수 있는 결함 1건 추가 발견(자연 해소되지만 미해결 — 섹션 8 표 참고). **2026-08-05 세션은 여기서 종료 — 다음 세션 최우선 순위는 여전히 "해석" 버튼 API 오류(원인 미확인) 재현·파악.** 그 외 남은 건 Xcode/Android Studio 빌드·개발자 계정 가입 등 사용자 본인이 해야 하는 부분뿐 — 7-O-4 참고))
> **참고 파일**: `mbti.pdf`, `동추원만세력.pdf`, `sa_ju_mbti_interactive_app.tsx`, `고전점성술_기초시트.pdf`, `william-lilly-christian-astrology-i-ii-iii-932p.pdf`, `1.-타로-카드-의미-요약본.pdf`, `tarot-78-cheatsheet-v1.pdf`, `tarot-major-arcana-v1.pdf`, `tarot-minor-arcana-v1.pdf`

> [!WARNING]
> **이 문서의 이전 버전은 실제 코드 상태와 맞지 않는 "✅ 완료" 표시가 다수 포함되어 있었습니다.**
> 2026-07-31 세션에서 소스 코드를 직접 대조 검증하여 전면 재작성했습니다. 아래 내용은 모두 실제 코드를 읽고 확인한 사실만 기재합니다.

---

## 1. 프로젝트 개요

생년월일시와 MBTI를 입력하면, **정밀 만세력 기반 사주 계산** + **Google Gemini AI 실시간 해석**을 결합하여 나만의 사주 × MBTI 분석 결과를 제공하는 웹 애플리케이션 (React + Vite, Vercel 배포).

---

## 2. 기술 스택 (실제 상태 기준)

| 항목 | 사용 기술 / 설정 | 실제 상태 |
|------|-----------------|-----------|
| 프레임워크 | React 18 + Vite 5 | ✅ |
| 언어 | TypeScript (`tsc --noEmit` 통과) | ✅ |
| 스타일 | Vanilla CSS (글라스모피즘, 우주/별자리 다크 테마) | ✅ |
| AI 해석 엔진 | Google Gemini API (`gemini-3.6-flash` → `gemini-3.5-flash-lite` → `gemini-2.5-flash` → `gemini-2.5-pro` 4단계 릴레이, 모델당 최대 3회 재시도, 20초 타임아웃) | ✅ |
| API 키 관리 | `.env`의 `VITE_GEMINI_API_KEY` 빌드타임 내장. **사용자 입력 UI 없음** (2026-07-31 제거) | ✅ |
| PDF 출력 | `window.open()` + 브라우저 인쇄 다이얼로그, 흰 배경 인쇄용 스타일 (2026-07-31 재검토 후 원래 방식으로 되돌림 — 8-A 섹션 참고) | ✅ |
| 데이터베이스 | 1900~2100년 24절기(節氣) 정밀 시각 데이터 내장 (`solarTerms.ts`, astronomy-engine 계산 기반 201개년, 2026-08-18 전면 재생성 — 섹션 7-AV 참고) | ✅ |
| 배포 | Vercel (`https://mbti-delta-red.vercel.app/`) + 카카오톡 공유 연동 | ✅ |
| Electron 데스크톱 패키징 | ✅ **제거 완료** (2026-07-31) | 아래 참고 |
| PDF 라이브러리 | 없음 (외부 라이브러리 미사용, `window.print()` 그대로) — `html2canvas`/`jsPDF`는 시도 후 되돌리며 제거함 | ✅ |

> [!NOTE]
> **Electron 패키징을 완전히 제거했습니다.** `electron-main.js`는 git 전체 이력을 뒤져도 한 번도 존재한 적이 없었고(즉 "고장난 것"이 아니라 처음부터 미완성 설정), `electron` 패키지 자체도 설치 스크립트가 차단되어 정상 설치된 적이 없었습니다. `package.json`에서 `main`/`build`/`dist` 스크립트와 `electron`, `electron-builder`, `electron-packager`, `concurrently`, `wait-on` devDependency를 모두 삭제 (`npm install` 결과 400개 패키지 제거). 이 앱은 웹(Vercel)/카카오톡 공유 중심으로 계속 운영합니다. 나중에 데스크톱 버전이 필요해지면 그때 새로 설계하는 편이 낫다고 판단했습니다.

---

## 3. 기능 구현 현황 (2026-08-01 실측 기준)

| 기능 | 상태 | 비고 |
|------|------|------|
| 정밀 절기 만세력 계산 (연/월/일/시주) | ✅ | `sajuCalculator.ts`, 절기 데이터 1900~2100 + 1954~1961년 UTC+8:30 표준시 보정 (섹션 7-AV) |
| Gemini AI 심층 해석 (성격/커리어/연애/재물/처방전) | ✅ **08-01 로딩 구조 변경** | 로딩 화면엔 `generateSajuIntro()`(제목+융심리+쉬운풀이)만 자동 생성. 성격/커리어/연애/재물 4개 카테고리 + 처방전은 각 탭 진입 시 버튼으로 개별 생성 (`generateCategoryInterpretation()`, `generatePrescriptions()`). 모델 폴백 + 재시도 + 타임아웃은 기존과 동일 |
| **개인화 질문·답변 기반 해석** | ✅ **08-01 신규 구현** | 커리어·연애·재물 3개 카테고리에 한해, 생성 버튼을 누르기 전 카테고리당 2문항(선택 사항)에 답하면 그 답변을 AI가 직접 언급하며 해석에 반영. `src/data/categoryQuestions.ts` (섹션 7-D 참고) |
| **공유용 이미지 카드 저장** | ✅ **08-01 신규 구현, 08-01 장식 업그레이드** | 인스타 스토리용 세로(9:16) PNG 카드를 Canvas API로 생성해 다운로드/공유. 이름·MBTI, 일주 한자, 팩폭 한줄평 포함 + 별자리 장식·MBTI 이모지 배지 (섹션 7-E, 7-G-6 참고) |
| 오행 과부족 차트 + 강함/부족 배지 + 맞춤 해설 | ✅ | `src/data/elementTypes.ts` — 오행별 일반 성향 + 사용자 분포 기반 강함/부족 맞춤 해설 |
| **오행 종합 AI 해설** | ✅ **08-01 신규 구현** | `generateElementSummaryInterpretation()` — 오행 5개 수치 전체를 종합 해설하는 버튼 생성형 텍스트 (섹션 7-B-1) |
| 결과 화면 상단 탭 네비게이션 (운세/MBTI카드/AI해석/궁합/풍수) | ✅ | 프로필+사주원국은 상단 고정, 나머지는 탭으로 전환 |
| 사주 4기둥 클릭 시 상세 모달 | ✅ **08-01 전체 확장** | 연/월/일/시주 **4기둥 모두 클릭 가능**. 시주는 정적 설명 즉시 표시+AI 버튼, 연/월/일주는 AI 버튼만 표시(`generatePillarInterpretation()`, 섹션 7-C-2) |
| 북마크 저장/삭제 (localStorage) | ✅ | |
| **PDF 저장** | ✅ | 흰 배경 인쇄용 HTML(`window.print()`) 방식. 저장 시점에 미생성 AI 항목(카테고리 4개, 처방전, 오행/궁합 종합 해설, 풍수, 대운/세운, 기둥별 해설 4개)을 전부 자동 생성 후 포함 (섹션 6-A, 7-C-3) |
| 카카오톡 공유 | ✅ **08-01 카드 문구 개선** | 팩폭 멘트 중심 카드(`🔮 {name}님의 {mbti} 사주 팩폭 결과` + personality factBomb) 자동 생성 후 공유. SDK 대기 로직에 폴링 백업 추가로 무한 대기 위험 해소 (섹션 7-A) |
| **MBTI 유형카드** | ✅ **08-02 위치 변경** | `src/data/mbtiTypes.ts` — 16유형 정적 데이터 + 일간 오행 시너지 문구. 독립 탭에서 "나풀이 해석 › 성격" 서브탭 상단으로 통합 (섹션 7-H-1) |
| **대운/세운 흐름표** | ✅ | 가로 스크롤 카드 UI + 현재 만 나이/연도 하이라이트 |
| **대운/세운 AI 운세 해설** | ✅ | `generateFortuneInterpretation()` — 현재 대운+최근 3개년 세운을 쉬운 비유로 풀어주는 버튼 생성형 해설, 연도 기준 캐싱 |
| **궁합 조합표** | ✅ | `src/data/compatibility.ts` — 일지 기준 삼합·육합·충·형·파·해 |
| **궁합 종합 AI 해설** | ✅ **08-01 신규 구현, 08-02 예시 강화** | `generateCompatibilitySummaryInterpretation()` — 삼합/육합/충/형/파/해 결과 전체를 비전공자 눈높이로 종합 해설. 육합/충은 구체적 예시 장면 포함하도록 강화 (섹션 7-B-2, 7-H-2) |
| **풍수 수리 가이드** | ✅ | `generateFengShuiInterpretation()` — 버튼 + localStorage 캐싱 |
| API 키 사용자 입력 UI | ✅ 제거됨 | 내장 키(`VITE_GEMINI_API_KEY`)만 사용 |
| 잘못된 날짜 입력 시 크래시 방지 | ✅ **08-01 신규 수정** | 로딩 `useEffect`의 `calculateSaju()` 호출을 try/catch로 감싸 예외 시 입력 화면으로 복귀 (섹션 8 "해결됨") |
| **태어난 시간 모름 대응** | ✅ **08-01 신규 구현** | 입력폼 체크박스로 시주를 계산에서 제외, 연/월/일주 3기둥만으로 재계산·재해석. 결과화면·PDF도 3칸만 표시 (섹션 7-G-1) |
| **오늘의 나풀이 (데일리 운세)** | ✅ **08-01 신규 구현** | `generateDailyFortune()` — 일주와 오늘 일진의 관계를 바탕으로 한 짧은 데일리 운세, 날짜 기준 캐싱 (섹션 7-G-3) |
| **나풀이 다이어리** | ✅ **08-01 보관함 확장** | 저장 문구 상세보기(2줄 잘림 해결) + snapshot 기반 "전체 결과 다시 보기" 추가 (섹션 7-G-4) |
| **내 기록 내보내기/불러오기** | ✅ **08-01 신규 구현** | localStorage 데이터를 JSON으로 내보내기/불러오기 — 로그인 없이 기기 간 수동 이전 (섹션 7-G-5) |
| 시·분(HH:mm) 정밀 출생시각 입력 | ❌ 미구현 | 12시진(2시간 단위) 선택만 가능 |
| 날짜 유효성 검증 | ⚠️ 부분 구현 | HTML5 `min`/`max`만 적용, 2월 30일 등 논리적 검증 없음 (단, 크래시 자체는 위에서 방지됨) |
| 자시(子時) 야자시/조자시 정책 | ⚠️ 부분 구현 | 23시대는 다음날로 보정하나, 자시를 2구간(23~24시/0~1시)으로 나눠 입력받지 못함 |
| Electron 데스크톱 빌드 | ✅ 제거 완료 | 처음부터 미완성 상태였던 설정을 정리 |
| 모바일/데스크톱 반응형 | ✅ | `App.css` 내 480px/500px/520px/600px/700px 기준 media query |

---

## 4. 2026-07-31 세션 작업 내역

### 4-1. 카카오톡 공유 버그 수정 (근본 원인 특정)

- **증상**: 4019(도메인 미등록) 에러는 사라졌지만, 공유 버튼을 눌러도 아무 반응이 없음.
- **원인**: `index.html`의 카카오 SDK `<script>` 태그에 걸려있던 `integrity`(SRI 해시) 값이 **실제 CDN 파일과 일치하지 않음.** 브라우저가 무결성 검증에 실패한 스크립트를 조용히 차단(콘솔 에러만 발생, 화면상 아무 표시 없음)하여 `window.Kakao`가 로드되지 않았고, `App.tsx`의 동적 로드 폴백 로직도 이미 지나간 `error` 이벤트를 뒤늦게 구독하는 구조라 복구되지 않았음.
  - 실제 파일 해시(`curl` + `openssl dgst -sha384`로 직접 검증): `sha384-TiCUE00h649CAMonG018J2ujOgDKW/kVWlChEuu4jK2vxfAAD0eZxzCKakxg55G4`
  - 기존 기재값: `sha384-ih7gWZI1AXptE6CX17nyJY9Qs85pJFHGk84spMXxeFn9pxCo5VZ4DfUQDn61urFI` (불일치)
  - `git log`로 확인 결과 이 값은 커밋 `a8b4ea6`(7/29, 최초 도입 시점)부터 계속 틀려있었음.
- **조치**: `integrity`/`crossorigin` 속성 제거 (카카오 CDN은 공식 SRI 해시를 제공/보증하지 않음).
- **추가 수정**: 공유 카드 `imageUrl`이 이 앱과 무관한 위키피디아 개미 사진(`Camponotus_flavomarginatus_ant.jpg`) 플레이스홀더로 남아있던 것을 앱 자체 히어로 이미지(`src/assets/hero.png`)로 교체.

### 4-2. 신규 기능 4종 구현 (계획안에는 "완료"로 적혀있었으나 실제로는 미구현이었던 항목)

`App.tsx`를 실측 검토한 결과, 대운/세운·풍수 가이드는 **계산 로직/프롬프트 함수만 존재하고 어디에서도 호출·렌더링되지 않았으며**, MBTI 유형카드·궁합 조합표는 **아예 코드가 없었음**. 아래와 같이 신규 구현:

- `src/data/mbtiTypes.ts` (신규) — MBTI 16유형별 닉네임/키워드/특성 정적 데이터
- `src/data/compatibility.ts` (신규) — 일지 기준 삼합·육합·충·형·파·해 룩업 테이블 및 조회 함수
- `App.tsx`에 4개 결과 화면 섹션 추가: MBTI 유형카드, 대운/세운 가로 스크롤 표(현재 만 나이/연도 하이라이트), 궁합 조합표, 풍수 수리 가이드(AI 생성 버튼 + localStorage 캐싱)
- PDF(인쇄) 출력물에도 풍수 가이드 텍스트 포함되도록 연동
- `App.css`에 신규 섹션용 스타일 클래스 추가 (`.mbti-card`, `.daeun-scroll`, `.daeun-card`, `.compat-grid`, `.compat-block`, `.compat-tag` 등)

### 4-3. API 키 입력 UI 제거

- 무료 키라도 화면에 노출되고 입력이 번거롭다는 피드백에 따라, 입력 화면의 "🔑 Google Gemini API 키" 입력란/표시 토글 버튼을 완전히 삭제.
- `apiKey`/`showApiKey` 상태와 `localStorage`의 `saju_gemini_key` 저장 로직도 함께 제거.
- 내장 키(`import.meta.env.VITE_GEMINI_API_KEY`)만 사용하는 모듈 상수 `GEMINI_API_KEY`로 단순화.
- 결과 화면의 "AI 키 없음" 안내 문구도 사용자가 더 이상 키를 입력할 수 없는 상황에 맞게 재작성 (재시도 안내로 변경).

### 4-4. 자체 코드 리뷰로 발견/수정한 버그 3건

1. **궁합 조합표 자형(自刑) 버그**: 일지가 진(辰)/오(午)/유(酉)/해(亥)인 경우 "형(刑)" 항목이 빈 칸으로 나오는 로직 오류 발견. 자형 그룹(자기 자신과 형)은 그룹 내 "자기 자신을 제외한 나머지"를 필터링하면 빈 배열이 되는데, 이 케이스를 별도 처리하지 않았던 것이 원인. `src/data/compatibility.ts`의 `getBranchRelations()` 수정.
2. **MBTI 카드 문구 비문(非文)**: "…의 결과 ENTP 특유의 성향이…" 처럼 조사가 잘못 붙어 어색했던 문장을 "…의 기질이 더해져 ENTP 특유의 성향이…"로 수정.
3. **대운/세운 하이라이트 나이 오차**: 현재 나이를 세는나이(`올해연도 - 출생연도 + 1`)로 계산해 생일 전인 사람은 실제 만 나이와 최대 1살 어긋날 수 있었음. 출생월/일까지 반영한 정확한 만 나이 계산으로 교체.
4. `import.meta.env` 타입 선언이 없어 `(import.meta as any).env`로 임시 캐스팅되어 있던 부분을 `src/vite-env.d.ts`(신규, `/// <reference types="vite/client" />`) 추가로 정식 타이핑하고 캐스팅 제거.

### 4-5. Electron 정리 + PDF 6페이지 다크 테마 보고서 전면 재구현

사용자에게 두 가지 미해결 사안(Electron 패키징, `html2pdf.js` 미사용 의존성)에 대해 추천과 근거를 안내한 뒤, 다음과 같이 결정·진행:

- **Electron 제거**: `package.json`에서 `main`/`build`/`dist` 스크립트, `electron`/`electron-builder`/`electron-packager`/`concurrently`/`wait-on` devDependency 삭제. `npm install` 결과 400개 패키지 정리됨.
- **PDF를 실제 6페이지 다크 테마 보고서로 신규 구현**: 결과 화면에 화면 밖(`position: fixed; left: -10000px`)에 항상 렌더링되는 6개 `.pdf-page` 섹션(표지/사주원국, MBTI 유형카드, AI 심층분석, 대운·세운, 궁합 조합표, 풍수 가이드)을 추가하고, PDF 저장 버튼 클릭 시 각 섹션을 캡처해 하나의 PDF로 합침. (5번 섹션 참고: 이후 잠깐 `zIndex: -9999` 방식으로 바꿨다가 화면 고스팅 버그가 생겨 다시 이 방식으로 되돌림)
  - 최초 시도는 `html2pdf.js` 라이브러리를 그대로 사용했으나, **완전히 빈 흰 페이지만 나오는 버그**를 발견. 원인 추적 결과 이 라이브러리는 캡처 대상 DOM을 `opacity: 0`으로 설정한 오버레이 안에 복제해 넣은 뒤 캡처하는 내부 구조(`toContainer()`)를 갖고 있는데, 이 opacity가 캡처 결과에도 그대로 반영되어 콘텐츠가 통째로 투명하게 찍히는 문제였음.
  - **해결**: `html2pdf.js` 의존성을 제거하고, 그 내부에서 실제로 쓰이는 `html2canvas` + `jsPDF`를 직접 의존성으로 추가해 오버레이 없이 화면 밖 라이브 DOM을 바로 캡처하도록 재구현. 섹션(특히 AI 분석처럼 내용이 긴 섹션)이 한 페이지 높이를 넘으면 캔버스를 세로로 슬라이스해 자동으로 다음 PDF 페이지로 이어붙이도록 처리(내용 찌그러짐 방지).
  - Playwright로 실제 다운로드된 PDF 파일을 직접 열어 7페이지(AI 분석 섹션이 길어 2페이지로 자동 분할됨) 전체가 다크 테마 그대로, 데이터 정확하게 렌더링되는 것을 시각적으로 확인.

### 4-6. 검증

- `npx tsc --noEmit` 통과, `npm run build` 성공.
- Playwright(임시 설치, `--no-save`로 `package.json` 변경 없음)로 개발 서버를 직접 구동하여 이름 입력 → 제출 → 결과 화면까지 전 구간을 스크린샷/콘솔 에러 없이 확인.
- 실제 Gemini API 호출도 정상 동작 확인 (반복 테스트로 인한 429 레이트리밋은 코드 문제 아님).
- 궁합 조합표는 여러 일지(酉 등)로 실제 렌더링해 삼합/육합/충/형/파/해 값이 룩업 테이블과 일치하는지 스크린샷으로 대조.
- PDF 다운로드도 실제로 트리거해 파일을 직접 열어 7페이지 전체 시각 검증 완료.

---

## 5. 2026-07-31 UI 재구조화 + 오행 해석 강화

> **상태**: ✅ 구현 완료
> **배경**: 이 세션 앞부분에서 신규 섹션 4종(MBTI카드/대운세운/궁합/풍수)이 추가되면서 결과 화면이 세로로 매우 길어짐. 20~30대 사용자를 타깃으로 한 UX 개선 필요성이 제기되어, 단계별 질문으로 아래 구조를 확정하고 구현함.

### 5-1. 결과 화면 상단 탭 네비게이션 도입

- **상단 고정 영역** (스크롤해도 항상 먼저 보임): 프로필 배너(이름/MBTI뱃지/생년월일 + 결과저장·PDF저장·카톡공유 버튼) + 사주원국 4기둥 카드
- **탭으로 전환되는 영역** (기본 선택 탭: **운세**):
  1. **운세** — 오행 분포 + 시주(時柱) 상세 설명 + 대운/세운 흐름표
  2. **MBTI카드**
  3. **AI해석** — 기존 내부 5개 서브탭(성격/커리어/연애/재물/처방전) 구조 그대로 유지
  4. **궁합** — 궁합 조합표
  5. **풍수** — 풍수 수리 가이드
- **하단 고정**: "새로운 사주 분석하기" 버튼은 탭과 무관하게 페이지 최하단 유지
- 채택 이유: 이미 AI해석 내부에 탭 UI가 있어 사용자가 학습할 필요 없이 일관된 패턴이며, 20~30대에게 익숙한 토스/카카오뱅크/인스타그램식 프로필 탭 패턴과 동일함. 아코디언(설정 화면 느낌)이나 요약+바로가기(스크롤 길이 자체는 그대로)보다 적합하다고 판단.

### 5-2. 오행(五行) 해석 강화

- 기존에는 오행 분포를 개수(예: 목 3개, 화 1개)로만 보여주고 **각 오행이 성격적으로 무엇을 의미하는지 설명이 전혀 없었음.**
- `src/data/elementTypes.ts` (신규): 목/화/토/금/수 5개 오행의 일반적 성향(추진력/열정/신뢰/결단력/지혜 등)과, 해당 오행이 사주에서 강할 때/부족할 때 각각의 해석 문구를 정적 데이터로 정의.
- "운세" 탭의 오행 분포 카드에 각 오행 개수 옆에 "강함"/"부족" 배지를 표시하고, 사용자의 실제 분포(가장 많은 오행·0개인 오행)를 기준으로 자동 조합한 맞춤 해설 문단을 추가.

### 5-3. 구현 및 검증 결과

- ✅ `src/data/elementTypes.ts` 신규 작성 (오행 5종 × 일반 성향/강할 때/부족할 때 해설)
- ✅ `App.tsx` 결과 화면을 프로필+사주원국 고정 + 5개 탭(운세/MBTI카드/AI해석/궁합/풍수) 구조로 재구성. 탭 상태는 `activeSection`으로 관리하며 "다시 하기" 시 `fortune`으로 리셋
- ✅ 오행 분포 카드에 강함(최댓값)/부족(0개) 배지 + 사용자 맞춤 해설 문단 추가
- ✅ `App.css`에 `.section-tab-nav`(상단 고정 스크롤 시 sticky 유지), `.element-badge`, `.element-interpretation` 스타일 추가
- 🐛 **구현 중 발견한 버그**: PDF 캡처용 숨김 컨테이너(`pdfRef`)를 앞서 `position: fixed; left: 0; top: 0; zIndex: -9999`로 옮겨뒀던 것이, 화면의 글래스모피즘 카드(`backdrop-filter: blur()`)에 해당 숨김 콘텐츠가 흐릿하게 비쳐 보이는 시각적 버그를 유발함. `left: -10000px` (완전히 화면 밖)로 되돌려 해결 — html2canvas 직접 캡처 방식에서는 이 위치에서도 정상 캡처됨을 재확인(기존 블랭크 페이지 버그는 `html2pdf.js`의 opacity:0 오버레이가 원인이었고 좌표 자체는 무관했음).
- ✅ `tsc --noEmit` / `npm run build` 통과. Playwright로 5개 탭 전체를 실제 클릭·스크린샷하여 콘텐츠 정상 표시 확인, 오행 강함/부족 배지 및 맞춤 해설도 실제 사주(목3·화1·토0·금3·수1)로 검증. PDF 다운로드도 재검증(7페이지 정상, 고스팅 없음).

---

## 6. 2026-07-31 대운/세운 AI 운세 해설 추가

> **상태**: ✅ 구현 완료
> **배경**: 대운/세운 흐름표가 간지(예: "戊子", "24세~")만 나열되어 있어 비전공자는 이게 무슨 뜻인지 알기 어렵다는 피드백. 단계별 질문으로 범위/생성 방식을 확정.

### 6-1. 확정 사양

- **범위**: 현재 해당하는 대운 1개 + 최근 3개년 세운(작년/올해/내년) — 대운 10개·세운 11개 전체를 해설하면 너무 길고 AI 호출 비용도 커서 제외.
- **생성 방식**: 풍수 수리 가이드와 동일한 패턴 — "운세 해설 생성하기" 버튼 클릭 → Gemini 호출 → `localStorage` 캐싱(이름+생년월일+**연도** 기준, 세운은 매년 바뀌므로 연도를 캐시 키에 포함).
- **위치**: "운세" 탭의 대운/세운 흐름표 카드 바로 아래.

### 6-2. 구현

- `src/utils/geminiApi.ts`: `generateFortuneInterpretation()` 신규 추가. 일간 + 현재 대운 간지 + 최근 3개년 세운 간지를 입력받아, 전문 용어 없이 "계절/파도/바람" 비유로 흐름을 설명하는 6~8줄 텍스트를 생성.
- `src/App.tsx`: `unseText`/`unseLoading` 상태, `unseCacheKey()`(연도 포함), `handleGenerateUnse()` 핸들러, "운세" 탭 내 카드 UI 추가.
- PDF 저장 시에도 풍수 가이드와 마찬가지로 미생성 상태면 자동 생성을 시도한 뒤 P4(대운/세운) 페이지 하단에 함께 포함.

### 6-3. 검증

- Playwright로 실제 "운세 해설 생성하기" 버튼을 클릭해 Gemini가 실제로 생성한 텍스트를 확인 — 일간·대운 간지에 맞춰 "맑은 샘물 속 비옥한 흙" 등 비유로 "내실과 성장의 시기"를 설명하고, 작년/올해 흐름을 자연스럽게 이어서 서술함을 확인.
- `tsc --noEmit` / `npm run build` 통과.
- PDF 임베딩 코드는 이미 검증된 풍수 가이드 블록과 동일한 패턴으로 작성함. 다만 이 검증 시점에 오늘 세션의 반복 테스트로 Gemini 무료 티어 호출량이 소진된 것으로 보여, PDF 다운로드까지 포함한 전체 흐름의 실시간 재확인은 완료하지 못함 — **다음에 실제 사용 시 PDF에 운세 해설이 정상적으로 들어가는지 한 번 확인 필요**.

---

## 6-A. 2026-07-31 PDF 출력 방식 원복 (완료)

> **상태**: ✅ 구현 완료
> **배경**: 앞서(섹션 4-5) `html2canvas`+`jsPDF`로 앱 UI(다크 테마, 글래스모피즘 카드)를 그대로 캡처하는 6페이지 PDF를 새로 만들었으나, 실제 사용해보니 "괜히 앱 UI랑 비슷하게 하니까 오히려 별로"라는 피드백. 인쇄 문서는 인쇄 문서답게 깔끔한 흰 배경 스타일이 낫다고 판단해 원래 방식으로 되돌림.

### 6-A-1. 확정 사항

- PDF 저장 버튼을 누르면 다시 원래처럼 **`window.open()` + 브라우저 인쇄 다이얼로그** 방식으로 동작 (흰 배경, 표/텍스트 위주의 인쇄용 HTML).
- 다만 콘텐츠 범위는 줄이지 않음 — 오늘 추가된 MBTI 유형카드, 대운/세운 흐름표, 궁합 조합표, AI 운세 해설도 같은 인쇄 스타일(`.report-section`/`.report-block`)로 전부 포함.

### 6-A-2. 구현

- `src/App.tsx`: `handleDownloadPDF()`를 다시 `window.open()` 기반으로 재작성. `handleGenerateFengShui()`/`handleGenerateUnse()`가 생성한 텍스트를 반환하도록 시그니처를 `Promise<string | null>`로 변경해, PDF 저장 시 state 갱신을 기다리지 않고 바로 로컬 변수로 받아 사용하도록 함(React state는 비동기라 같은 함수 안에서 곧바로 최신값을 읽을 수 없기 때문).
- PDF 전용으로 화면 밖에 항상 렌더링해두던 6개 `.pdf-page` 숨김 DOM(`pdfRef`)과 관련 `useRef` 제거.
- `src/App.css`에서 `.pdf-page`/`.pdf-*` 관련 스타일 전체 삭제.
- `package.json`에서 `html2canvas`, `jspdf` 의존성 제거 (`npm install` 결과 번들 크기가 약 600KB 감소: `html2canvas.esm` ~200KB + `jspdf.es.min` ~390KB 청크가 사라짐).

### 6-A-3. 검증

- `tsc --noEmit` / `npm run build` 통과, 번들 크기 감소 확인.
- Playwright로 실제 PDF 저장 버튼을 클릭해 열리는 인쇄 팝업 창의 본문 텍스트를 검사 — MBTI 유형카드, 대운/세운, 궁합, AI 운세 해설, 풍수 가이드 섹션이 모두 포함되어 있음을 확인. 콘솔 에러 없음.

---

## 7. AI 로딩 속도 최적화 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "AI 로딩이 많이 길다"는 피드백. 원인은 `generateSajuInterpretation()`이 성격/커리어/연애/재물/처방전 5개 카테고리 + 사주풀이를 **한 번의 거대한 요청**(`maxOutputTokens: 8192`)으로 모두 생성해, 사용자가 첫 화면(성격 탭)만 봐도 5개 분량을 다 기다려야 했던 것.

### 7-1. 구현

- `src/utils/geminiApi.ts`를 `generateSajuIntro()`(타이틀+사주풀이만, 가벼움) + `generateCategoryInterpretation(category, ...)`(성격/커리어/연애/재물 중 1개씩) + `generatePrescriptions()`(처방전)로 완전히 분리. 기존의 거대한 단일 프롬프트/함수는 제거.
- `AppResult.aiData` → `AppResult.aiIntro`(타이틀+사주풀이만)로 축소. 로딩 화면에서는 이것만 자동 생성.
- `categoryData`(4개 카테고리) / `prescriptionsData` 상태를 별도 관리하고, 각 탭 진입 시 "생성하기" 버튼을 눌러야 개별 API 호출되도록 UI 재구성. 한 번 생성되면 `localStorage`에 이름+생년월일+MBTI+카테고리 기준으로 캐싱해 재방문 시 재사용.
- 북마크 저장 버튼들도 새 데이터 구조(`categoryData[cat]`, `prescriptionsData`)를 참조하도록 함께 수정.
- PDF 저장 시에는 아직 생성 안 된 카테고리/처방전을 전부 자동 생성 후 포함 (7-C-3 참고).

### 7-2. 검증

- Playwright로 실제 결과 화면 도달 시간을 측정 — 카테고리 5개를 한 번에 생성하던 기존 방식 대비, 이제는 인트로만 생성하면 되어 체감 대기가 크게 줄었음(실측 약 14~15초, 이전 세션에서 5개 카테고리를 한 번에 기다릴 때보다 짧음).
- "성격 진단 생성하기" 버튼 클릭 → 약 14초 후 팩폭/럭키아이템까지 정상 생성됨을 실제 클릭 테스트로 확인.
- `tsc --noEmit` / `npm run build` 통과.

---

## 7-A. 카카오톡 공유 카드 개선 (완료)

> **상태**: ✅ 구현 완료 (카드 문구 개선 + Ⓒ옵션 채택). Ⓐ/Ⓑ 옵션(공유 링크가 실제 결과를 재현하는 것)은 백엔드가 필요해 **보류**.
> **배경**: 카카오 공유 버튼은 정상 동작(4019 에러 해결됨)하지만, 사용자가 공유된 카드를 눌러보니 "결과가 아니라 그냥 사이트"만 보인다는 피드백. 원인은 이 앱이 결과별 고유 URL이 없는 순수 프론트엔드 SPA라서, 카드의 링크(`webUrl`/`mobileWebUrl`)가 항상 사이트 첫 화면(입력 폼)으로만 연결되기 때문.

### 7-A-1. 구현 — 카드 문구

- **제목**: `🔮 {이름}님의 {MBTI} 사주 팩폭 결과` — 이름+MBTI를 짧게 포함.
- **설명**: 성격 팩폭(`personality.factBomb`) 한줄평을 **그대로 메인 문구**로 사용 (기존의 MBTI+사주팔자+팩폭을 다 욱여넣던 방식에서 팩폭 중심으로 단순화).
- **팩폭이 아직 생성 안 된 상태에서 공유 버튼을 누른 경우**: 카카오 공유 자체가 성격 카테고리(7번 참고)를 필요로 하므로, `handleKakaoShare()`가 먼저 `handleGenerateCategory('personality')`를 자동 호출해 팩폭 문구를 생성한 뒤 공유를 진행하도록 구현(성격 탭을 미리 안 봤어도 공유 가능).
- 곁들여, 섹션 8에 기록되어 있던 SDK 로드 대기 로직의 잠재적 재발 위험(`readyState` 죽은 분기)도 이번에 함께 정리 — `load`/`error` 이벤트 리스너에만 의존하지 않고 100ms 간격 폴링(최대 5초)을 백업으로 추가해, 이벤트가 이미 지나가버린 경우에도 무한 대기하지 않도록 함. 디버그용 `console.log('KAKAO_APP_KEY=', ...)`도 제거.

### 7-A-2. 보류 — 카드를 클릭했을 때 어디로 보낼지

논의된 옵션 3가지 중, 백엔드가 필요 없는 **옵션 C(링크는 사이트 대문 유지)**로 우선 구현. "나도 분석해보기" 버튼 문구와도 자연스럽게 맞음. 옵션 A(URL 파라미터로 재계산)/B(서버 스냅샷)는 추가 논의·구현이 필요해 이번 범위에서는 보류.

### 7-A-3. 검증

- Playwright로 카톡 공유 버튼을 클릭 → 성격 카테고리가 캐시에 없는 상태에서도 자동으로 생성된 뒤(콘솔 에러 없이) 공유가 시도됨을 확인. 공유 후 AI 해석 탭에서 성격 분석이 이미 채워져 있는 것도 확인(생성 결과가 상태에 정상 반영됨).
- Kakao SDK 자체의 실제 카드 발송은 카카오 개발자 콘솔에 등록된 배포 도메인에서만 동작하므로(로컬 개발 서버에서는 도메인 미등록으로 정상적으로 실패), 실제 배포 환경에서 카드 모양을 한 번 확인 권장.

---

## 7-B. 오행/궁합 AI 종합 해설 추가 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "오행 해석이 더 자세했으면 좋겠고, 궁합표도 비전공자가 봐도 이해하기 쉬웠으면 좋겠다"는 피드백. **풍수 수리 가이드 / 대운·세운 운세 해설과 동일한 패턴**(정적 정보는 유지 + AI 종합 해설을 버튼으로 추가 생성)으로 확장.

### 7-B-1. 오행 종합 AI 해설

- 기존 오행 분포 카드(개수, 강함/부족 배지, `elementTypes.ts` 기반 정적 해설 문단)는 그대로 유지.
- 그 아래에 "오행 종합 해설 생성하기" 버튼 추가 → `generateElementSummaryInterpretation()`이 오행 5개 수치 전체(강한 것/부족한 것뿐 아니라 전체 분포 균형)를 종합해서 맞춤형으로 다시 풀이.

### 7-B-2. 궁합 종합 AI 해설

- 기존 궁합 조합표(삼합/육합/충/형/파/해 관계별 띠 태그)는 그대로 유지.
- 그 아래에 "궁합 종합 해설 생성하기" 버튼 추가 → `generateCompatibilitySummaryInterpretation()`이 삼합/육합/충/형/파/해 결과 전체를 종합해서, 한자 용어 없이 비전공자가 이해할 수 있는 문장으로 풀이.

### 7-B-3. 구현

- `src/utils/geminiApi.ts`: `generateElementSummaryInterpretation()`, `generateCompatibilitySummaryInterpretation()` 신규 추가 (기존 `callGeminiPlainApi()` 기반 패턴 재사용).
- `src/App.tsx`: `elementSummaryText`/`compatSummaryText` 상태 + 캐시 키(이름+생년월일 기준, 연도 미포함) + 핸들러 + 버튼/카드 UI 추가.
- PDF 저장 시 미생성 상태면 자동 생성 후 각각 오행 분포 섹션 / 궁합 조합표 섹션에 포함.

### 7-B-4. 검증

- Playwright로 "오행 종합 해설 생성하기" / "궁합 종합 해설 생성하기" 버튼을 각각 클릭해 실제 Gemini 응답이 정상 표시됨을 확인. 콘솔 에러 없음.

---

## 7-C. AI 최적화 관련 추가 확정 사항 (완료)

> **상태**: ✅ 구현 완료
> **배경**: 섹션 7(AI 로딩 최적화)을 다시 검토하며 "자동 출력을 최소화하고 버튼으로 추가 생성"이라는 방향을 앱 전체에 더 일관되게 적용하기 위해 추가로 확정한 사항들.

### 7-C-1. 메인 AI 해석 중 자동 생성 범위 재확인

- `title` + `jungianNote` + `sajuExplanation`(쉬운 사주풀이)은 **로딩 화면에서 자동 생성 유지** — 결과 화면 진입 즉시 최소한의 "분석된 느낌"은 있어야 하므로. (섹션 7의 기존 방향과 동일)
- 성격/커리어/연애/재물/처방전 5개 카테고리는 섹션 7 그대로 각 탭 진입 시 버튼 생성. 구현/검증 완료.

### 7-C-2. 죽은 코드 복원 — 사주 4기둥 AI 심층 해석 (완료)

- **기존 문제**: `src/utils/geminiApi.ts`의 `generatePillarInterpretation()`이 `App.tsx` 어디에서도 호출되지 않는 죽은 코드였음. 시주(時柱) 카드만 클릭 가능했고, 클릭해도 `HOUR_BRANCHES`의 고정 텍스트만 보여줬음.
- **구현**: 연주/월주/일주/시주 **4기둥 모두 클릭 가능**하도록 확장 완료.
  - **시주**: 12지지 고정 설명(`hourBranch.desc`)을 모달을 열자마자 바로 보여주고, 그 아래 "AI 심층 해설 생성하기" 버튼 추가.
  - **연주/월주/일주**: 60갑자 조합이라 고정 설명 없이, 모달을 열면 바로 "AI 해설 생성하기" 버튼만 표시하고 누르면 `generatePillarInterpretation()` 호출.
  - 기둥별 캐싱: 이름+생년월일+기둥종류(`PillarKey`) 키로 `pillarCacheKey()` 구현. `pillarModal`/`pillarAiData`/`pillarAiLoading` 상태 + `handlePillarClick()`/`handleGeneratePillarAi()` 핸들러 추가.
- **검증**: Playwright로 4기둥 각각 클릭 → 시주는 정적 설명 즉시 표시 + AI 버튼, 연/월/일주는 AI 버튼만 표시되는 것을 확인. AI 해설 생성 버튼 클릭 시 실제 Gemini 응답 정상 표시, 콘솔 에러 없음.

### 7-C-3. PDF 저장의 자동 생성 범위 재확인 (완료)

- PDF 저장 버튼을 누르면, 그 시점에 **아직 생성되지 않은 모든 AI 항목(카테고리 4개, 처방전, 오행/궁합 종합 해설, 풍수, 대운/세운, 기둥별 심층 해석 4개)을 자동으로 한꺼번에 생성**한 뒤 인쇄용 HTML을 구성하도록 `handleDownloadPDF()`에 순차 await 로직 구현.
- **검증**: 콜드 스타트(아무 AI 항목도 생성 안 된 상태)에서 PDF 저장 버튼 클릭 → 약 12회의 순차 Gemini 호출(~130초) 후 인쇄 미리보기 창에 모든 섹션(MBTI 카드, 오행 종합 해설, 대운/세운, 궁합 조합표+종합해설, AI 카테고리 4종, 4기둥 AI 심층 해설, 운세 해설, 풍수 가이드)이 정상 포함됨을 Playwright로 확인. 콘솔/페이지 에러 없음.

---

## 7-D. 2026-08-01 개인화 질문·답변 기반 AI 해석 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "질문에 답변하는 내용에 따라 해석에 포함·비교해주는 기능이 있으면 좋겠다"는 요청. "질문을 1개씩" 방식으로 방향을 좁혀감 — 최종적으로 **적중도 비교형이 아닌 개인화 강화형**으로 확정 (사주 예측과 실제 답변을 대조해 맞고 틀림을 보여주는 방식은 재미보다 신뢰도 훼손 리스크가 크다고 판단).

### 7-D-1. 확정된 사양

- **적용 범위**: 커리어·연애·재물 3개 카테고리만 (성격은 "타고난 기질" 섹션이라 제외, 처방전/궁합/풍수는 이번 범위에서 제외).
- **질문 시점**: 각 카테고리 탭에서 아직 AI 해석을 생성하지 않은 상태일 때, "생성하기" 버튼 위에 카테고리당 2문항(4지선다, 팩폭 톤 이모지 옵션)을 표시.
- **필수 여부**: 선택 사항. 답변 없이 그냥 "생성하기"를 눌러도 기존과 동일한 일반 해석이 나옴 — 별도의 "건너뛰기" 버튼 없이, 답변을 하나도 안 고르면 자동으로 일반 버전으로 생성됨.
- **AI 반영 방식**: 답변을 프롬프트에 그대로 전달하고, AI가 분석 문장 안에서 답변 내용을 직접 언급하며 풀어가도록 지시(예: "지금 프리랜서·자영업 상태라고 하셨는데…").
- **캐싱**: 이름+생년월일+MBTI+답변 조합을 키로 캐싱. 같은 답변으로 다시 들어오면 재사용, 답변을 바꾸면 새로 생성.

### 7-D-2. 구현

- `src/data/categoryQuestions.ts` 신규 — `CATEGORY_QUESTIONS`(커리어/연애/재물 각 2문항 × 4지선다 정적 데이터), `QuestionableCategory` 타입.
- `src/utils/geminiApi.ts` — `generateCategoryInterpretation()`에 선택적 `userAnswers?: CategoryUserAnswer[]` 매개변수 추가. 답변이 있으면 프롬프트에 "사용자의 실제 답변" 블록과 "답변을 직접 언급하며 해석하라"는 지침을 추가 삽입.
- `src/App.tsx` — `categoryAnswers` 상태(카테고리별 [질문1 답, 질문2 답]) 추가, `categoryCacheKey()`에 답변 조합을 캐시 키에 반영하는 로직 추가, `handleGenerateCategory()`가 답변 배열을 받아 API와 캐시 키에 전달하도록 확장. 커리어/연애/재물 탭의 미생성 상태 UI에 질문 선택 버튼(기존 `hour-btn` 스타일 재사용) 렌더링 추가.
- PDF 자동 생성 로직(`handleDownloadPDF`)은 그대로 유지 — 배치 생성 특성상 질문에 답할 수 없으므로 미생성 카테고리는 답변 없는 일반 버전으로 자동 생성됨 (기존 동작과 동일).

### 7-D-3. 검증

- Playwright로 커리어 탭에서 "🚀 내 사업 내가"(프리랜서·자영업) + "🤔 이 길이 맞나 싶음"(적성·진로 고민) 답변 선택 후 생성 → 실제 Gemini 응답이 "지금 프리랜서·자영업 상태라고 하셨는데… 요즘 적성·진로 방향성에 대한 고민으로 스트레스를 받는 이유는…"처럼 두 답변을 모두 자연스럽게 직접 언급하며 해석에 반영함을 확인. `tsc --noEmit`, `npm run build` 클린.

---

## 7-E. 2026-08-01 공유용 이미지 카드 생성 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "3번 추천 기능(공유용 결과 카드 이미지)이 있으면 좋겠다"는 요청. 카드 내용(간결하게 3줄)과 비율(인스타 스토리용 세로 9:16)을 확인 후 구현.

### 7-E-1. 사양

- **비율/해상도**: 1080×1920px (인스타 스토리 표준, 9:16).
- **내용**: ① 이름 · MBTI ② 일주(日柱) 한자 2글자를 사주 정체성 대표 표기로 크게 강조 ③ 성격 카테고리의 팩폭 한줄평. 하단에 사이트 URL 워터마크.
- **디자인**: 앱 다크 테마와 통일된 보라~네이비 그라데이션 배경 + 별 장식, 골드(#f5c842) 포인트 컬러.
- **저장 방식**: `navigator.canShare`/`navigator.share`(파일)가 지원되는 환경(주로 모바일)에서는 네이티브 공유 시트를 우선 시도하고, 지원하지 않거나 사용자가 취소하면 PNG 파일 다운로드로 대체.

### 7-E-2. 구현

- `src/App.tsx`에 `handleDownloadImageCard()` 신규 추가 — Canvas API로 직접 그려 `canvas.toBlob()`으로 PNG Blob 생성. 카카오 공유와 동일하게 성격(personality) 카테고리가 아직 생성되지 않았다면 자동 생성 후 팩폭 문구를 사용.
- `wrapCanvasText()` 헬퍼로 팩폭 한줄평 텍스트를 카드 폭에 맞게 자동 줄바꿈.
- 프로필 액션 영역(카톡 공유 버튼 옆)에 "📸 이미지 카드 저장" 버튼 추가, `imageCardGenerating` 상태로 로딩 표시.

### 7-E-3. 검증

- Playwright로 실제 다운로드 이벤트를 캡처해 PNG 파일(약 340KB, 유효한 PNG 시그니처)이 정상 생성됨을 확인. 생성된 이미지를 직접 열어 레이아웃(워터마크·이름/MBTI·일주 한자·팩폭 문구 줄바꿈·하단 URL)이 의도대로 렌더링됨을 시각적으로 확인. `tsc --noEmit`, `npm run build` 클린.

> [!NOTE]
> 이번 검증 중 성격 카테고리 팩폭(factBomb) 응답에 AI가 "🔥 뼈 때리는 팩폭 한줄평 (존댓말 매운맛):" 같은 지침 문구 일부를 그대로 되풀이해 포함시키는 경우가 관찰됨. 카드 기능 자체의 결함은 아니고 기존 `generateCategoryInterpretation()` 프롬프트의 응답 품질 이슈라, 섹션 8에 낮은 우선순위 이슈로 등록만 해둠.

---

## 7-F. 2026-08-01 "나풀이" 리브랜딩 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "지금 앱 이름이 특색이 없다"는 피드백. "질문을 1개씩" 방식으로 톤(팩폭·MZ + 심플한 테크 브랜드 느낌)/언어(순한글)/핵심 컨셉(정체성 + 사주×MBTI 융합)/설명력(사주 맥락이 살짝 드러나야 함)을 순서대로 확정한 뒤, 후보 4개(나풀이/사주딱/찐사주/결풀이) 중 **"나풀이"**(나+사주풀이의 '풀이')로 확정.

### 7-F-1. 로고

- **형태**: 심볼(크리스탈볼 속에서 빛나는 별) + "나풀이" 워드마크 조합. 기존 앱의 🌌/🔮 테마를 그대로 계승.
- **컬러**: 기존 `App.css`의 `--purple`(#8b5cf6)/`--gold`(#f5c842) 토큰을 그대로 재사용해 앱 전체 톤과 통일.
- Artifact로 파비콘 크기부터 헤더 락업, 세로형 스플래시, 라이트 배경 버전까지 미리보기 시안을 먼저 확인받은 뒤 적용.

### 7-F-2. 도메인 — 보류

- Vercel 프로젝트명을 무료로 바꿔 `napuri.vercel.app` 같은 새 주소를 쓰는 방안을 검토했으나, 카카오 공유 SDK가 **사이트 도메인을 카카오 개발자 콘솔에 별도로 등록**해야 하는 의존성이 있음을 확인(자바스크립트 키 자체는 도메인과 무관해 안 바뀜). 코드 안에도 `mbti-delta-red.vercel.app`가 하드코딩된 곳이 2군데(로컬 개발 시 `shareUrl` 폴백, 이미지 카드 하단 워터마크) 있어 함께 고쳐야 함을 확인. **사용자 판단으로 이번엔 도메인 변경은 보류**하고 기존 `mbti-delta-red.vercel.app`를 유지하기로 함 — 코드의 두 하드코딩 지점도 그대로 둠.

### 7-F-3. 구현

- `public/favicon.svg` — 기존의 앱과 무관한 기본 보라색 추상 도형을 크리스탈볼+별 심볼로 전면 교체.
- `index.html` — `<title>`, `og:title`, `og:description`, `meta description`에 "나풀이" 반영.
- `src/App.tsx`:
  - `NapuliMark` 컴포넌트(인라인 SVG) 신규 추가, 헤더 로고 아이콘(`🌌` 이모지)을 이 심볼로 교체하고 `logo-text`를 "나풀이"로 변경.
  - PDF 리포트 `<title>`/`<h1>`, 이미지 카드 상단 워터마크, 카카오 공유 카드 제목, 이미지 카드 `navigator.share` 타이틀에 "나풀이" 브랜드명 삽입.
- `package.json`의 `name` 필드를 `saju-mbti-app` → `napuli`로 변경(배포 도메인과는 무관한 내부 식별자).

### 7-F-4. 검증

- `tsc --noEmit`, `npm run build` 클린.
- Playwright로 브라우저 탭 타이틀("🔮 나풀이 — 사주 × MBTI AI 분석"), 헤더 로고, 파비콘 응답(200)을 확인. 이미지 카드도 실제로 다운로드해 상단 워터마크 문구("🔮 나풀이 · 사주 × MBTI 분석")가 카드 폭 안에서 잘리지 않고 정상 표시됨을 시각적으로 확인.

---

## 7-G. 2026-08-01 시간 모름 대응, AI 문구 제거, 데일리 운세·다이어리·카드 업그레이드 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "태어난 시간을 안 쓰면 해석이 별로다", "곳곳에 AI/Gemini 단어를 지워달라"는 요청으로 시작. "질문을 1개씩" 방식으로 방향을 좁히던 중 보관함·공유 카드 개선까지 함께 다루기로 확장되어, 아래 5개 항목을 한 번에 구현.

### 7-G-1. 태어난 시간 모름 대응

- **문제**: 입력 폼에 "모름" 옵션이 없어 시간을 모르는 사용자도 12시진 중 하나를 억지로 골라야 했고, 그 추측값(시주)이 오행 분포·AI 해석 품질을 왜곡시켰음.
- **구현**: 입력 폼에 "☑ 태어난 시간을 모릅니다" 체크박스 추가(체크 시 12시진 그리드 비활성화). `calculateSaju()`에 `hourUnknown` 매개변수 추가 — 체크 시 시주를 계산에서 완전히 제외하고 연/월/일주 3기둥(6글자)만으로 오행 분포를 재계산. `SajuResult.hourPillar`를 `Pillar | null`로 변경.
- 결과 화면 사주원국 그리드·PDF 모두 시간을 모르면 3칸만 표시(시주 칸 자체 제거), "시주 정보" 카드도 조건부로 숨김. AI 프롬프트(`generateSajuIntro`)에도 시간 미상임을 명시해 3주 기준으로 자연스럽게 풀이하도록 지시문 추가.
- 대운수 계산에 필요한 시각값은 시간 미상 시 정오(12시)를 중립값으로 사용(대운 간지 자체엔 영향 없음, 진입 나이만 근사).
- **검증**: Playwright로 체크박스 클릭 → 그리드 비활성화 확인 → 제출 → 결과 화면 사주원국이 정확히 3칸만 표시됨을 확인.

### 7-G-2. "AI"/"Gemini" 단어 제거

- **범위**: 사용자에게 보이는 텍스트(버튼·라벨·토스트·안내문구·PDF·index.html 메타)만 수정, 함수명·코드 주석 등 개발자용 코드는 그대로 둠.
- "나풀이"를 주어로 의인화 — 예: "AI 해석" 탭 → "🔮 나풀이 해석", "Gemini AI가 MBTI와 융합 분석해 드립니다" → "나풀이가 MBTI와 융합 분석해 드립니다", "🤖" 로봇 이모지도 브랜드 이모지 "🔮"로 통일.
- `src/utils/geminiApi.ts`의 최종 실패 에러 메시지("Gemini API 서버 응답 처리에 실패...")도 사용자에게 그대로 노출될 수 있어 "나풀이 서버 응답 처리에 실패..."로 수정.
- **검증**: Playwright로 결과 화면 전체 텍스트를 정규식(`/\bAI\b|Gemini/`)으로 스캔해 사용자 화면에 더 이상 노출되지 않음을 확인.

### 7-G-3. 오늘의 나풀이 (데일리 운세)

- 일주(본인 고유 간지)와 오늘 날짜의 일진(오늘의 간지) 관계를 바탕으로 한 짧은 2~3문장 데일리 운세.
- `src/utils/sajuCalculator.ts`의 `calcDayPillar()`를 export해 임의의 날짜(오늘) 일주를 계산할 수 있도록 함. `src/utils/geminiApi.ts`에 `generateDailyFortune()` 신규 추가.
- "운세" 탭 최상단에 골드 강조 카드로 배치. 캐시 키에 오늘 날짜(`YYYY-MM-DD`)를 포함해 하루 한 번만 생성, 자정이 지나면 자동으로 새로 생성 가능.
- 생성 시 자동으로 나풀이 다이어리(7-G-4)에도 기록됨.

**2026-08-02 개선 — 팩폭 톤 추가**: "멘트가 밋밋하다"는 피드백으로 카테고리 해석과 같은 구조로 확장. "질문을 1개씩" 방식으로 확인한 결과 ①기존 문체(설명+행동팁)는 유지하면서 ②카테고리 해석처럼 별도 "팩폭 한줄" 요소를 추가하는 방향으로 확정.
- `generateDailyFortune()`의 반환 타입을 `string` → `DailyFortune { analysis, factBomb }`로 변경(`callGeminiPlainApi` → `callGeminiJsonApi` 사용, `CategoryInterpretation`과 동일한 패턴).
- `analysis`: 기존과 동일한 설명+행동팁(2~3문장). `factBomb`: "오늘 당신은 분명 ○○할 겁니다" 식으로 오늘 할 법한 행동을 위트있게 찌르는 팩폭 한 줄(카테고리 해석과 동일한 반전 유머 톤).
- `App.tsx`: `dailyFortuneText: string` 상태를 `dailyFortuneData: DailyFortune | null`로 변경, localStorage 캐싱도 JSON 직렬화로 전환. UI에 기존 `.fact-bomb-box` 스타일을 재사용해 팩폭 한줄을 강조 박스로 표시.
- **검증**: Playwright로 실제 생성 결과 확인 — "오늘 그렇게 다 이해한다는 표정을 짓고 계셔도, 속으로는 '내 말이 맞는데 왜 저러지?' 하며 상대방을 묵살할 꿍꿍이를 다 꾸미고 계실 걸요!" 같은 카테고리 해석과 동일한 톤의 팩폭이 정상 생성됨을 확인. (테스트 중 API 레이트리밋으로 factBomb 필드만 폴백 문구로 나온 사례가 1건 있었으나, 이는 필드 단위 방어 로직이 정상 동작한 것으로 재확인 시 정상 생성됨 — 앱 결함 아님.)

### 7-G-4. 보관함 → 나풀이 다이어리 확장

- **문제 2가지**: ① 저장된 문구가 2줄로 잘려(`-webkit-line-clamp: 2`) 전체 내용을 읽을 방법이 없었음. ② 저장은 되지만 그 사람의 전체 사주 결과 화면으로 돌아갈 방법이 없었음(카테고리/제목/문구/날짜만 저장, 원본 입력값 미보관).
- 지난번 논의했던 "나풀이 다이어리(히스토리 타임라인)" 아이디어와 개념이 겹쳐 **하나로 통합** — 보관함을 다이어리로 확장.
- `Bookmark` 인터페이스에 `snapshot?: FormData` 필드 추가 — 저장 시점의 입력값(이름/생년월일/시간/성별/MBTI)을 함께 저장.
- 다이어리 항목 클릭 시 전체 내용을 보여주는 상세보기 모달 신규 추가(2줄 잘림 문제 해결). snapshot이 있으면 "🔮 전체 결과 다시 보기" 버튼 표시 — 클릭 시 `handleLoadDiarySnapshot()`이 저장된 snapshot을 `formData`에 채우고 기존 제출 흐름(`step: 'loading'`)을 그대로 재사용해 그 사람의 전체 결과 화면으로 복귀. 이미 캐시된 AI 콘텐츠(카테고리/오행 요약/풍수 등)는 기존 캐시 로딩 로직이 자동으로 복원.
- 헤더/화면 라벨을 "다이어리"로 변경, 빈 상태 문구도 갱신.
- **검증**: Playwright로 결과 저장 → 다이어리에서 항목 클릭 → 상세 모달 확인 → "전체 결과 다시 보기" 클릭 → 원래 이름이 다시 화면에 표시됨을 확인.

### 7-G-5. 내 기록 내보내기/불러오기

- 로그인 시스템은 지금 구조(서버 없는 순수 프론트엔드, API 키도 클라이언트에 내장)에서는 백엔드 신설이 필요한 큰 작업이라 보류하고, 기기 간 이전을 위한 가벼운 대안으로 채택.
- 다이어리 화면에 "⬇ 내보내기"(localStorage의 `saju_` 접두어 데이터 전체를 JSON으로 다운로드) / "⬆ 불러오기"(JSON 파일을 다시 localStorage에 기록) 버튼 추가. 이미지 카드 다운로드에 쓰던 Blob 패턴을 재사용.
- 실시간 동기화가 아닌 수동 스냅샷 이전이라는 한계는 사용자에게 사전 안내됨(계획 논의 단계에서 공유).
- **검증**: Playwright로 내보내기 버튼 클릭 → 실제 JSON 파일 다운로드 및 내용 확인.

### 7-G-6. 공유 카드 업그레이드 (별자리 장식 + MBTI 이모지)

- "카톡/이미지 카드 설명이 밋밋하다"는 피드백. 실제 일러스트 이미지 제작은 이미지 생성 도구가 없어 불가함을 안내하고, Canvas 장식 그래픽 정교화 + 기존 `mbtiTypes.ts`의 MBTI 이모지 활용으로 방향을 좁힘. Artifact로 개선 시안을 먼저 확인받은 뒤 적용.
- `handleDownloadImageCard()`에 별자리(점을 선으로 이은 장식) 추가, MBTI 이모지를 발광 효과가 있는 원형 배지로 크게 배치, 팩폭 한줄평을 은은한 테두리의 프레임 박스 안에 배치하도록 레이아웃 재구성(세로 좌표 재계산으로 겹침/잘림 없이 배치).
- **검증**: Playwright로 실제 카드를 다운로드해 시각적으로 확인 — 별자리 연결선, MBTI 이모지 배지(발광 효과), 프레임 처리된 팩폭 텍스트 모두 잘림 없이 정상 렌더링됨을 확인.

### 7-G-7. 공통 검증

- `tsc --noEmit`(strict 모드), `npm run build` 클린.
- Playwright로 11개 시나리오(시간 모름 체크박스/3칸 그리드/AI 단어 제거/오늘의 나풀이/다이어리 저장·상세보기·복귀/내보내기/이미지 카드)를 하나의 스크립트로 실행해 전부 통과 확인. 콘솔/페이지 에러 없음.

---

## 7-H. 2026-08-02 MBTI 카드 탭 통합 + 궁합 해설 예시 강화 (완료)

> **상태**: ✅ 구현 완료
> **배경**: "MBTI카드 탭은 지우고 나풀이 해석에 포함하는 게 나을 것 같다", "궁합 해설이 무슨 띠가 어떤지 잘 안 보인다"는 피드백. "질문을 1개씩" 방식으로 배치 위치·깊이를 확정한 뒤 구현.

### 7-H-1. MBTI 카드 → "나풀이 해석" 성격 탭으로 통합

- 결과 화면 상단 탭이 5개(운세/MBTI카드/나풀이 해석/궁합/풍수)에서 **4개(운세/나풀이 해석/궁합/풍수)**로 축소 — 독립 "MBTI카드" 탭 제거.
- MBTI 카드(이모지·닉네임·키워드 태그·특징 문장·오행 시너지 문구, 전부 정적 데이터)는 "나풀이 해석" 탭의 **성격 서브탭 최상단**에 고정 배치 — AI 생성 여부와 무관하게 항상 보임("정체성 소개 → AI 심층 분석" 흐름).
- `activeSection` 타입에서 `'mbti'` 제거. PDF 리포트는 별도 매체(탭 개념 없음)라 기존 독립 섹션 그대로 유지(범위 밖).
- **검증**: Playwright로 상단 탭이 정확히 4개인지, "MBTI카드" 라벨이 사라졌는지, 성격 탭 진입 시 MBTI 카드가 표시되는지 확인.

### 7-H-2. 궁합 종합 해설 — 구체적 예시 중심으로 강화

- 기존 프롬프트는 6~8줄 분량으로 모든 관계(삼합/육합/충/형/파/해)를 비슷한 비중으로 추상적으로만 다뤄, "이 띠 사람이 실제로 어떤지"가 잘 안 드러났음.
- **확정 방향**: 육합(찰떡 궁합)·충(갈등 주의) 2개만 그 띠 특유의 성격 + 구체적 예시 장면(예: "같이 여행 계획 짤 때", "의견 갈릴 때")을 넣어 깊게 다루고, 나머지(삼합/형/파/해)는 1문장씩 짧고 위트있게만 언급. 분량은 8~12줄로 확대.
- `generateCompatibilitySummaryInterpretation()` 프롬프트 지침을 5개 항목으로 재작성(육합/충 각각 예시 장면 요구, 나머지 간략화 명시).
- **검증**: Playwright로 실제 생성 결과 확인 — 육합(토끼띠)은 "해외여행 계획을 짤 때" 역할 분담 예시, 충(용띠)은 "카페 갈지 영화 볼지" 의견 충돌 예시로 구체화되고, 나머지 관계는 짧게 언급됨을 확인.

---

## 7-I. 2026-08-02 심화해석 기능 (십신·MBTI 상세 근거) — 구현 완료

> **상태**: ✅ 구현 완료
> **배경**: "해석이 짧다"는 사용자 피드백. 참고 자료로 받은 두 문서를 학습한 뒤(`명리심리상담사_전정훈_교안모음.pdf`, `mbti.pdf`), "질문을 1개씩" 방식으로 방향을 확정.

### 7-I-1. 참고 자료 학습 내용

- **명리심리상담사_전정훈_교안모음.pdf** (30p, 전정훈 저 『기초자평명리학』 기반 강의 교안): 명리학 역사부터 실전 심리 상담 기법까지 다룸. 그중 **6~7차시 "십신(十神)의 심리"**가 가장 유용 — 일간(日干)과 나머지 글자의 오행·음양 관계에 따라 비견/겁재/식신/상관/편재/정재/편관/정관/편인/정인 10가지로 나누고, 각각의 성격·직업·인간관계 특성을 상세히 설명. 11차시 "궁(宮)에 의한 심리 구분"도 확인(연지=조상궁/월지=부모궁/일지=배우자궁/시지=자식궁, 나이대까지 명시 — 지금 앱의 사주원국 카드 라벨과 이미 일치함을 확인).
- **mbti해석.pdf** (20p): 예상과 달리 "MBTI 성격유형과 점성학 성격유형의 관계"를 다룬 교육학 박사논문(2010). 서양 점성술과의 통계적 상관관계 연구라 실전 콘텐츠는 적어 참고용으로만 남김.
- **mbti.pdf** (19p, 김정택 신부 MBTI 강의자료): **16개 유형 전체**에 대해 성격특성·심리적 기능·**진로 및 업무스타일(추천 직업 포함)**·**배우자 및 가족관계 특성**·업무 및 사회활동 특성을 상세 기술. 지금 앱의 `mbtiTypes.ts`(유형당 이모지+닉네임+키워드+한줄특징)보다 훨씬 풍부해서, 특히 "배우자 및 가족관계 특성"은 연애 카테고리에, "진로 및 업무스타일"은 커리어 카테고리에 바로 근거로 쓸 수 있음.

### 7-I-2. 확정된 사양

- **UI 패턴**: 기존 짧은 버전(5~7줄)은 그대로 유지 — 로딩이 길어지는 걸 싫어하는 사용자도 있어 기본 경험은 그대로 둠. 그 아래에 **"심화해석" 버튼을 새로 추가**해서, 누르면 별도로 3배 이상 분량(15~20줄+)의 긴 해석을 추가 생성.
- **적용 범위**: 성격·커리어·연애·재물 4개 카테고리 + 풍수·운세·오행 종합·궁합 종합, 총 **AI 생성형 항목 8개 전부**.
- **근거 자료 반영 수준**:
  - **십신(十神)**: 프롬프트에 "참고하라"는 지침만 주는 게 아니라, `sajuCalculator.ts`에 **실제 십신 계산 로직을 새로 구현**해서 이 사람의 사주 8글자(또는 시간 모름 시 6글자)가 일간 기준으로 각각 무슨 십신인지 정확히 계산 → 그 결과를 AI 프롬프트에 데이터로 전달.
  - **MBTI 16유형 상세 해설**: mbti.pdf 내용을 정적 데이터 파일로 추출해서, 해당 MBTI 유형의 진로·배우자 특성 문단을 AI 프롬프트에 근거로 전달.
- **PDF**: 심화해설까지 전부 자동 생성 대상에 포함(생성 시간이 지금보다 크게 늘어나더라도 포함하기로 확정 — 가장 풍부한 보고서를 지향).

### 7-I-3. 십신(十神) 계산 규칙 (검증 완료)

- **분류 기준**: 일간(기준)과 대상 글자의 오행 관계 × 음양 일치 여부.
  | 오행 관계 | 음양 같음 | 음양 다름 |
  |---|---|---|
  | 같은 오행(비화) | 비견 | 겁재 |
  | 내가 생(我生) | 식신 | 상관 |
  | 내가 극(我剋) | 편재 | 정재 |
  | 나를 극(剋我) | 편관(칠살) | 정관 |
  | 나를 생(生我) | 편인 | 정인(인수) |
- **적용 대상**: 일간을 제외한 연간·연지·월간·월지·일지·시간·시지(시간 모름이면 시간·시지 제외, 6글자만).
- **지지(地支) 처리**: 지지는 지장간의 정기(正氣) 오행·음양을 기준으로 판단. 다행히 정기의 오행은 항상 지지 자체의 오행과 같아서(`EARTHLY_BRANCHES[i].element` 그대로 사용 가능) 오행은 바꿀 필요가 없고, **음양만 예외 처리**가 필요함.
- **음양 예외 (검증됨)**: 자(子)·오(午)·사(巳)·해(亥) 4개 지지만 정기의 음양이 지지 자체의 명목상 음양과 반대임(자→정기 癸=음이지만 지지 자체는 양, 오→정기 丁=음이지만 지지 자체는 양, 사→정기 丙=양이지만 지지 자체는 음, 해→정기 壬=양이지만 지지 자체는 음). 나머지 8개 지지(축인묘진신유술미)는 정기 음양과 지지 명목 음양이 일치하여 그대로 쓰면 됨. → `EARTHLY_BRANCHES` 배열 기준 인덱스 0(자)·5(사)·6(오)·11(해)만 음양을 반전해서 판단.

### 7-I-4. 심화해석과 십신·MBTI 데이터의 연결 방식

- **성격**: 십신 전체 분포(어떤 십신이 몇 개인지, 특히 많거나 아예 없는 것)를 종합 근거로 사용.
- **커리어**: 관성(편관·정관)·식상(식신·상관) 비중을 중심으로 + MBTI "진로 및 업무스타일" 문단.
- **연애**: 십신은 전통적으로 남녀에 따라 배우자궁 십신이 다르게 해석되지만(남명=재성, 여명=관성), 이 앱은 이미 성별을 구분해서 사용하고 있으므로 **성별에 맞는 배우자 상징 십신**(남성=재성, 여성=관성)을 근거로 쓰되, 문구 자체는 성별 고정관념적으로 들리지 않게 순화 + MBTI "배우자 및 가족관계 특성" 문단.
- **재물**: 재성(편재·정재) 비중이 핵심 근거.
- **풍수·운세·오행 종합·궁합 종합**: 십신은 부가 참고 수준(주 근거는 기존처럼 오행 분포·대운/세운·궁합표), MBTI 상세 데이터는 사용하지 않음(이 4개는 MBTI보다 사주 자체가 중심 소재).

### 7-I-5. 데이터/코드 구조

1. `src/utils/sajuCalculator.ts`: `SipsinType`(10가지) 타입 + `calcSipsin(dayStemIdx, yearPillar, monthPillar, dayPillar, hourPillar)` 함수 신규 추가. 반환값은 `SipsinProfile { yearStem, yearBranch, monthStem, monthBranch, dayBranch, hourStem, hourBranch, counts }`(시간 모름 시 `hourStem`/`hourBranch`는 `null`, `counts`는 십신별 개수 집계). `SajuResult`에 `sipsin: SipsinProfile` 필드 추가, `calculateSaju()` 내부에서 계산해 채움.
2. `src/data/mbtiDetailed.ts` 신규: `interface MbtiDetailed { career: string; spouse: string }` 형태로 16유형별 "진로 및 업무스타일"·"배우자 및 가족관계 특성" 문단을 mbti.pdf에서 추출해 정리(ISTP·ISFP는 원문에 배우자 항목이 없어 같은 문서의 기질 설명을 근거로 자연스럽게 합성).
3. `src/utils/geminiApi.ts`: `generateCategoryDeepInterpretation(apiKey, name, gender, mbti, sajuResult, category, userAnswers?)` 하나로 성격/커리어/연애/재물 4개를 처리(기존 `generateCategoryInterpretation()`과 동일하게 `category` 파라미터로 분기), `generateFengShuiDeepInterpretation()`/`generateFortuneDeepInterpretation()`/`generateElementSummaryDeepInterpretation()`/`generateCompatibilitySummaryDeepInterpretation()` 4개 추가로 총 8개 항목 커버. `formatSipsinProfile()`/`buildDeepGrounding()` 헬퍼가 카테고리별로 관련 십신(커리어=관성/식상, 연애=성별에 맞는 재성/관성, 재물=재성)만 추려 프롬프트에 전달.
4. `src/App.tsx`: 8개 섹션 각각에 기존 텍스트 바로 아래 "🔍 심화해석 더보기" 버튼 + 로딩 상태(`categoryDeepLoading` 등) + 캐싱 + 다이어리 저장(🔖) 연동 추가. 캐시 키는 기존 캐시 키 뒤에 `_deep` 접미사(예: `categoryDeepCacheKey()`).
5. `handleDownloadPDF()`: 8개 심화해설도 미생성 시 자동 생성 후 각 리포트 섹션에 "🔍 심화해석/심화해설" 블록으로 함께 포함.

### 7-I-6. 구현 중 발견한 버그와 수정 (검증 완료)

- **문제**: 심화해석을 실제로 Playwright로 생성해보니 결과가 100~140자 수준에서 뚝 끊기고, 내용도 "Tone: Polite, column-style, engaging? Yes." 같은 **영어 사고과정(thinking) 텍스트가 그대로 노출**되는 현상 발견.
- **원인**: `callGeminiPlainApi()`가 쓰는 `MODELS` 배열의 최신 모델(`gemini-3.6-flash` 등)은 최종 답변 전에 내부적으로 "사고(thinking)" 토큰을 소모하는 모델인데, 기존 `maxOutputTokens: 2048`(짧은 해석용으로 책정된 값)이 사고 토큰만으로 다 소진되어 `finishReason: MAX_TOKENS`로 조기 종료 — 실제 답변 본문이 시작되기도 전에 잘림.
- **수정**: `callGeminiPlainApi()`에 `maxOutputTokens`(기본 2048, 기존 호출부는 그대로 유지)·`timeoutMs`(기본 20000ms) 파라미터를 추가하고, 8개 심화해석 호출부에만 `maxOutputTokens: 8192, timeoutMs: 45000`을 지정. 사고 토큰 + 15~20줄 이상의 본문을 모두 감당할 수 있는 여유를 확보.
- **검증**: 수정 후 Playwright로 8개 항목 전부 재생성 — 성격 1619자, 오행 1421자, 운세 1684자, 궁합 1504자, 풍수 2292자 등 모두 정상 길이로 완결된 문장으로 생성됨을 확인. 십신 용어(비견/편재/상관/정재/편관 등)와 MBTI 특성이 실제로 근거로 인용되고, 구체적 장면 예시(직장 회의, 데이트 상황 등)도 포함됨을 텍스트로 직접 확인.

---

## 7-J. 2026-08-02 디자인 톤업 (우주 컨셉 유지 + 밝기 개선) — 구현 완료

> **상태**: ✅ 구현 완료
> **배경**: 사용자가 직접 4가지 디자인 변형 시안(HTML)을 만들어 검토 요청. Playwright로 4개 버전을 실제 렌더링/스크린샷하여 시각적으로 비교 검토.

### 7-J-1. 검토한 4가지 시안

| 버전 | 내용 | 평가 |
|---|---|---|
| v1 은은한 톤업 | 카드 투명도·별 밀도를 최소한으로만 상향, Gowun Batang 세리프 제목 | 안전하지만 변화 체감이 약함 |
| **v2 별빛 강조 (채택)** | 별을 더 크고 진하게(일부 골드색 포함), 카드 배경/테두리를 더 밝게 | 우주 컨셉 유지하며 가장 뚜렷하게 밝아진 체감. 문제 없음 |
| v3 고급 세리프 | Cormorant Garamond 이탤릭 세리프 + 골드 아웃라인 배지 | **기술적 결함 발견**: Cormorant Garamond는 라틴 전용 폰트라 한글 제목에는 적용되지 않고 시스템 기본 세리프(명조체 계열)로 조용히 대체됨 — 의도한 "고급스러움" 대신 오히려 공문서 느낌이 날 위험 |
| v4 (v2+v3 결합) | v2의 밝기 + v3의 세리프 | v3와 동일한 폰트 결함을 그대로 안고 있음 |

### 7-J-2. 확정된 방향

- **v2(별빛 강조)를 채택**. 세리프 폰트를 통한 추가 차별화는 이번엔 보류(적용하려면 Cormorant Garamond 대신 실제 한글 세리프 — 예: Noto Serif KR 굵은 두께 — 로 별도 검토 필요).
- 적용 범위: 입력 화면부터 결과 화면까지 앱 전체의 별 장식·카드 배경/테두리 밝기를 v2 시안 수준으로 통일.

### 7-J-3. 구현 내역

1. `src/App.css` `:root` 변수: `--bg-card`(`rgba(10,12,35,.75)` → `rgba(24,20,58,.82)`), `--bg-card-hover`, `--border`(`rgba(120,100,255,.2)` → `rgba(150,130,255,.32)`), `--border-gold`(`.3` → `.42`)를 v2 방향(밝고 채도 높은 보라)으로 상향. `.glass-card`/`.glass-card-gold`의 `box-shadow`도 글로우를 더 진하게(`inset 0 1px 0 rgba(255,255,255,.05)` → `.09`, 골드 글로우 `.08` → `.14`).
2. `.stars-bg::before`(배경 별 파티클): 점 크기를 전반적으로 1px/1.5px → 1.5px/2px로 키우고, 18개 중 3개를 골드 톤(`rgba(255,220,150,…)`)으로 바꿔 "일부 골드색 포함"을 반영. `filter: drop-shadow(0 0 3px rgba(255,255,255,.35))`를 추가해 전체적으로 더 진하고 반짝이게 처리.
3. 입력 폼 요소(`.form-input`/`.form-select`/`.hour-btn`/`.hour-btn-animal`)도 같은 방향으로 배경·테두리를 밝게 통일 — v2 시안이 "입력 화면부터" 밝아지는 것을 강조했기 때문에 카드뿐 아니라 실제 입력 필드들도 함께 조정.
4. 이미지 카드(`handleDownloadImageCard`)의 별자리 장식은 원래 계획대로 손대지 않음 — 이미 톤업된 골드 별 스타일이라 이번 변경 범위 밖.
5. **검증**: Playwright로 입력 화면 스크린샷 촬영 — 카드 테두리가 뚜렷해지고 별빛이 눈에 띄게 밝아진 것을 시각적으로 확인. `tsc --noEmit`/`npm run build` 클린.

---

## 7-K. 2026-08-03 전체 코드베이스 정밀 검토 + "8. 알려진 이슈" 표 전면 정리 (완료)

> **상태**: ✅ 검토 완료. 전수 검토에서 발견한 8건 + 기존 "8. 알려진 이슈" 표에 쌓여있던 나머지 항목까지 전부 처리(데이터 소스 확보가 필요한 2건 제외). "코드 검토해줘" → "둘 다 처리해둬" → "계획안 이어서 작업해줘" → "다른것도 작업 다 해줘"로 이어진 네 차례 요청을 거치며 순차적으로 진행.
> **배경**: "코드 전체를 오류 없는지 자세히 검토해달라"는 요청으로, `sajuCalculator.ts`/`geminiApi.ts`/`App.tsx`(2848줄)/데이터 파일 전체/`App.css`를 전수 정독.

### 7-K-1. 즉시 수정한 2건 (배포 완료, 커밋 `62de3fd`)

**① PDF 생성 시 저장형 XSS 취약점**
- **문제**: `handleDownloadPDF()`가 이름·MBTI·생년월일·AI 생성 텍스트 전체를 이스케이프 없이 템플릿 문자열로 조립해 `printWindow.document.write(htmlContent)`에 그대로 주입하고 있었음. 이름 입력창(`<input type="text">`)에는 길이·문자 제한이 전혀 없고, "내 기록 불러오기"(`handleImportDiary`)는 `saju_` 접두어 키의 문자열 값이면 내용 검증 없이 그대로 localStorage에 씀. 두 지점을 결합하면: 공격자가 다이어리 북마크의 `snapshot.name`에 스크립트를 심은 백업 JSON 파일을 지인에게 공유 → 피해자가 "불러오기" 후 그 스냅샷으로 "전체 결과 다시 보기" → PDF 저장 시 팝업 창에서 스크립트가 실행되어 `window.opener`(원본 앱 탭, 동일 출처)의 `saju_*` localStorage 전체(이름·생년월일·다이어리 등)를 외부로 유출할 수 있었음.
- **수정**: `escapeHtml()`/`escapeHtmlBreaks()` 헬퍼를 추가해 PDF 템플릿 내 사용자·AI 생성 텍스트 삽입 지점(이름/MBTI/생년월일, `aiIntro`, 카테고리 4종 분석·팩폭·럭키아이템·심화해석, 처방전, 오행/궁합/풍수/운세 종합·심화해설, 사주 4기둥 AI 해설) 전부에 적용. 애니멀/간지 등 고정 데이터(`EARTHLY_BRANCHES` 등에서 파생된 값)는 사용자 입력이 아니므로 이스케이프 대상에서 제외.
- **검증**: `<img src=x onerror=...>`, `<script>...</script>`, `"><script>...`, `</title><script>...<title>` 등 5개 페이로드를 실제 `document.write()` 파이프라인에 통과시켜 전부 스크립트 미실행·순수 텍스트로만 렌더링됨을 Playwright로 직접 확인.

**② AI JSON 응답 파싱 실패 시 무재시도 폴백 버그**
- **문제**: `callGeminiJsonApi()`는 HTTP 요청은 성공했지만 응답 본문이 파싱 가능한 JSON이 아닐 경우(`extractJsonObject()` → `null`) 재시도나 다음 모델 시도 없이 **즉시** `null`을 반환하고 있었음. 호출부(사주 인트로/성격·커리어·연애·재물 해석/처방전/오늘의 나풀이 4곳) 전부 `parsed?.field?.trim() || fallback` 패턴이라 이 경우 에러 없이 조용히 하드코딩된 정형 문구로 대체됨 — 사용자에게는 "짧고 밋밋한 AI 해석"처럼만 보임.
- **원인**: 이번 세션 심화해석 기능 개발 중 실측으로 확인한 것과 동일한 원인. `MODELS` 1순위 모델(`gemini-3.6-flash`)이 사고형(thinking) 모델이라 최종 답변 전에 내부 추론 토큰을 소모하는데, 이 4개 호출은 `maxOutputTokens`가 1024~2048로 책정돼 있어 추론만으로 토큰이 소진되고 `finishReason: MAX_TOKENS`로 조기 종료됨. 심화해석용 `callGeminiPlainApi` 호출은 이 세션 초반에 8192로 상향해 고쳤지만, 원조 4개 JSON 호출에는 같은 수정이 반영되어 있지 않았음. 이 앱의 "심화해석" 기능 자체가 애초에 "해석이 짧다"는 사용자 피드백에서 출발했는데, 그 피드백의 실제 원인이 이 버그였을 가능성이 있음.
- **수정**: `callGeminiJsonApi()`에 `hadSuccessfulResponse` 플래그를 추가해, 파싱 실패를 HTTP 429/503/500과 동일하게 "재시도 대상"으로 취급하도록 재구성(모델·재시도 루프를 그대로 통과시키고, 응답을 한 번이라도 성공적으로 받았다면 최종적으로는 여전히 `null`을 반환해 호출부의 기존 폴백 계약은 유지 — 순수 네트워크 장애로 전량 실패한 경우에만 예외를 던짐). 4개 호출부(`generateSajuIntro`/`generateCategoryInterpretation`/`generatePrescriptions`/`generateDailyFortune`) 모두 `maxOutputTokens: 8192`, `timeoutMs: 45000`으로 상향.
- **검증**: `tsc --noEmit`/`npm run build` 클린. (사고형 모델의 토큰 소진은 확률적으로 재현되는 현상이라 결정론적 재현 테스트는 어려워, 로직 검토 + 동일 원인으로 확인·검증된 심화해석 수정 사례를 근거로 안전성 확인.)

### 7-K-2. 후속 과제 6건 — 전부 수정 완료 (우선순위 순서대로 처리)

아래 순서대로(영향도·리스크·작업량 기준) 이어서 처리, 전부 완료:

1. **[1순위] 캐시 키에 출생시간 반영** ✅ — `App.tsx`에 `baseKeyId(f: CacheKeyBase)` 헬퍼를 신설해 `${name}_${birthYear}${birthMonth}${birthDay}_${hourUnknown ? 'unknown' : birthBranch}` 형태로 통일. `CacheKeyBase` 타입 자체에 `birthBranch`/`hourUnknown`을 추가하고, `fengShuiCacheKey` 등 12개 캐시 키 함수 전부를 이 헬퍼 기반으로 재작성(중복 코드도 함께 정리됨). `result.formData`가 이미 두 필드를 가지고 있어 호출부 변경은 필요 없었음.
2. **[2순위] `solarTerms.ts` 주석 정정** ✅ — 상단 주석을 실제 데이터 범위(1940~2030)로 정정하고, `max="2030"` 하드코딩과 데이터 확장 시 함께 갱신해야 한다는 안내를 주석에 명시. (2031년 이후 출생자를 위한 절기 데이터 자체를 새로 확보해 추가하는 작업은 범위 밖 — 8번 표에 별도 항목으로 유지)
3. **[3순위] 궁합 탭 인해(寅亥) 동시 표시 안내 문구** ✅ — 육합 파트너와 파 파트너의 `branchIdx`가 같을 때(인·해 조합) "육합이면서 동시에 파에도 해당하는 특수 조합"이라는 안내 문구를 궁합 조합표 하단에 조건부로 추가.
4. **[4순위] `App.css` 클래스 중복 정의 정리** ✅ — `.lucky-item-box`/`.prescription-card`의 죽은(완전히 덮어써지던) 첫 번째 정의를 삭제. `.prescription-card`는 삭제 전 실제로 계속 적용되고 있던 `transition: all 0.2s`를 살아남은 정의로 옮겨 시각적 회귀 없이 정리.
5. **[5순위] 교차 필드 날짜 유효성 검증 추가** ✅ — `handleSubmit()`에서 `new Date(year, month-1, day)`로 만든 날짜의 연/월/일이 입력값과 정확히 일치하는지 재검증, 불일치 시(2월 30일 등) 토스트를 띄우고 제출을 막도록 수정.
6. **[6순위] 자시(子時) 2구간 분리 + 야자시 보정 로직 활성화** ✅ — `HOUR_BRANCHES`에 `야자시`(23:00~24:00) 항목을 신설하고 기존 `자시`는 `00:00~01:00`(조자시)으로 시간 표기를 명확히 정정. `calculateSaju()`가 `hourBranch.id === '야자시'`일 때 `calcHour=23`을 실제로 사용하도록 고쳐, 그동안 도달 불가능했던 야자시 다음날 이월 로직을 살렸음. 이 과정에서 발견한 부수 결함(이월된 날짜+`hour=23`을 연주/월주 절기 판정에도 그대로 넘기던 것)도 함께 바로잡아, 연주·월주는 실제 출생 시각(원래 날짜) 기준으로, 일주(日柱)만 다음 날로 이월되도록 분리. `HOUR_BRANCHES[6]` 하드코딩 인덱스 폴백 2곳도 `.find(h => h.id === '오시')`로 교체(배열 순서 변경에도 안전하도록).
   - **검증**: Playwright로 동일 생년월일(1995-09-27)에 대해 자시 선택 시 일주 辛酉, 야자시 선택 시 일주 壬戌(60갑자 순서상 정확히 다음 날)로 서로 다르게 나옴을 확인.

### 7-K-3. 전체 회귀 검증

- `tsc --noEmit`(strict)/`npm run build` 클린.
- Playwright로 5개 시나리오 일괄 검증: 야자시 옵션 노출, 자시/야자시 일주 차이(위 6번 항목), 2월 30일 제출 차단 + 토스트, 인해(寅亥) 안내 문구 노출(1990-02-15, 일지=亥 확인). 전부 통과.

### 7-K-4. "8. 알려진 이슈" 표에 남아있던 나머지 항목 처리 (완료)

"다른 것도 작업 다 해줘" 지시에 따라, 데이터 확보가 필요해 보류한 2건(1930년대·2031년 이후 절기 데이터)을 제외한 나머지 전부를 처리:

1. **시·분 정밀 출생시각 입력** — `calculateSaju()`가 이미 받고 있던 `exactHour`/`exactMinute` 매개변수를 그동안 아무 UI도 연결하지 않고 있었음. `FormData`에 `useExactTime`/`exactHour`/`exactMinute` 필드를 추가하고, 체크하면 12시진 그리드 대신 시(0~23)·분(0~59) 숫자 입력 2칸이 나타나도록 구현("시간 모릅니다"와 상호배타). `calculateSaju()` 내부도 손봐서, 정확한 시각이 주어지면 12시진 버튼 선택과 무관하게 그 시각으로 직접 시진 지지(`hourBranchIdx`)를 산출하도록 수정(기존엔 버튼으로 고른 `hourBranchId`만 신뢰해서, 시각과 버튼이 어긋나면 잘못된 시주가 나올 수 있었음). 제출 시 정확한 시각에 대응하는 12시진 id를 `hourBranchIdFromExactTime()`으로 미리 구해 `birthBranch`에 저장해, 캐시 키·표시 로직 등 기존 코드와 완전히 호환되도록 함. Playwright로 23:30 정확 입력 시 야자시 규칙에 따라 일주가 壬戌로 정확히 이월됨을 확인.
2. **factBomb 등 AI 응답에 프롬프트 지침 문구가 섞여 나오는 문제** — JSON 프롬프트에 예시로 넣어둔 placeholder 문구(예: `"🔥 뼈 때리는 팩폭 한줄평 (존댓말 매운맛)"`)를 모델이 가끔 그대로 앞부분에 반환하는 게 원인. `geminiApi.ts`에 `cleanField()` 헬퍼를 추가해, 응답이 알려진 placeholder로 시작하면 그 부분을 제거하고 남은 내용을 사용(제거 후 내용이 없으면 fallback 사용). `generateSajuIntro`/`generateCategoryInterpretation`/`generateDailyFortune`의 모든 관련 필드에 적용.
3. **전역 ErrorBoundary 부재** — `src/ErrorBoundary.tsx` 신설(클래스 컴포넌트), `main.tsx`에서 `<App />`을 감싸도록 적용. 렌더링 오류 시 흰 화면 대신 "새로고침" 버튼이 있는 복구 UI 표시.
4. **접근성(ARIA) 속성 전무** — 모달 3곳(`selectedModal`/`pillarModal`/`diaryDetail`) 전부에 `role="dialog"` `aria-modal="true"` `aria-label`, 닫기 버튼에 `aria-label="닫기"` 추가. 토스트에 `role="status"` `aria-live="polite"` 추가.
5. **로딩 `useEffect` 의존성 배열 불완전** — 이펙트가 실제로 읽는 `formData`를 의존성 배열에 명시(`[step, formData]`). 로딩 중에는 `formData`가 갱신되지 않아(제출 시점에 고정) 참조가 안 바뀌므로 재실행 없이 동일하게 동작.
6. **다이어리 재방문 시 aiIntro 매번 재생성** — `aiIntroCacheKey()` 추가, 이름+생년월일+시간+MBTI 기준으로 `generateSajuIntro()` 결과를 localStorage에 캐싱. 로딩 시 캐시 확인 → 있으면 API 호출 자체를 건너뜀. Playwright로 캐시 키(`saju_aiintro_...`)가 저장되고 재제출 시 재사용됨을 확인.

1930년대·2031년 이후 절기 데이터 부재 2건은 신뢰할 수 있는 정밀 천문 데이터 소스 확보가 선행 조건이라 이번 범위에서는 1930년대 쪽에 UI 경고 문구만 추가하고 근본 해결은 보류(8번 표에 계속 추적).

---

## 7-L. 2026-08-03 서양 고전점성술(홀사인) 기능 — 구현 완료

> **상태**: ✅ 구현 완료 및 검증 완료.
> **배경**: 사용자가 `고전점성술_기초시트.pdf`(백아, astro.vg)와 `William Lilly, Christian Astrology`(1647, 932p 원전 확인됨)를 학습 요청. "나풀이에 적용할 수 있는 기능"을 "질문을 1개씩" 방식으로 좁혀가다가, 하우스·어센던트까지 포함한 완전한 네이탈 차트로 방향이 정해짐.

### 7-L-1. 참고 자료 요약

- **고전점성술_기초시트.pdf** (4p): 기호·4원소·행성(길흉/조이 하우스)·섹트(주간/야간)·별자리(원소/모달리티/성별/지배행성/신체부위)·본질적 품위(도머사일/익절테이션/디트리먼트/폴)·하우스(의미/조이/앵귤러·석시던트·케이던트)·5대 애스펙트를 정리한 실전용 요약표. 애스펙트 계산법이 "3번째 사인" 식으로 표기되어 있어 **홀사인(Whole Sign) 하우스 시스템**을 전제로 함 — 최근 대중적인 플라시더스 하우스와는 다른, 고전/헬레니스틱 전통점성술 계열.
- **William Lilly, Christian Astrology** (1647/1659, 932p 확인): 영어권 전통점성술의 원전. 1권=입문(에페메리스·차트작성·본질적/우발적 품위), 2권=호라리(하우스별 판단 사례 대량 수록), 3권=네이탯(출생차트 판단·교정·디렉션/프로펙션/리볼루션/트랜짓). 위 치트시트 개념들의 이론적 근거.

### 7-L-2. 확정된 사양

- **적용 범위**: 행성 별자리 + **하우스·어센던트까지 완전하게**. 출생 도시(좌표) 입력을 폼에 새로 추가.
- **하우스 시스템**: 홀사인(Whole Sign) — 어센던트가 속한 별자리를 1하우스로 삼아 순서대로 한 별자리 = 한 하우스. 플라시더스 등 복잡한 반복계산 시스템은 채택하지 않음(치트시트가 이미 홀사인 전제).
- **좌표 데이터**: 외부 지오코딩 API 없이 주요 한국 도시(약 40곳)를 좌표와 함께 하드코딩한 드롭다운 — 기존 `solarTerms.ts`처럼 자체 완결적인 방식 유지. 목록에 없는 지역은 가장 가까운 도시 선택 안내.
- **행성 위치 계산**: `astronomy-engine`(MIT 라이선스, 의존성 없는 순수 TS/JS 천문 계산 라이브러리) npm 패키지 사용. 태양·달·수성·금성·화성·목성·토성 7개 전통 행성만 다룸(천왕성/해왕성/명왕성은 고전점성술 범위 밖이라 제외).
- **UI 배치**: 기존 상단 탭(운세/나풀이 해석/궁합/풍수)에 **"🌌 별자리"** 탭 신규 추가.

### 7-L-3. 기술 검증 (완료 — 실측 대조)

- **문제**: `astronomy-engine`의 `EclipticLongitude(body, date)`는 문서상 **태양중심(heliocentric)** 황경을 반환 — 점성술에 필요한 **지구중심(geocentric)** 황경과 다름(특히 달·화성·목성·토성은 값이 크게 어긋남). 검증 과정에서 직접 발견.
- **수정**: 태양은 `SunPosition(date).elon`, 달은 `EclipticGeoMoon(date).lon`, 나머지 행성은 `GeoVector(body, date, true)` → `Ecliptic(vector).elon`으로 지구중심 황경을 구하도록 확정.
- **교차검증 방법**: 완전히 독립된 두 번째 경로로 Python `skyfield` + **NASA JPL 공식 천체력(DE421)**을 로컬에 내려받아 동일한 시각(1995-09-27 02:30 UT, 서울 좌표)의 어센던트·7행성 위치를 별도로 계산해 대조.
- **결과**: 어센던트·태양·달·수성·금성·화성·목성·토성 전부 **0.002° 이내(약 7각초) 일치**. 점성술에 필요한 정밀도(1° 단위)보다 수천 배 정확 — 안전하게 채택.
- **어센던트 공식**(검증됨): `tan(ASC) = cos(RAMC) / (−sin(ε)·tan(φ) − cos(ε)·sin(RAMC))`, RAMC = 그리니치 항성시(`SiderealTime()`)×15 + 경도, ε = 진짜 황도경사(`e_tilt().tobl`), φ = 위도.

### 7-L-4. 알려진 정밀도 한계 (미해결, 문서화)

- **한국 하절기(서머타임) 보정 미반영**: 대한민국은 1987~1988년(88올림픽 대비, 5월~10월)에 서머타임을 시행했고, 1948~1960년 사이에도 간헐적으로 시행한 기록이 있음. 어센던트는 4분마다 약 1° 이동할 정도로 시각에 민감해, 서머타임 기간 출생자는 실제와 최대 1시간 어긋난 시각으로 계산되면 어센던트가 다른 별자리로 나올 수 있음. 1987~1988년은 날짜가 명확히 알려져 있어 보정 테이블을 추가할 수 있지만, 1948~1960년은 연도별 정확한 날짜를 신뢰성 있게 확인할 방법이 없어(웹 접근 불가) 이번 범위에서는 **1987~1988년만 보정하고 1948~1960년은 미보정 상태로 남김** — UI에 안내 문구로 고지.
- **서머타임 보정이 일(日) 단위**: `isKnownDstDate()`는 시각이 아닌 날짜만 비교해 전환일(1987-05-10/10-11, 1988-05-08/10-09) 전체를 동일하게 처리함. 실제 전환은 해당일 특정 시각(새벽)에 발생했을 것이므로, 전환일 새벽 시간대 출생자는 표준시/서머타임 판정이 실제와 어긋날 수 있음. 정확한 전환 시각을 신뢰성 있게 확인할 방법이 없어(위와 동일한 사유) 수정하지 않고 한계로 남김 — 2026-08-04 전체 코드 재검토(7-M)에서 발견.

### 7-L-5. 데이터/코드 구조 (구현 완료)

1. `package.json`: `astronomy-engine`(v2.1.19) dependency 추가 (신규 취약점 없음, 기존 vite/postcss 관련 2건은 무관한 기존 이슈).
2. `src/utils/astrologyCalculator.ts` 신규:
   - `KOREAN_CITIES`(41개 주요 도시 좌표), `ZODIAC_SIGNS`(12별자리 원소/모달리티/성별/지배행성/키워드/신체부위), `PLANETS`(7행성 길흉/조이 하우스), `DIGNITY 테이블`(도머사일/익절/디트리먼트/폴), `HOUSES`(1~12하우스 의미/앵귤러 강도) — 치트시트 표 그대로 정적 데이터화.
   - `calculateAstrology(year, month, day, hour, minute, lat, lon, timeConfidence)` — 어센던트(검증된 공식), 7행성의 지구중심 황경·별자리·도수·홀사인 하우스·본질적 품위, 섹트(태양의 하우스로 주간/야간 판정), 5대 애스펙트(오브 6도 균일 적용) 산출.
   - `isKnownDstDate()`/`toUtcDate()` — 1987/1988년 서머타임 보정 반영한 KST→UTC 변환.
3. `src/utils/geminiApi.ts`: `generateAstrologyInterpretation()`(analysis/factBomb/luckyItem, 기존 카테고리 해석과 동일 패턴) / `generateAstrologyDeepInterpretation()`(심화, 하우스 전체 배치+애스펙트 반영) 추가.
4. `src/App.tsx`:
   - `FormData`에 `birthCity` 필드 추가(기본값 '서울'), 입력 폼에 41개 도시 드롭다운 신설.
   - 상단 탭에 **"🪐 별자리"** 추가(5번째 탭). 어센던트 카드(골드 강조)/행성 배치표(별자리·도수·하우스·품위)/하우스표(1~12H 홀사인)/애스펙트 목록/AI 종합 해설(팩폭+럭키아이템)/심화해석 버튼으로 구성.
   - 로딩 단계에서 `timeConfidence`('exact'/'approximate'/'unknown')를 판정해 어센던트 카드에 정밀도 경고 문구 조건부 표시.
   - 캐시 키(`astrologyCacheKey`/`astrologyDeepCacheKey`)에 `birthCity`를 추가 반영(도시가 바뀌면 결과가 달라지므로 기존 `baseKeyId`와 별개 처리).

### 7-L-6. 최종 검증 (완료)

- `tsc --noEmit`(strict)/`npm run build` 클린 (번들 크기 263KB→331KB, `astronomy-engine` 추가분).
- **계산 정확도 재확인**: 실제 앱 코드(`astrologyCalculator.ts`)를 `tsx`로 직접 실행해 7-L-3에서 Python `skyfield`+NASA JPL로 검증한 것과 동일한 테스트 케이스(1995-09-27 11:30, 서울)를 대조 — 어센던트·7행성 위치·품위 판정(태양·달 폴, 금성·화성·목성 도머사일)·애스펙트(수성-금성 컨정션, 금성-목성 섹스타일) 전부 사전 검증값과 정확히 일치.
- **Playwright 엔드투엔드 검증**: 도시 드롭다운 노출 → 정확한 시:분(11:30)+서울로 제출 → "🪐 별자리" 탭에서 어센던트(사수자리 4.6°)·행성 배치·하우스 배치·태양 별자리(천칭자리) 실제 렌더링 확인 → AI 종합 해설 생성(analysis/factBomb/luckyItem) → 심화해석 생성(2,359자, 하우스·품위·애스펙트를 구체적으로 인용하는 내용) 전부 확인. 전 항목 통과.
- **배포 완료**: 커밋 `6aa44b6`, `origin main`에 푸시 완료(Vercel 자동 배포).
- **후속 발견 및 수정 — PDF 리포트 누락**: 전체 코드 재검토 중 `handleDownloadPDF()`가 오행·궁합·사주기둥·운세·풍수는 자동 생성해 포함하면서 새로 추가한 별자리(어센던트·행성·하우스·애스펙트·AI 해설)는 리포트에서 빠뜨리고 있던 것을 발견. `astrologyForPdf`/`astrologyDeepForPdf` 자동생성과 `astrologyHtml` 블록을 추가해 다른 섹션과 동일한 패턴으로 PDF에 포함시킴. Gemini API 레이트리밋(429/503) 때문에 실제 API로는 엔드투엔드 검증이 너무 오래 걸려, Playwright에서 `generativelanguage.googleapis.com` 요청을 목업 응답으로 가로채는 방식으로 검증 — PDF 팝업에 "서양 고전점성술", "어센던트(상승궁)", "홀사인", "나풀이 별자리 종합 해설" 텍스트와 목업 AI 텍스트가 전부 정상 렌더링됨을 스크린샷으로 확인.

---

## 7-M. 2026-08-04 전체 코드베이스 정밀 재검토 (완료)

> **배경**: 사용자가 "시간이 오래 걸려도 좋으니 전체적으로 잘못된 것 있는지 세밀하게 검토해달라"고 요청. `sajuCalculator.ts`/`solarTerms.ts`/`compatibility.ts`(사주 엔진+데이터), `astrologyCalculator.ts`/`geminiApi.ts`(별자리 엔진+AI 연동), `App.tsx` 상태·핸들러 절반, `App.tsx` JSX·`App.css`(UI/스타일) 4개 영역으로 나눠 서브에이전트 4개를 병렬로 실행해 정밀 검토하고, 각 발견 사항을 코드로 직접 재확인한 뒤 실제 버그만 수정.

### 7-M-1. 발견 및 수정한 실제 버그 3건

| 버그 | 파일 | 내용 | 수정 |
|------|------|------|------|
| 🔴 1930년대 출생자 월주 오류 | `src/data/solarTerms.ts:166,175` | 절기 데이터가 없는 연도(1930~1939, 입력 폼은 1930년부터 허용)의 월지 폴백 공식이 `(month+1)%12`로 되어 있어 실제보다 한 달(지지 하나) 밀려 계산됨. 예: 1935년 6월생은 오월(午月)이어야 하는데 미월(未月)로 계산되어 월주 천간·지지, 오행 분포, 십신까지 연쇄적으로 왜곡. | `SOLAR_TERMS_BRANCH_IDX[month-1]`와 동일한 규칙인 `month % 12`로 수정. |
| 🔴 Gemini 심화해설 빈 응답이 재시도/폴백 없이 성공 처리 | `src/utils/geminiApi.ts:1035-1039` | `callGeminiPlainApi`(심화해설 9종에 사용)가 HTTP 200이지만 콘텐츠가 비어있는 응답(세이프티 필터 등)을 받으면 재시도나 `fallbackText` 없이 빈 문자열을 그대로 반환 — "심화해석 더보기" 클릭 시 아무 반응 없이 버튼만 남는 증상. | `callGeminiJsonApi`와 동일한 패턴으로, 빈 응답을 네트워크 오류와 동일하게 재시도하도록 수정. |
| 🟡 별자리 캐시가 출생 "분(分)" 단위 미반영 | `src/App.tsx:230-237` | 정확한 시:분 입력 시 어센던트는 분 단위로 계속 이동하는데, 캐시 키는 2시간 단위 시진(時辰)까지만 구분해 같은 시진 안에서 시각을 수정해 재생성하면(예: 14:05→14:55) 화면 수치는 새로 계산되지만 AI 해설 텍스트는 예전 캐시가 그대로 노출됨. | `astrologyCacheKey`/`astrologyDeepCacheKey`가 `useExactTime`이 true일 때 `exactHour:exactMinute`을 캐시 키에 추가로 반영하도록 수정. |

### 7-M-2. 정리한 사소한 항목

- `App.tsx:2065` 성별 선택 버튼 라벨의 삼항연산자가 항상 빈 문자열(`'' : ''`)이던 죽은 코드 제거.
- PDF 템플릿에서 `mbtiInfo.coreTrait`만 `escapeHtml` 누락(다른 모든 필드는 적용됨) — 정적 데이터라 실제 위험은 없었지만 "모든 텍스트는 이스케이프" 원칙 일관성을 위해 적용.
- 결과 화면 섹션 탭(운세/나풀이 해석/궁합/풍수/별자리)과 나풀이 해석 하위 탭(성격/커리어/연애/재물/처방전)에 `role="tablist"`/`role="tab"`/`aria-selected` 추가, 보관함 삭제 버튼(🗑)에 `aria-label="기록 삭제"` 추가 — 이미 모달/토스트에 적용된 접근성 패턴과 일관되게 정리.
- `App.css`에서 실제 사용처가 없는 죽은 클래스 정리: `.ai-card*`(7종)/`.ai-cards-grid`, `.report-tab-btn*`(3종), `.fire-factbomb-*`(3종, 실사용 중인 `.fact-bomb-*`와는 별개), `.api-key-*`(5종), `.ai-loading*`(2종, `@keyframes dot-bounce` 포함), `.prescription-item*`(2종, 실사용 중인 `.prescription-card`와는 별개) — App.tsx 전체에서 참조 0건 확인 후 삭제.
- 계획안.md의 "39개 도시" 표기를 실제 배열 길이(41개)로 정정.

### 7-M-3. 문서화만 하고 수정하지 않은 엣지케이스 (근거: 발생 빈도 매우 낮음 / 정확한 데이터 검증 불가)

- **운세(대운/세운) 캐시가 대운 교체 시점을 못 잡음** (`App.tsx:192-194`): `unseCacheKey`가 연도로만 키가 잡혀 있어, 생일이 대운 교체와 겹치는 해에 생일 전/후로 각각 재생성하면 캐시된 AI 해설이 새 대운을 반영하지 못할 수 있음. 화면의 대운/세운 표 자체는 매 렌더링마다 새로 계산되어 항상 정확 — AI 해설 텍스트만 드물게 어긋날 수 있는 좁은 범위의 문제.
- **서머타임 보정이 일(日) 단위**: 7-L-4에 추가 기록.

### 7-M-4. 검증

- `tsc --noEmit`/`npm run build` 클린.
- Playwright로 (1) 1935-06-15 등 1930년대 생년월일 입력 시 월주가 올바르게(오월) 계산되는지, (2) 정확한 시:분을 같은 시진 내에서 수정 후 재생성 시 캐시 키가 분리되어 새 해설이 생성되는지, (3) 성별/탭 UI가 시각적으로 회귀 없이 정상 동작하는지 확인.
- 커밋 후 `origin main`에 배포.

---

## 7-N. 2026-08-05 클라우드 동기화 + 오늘의 트랜짓 운세 + 오늘의 타로 — 구현 완료

> **배경**: "필요한 기능이 뭐가 있을까" 브레인스토밍에서 사용자가 3가지를 지목: (1) 기기 바꾸면 기록이 날아가는 문제(클라우드 동기화), (2) 이미 만들어둔 서양점성술 엔진을 활용한 오늘의 트랜짓 운세, (3) 가벼운 재미용 타로 결합. "질문을 1개씩" 방식으로 세부 사양을 확정한 뒤 구현.

### 7-N-1. 확정된 사양

- **클라우드 동기화**: Firebase(Auth+Firestore) 채택, **로그인은 선택사항**(로그인 안 해도 기존처럼 localStorage로 완전 동작), 동기화 대상은 **다이어리(저장한 기록)만**(AI 캐시는 동기화 안 함, 새 기기에서 재생성됨), 로그인 시 로컬↔클라우드 기록을 **id 기준으로 합쳐서**(합집합) 데이터 손실 방지.
- **오늘의 트랜짓 운세**: "🪐 별자리" 탭 안에 새 카드로 추가(기존 "오늘의 나풀이"와는 별개 유지).
- **오늘의 타로**: "🌌 운세" 탭에 카드 추가, 하루 한 번 버튼을 눌러 뽑는 방식(같은 날 재요청 시 같은 카드).

### 7-N-2. 데이터/코드 구조

1. `package.json`: `firebase`(v12) 의존성 추가.
2. `.env`: `VITE_FIREBASE_*` 6개 변수 추가(사용자가 Firebase 콘솔에서 직접 프로젝트 생성 후 발급받은 값 — API 키 자체는 클라이언트에 공개되어도 안전한 값이며, 실제 접근 제어는 Firestore 보안 규칙으로 별도 수행).
3. `src/firebase.ts` 신규: `initializeApp`/`getAuth`/`getFirestore` — 환경변수가 없으면 `null`을 export해 클라우드 기능이 죽어도 앱 전체가 죽지 않도록 방어.
4. `src/utils/cloudSync.ts` 신규: `signInWithGoogle`/`signOutUser`/`subscribeToAuthState`(구글 로그인 팝업, Firebase Auth 상태 구독), `mergeBookmarks`(id 기준 합집합 병합, 제네릭), `fetchCloudBookmarks`/`pushBookmarksToCloud`(Firestore `users/{uid}` 문서에 다이어리 배열 전체를 저장).
5. `src/utils/astrologyCalculator.ts`: `calculateTodayTransits(natal, now)` 추가 — 오늘 실제 태양·달·수성·금성·화성(빠르게 움직이는 5개 행성만, 목성/토성은 하루 단위 변화가 거의 없어 제외)의 지구중심 황경을 계산해, 네이탈 차트의 태양·달·어센던트와 이루는 각도를 기존 5대 애스펙트 로직으로 판정.
6. `src/data/tarotCards.ts` 신규: 메이저 아르카나 22장(정/역방향 의미 포함) + `drawDailyTarotCard(seed)` — 이름+생년월일+오늘 날짜를 시드로 한 결정론적 해시 뽑기(같은 사람이 같은 날 다시 눌러도 항상 같은 카드/방향).
7. `src/utils/geminiApi.ts`: `generateTransitInterpretation()`(오늘의 트랜짓 요약+팩폭, JSON), `generateTarotInterpretation()`(MBTI 맥락을 섞은 가벼운 톤의 카드 해석, 일반 텍스트) 추가.
8. `src/App.tsx`:
   - 로그인 상태(`currentUser`)를 구독하고, 로그인 시 로컬↔클라우드 다이어리 기록을 병합해 양쪽에 다시 씀. `addBookmark`/`removeBookmark`가 로그인 상태면 자동으로 클라우드에도 반영.
   - 다이어리 화면에 로그인/로그아웃 UI와 동기화 상태 안내 카드 추가.
   - 별자리 탭에 "오늘의 트랜짓" 카드, 운세 탭에 "오늘의 타로" 카드 추가(기존 "오늘의 나풀이"와 동일한 UI/캐싱 패턴 — analysis+factBomb JSON, `localStorage` 날짜별 캐시).

### 7-N-3. 성능 이슈 발견 및 수정 — 번들 크기

- **문제**: `firebase` 패키지를 일반 import로 추가하자 프로덕션 번들이 263KB→1MB(gzip 123KB→293KB)로 4배 가까이 커짐. 로그인 기능을 쓰지 않는 대다수 사용자도 이 무거운 코드를 전부 내려받아야 하는 상태였음(모바일 앱이라 초기 로딩 속도가 특히 중요).
- **수정**: `src/utils/cloudSync.ts`(및 그 안에서만 참조되는 `firebase.ts`)를 `import()` 동적 임포트로 전환해 별도 청크로 분리. 로그인 상태 확인이 필요한 시점(앱 마운트 시 1회)에만 비동기로 불러오되, 이 로딩이 메인 번들의 렌더링을 막지 않도록 처리.
- **결과**: 메인 번들 345KB(gzip 128KB, 트랜짓/타로 코드 포함해도 기존과 거의 동일)로 복귀, Firebase는 `cloudSync-*.js` 658KB(gzip 165KB) 별도 청크로 분리되어 클라우드 기능을 실제로 쓸 때만 네트워크 요청됨.

### 7-N-4. Firestore 보안 규칙 (사용자 조치 완료)

- Firestore를 "테스트 모드"로 생성하면 기본적으로 인증 여부와 무관하게 전체 컬렉션을 아무나 읽고 쓸 수 있어, 다이어리에 포함된 이름·생년월일 등 개인정보가 노출될 위험이 있었음.
- 아래 규칙을 Firebase 콘솔(Firestore Database → 규칙)에 게시하도록 안내해 **로그인한 본인의 `users/{uid}` 문서만 본인이 읽고 쓸 수 있도록** 제한 — 사용자가 직접 게시 완료 확인.

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 7-N-5. 검증

- `tsc --noEmit`/`npm run build` 클린(동적 임포트 분리 후 번들 크기 확인 포함).
- Playwright(Gemini API 목업 응답)로: 오늘의 타로 뽑기 → 카드명·정/역방향·AI 해석 렌더링 및 다이어리 자동 저장 확인, 별자리 탭 "오늘의 트랜짓 보기" → AI 해석 카드 렌더링 확인, 다이어리 화면에 "구글로 로그인" 버튼이 노출되는지(=Firebase 설정값이 실제로 유효하게 초기화되어 `cloudSyncAvailable`이 `true`가 되는지) 확인.
- 실제 구글 로그인 팝업/OAuth 플로우 자체는 헤드리스 자동화로 완전히 검증하기 어려운 영역이라(구글 자체의 자동화 차단), 코드 리뷰 + 위 자동화 가능한 범위까지만 확인했다는 한계를 문서화. Firestore 보안 규칙 게시는 사용자가 직접 확인.
- **배포**: 커밋 후 `origin main`에 푸시.

---

## 7-O. 2026-08-05 앱스토어 출시 준비 — ✅ 1차 구현 완료 (검증됨)

> **배경**: 사용자가 앱스토어(iOS/Android) 출시를 계획 중이라, 지금까지 만든 기능을 앱스토어 심사·보안 관점에서 검토해달라고 요청. 검토 결과를 우선순위별로 정리해 실행. 중간에 한 번 "여기까지만 하고 문서화해달라"는 지시로 아이콘 생성 직전에 중단했다가, 이후 "이어서 진행해줘" 지시로 아이콘/스플래시 생성 → 플랫폼별 리소스 자동 생성 → 최종 검증까지 완료.

### 7-O-1. 검토에서 발견한 항목 (우선순위순) — 최종 상태

| 우선순위 | 항목 | 상태 |
|---|---|---|
| 🔴 필수 | 계정 삭제 기능 부재 (Apple 가이드라인 5.1.1(v)) | ✅ 완료 |
| 🔴 필수 | 개인정보처리방침 부재 | ✅ 완료 |
| 🔴 필수 | "재미/참고용" 고지 문구 부재 (Apple 가이드라인 1.4.3 부근) | ✅ 완료 |
| 🔴 필수 | 네이티브 앱 셸(Capacitor) 부재 | ✅ 설치·초기화·아이콘/스플래시까지 완료(실제 빌드/서명/제출은 사용자가 Xcode·Android Studio에서 직접 진행해야 함 — 7-O-2-5 참고) |
| 🟠 위험 | Gemini API 키가 클라이언트 번들에 노출됨 | ✅ 코드 전환 완료, ⚠️ Vercel 배포 반영·키 회전은 사용자 조치 필요(7-O-4 참고) |
| 🟡 UX | "오늘의 OO" 카드 3개가 서로 다른 탭에 분산(오늘의 나풀이/타로는 운세 탭, 오늘의 트랜짓은 별자리 탭), 운세 탭에 콘텐츠 과밀 | ❌ 미착수 — 사용자와 재논의 필요(디자인 결정 사항, 이번 스코프에서 제외) |
| 🟢 권장 | `package.json` 버전 고정, 카카오 SDK 네이티브 웹뷰 재검증, 오프라인 처리 | ❌ 미착수 |

### 7-O-2. 완료된 작업

1. **계정 삭제**: `src/utils/cloudSync.ts`에 `deleteAccount(user)` 추가 — Firestore `users/{uid}` 문서 삭제 후 Firebase Auth 계정 삭제. `auth/requires-recent-login` 에러 시 구글 재인증 팝업 후 재시도. `src/App.tsx`에 `handleDeleteAccount` 핸들러와 다이어리 화면 로그인 카드에 "계정 삭제" 버튼(빨간색, `window.confirm` 확인 후 실행) 추가. **로컬 다이어리 기록은 계정 삭제와 무관하게 유지됨**(로컬 저장은 로그인과 독립적으로 동작하는 설계이므로).
2. **개인정보처리방침·이용약관**: `public/privacy.html`, `public/terms.html` 신규 — Vercel이 정적 파일로 그대로 서빙하므로 배포 후 `/privacy.html`, `/terms.html`로 고정 URL 접근 가능(앱스토어 제출 시 URL 입력란에 사용). 수집 항목/이용목적/보관위치/제3자 위탁(Google Gemini·Firebase)/이용자 권리(계정삭제)/문의처(dsd1310@gmail.com)를 명시. 이용약관에는 "오락 목적, 의학적·법률적·재정적 조언 아님" 고지 포함.
3. **재미 고지 문구**: 입력 폼 화면(첫 화면) 제출 버튼 바로 아래에 "🔮 나풀이의 모든 해석은 재미와 자기 이해를 위한 참고용 콘텐츠이며, 의학적·법률적·재정적 조언을 대체하지 않아요" 문구 + 개인정보처리방침/이용약관 링크 상시 노출.
4. **Gemini API 키 서버리스 프록시 전환**:
   - `api/gemini.ts` 신규 — Vercel 서버리스 함수(Node 런타임, `req.query.model`로 모델명을 받아 서버 환경변수 `GEMINI_API_KEY`(VITE_ 접두어 없음)로 Google API를 대신 호출해 결과를 그대로 반환하는 얇은 프록시).
   - `src/utils/geminiApi.ts`의 `callGeminiJsonApi`/`callGeminiPlainApi`가 더 이상 `https://generativelanguage.googleapis.com/...?key=...`를 직접 호출하지 않고 `/api/gemini?model=...`(같은 출처, 키 없음)를 호출하도록 변경. `apiKey` 매개변수는 `_apiKey`로 이름만 남기고(호출부 시그니처는 그대로 유지해 App.tsx의 15개+ 호출부를 건드리지 않음) 실제로는 사용하지 않음.
   - `src/App.tsx`의 `GEMINI_API_KEY` 상수를 `'server-managed'` 문자열로 변경(기존 `if (!GEMINI_API_KEY)` 활성화 검사 로직과의 하위 호환 유지용, 실제 키 값 아님).
   - `.env`: `VITE_GEMINI_API_KEY` → `GEMINI_API_KEY`(접두어 제거, 서버 전용)로 이름 변경. **주석으로 기존 키 값이 이미 노출된 것으로 간주해 회전(재발급) 권장을 명시해둠.**
   - **⚠️ 아직 안 된 것**: (a) Vercel 대시보드에 새 환경변수 `GEMINI_API_KEY`(서버용, VITE_ 없음) 추가 + 기존 `VITE_GEMINI_API_KEY` 삭제 + 재배포, (b) Google AI Studio에서 새 Gemini 키를 발급해 교체(기존 키는 지금까지 프로덕션 번들에 노출되어 있었으므로 유출된 것으로 간주해야 함), (c) `/api/gemini` 프록시의 실제 동작을 Playwright로 검증(로컬 `vite dev`는 Vercel 서버리스 함수를 실행하지 않으므로, 이번 세션에서 테스트할 때는 목업 대상을 `generativelanguage.googleapis.com`에서 `/api/gemini`로 바꿔야 함 — 실제 종단 검증은 배포 후에만 가능).
5. **Capacitor 설치/초기화 + 아이콘/스플래시**:
   - `npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/ios` 완료.
   - `npx cap init "나풀이" "com.napuli.app" --web-dir dist` → `capacitor.config.ts` 생성(appId `com.napuli.app`는 임시값 — **앱스토어에 실제 등록하기 전에 확정해야 하며, 한 번 스토어에 등록하면 사실상 변경 불가**하므로 실제 제출 전 최종 확정 필요).
   - `npm run build` 후 `npx cap add android`, `npx cap add ios` 둘 다 성공 — `android/`, `ios/` 네이티브 프로젝트 스캐폴드가 저장소에 생성됨(Git 커밋 대상, `node_modules`와 달리 버전관리 포함이 표준 관행).
   - iOS는 스캐폴드 생성 자체는 Windows에서도 성공했지만(Capacitor 8이 SwiftPM 기반이라 CocoaPods 불필요), **실제 iOS 빌드/실행/서명은 macOS+Xcode에서만 가능**(Apple 정책상 Windows에서는 원천적으로 불가능) — 이 환경(Windows)에서는 검증 불가.
   - **아이콘/스플래시**: 기존 `NapuliMark`(크리스탈볼+별) 로고를 기반으로 나풀이 브랜드 컬러(`#050510`/`#8b5cf6`/`#f5c842`) 배경을 채운 1024×1024 SVG 소스를 만들고, Playwright로 `assets/icon.png`(1024×1024)·`assets/splash.png`(2732×2732)를 렌더링. `@capacitor/assets`(일회성 devDependency, 사용 후 즉시 제거)로 `npx capacitor-assets generate` 실행 → Android(적응형 아이콘 포함 87개 파일)·iOS(단일 1024 사이즈, 최신 Xcode 규격)·PWA(웹용 아이콘 7종, `public/icons/`로 이동 배치 — 향후 PWA 설치 지원 시 바로 쓸 수 있도록 보관) 리소스 전부 자동 생성. 도구가 함께 만든 `manifest.webmanifest`는 아이콘 배열만 있는 불완전한 조각(이름/시작 URL 등 누락, 경로도 이동 후 어긋남)이라 그대로 두면 오해의 소지가 있어 삭제 — PWA 설치 지원을 실제로 원할 때 처음부터 제대로 된 `manifest.json`을 작성해야 함(이번 스코프에 포함 안 됨). `npx cap sync`로 최신 웹 빌드도 함께 반영.
   - `@capacitor/assets`가 일시적으로 끌고 온 `sharp`/`tar`/`minimatch` 관련 high~critical 취약점(전부 개발 도구 체인 한정, 배포 번들에는 포함 안 됨)은 도구를 쓰고 나서 바로 `npm uninstall`해 제거 — 최종 `npm audit`는 기존 vite/postcss/@capacitor-cli 관련 6건(5 moderate, 1 high, 전부 사전 확인된 devDependency 이슈)만 남음.

### 7-O-3. 최종 검증

- `tsc --noEmit` / `npm run build` 클린(메인 번들 345KB, `cloudSync` 지연로딩 청크 659KB — 7-N에서 확인한 수치와 동일하게 유지).
- `npx cap sync`로 `android/`·`ios/`에 최신 웹 빌드 + 아이콘/스플래시 리소스 반영 확인.
- Playwright(네트워크 목업)로: (1) `/privacy.html`·`/terms.html`이 실제로 서빙되고 제목·문의처 이메일·오락목적 고지가 렌더링되는지, (2) 입력 화면에 재미 고지 문구와 개인정보처리방침 링크가 노출되는지, (3) **가장 중요한 확인 — 결과 화면 진입 시 AI 해석 요청이 `https://generativelanguage.googleapis.com`으로 직접 나가지 않고 `/api/gemini`로만 나가는지**(직접 호출 발생 시 실패하도록 감시 라우트를 별도로 걸어 회귀 여부 확인, 결과: 직접 호출 0건), (4) 다이어리 화면에 로그인 버튼이 정상 노출되는지 — 전부 통과.
- 계정 삭제 버튼의 실제 Firebase 계정 삭제 동작은 (이전 세션과 동일하게) 실제 구글 로그인 세션이 필요해 헤드리스 자동화로 끝까지 검증하기 어려운 영역 — 코드 리뷰 수준까지만 확인.

### 7-O-4. ⚠️ 사용자가 직접 해야 하는 후속 조치 (코드로 대신할 수 없는 것들)

1. ~~Vercel 환경변수 정리 + 재배포~~ — ✅ 완료(서버 전용 `GEMINI_API_KEY` 추가, 기존 `VITE_GEMINI_API_KEY` 삭제, 재배포까지 사용자가 직접 확인).
2. ~~Gemini API 키 회전(재발급)~~ — ✅ 완료(Google AI Studio에서 새 키 발급 → Vercel·로컬 `.env` 모두 교체, 기존 노출됐던 키는 폐기).
3. **`capacitor.config.ts`의 `appId`(`com.napuli.app`) 최종 확정**: 앱스토어에 한 번 등록하면 사실상 변경 불가한 값이므로, 실제 제출 전에 원하는 값인지 재확인 필요.
4. **iOS 실제 빌드/실행/서명**: macOS + Xcode 환경에서 `ios/App/App.xcworkspace`(SwiftPM 기반이라 `.xcworkspace` 대신 `.xcodeproj`를 직접 열어도 됨)를 열어 진행 — 이 환경(Windows)에서는 대신할 수 없음.
5. **Android 실제 빌드/서명**: Android Studio에서 `android/` 폴더를 열어 서명된 APK/AAB 빌드.
6. **개발자 계정 가입**: Apple Developer Program(연 $99), Google Play Console(1회 $25) — 본인 명의 결제/가입 필요.
7. ~~"오늘의 OO" 카드 3개 UX 재배치~~ — ✅ 완료(섹션 7-P 참고).

---

## 7-P. 2026-08-05(2차) 백엔드 정밀 재검토 + "오늘" 탭 통합 — 완료

> **배경**: 7-O 작업 완료 후 사용자가 "나머지 작업도 해줘"라고 요청 — 처음엔 범위가 불명확해 Q&A로 좁힌 결과 (1) 최근 추가된 코드(클라우드 동기화/Gemini 프록시/트랜짓/타로)에 대한 정밀 재검토, (2) 7-O에서 스코프 제외했던 "오늘의 OO" 카드 3개 UX 재배치, 이 두 가지로 확정됨.

### 7-P-1. 정밀 재검토로 발견 및 수정한 버그 5건

서브에이전트 2개(백엔드: cloudSync/firebase/api-proxy/트랜짓/타로 vs App.tsx 신규 연동부)로 나눠 병렬 검토 후 전부 직접 재확인.

| 심각도 | 항목 | 수정 내용 |
|---|---|---|
| 🔴 높음 | `api/gemini.ts`가 인증 없는 오픈 릴레이 상태 — 누구나 임의 모델/프롬프트로 호출해 서버의 Gemini 과금·할당량을 소모시킬 수 있었음 | 클라이언트가 실제 쓰는 4개 모델명만 허용하는 화이트리스트 추가, `Origin` 헤더가 요청을 받은 호스트(`req.headers.host`)와 정확히 일치할 때만 통과하도록 검사 추가(완벽한 인증은 아니지만 스크립트성 무단 호출의 대다수를 차단), `model` 값 `encodeURIComponent` 처리로 URL 조작 가능성 차단 |
| 🟡 중간 | 로그인 시 로컬↔클라우드 병합 로직이 "fetch 시작 전" 로컬 스냅샷을 써서, fetch를 기다리는 동안 발생한 `addBookmark`/`removeBookmark`를 병합 결과가 덮어써 사라질 수 있었음(특히 페이지 로드 직후 Firebase가 세션을 자동 복원하면서 동시에 자동 북마크가 찍히는 경우) | 로컬 스냅샷을 읽는 시점을 `fetchCloudBookmarks` 완료 "직후"(병합 직전)로 옮겨 경쟁 구간을 크게 좁힘 |
| 🟡 중간 | `deleteAccount()`가 Firestore 문서 삭제 실패를 `.catch(() => {})`로 무시하고 그대로 Auth 계정 삭제를 진행 — 실패 시 그 uid로 재로그인이 불가능해져 고아 문서를 영원히 정리할 방법이 없어짐 | Firestore 삭제 실패 시 에러를 그대로 전파해 Auth 계정 삭제를 진행하지 않도록 수정(재시도 가능한 상태로 안전하게 실패) |
| 🟢 낮음 | `src/firebase.ts`의 `isConfigured`가 6개 환경변수 중 2개(`apiKey`,`projectId`)만 검사 — 예를 들어 `authDomain`만 빠져도 "설정 없음"으로 우아하게 넘어가지 못하고 로그인 시도 시점에 예기치 못한 런타임 에러로 이어질 수 있었음 | `Object.values(firebaseConfig).every(Boolean)`로 6개 필드 전부 검사하도록 수정 |
| 🟢 낮음 | 다이어리 북마크 id를 `Date.now()`(밀리초)로만 생성 — 같은 밀리초에 두 건이 생기면 충돌 가능(현재 호출부는 전부 단일 클릭이라 실제 발생 가능성은 낮음) | `Date.now() * 1000 + 랜덤(0~999)`로 변경, 시간순 정렬은 그대로 유지하면서 충돌 가능성 사실상 제거 |

같은 리뷰에서 확인된 "문제 없음" 항목: 타로 카드의 결정론적 시드 생성 로직(핸들러와 화면 렌더링 두 지점에서 완전히 동일한 시드 문자열 사용 — 직접 문자 단위로 대조 확인), `mergeBookmarks`의 합집합/정렬 로직, `calculateTodayTransits`의 애스펙트 계산(기존 검증된 네이탈 애스펙트 로직과 동일 패턴), `tarotCards.ts`의 22장 고유 id/이름 및 해시 함수의 오버플로우 처리, `geminiApi.ts`에 구글 직접 호출 URL/키가 남아있지 않음.

api/gemini.ts의 보안 로직은 실제 서버(Vercel Functions) 환경 없이도 `node`로 handler 함수를 직접 호출해 (1) 잘못된 origin → 403, (2) origin 헤더 없음 → 403, (3) 허용 안 된 모델명 → 400, (4) 정상 요청은 실제 Google 서버까지 도달(가짜 키로 "API key not valid" 응답을 정상적으로 그대로 전달받음, 즉 프록시 자체는 완전히 살아있음을 확인)까지 전부 검증.

### 7-P-2. "오늘" 탭 신설 — "오늘의 OO" 3종 통합

Q&A로 확정한 사양: 새 "✨ 오늘" 탭을 탭 목록 맨 앞에 추가(오늘/운세/나풀이 해석/궁합/풍수/별자리), 안에 오늘의 나풀이·오늘의 타로·오늘의 트랜짓 3개 카드를 모음. 결과 화면 진입 시 기본 선택 탭은 그대로 "운세"로 유지(사용자가 명시적으로 "오늘" 탭을 클릭해야 봄).

- 기존에 운세 탭 상단에 있던 "오늘의 나풀이"·"오늘의 타로" 카드와, 별자리 탭에 있던 "오늘의 트랜짓" 카드를 그대로 새 "오늘" 탭으로 이동(로직/캐시/버튼 동작은 전혀 변경 없이 위치만 이동).
- 트랜짓 카드의 안내 문구에 "별자리 탭에서 어센던트·행성 배치를 먼저 확인하면 더 잘 이해돼요"를 추가 — 더 이상 별자리 탭에 바로 붙어있지 않아 맥락이 끊길 수 있어 보완.
- Playwright로 (1) 결과 화면 기본 진입 시 여전히 운세 탭(오행 분포)이 보이는지, (2) 탭 목록 첫 번째가 "오늘"인지, (3) "오늘" 탭 안에 3개 카드가 전부 있고 각각 정상적으로 AI 해설이 생성되는지, (4) 운세·별자리 탭에서 해당 카드들이 완전히 사라졌는지 전부 확인.

### 7-P-3. 검증

- `tsc --noEmit`/`npm run build` 클린(번들 크기 변화 없음: 메인 346KB, cloudSync 청크 659KB).
- 위 7-P-1, 7-P-2에 기술한 Playwright/Node 직접 호출 검증 전부 통과.
- **참고**: 사용자가 Google AI Studio에서 Gemini API 키를 새로 발급해 Vercel `GEMINI_API_KEY`와 로컬 `.env`에 반영 완료 — 기존 노출됐던 구 키는 폐기됨(7-O-4-2 완료 처리).

### 7-P-4. 2단 탭 구조로 재구성 (앱스토어 출시를 앞둔 "정체성 정리" 논의에서 이어짐)

> **배경**: 사용자가 "앱스토어에 종합운세앱으로 내고 싶은데 좀더 깔끔했으면 좋겠다"고 요청. Q&A로 "탭 구조/네비게이션 정리" 방향으로 좁힌 뒤, 기존 평면 6탭(오늘/운세/해석/궁합/풍수/별자리)을 대분류+서브탭 2단 구조로 재구성하기로 확정.

- **확정 사양**: 대분류 탭 3개 — [✨ 오늘] [🔮 사주] [🪐 별자리]. "사주"를 누르면 그 아래 서브탭 4개(운세/해석/궁합/풍수)가 나타남. "오늘"·"별자리"는 서브탭 없이 단일 화면 그대로. 결과 화면 진입 시 기본은 "사주" 대분류의 "운세" 서브탭(기존 첫 화면과 동일하게 유지).
- **구현**: `activeSection` state를 6종 유니언에서 `'today' | 'saju' | 'astrology'` 3종으로 축소하고, 기존 운세/해석/궁합/풍수 4개 조건을 담당하던 새 `activeSajuTab` state(`'fortune' | 'ai' | 'compat' | 'fengshui'`)를 추가. 대분류 탭 nav 아래에 `activeSection === 'saju'`일 때만 보이는 서브탭 nav를 추가하고, 기존 4개 콘텐츠 블록을 `{activeSection === 'saju' && (<>...</>)}`로 감싼 뒤 각자의 조건을 `activeSection === 'X'`에서 `activeSajuTab === 'X'`로 변경. `handleReset()`의 상태 초기화도 함께 갱신.
- 참고로 "나풀이 해석"(현 "해석" 서브탭) 안에는 원래부터 있던 3단계 탭(성격 진단/커리어/연애/재물/처방전, `activeTab` state)이 그대로 유지됨 — 결과적으로 대분류→서브탭→내부탭까지 최대 3단 구조가 되지만, 각 단계가 시각적으로 분리되어 있어(상단 고정 대분류 / 그 아래 서브탭 / 카드 내부 탭) 실제 사용성 테스트(Playwright)에서 혼란 없이 잘 구분됨을 확인.
- **검증**: Playwright로 (1) 대분류 탭이 정확히 3개인지, (2) 기본 진입 시 사주>운세(오행 분포)가 보이는지, (3) 서브탭이 정확히 4개이고 해석/궁합/풍수 전환이 각각 정상 동작하는지(궁합은 일지 관련 텍스트, 풍수는 가이드 카드, 해석은 내부 3단계 탭까지), (4) 오늘·별자리 대분류에서는 서브탭 자체가 렌더링되지 않는지 전부 확인 — 통과.
- `tsc --noEmit`/`npm run build` 클린.

---

## 7-Q. 2026-08-05(3차) 타로 78장 풀덱 확장 — 완료

> **배경**: 사용자가 타로 관련 PDF 4종(`1.-타로-카드-의미-요약본.pdf`, `tarot-78-cheatsheet-v1.pdf`, `tarot-major-arcana-v1.pdf`, `tarot-minor-arcana-v1.pdf`)을 제공하며 학습을 요청. 기존 "오늘의 타로" 기능은 메이저 아르카나 22장, 카드당 정/역방향 각 한 문장짜리 단순 데이터였는데, 이 자료들을 활용해 정식 78장(메이저 22 + 마이너 56) 풀덱으로 확장.

### 7-Q-1. 자료 확인 과정에서의 이슈와 해결

- `tarot-minor-arcana-v1.pdf`, `tarot-major-arcana-v1.pdf`를 Read 도구로 처음 열었을 때, 내부적으로 필요한 `pdftoppm`(poppler-utils)이 이 환경에 없어 페이지 렌더링에 실패하거나(마이너 아르카나는 페이지 지정 없이 재시도 시 우연히 성공, 메이저 아르카나는 매번 빈 확인 메시지만 돌아오고 실제 내용이 컨텍스트에 들어오지 않는 문제가 있었음).
- `pdftotext -layout -enc UTF-8`로 텍스트를 파일로 추출한 뒤 그 텍스트 파일을 Read로 읽는 방식으로 전환해 안정적으로 전체 내용을 확인. 이 환경에는 `pdftoppm`은 없지만 `pdftotext`는 설치되어 있었음(경로: `/mingw64/bin/pdftotext`).
- PDF의 2단 레이아웃(라벨/값이 서로 다른 컬럼)이 텍스트로 추출되며 줄 순서가 다소 뒤섞이는 부분이 있어(예: "점성술" 라벨 다음 줄에 실제 값이 아니라 다음 필드명이 먼저 나옴), 카발라 전통의 메이저 아르카나-점성술 대응 관계(바보=천왕성/공기, 마법사=수성 등, 표준 골든던 체계와 일치)를 대조 기준 삼아 값을 정확히 재정렬해 옮겨적음.

### 7-Q-2. 데이터 구조 재설계 (`src/data/tarotCards.ts`)

- `SuitInfo`(수트 정보: 원소/계절/점성술 대응/테마) + `TAROT_SUITS`(완드·컵·소드·펜타클 4종) 신설.
- `TarotCard` 인터페이스 확장: 기존 `name`/`nameEn`/`emoji`/`meaningUpright`/`meaningReversed`에 `arcana`('major'|'minor'), `suit`(마이너 전용), `tagline`(시적 한 줄, 메이저+마이너 에이스만 보유), `element`, `astrology`, `keywordsUpright`/`keywordsReversed`(다중 키워드), `keySymbols`(핵심 상징, 메이저만) 추가.
- 메이저 아르카나 22장: 카드별 원소·점성술·히브리 문자 대응·정역 키워드 5개+3개·핵심 상징 4개·태그라인까지 전부 반영.
- 마이너 아르카나 56장: `MinorSeed` 원본 데이터(에이스 상세 + 핍 2~10 정역 한 줄 + 코트 4장 정역 한 줄)를 수트별로 정의하고, `buildMinorArcana()` 함수가 이를 순회하며 id 22~77까지 카드 78장을 기계적으로 생성(수트당 14장: 에이스→2~10→페이지→나이트→퀸→킹).
- `meaningUpright`/`meaningReversed`는 항상 채워지도록 유지(에이스는 키워드 상위 3개를 join, 핍/코트는 원문 한 줄 그대로) — 기존 AI 프롬프트/폴백 표시 로직과 100% 호환.
- 검증: 총 78장, id 0~77 중복 없음, 카드명 중복 없음, 수트별 정확히 14장씩 확인.

### 7-Q-3. 연동 코드 업데이트

- `src/utils/geminiApi.ts`의 `generateTarotInterpretation()` 프롬프트에 카드의 상세 키워드 목록과 태그라인(있는 경우)을 추가로 포함시켜, 메이저 아르카나·에이스처럼 데이터가 풍부한 카드일수록 AI가 더 구체적인 해석을 생성하도록 보강.
- `src/App.tsx`의 "오늘의 타로" 카드 UI에 `card.tagline`이 있을 때만(메이저·에이스) 시적 한 줄을 골드 컬러 이탤릭으로 표시하도록 추가 — 마이너 핍/코트 카드는 태그라인이 없으므로 자연스럽게 생략됨(조건부 렌더링, 별도 분기 불필요).
- `drawDailyTarotCard()`의 해시/모듈로 로직은 카드 수 확장에 영향받지 않고 그대로 정상 동작(78장 기준으로 자동 적용).

### 7-Q-4. 검증

- 데이터 무결성: Node로 `TAROT_CARDS` 직접 import해 카드 수(78)/id 범위/이름 중복/수트별 개수 확인.
- 서로 다른 5개 시드로 `drawDailyTarotCard()`를 호출해 메이저·마이너 카드가 고루 나오는지, 매핑이 깨지지 않는지 확인.
- Playwright로 실제 앱에서 (1) 메이저 아르카나가 뽑혔을 때 태그라인이 정상 표시되는지("운명의 수레바퀴" 사례로 확인), (2) 마이너 아르카나(코트 카드)가 뽑혔을 때 태그라인 없이도 크래시 없이 정상 렌더링되는지("컵 킹" 사례로 확인) 둘 다 확인.
- `tsc --noEmit`/`npm run build` 클린(번들 크기 346KB→356KB, 데이터 확장분만큼 소폭 증가).

---

## 7-R. 2026-08-05(4차) API 남용 방지(IP별 하루 250회 제한) + 개발자 우회 키 — 완료

> **배경**: "추가하면 좋을 기능" 브레인스토밍에서 짚었던 "비용 구조" 문제를 먼저 다루기로 함. 무료로 계속 서비스하되, 스크립트성 대량 호출로 Gemini API 비용/할당량이 소진되는 것만 막기로 결정(일반 사용자는 하루 20~25회 정도만 써도 앱을 다 둘러보는 수준이라, "명백히 스크립트다" 싶은 수준인 IP당 하루 250회로 합의).

### 7-R-1. 설계

- **식별 기준**: IP 주소(서버측). 로그인 없이 쓰는 앱이라 기기/브라우저 기준(localStorage)은 저장소 삭제로 쉽게 우회되므로, `api/gemini.ts` 프록시에서 요청자 IP를 직접 세는 방식을 채택.
- **저장소**: 이미 있는 Firebase를 재활용하되, 이번엔 **클라이언트 SDK가 아니라 관리자 권한(Firebase Admin SDK)**으로 접근 — 그래야 아무나 자기 카운트를 Firestore 클라이언트로 직접 조작해 제한을 우회할 수 없음. 이를 위해 사용자가 Firebase 콘솔에서 서비스 계정 키(JSON)를 새로 발급해 전달 → 로컬 `.env`(gitignore 대상)와 Vercel 환경변수 `FIREBASE_SERVICE_ACCOUNT_KEY`에만 저장(코드에는 값이 전혀 들어가지 않음).
- **개발자 우회**: 사용자(개발자) 본인은 테스트할 때 제한에 걸리면 곤란하므로, 별도 우회 키(`DEV_BYPASS_KEY`, 서버 환경변수)를 도입. **클라이언트 코드에는 이 값을 절대 심지 않고**(심으면 번들에서 누구나 추출해 제한을 완전히 무력화할 수 있으므로), 브라우저에 개별적으로 설정해두는 방식으로 안전하게 처리.

### 7-R-2. 구현

1. `npm install firebase-admin` 추가.
2. `api/_rateLimit.ts` 신규 — `checkAndConsumeRateLimit(ip)`가 Firestore `rateLimits/{ip의 sha256 해시}` 문서를 트랜잭션으로 읽고 증가시켜, 하루(UTC 날짜 기준) 250회를 넘으면 `false` 반환. 날짜가 바뀌면 자동으로 카운트 리셋. 서비스 계정 키가 없는 환경(예: 로컬 `vite dev`)에서는 제한 없이 통과(fail-open — 서비스 중단보다 남용 허용이 나음).
3. `api/gemini.ts`: 요청마다 `X-Dev-Key` 헤더가 `DEV_BYPASS_KEY`와 일치하면 속도제한을 건너뜀. 아니면 `extractClientIp()`로 IP를 뽑아 `checkAndConsumeRateLimit()` 통과 여부 확인, 초과 시 429 응답.
4. `src/utils/geminiApi.ts`: `buildGeminiRequestHeaders()` 헬퍼를 추가해 `callGeminiJsonApi`/`callGeminiPlainApi`의 fetch 요청에 브라우저 `localStorage`의 `napuli_dev_key` 값이 있으면 `X-Dev-Key` 헤더로 함께 실어 보내도록 수정.
5. **모바일 대응**: 데스크톱은 개발자도구 콘솔에 `localStorage.setItem('napuli_dev_key', ...)`을 직접 입력하면 되지만, 모바일 브라우저는 콘솔 접근이 어려움. `src/main.tsx`에 앱 시작 시 URL의 `?devkey=` 쿼리파라미터를 읽어 자동으로 localStorage에 저장하고 URL에서는 즉시 제거하는 로직을 추가 — 이 링크를 한 번 열기만 하면(북마크 등) 모바일에서도 동일하게 동작. 주소창/방문기록에 값이 남지 않도록 `history.replaceState`로 URL 정리.

### 7-R-3. 트러블슈팅 — Firestore Admin SDK "5 NOT_FOUND"

- 서비스 계정으로 실제 Firestore에 접근했을 때 `5 NOT_FOUND` 오류 발생(인증은 통과했지만 데이터베이스를 못 찾는 상태). 기존 클라이언트 SDK 기반 로그인/동기화는 문제 없이 계속 동작해 원인 파악에 혼선이 있었음.
- Firebase API 활성화 여부, 프로젝트 ID 등을 순서대로 배제해가며 확인한 결과, 이 프로젝트의 Firestore가 **최근 출시된 "Enterprise" 에디션**으로 생성되어 있고, 데이터베이스 ID가 관용적인 특수값 `(default)`가 아니라 **문자 그대로 `default`**로 되어 있는 게 원인으로 확인됨(Google Cloud 콘솔의 Firestore 데이터베이스 목록에서 직접 확인).
- `getFirestore(app, 'default')`처럼 데이터베이스 ID를 명시적으로 지정하도록 수정해 해결. 이후 실제 Firestore에 연결해 (1) 정상 카운트 증가, (2) 249→250 허용·250 초과 시 거부되는 경계값, (3) 날짜가 바뀌면 카운트가 1로 리셋되는 것까지 전부 실제 데이터로 검증 완료(테스트 문서는 검증 후 정리).

### 7-R-4. 검증

- `tsc --noEmit`(api/ 폴더는 별도 옵션으로 독립 검증 — `tsconfig.json`의 `include`가 `src`뿐이라 기본 빌드 대상에는 안 잡히므로)와 `npm run build` 클린.
- Node 스크립트로 실제 Firestore에 연결해 속도제한 로직의 경계값·리셋 동작을 직접 검증(위 7-R-3 참고).
- Playwright로 (1) 정상 흐름 회귀 없음, (2) `localStorage`에 데브키가 있으면 실제로 `X-Dev-Key` 헤더가 전송되는지, (3) `?devkey=` URL로 접속 시 localStorage에 저장되고 URL에서 파라미터가 제거되며 새로고침 후에도 유지되는지 확인.
- `firebase-admin`이 끌고 온 전이 의존성(`@google-cloud/storage`→`teeny-request`/`gaxios`→`uuid`) 관련 moderate~high 취약점이 `npm audit`에 새로 추가됨 — 서버 전용 코드 경로일 뿐 우리 코드가 실제로 쓰는 기능(Firestore 트랜잭션)과 무관한 하위 의존성이라 당장 위험하지 않으나 기록해둠.
- **배포**: 사용자가 Vercel에 `FIREBASE_SERVICE_ACCOUNT_KEY`/`DEV_BYPASS_KEY` 환경변수 등록 완료 확인.

---

## 7-S. 2026-08-05(5차) 매일 알림(네이티브 전용) — 완료

> **배경**: "추가하면 좋을 기능" 중 2번 "재방문 유도"를 다룸. 웹(브라우저)까지 포함하는 방안과 네이티브 앱만 지원하는 방안을 비교 설명한 뒤, 웹 푸시는 서비스 워커·VAPID·서버 스케줄러가 통째로 새로 필요하고 그럼에도 iOS Safari는 "홈 화면에 추가"하지 않으면 아예 작동하지 않는다는 한계 때문에 **이번에는 네이티브 앱(Capacitor)에만 적용하고, 웹 푸시는 반응을 보고 나중에 추가 검토**하기로 결정.

### 7-S-1. 확정된 사양

- **대상**: 네이티브 앱(iOS/Android)에서만 노출. 브라우저로 접속하는 웹 버전에는 알림 관련 UI 자체가 보이지 않음.
- **방식**: 완전히 기기 안에서 예약되는 로컬 알림(서버 왕복 없음) — 매일 오전 9시 반복.
- **내용**: 개인화된 실제 해석 대신 "오늘의 나풀이가 도착했어요" 같은 일반 안내 문구만 발송(실제 해석은 앱을 열어 AI를 호출해야 생성되므로, 알림 시점에 미리 만들어둘 수 없음).
- **위치**: "✨ 오늘" 탭 맨 위에 켜기/끄기 토글 카드.

### 7-S-2. 구현

1. `npm install @capacitor/local-notifications`.
2. `src/utils/notifications.ts` 신규 — `isNativePlatform()`(Capacitor.isNativePlatform() 래핑), `enableDailyNotification()`(권한 요청 → 매일 09:00 반복 알림 예약), `disableDailyNotification()`(예약 취소). 플러그인 자체는 **동적 import**로 불러와(웹 사용자의 번들 크기에 영향 없도록), 웹 환경(`isNativePlatform() === false`)에서는 아예 로드하지 않음.
3. `src/App.tsx`: "오늘" 탭 맨 위에 `isNativePlatform()`이 true일 때만 렌더링되는 토글 카드 추가, `handleToggleNotifications` 핸들러로 연결. 알림 on/off 상태는 `localStorage`에 저장해 앱 재실행 후에도 유지.
4. `npx cap sync`로 android/ios 네이티브 프로젝트에 플러그인 반영 확인 — `@capacitor/local-notifications`가 필요한 Android 권한(`POST_NOTIFICATIONS`, `RECEIVE_BOOT_COMPLETED`, `WAKE_LOCK`)과 브로드캐스트 리시버(재부팅 후 예약 복원용)를 플러그인 자체 매니페스트에 이미 선언하고 있어, Gradle 빌드 시 앱 매니페스트에 자동 병합됨을 플러그인 소스에서 직접 확인.

### 7-S-3. 검증 및 한계

- `tsc --noEmit`/`npm run build` 클린(메인 번들 356KB→367KB — `Capacitor.isNativePlatform()` 판별 자체는 정적 import라 소폭 증가, 플러그인 본체는 동적 청크로 분리되어 웹 사용자에게는 로드 안 됨).
- Playwright로 웹 브라우저 환경에서 (1) Capacitor 관련 import로 인한 크래시가 없는지, (2) 알림 토글 UI가 실제로 노출되지 않는지, (3) "오늘" 탭의 나머지 기능(오늘의 나풀이 등)은 정상 동작하는지 확인.
- **실제 기기/에뮬레이터에서의 알림 동작 자체는 검증하지 못함** — 이 환경(Windows, Android Studio/Xcode 없음)에서는 실제 네이티브 빌드·실행이 불가능하므로, 플러그인 설정과 웹 환경에서의 안전한 격리까지만 검증하고 실제 알림 수신은 사용자가 실기기 빌드 후 확인해야 함(계획안.md 7-O-4 "사용자가 직접 해야 하는 후속 조치"와 동일한 성격의 한계).

### 7-S-4. 보류한 확장 — 웹 푸시 알림 (미착수, 향후 검토용 메모)

브라우저로 접속하는 사용자에게도 알림을 주려면 아래가 전부 새로 필요함(2026-08-05 시점 미착수, 네이티브 반응을 보고 필요성 판단 후 진행 여부 결정):

1. **서비스 워커**(`public/sw.js` 등) — 백그라운드에서 푸시 이벤트를 받아 알림을 띄우는 스크립트, 앱에 등록 로직 추가 필요.
2. **VAPID 키 쌍** — 웹 푸시 인증용. `web-push` npm 패키지로 한 번 생성해 공개키는 클라이언트, 비밀키는 서버(Vercel 환경변수)에 보관.
3. **구독 저장소** — 사용자가 알림을 허용하면 브라우저별로 고유한 "구독 정보(endpoint)"가 생기는데, 이걸 Firestore에 새 컬렉션(예: `pushSubscriptions`)으로 저장해야 함.
4. **서버 스케줄러** — 매일 정해진 시각에 저장된 구독 전체에 알림을 발송하는 로직을 트리거할 주체가 필요. Vercel Cron Jobs(서버리스 함수를 정해진 스케줄로 자동 실행하는 기능)를 새로 설정하면 됨.
5. **알려진 한계**: iOS Safari는 사이트를 "홈 화면에 추가"(PWA 설치)하지 않으면 웹 푸시 자체가 동작하지 않음(iOS 16.4+ 필요). 그냥 브라우저 탭으로 방문하는 아이폰 사용자는 이 방식으로는 알림을 받을 수 없음 — 이 한계 자체가 "네이티브 앱만 먼저" 결정의 핵심 근거였음.

---

## 7-T. 2026-08-05(6차) 정밀 궁합 — 실제 두 사람 사주 비교 — 완료

> **배경**: "추가하면 좋을 기능" 브레인스토밍에서 처음부터 짚었던 "궁합 탭이 띠 기반 정적 관계만 보여줘서 아쉽다"는 지적을 실제로 반영. 기존 띠 기반 궁합(일반적 경향)은 그대로 두고, 상대방의 실제 생년월일을 입력하면 두 사람의 진짜 일주(日柱)를 비교하는 기능을 궁합 서브탭 안에 선택적으로 추가.

### 7-T-1. 확정된 사양

- **위치**: 기존 "궁합" 서브탭 안(사주 대분류 하위)에 "+ 상대방 입력하고 정밀 궁합 보기" 버튼으로 선택적 추가. 기존 띠 기반 궁합 UI는 변경 없음.
- **상대방 입력 범위**: 이름 + 생년월일 + 성별만(출생 시간은 받지 않음) — 궁합의 핵심인 일주(일간+일지) 비교는 시간 없이도 정확히 계산되기 때문에, 입력 부담을 줄이면서도 정확도는 거의 그대로 유지.

### 7-T-2. 계산 로직 (`src/utils/pairCompatibility.ts` 신규)

- `comparePillars(sajuA, sajuB)` — 두 사람의 `SajuResult`를 받아:
  - **일지 관계**: 기존 `data/compatibility.ts`의 `getBranchRelations()`(삼합/육합/충/형/파/해 원본 데이터)를 그대로 재사용해, A의 일지 기준 관계표에서 B의 일지가 어디에 해당하는지 판정(여러 관계가 동시에 성립하는 경우도 배열로 전부 반환).
  - **일간 오행 관계**: `sajuCalculator.ts`에 이미 있던(십신 계산용) `ELEMENT_GENERATES`/`ELEMENT_CONTROLS` 상수를 `export`로 공개해 재사용 — 두 일간 오행이 같음/상생(A생B·B생A)/상극(A극B·B극A) 중 어디에 해당하는지 판정.
  - **오행 보완**: 한쪽이 0개인 오행을 상대가 2개 이상 가지고 있으면 "서로 채워주는 오행"으로 표시.
- **AI 해설**: `geminiApi.ts`에 `generatePairCompatibilityInterpretation()` 추가 — 위 계산 결과(일지 관계·일간 오행 관계·오행 보완)를 근거로, 실제 두 사람의 관계가 구체적으로 어떻게 드러날지 풀어주는 존댓말 텍스트 생성.
- **캐시**: `pairCompatCacheKey()` — 같은 상대(이름+생년월일+성별 조합)와 다시 비교하면 API를 다시 부르지 않고 저장된 결과를 바로 보여줌. 다이어리에도 자동 저장.

### 7-T-3. 검증

- **계산 로직 직접 검증**(Node/tsx로 `comparePillars()` 직접 호출):
  - 실제 두 생년월일(1995-09-27 신유일주 vs 1993-05-15 병신일주)로 계산 → 병(丙,화)이 신(辛,금)을 극하는 관계(화극금, 전통 오행 원칙과 일치)로 정확히 판정됨을 확인.
  - 자(子)-축(丑) 조합 → 육합 판정, 자(子)-오(午) 조합 → 충 판정 등 알려진 지지 관계 여러 건을 인위적으로 구성해 정확히 매칭되는지 확인.
  - 목생화(木生火) 등 오행 상생 관계, 한쪽이 0개·상대가 3개인 오행이 "보완 오행"으로 정확히 잡히는지 확인.
- **Playwright**: 궁합 서브탭에서 "정밀 궁합" 카드 노출 → 상대방 입력 폼 오픈 → **잘못된 날짜(2월 30일) 입력 시 결과로 넘어가지 않고 입력폼이 유지되는지**(기존 메인 폼과 동일한 날짜 검증 로직 재사용) 확인 → 정상 날짜로 재입력 후 두 사람의 일주 카드·일지 관계·AI 해설이 전부 정상 렌더링되는지 확인 → 다이어리에 "정밀 궁합"이 자동 저장되는지 확인. 전부 통과.
- `tsc --noEmit`/`npm run build` 클린(번들 크기 367KB→374KB).

---

## 7-U. 2026-08-05(7차) 온보딩 화면 — 완료

> **배경**: "1>2>3번 순으로 진행" 요청 중 2번째 항목. 앱을 처음 켰을 때 바로 생년월일 입력 폼부터 나와서, 이 앱이 사주뿐 아니라 MBTI·별자리·타로·궁합·풍수까지 다룬다는 걸 첫인상에서 알기 어렵다는 문제를 보완.

### 7-U-1. 확정된 사양

- 최초 실행(로컬에 `napuli_onboarding_seen` 플래그가 없을 때)에만 온보딩 화면 노출, "시작하기" 클릭 또는 재방문 시 다시 뜨지 않음.
- 별도 화면 이동 없이 기존 `Step` 상태 머신에 `'onboarding'` 단계를 추가하는 방식(신규 라우팅 불필요).
- 구성: 나풀이 로고 + 헤드라인 + 4개 기능 하이라이트 카드(사주×MBTI, 서양 점성술+타로, 궁합&풍수, 매일 새로워지는 오늘) + "시작하기" 버튼.

### 7-U-2. 구현 (`src/App.tsx`)

- `type Step`에 `'onboarding'` 추가, `ONBOARDING_SEEN_KEY = 'napuli_onboarding_seen'` 상수 추가.
- `useState<Step>` 초기값을 `localStorage.getItem(ONBOARDING_SEEN_KEY)` 존재 여부로 분기(있으면 `'input'`, 없으면 `'onboarding'`) — 최초 1회만 노출되도록 지연 계산.
- `handleFinishOnboarding()` — 플래그 저장 후 `'input'`으로 전환.
- 온보딩 JSX는 기존 `NapuliMark` 로고, `hero`/`glass-card`/`btn-primary` 등 기존 디자인 클래스를 그대로 재사용해 새 CSS 추가 없이 톤 일관성 유지.

### 7-U-3. 검증

- `tsc --noEmit`/`npm run build` 클린(번들 375.43KB).
- Playwright: (1) 최초 방문 시 온보딩 헤드라인·4개 기능 카드 전부 노출 확인, (2) "시작하기" 클릭 시 입력 폼으로 정상 전환 확인, (3) 전환 후 `napuli_onboarding_seen` 플래그가 `'true'`로 저장됨을 확인, (4) 플래그를 미리 심어둔 재방문 시나리오에서 온보딩을 건너뛰고 바로 입력 폼이 뜨는지 확인 — 전부 통과.

---

## 7-V. 2026-08-05(8차) 앱스토어 리뷰 유도 — 완료

> **배경**: "1>2>3번 순으로 진행" 요청의 마지막 항목. 사용자가 앱을 충분히 써본 뒤 자연스러운 시점에 OS 표준 인앱 리뷰(iOS `SKStoreReviewController` / Android In-App Review API)를 띄워 앱스토어 평점·리뷰 확보를 유도.

### 7-V-1. 확정된 사양

- 결과 화면을 **3회** 조회한 시점에 딱 한 번만 인앱 리뷰 다이얼로그 요청(이후 재요청 없음) — 알림 기능과 동일하게 네이티브 앱에서만 동작, 웹 브라우저 버전에는 노출 자체가 없음.
- OS 표준 다이얼로그를 그대로 사용하므로(iOS/Android가 실제 노출 여부·빈도를 자체적으로 제한) 별도 UI/버튼 없이 백그라운드에서 조용히 동작.

### 7-V-2. 구현 (`src/utils/appReview.ts` 신규, `@capacitor-community/in-app-review` 패키지 추가)

- `trackResultViewAndMaybeRequestReview()` — 결과 화면 진입마다(`App.tsx`의 `setStep('result')` 직후) 호출. `napuli_result_view_count`를 localStorage에 누적하고, 3회째에 처음 도달하는 순간에만 `napuli_review_requested` 플래그를 세우고 `InAppReview.requestReview()`를 호출.
- 알림 기능(`notifications.ts`)과 동일한 패턴 재사용: `isNativePlatform()` 가드 + 플러그인은 동적 `import()`로만 로드(웹 번들에 코드 자체가 포함되지 않음, 빌드 결과물의 별도 `web-*.js` 청크로 확인) + 전체를 try/catch로 감싸 실패해도 사용자 경험에 영향 없음.

### 7-V-3. 검증

- `tsc --noEmit`/`npm run build` 클린.
- Playwright: 웹 환경(모의 API 응답)에서 결과 화면까지 정상 진입하는지, `isNativePlatform()`이 false인 웹에서는 조회수 카운트조차 증가하지 않고(early return) 크래시 없이 조용히 스킵되는지 확인 — 통과. (실제 네이티브 리뷰 다이얼로그 노출 자체는 이 환경에서 검증 불가 — Xcode/Android Studio 실기기 빌드 시 사용자 본인 확인 필요.)

---

## 7-W. 2026-08-05(9차) "오늘" 탭 3종 콘텐츠 차별화 (1단계: 콘텐츠 깊이) — 완료

> **배경**: 섹션 8-2에서 기록한 "오늘" 탭 매력도 부족 피드백(콘텐츠 깊이/비주얼/습관화 3축) 중, 사용자가 1번(콘텐츠 깊이/차별화)부터 우선 착수 요청. "세 개 다 클릭하는 게 지루하고 비슷한 느낌"이라는 핵심 불만에 대응해, 세 콘텐츠가 서로 다른 톤·다른 데이터를 쓰도록 개선.

### 7-W-1. 확정된 사양

- 비주얼 전면 개편(타로 카드 이미지 등)이나 습관화 요소(스트릭 등)는 이번 범위 밖 — 프롬프트/데이터 차원의 콘텐츠 차별화만 우선 진행.
- 세 콘텐츠 각각에 "오늘의 키워드"(2~4글자) 칩을 카드 헤더에 표시해, 클릭하지 않고 훑어봐도 오늘 세 콘텐츠가 서로 다른 결과라는 게 한눈에 보이도록 함.

### 7-W-2. 구현 (`src/utils/geminiApi.ts`, `src/App.tsx`)

- **오늘의 나풀이**(`generateDailyFortune`): 입력값을 일간+오늘 일진뿐이던 것에서 **MBTI·사주 전체 오행 분포·시주**까지 확장. 프롬프트도 이 정보들을 실제 분석에 엮도록 지시문 보강. 응답에 `keyword` 필드 추가(오늘의 기운을 압축한 2~4글자, 예: "질주"·"재정비"). 골드 색상 칩으로 표시.
- **오늘의 트랜짓**(`generateTransitInterpretation`): 톤을 "오늘 하루 운세" → **"오늘의 하늘 예보"**(일기예보 캐스터 톤)로 명시적으로 재구성, 오행/십신 등 명리 용어 대신 행성·별자리·날씨 비유만 쓰도록 지시해 "오늘의 나풀이"와 어휘가 겹치지 않게 분리. 응답에 `keyword` 필드 추가(예: "폭풍전야"·"순풍"). 블루 색상 칩으로 표시.
- **오늘의 타로**: 별도 AI 필드 추가 없이, 이미 있는 카드 데이터를 재사용해 무료로 키워드 칩 구현. 메이저 아르카나+마이너 에이스는 기존 `keywordsUpright`/`keywordsReversed` 배열의 첫 항목을 쓰고, 나머지 마이너 카드(핍 2~10·페이지/나이트/퀸/킹, 총 52장)는 `keywordsUpright` 필드 자체가 없어서 — `meaningUpright`/`meaningReversed` 문자열이 원래 "·"로 구분된 짧은 구문 나열(예: "고수·축적·안정")이라는 점을 활용해 **첫 구문을 키워드로 재사용**하는 폴백을 추가, 78장 전부에서 칩이 뜨도록 함. 퍼플 색상 칩으로 표시.

### 7-W-3. 검증

- `tsc --noEmit`/`npm run build` 클린(번들 377.72KB).
- Playwright(모의 API 응답): 세 카드 모두 생성 후 각자의 키워드 칩("오늘의 기운: 질주" / "오늘의 하늘: 폭풍전야" / "오늘의 키워드: ○○")이 정상 노출되는지 확인. 타로는 처음에 마이너 핍 카드(펜타클 4)가 뽑혀 칩이 안 뜨는 회귀를 실제로 잡아냄 — `meaning` 첫 구문 폴백 추가 후 재검증해 통과.

### 7-W-4. 배포 직후 한정 알려진 결함 (미해결, 다음 세션에서 확인)

- **캐시된 구버전 데이터에 `keyword` 필드가 없어 "오늘의 기운: undefined" 칩이 뜰 수 있음**: `App.tsx:521,529`의 캐시 로드 로직이 `JSON.parse(cached)`를 형태 검증 없이 그대로 state에 넣음. 이번 배포 **이전에** 이미 "오늘의 나풀이"/"오늘의 트랜짓"을 생성해 localStorage에 캐시해 둔 사용자가, 배포 이후 페이지를 새로고침하면 구버전 캐시(키워드 필드 없음)를 그대로 불러와 `dailyFortuneData.keyword`/`transitData.keyword`가 `undefined`로 렌더링됨. 캐시 키에 오늘 날짜가 포함돼 있어 **자정이 지나면 자연히 해소**되지만, 배포일 당일 동안은 실사용자에게 노출될 수 있음. **추천 수정**: 캐시 로드 시 `keyword` 필드 존재 여부를 확인해 없으면 캐시를 무시(재생성 유도)하거나, 렌더링 쪽에서 `dailyFortuneData.keyword &&`처럼 falsy 체크를 추가.

### 7-W-5. 후속 정밀 재검토(작은 오차 위주)로 발견 및 수정 (2026-08-05)

7-W 배포 직후 "작은 오차·불필요한 부분" 위주로 한 번 더 검토해 아래 2건을 실제로 확인하고 즉시 수정(둘 다 결정이 필요 없는 사소하고 안전한 수정이라 바로 반영):

- **키워드 AI 응답 실패 시 폴백 문구가 라벨과 중복 표시됨** — `cleanField`의 keyword 폴백값을 각각 `'오늘의 기운'`/`'오늘의 하늘'`로 지어뒀는데, 정작 UI가 `오늘의 기운: {keyword}` / `오늘의 하늘: {keyword}` 형태로 라벨을 앞에 붙여서(`App.tsx:2711,2817`), 모델이 전부 실패해 폴백이 쓰이면 "오늘의 기운: 오늘의 기운" / "오늘의 하늘: 오늘의 하늘"로 라벨이 겹쳐 보이는 문구 버그였음. `geminiApi.ts:516,1117`의 폴백값을 실제 키워드다운 단어(`'평온'`/`'맑음'`)로 교체해 수정.
- **본 8-2/8절의 App.tsx 줄 번호 인용이 7-W 코드 추가로 밀려서 어긋나 있었음** — 타로 이모지 표시 위치가 `App.tsx:2760`이 아니라 `2773`, "오늘의 트랜짓" 섹션이 `2788`이 아니라 `2807`, 출생시간 근사 경고 위치가 `3631`이 아니라 `3655`로 정정(코드 자체엔 문제 없었고 문서의 줄 번호만 정정).

아래 3건도 이어서 확인함:
- **키워드 칩 색상 변수 짝짓기 — 재검토 결과 문제 아님**: 처음엔 퍼플 칩만 `--purple`/`--purple-light`를 섞어 쓴다고 의심했으나, 실제로 확인해보니 이 프로젝트는 텍스트 색상에 항상 `--purple-light`(밝은 톤, 어두운 배경에서 가독성 확보용)를 쓰고 `--gold`/`--blue`는 `-light` 변형 자체가 텍스트용으로 쓰인 적이 없는 게 기존 관례(`App.css`/`App.tsx` 전체에서 `color: var(--purple)`로 직접 쓰인 곳이 하나도 없음, `--blue-light` 변수 자체가 존재하지 않음). 즉 세 칩 다 기존 컨벤션을 정확히 따르고 있었음 — 수정하지 않음.
- **"오늘의 타로" 키워드 칩 위치를 나풀이·트랜짓과 동일하게(헤더 첫 줄, 저장 버튼 옆) 이동**: 카드 정보(이모지·이름)를 보여주는 둘째 줄에 있던 걸 헤더로 옮겨 세 카드의 헤더 구조를 통일. `App.tsx`의 헤더 블록과 본문 블록이 각각 독립적으로 `drawDailyTarotCard(seed)`를 호출하는 구조라 시드/카드 계산이 두 곳에 중복되지만, 결정론적 순수 함수라 비용은 무시할 수준.
- **`generatePairCompatibilityInterpretation`(`geminiApi.ts:621` 부근)의 로컬 `elementNames` 중복 제거**: 모듈 상단 `ELEMENT_KO`를 그대로 재사용하도록 변경(값이 완전히 동일했음).

**✅ 2026-08-06 세션에서 위 3건을 검증 완료.** `git diff`로 두 파일의 변경 내용이 위 설명과 정확히 일치함을 재확인했고(추가 변경 없음), Playwright(모의 API 응답)로 (1) 타로 헤더의 키워드 칩이 저장 버튼과 같은 줄(헤더)에 정상 노출됨을 스크린샷으로 확인, (2) `generatePairCompatibilityInterpretation`의 `ELEMENT_KO` 재사용이 순수 리팩터링(값 동일)임을 코드 대조로 확인. 통과 후 이 세션의 다른 수정 사항과 함께 커밋함(아래 7-X 참고).

---

## 7-X. 2026-08-06 전면 재검토 + AI API 에러 처리 구조 개선 (완료)

> **상태**: ✅ 구현 완료
> **배경**: 7-W-5 마무리 작업 착수 전, 최우선 미해결 버그("해석" 버튼 API 오류)의 단서를 찾기 위해 `api/`, `src/utils/`, `src/data/`, `src/App.tsx` 전체를 처음부터 재검토. 실제 Gemini API에 로컬 키로 직접 호출 테스트도 수행. 그 결과 계획안에 없던 버그 몇 건을 새로 발견해 전부 수정함.

### 7-X-1. [검증됨] Gemini 모델명 자체는 문제 없음

로컬 `.env`의 실제 키로 `gemini-3.6-flash`/`gemini-3.5-flash-lite`/`gemini-2.5-flash`/`gemini-2.5-pro`에 직접 `generateContent` 호출 테스트 — 4개 모델 전부 실재하며, 3개는 200 정상 응답, `gemini-2.5-pro`(폴백 마지막 순서)만 429(할당량 초과)였음. "모델명이 잘못됐다"는 가설은 배제됨. 다만 이 프로젝트의 2.5-pro 무료 할당량이 이미 소진돼 있다는 점은 참고할 사실(폴백 순서상 마지막이라 실사용 영향은 낮음).

### 7-X-2. 재발견·수정한 버그 4건

1. **🔴 `api/gemini.ts`의 자체 에러 응답이 클라이언트에 실제 메시지를 전달하지 못하던 버그**: `geminiApi.ts`의 모든 호출부가 에러 메시지를 `err.error.message`(중첩 객체)로 꺼내는데, `api/gemini.ts`는 자신이 직접 반환하는 에러(403 Origin 불일치, 429 rate limit, 500 키 누락, 400 잘못된 model)를 `{ error: "문자열" }`(중첩 없음)로 응답하고 있었음. 문자열에는 `.message` 속성이 없어 항상 `undefined`가 되고, 결과적으로 사용자에게는 "오늘 요청 가능한 횟수를 모두 사용했습니다" 같은 실제 안내 대신 뭉뚱그린 "API 오류 (429)" 류 메시지만 보였음. `api/gemini.ts`에 `errorBody()` 헬퍼를 추가해 업스트림 Gemini 에러와 동일한 `{ error: { message, code? } }` 모양으로 통일. Playwright로 429 응답을 모의해 실제 안내 문구가 화면에 그대로 노출됨을 확인.
2. **🔴 "재시도 불가능" 오류가 실제로는 계속 재시도되던 구조적 버그**: `callGeminiJsonApi`/`callGeminiPlainApi`의 재시도 루프는 429/503/500만 재시도하고 나머지(400/403 등)는 `throw`해서 즉시 실패시키려는 의도였으나, 그 `throw`가 **같은 함수 안의 바로 아래 `catch` 블록에 다시 잡혀서** 재던지기(rethrow) 없이 `lastError`만 갱신하고 다음 attempt/model로 계속 넘어가는 구조였음. 즉 Origin 불일치(403)처럼 재시도해도 절대 성공할 수 없는 오류도 4모델×3회 = 12번을 전부 소진한 뒤에야 실패 처리됐음(수십 초 낭비 + rate limit 할당량 12배 낭비). `NonRetryableApiError` 마커 클래스를 추가해 catch에서 즉시 재던지도록 수정, 두 함수 모두 적용. Playwright로 재현: 이 수정 전이었다면 12회 요청이 갔을 시나리오(설정 오류)가 수정 후 정확히 1회 요청으로 끝남을 확인.
3. **🟡 `GEMINI_API_KEY` 환경변수 누락(설정 오류)이 업스트림 과부하(500)와 동일하게 취급되던 문제**: 위 2번과 맞물려, Vercel에 이 환경변수가 실제로 빠져 있었다면 사용자는 매번 12회 재시도(수십 초)를 다 기다린 뒤에야 오류를 봤을 것. `api/gemini.ts`가 이 경우 `code: 'CONFIG_MISSING'`을 함께 응답하도록 하고, 클라이언트가 이 코드를 보면 과부하 여부와 무관하게 즉시 실패하도록 수정(위 `NonRetryableApiError`로 처리).
4. **🟢 `api/gemini.ts`의 model 화이트리스트 검증 순서**: 잘못된 `model` 파라미터로 오는 요청이 400으로 거부되기 전에 이미 하루 250회 rate limit 중 1회를 소비하던 순서 문제 — model 검증을 rate limit 소비보다 먼저 실행하도록 순서 변경.

### 7-X-3. "오늘의 나풀이"/"오늘의 트랜짓" undefined 키워드 칩 — 실제 수정 + 증상 정정

- 위 8절 표에 있던 🟡 항목("오늘의 기운: undefined" 노출 가능성)을 실제로 수정: `App.tsx`의 나풀이/트랜짓 키워드 칩을 타로 칩과 동일하게 `{dailyFortuneData.keyword && (...)}` / `{transitData.keyword && (...)}`로 값 자체를 가드하도록 변경(이전엔 부모 객체 존재 여부만 확인해 `dailyFortuneData && (...)` 안에서 `{dailyFortuneData.keyword}`를 그대로 렌더링).
- **증상 정정**: 기존 문서는 "undefined"라는 **문자열**이 화면에 뜬다고 기재돼 있었으나, 실제로는 JSX 표현식이라 React가 `undefined`를 빈 값으로 렌더링 — 즉 "오늘의 기운: " 뒤에 아무 글자 없이 칩만 뜨는 형태였음(리터럴 "undefined" 텍스트 아님). 수정 후에는 `keyword` 필드가 없으면 칩 자체가 아예 렌더링되지 않음. Playwright로 `keyword` 필드 없는 응답을 모의해 "오늘의 기운: undefined" 문자열도, 빈 칩도 뜨지 않음을 확인.

### 7-X-4. 검증

- `tsc --noEmit` / `npm run build` 클린.
- Playwright(임시 설치, `--no-save`)로 4가지 시나리오 실제 검증:
  1. `CONFIG_MISSING` 응답 모의 → 정확히 1회 요청으로 종료 + 실제 에러 메시지("...GEMINI_API_KEY가 설정되지 않았습니다") 화면 노출 확인.
  2. 일반 429(rate limit) 응답 모의 → 기존과 동일하게 4모델×3회 = 12회 재시도 후 종료(재시도 동작 자체는 보존됨을 확인) + 실제 안내 문구("오늘 요청 가능한 횟수를 모두 사용했습니다...") 노출 확인(수정 전이라면 "API 오류 (429)"로 뭉개졌을 상황).
  3. 타로 헤더 키워드 칩 위치 — 스크린샷으로 "오늘의 키워드: OO" 칩이 저장 버튼과 같은 헤더 줄에 있음을 시각 확인.
  4. `keyword` 필드 없는 나풀이 응답 모의 → "undefined" 문자열도 빈 칩도 노출되지 않음을 확인.

### 7-X-5. 미해결로 남긴 가설 (다음에 재현 시 참고)

- **[미확인] 카카오톡 등 인앱 브라우저의 Origin 헤더 이슈 가능성**: `api/gemini.ts`의 Origin 검사(`origin !== https://{host}`)가 네이티브 앱뿐 아니라, 공유 링크로 카카오톡/인스타그램 인앱 브라우저에 들어온 "웹" 사용자에게도 영향을 줄 가능성. 확실한 근거는 없으나(대부분의 최신 인앱 브라우저는 Chrome/WebKit 기반이라 Origin 헤더를 정상적으로 보낼 가능성이 높음), 이 앱은 카카오톡 공유가 핵심 성장 채널이라 배제하긴 이름. 다음에 최우선 버그("해석" 버튼 API 오류)를 재현할 때 사용자의 유입 경로(공유 링크/인앱 브라우저 여부)도 확인 항목에 추가할 것.
- **하루 250회 rate limit이 UTC 자정 기준으로 리셋됨**(`_rateLimit.ts:39`) — 한국시간으로는 오전 9시 리셋. 의도된 동작이라 이번엔 변경하지 않았으나, "하루 제한"을 자정 기준으로 오해하면 디버깅 시 혼선 소지가 있어 기록만 남김.

---

## 7-Y. 2026-08-06(2차) 섹션 8 잔여 이슈 정리 (완료)

> **상태**: ✅ 구현 완료
> **배경**: 7-X에 이어 섹션 8에 남아있던 항목 중 코드만으로 해결 가능한 것들을 마저 처리. 실제 절기 천문 데이터 확보가 선행 조건인 두 건(1930~39년/2031년 이후 데이터)은 이번에도 범위 밖으로 남김.

### 7-Y-1. 🔴 네이티브 앱 AI 기능 전체 실패 가능성 — 수정

- `src/utils/geminiApi.ts`에 `API_BASE` 상수 추가: `Capacitor.isNativePlatform()`일 때만 절대경로(`https://mbti-delta-red.vercel.app`)를 앞에 붙이고, 웹에서는 기존처럼 상대경로(`/api/gemini`) 유지. `callGeminiJsonApi`/`callGeminiPlainApi` 두 곳의 `url` 생성부에 모두 적용.
- `api/gemini.ts`의 Origin 검사에 `capacitor://localhost`/`https://localhost`/`http://localhost` 허용리스트(`NATIVE_ORIGINS`)를 추가해, 배포 도메인과 정확히 일치할 때만 통과하던 기존 검사를 네이티브 오리진도 함께 허용하도록 확장.
- Playwright로 웹 환경에서는 여전히 상대경로(`/api/gemini?...`)로만 요청이 나가는 것을 확인(회귀 없음). 다만 실제 iOS/Android 기기·시뮬레이터에서 native origin이 정확히 `capacitor://localhost` 형태로 오는지는 이번에 실기기 검증은 못 했으므로, Xcode/Android Studio 빌드 시 한 번 확인 권장.

### 7-Y-2. 🟡 `X-Forwarded-For` rate limit 우회 우려 — 코드는 유지, 문서만 정정

- Vercel 공식 문서(`vercel.com/docs/headers/request-headers`)를 직접 확인한 결과, Vercel은 `x-forwarded-for` 헤더를 자체적으로 덮어써서 실제 클라이언트 공인 IP만 실어 보내고 **클라이언트가 보낸 값은 전달하지 않는다**("does not forward external IPs")고 명시돼 있음. 이 제한을 우회하려면 엔터프라이즈 전용 "신뢰할 수 있는 프록시" 기능이 필요해, 일반 Vercel 호스팅을 쓰는 이 프로젝트에는 스푸핑 우회 시나리오가 해당하지 않음. `x-real-ip`/`x-vercel-forwarded-for`도 문서상 동일 값의 별칭이라 바꿔 쓸 이유가 없음.
- 코드는 그대로 두고 `api/_rateLimit.ts`의 `extractClientIp()`에 위 근거를 주석으로 남김. 섹션 8 표에서도 제거(근거 없는 우려였음을 확인했으므로).

### 7-Y-3. 🟢 정밀 궁합 상대방 폼에 절기 근사치 경고 문구 누락 — 수정

- 본인 정보 입력 폼(`App.tsx`)에 있던 "1940년 이전 출생자는 절기 정밀 데이터가 없어 근사값으로 계산돼요" 경고 블록과 동일한 것을 궁합 상대방 생년월일 입력 폼에도 추가(연도 1930~1939 입력 시에만 노출). Playwright로 상대방 연도에 1935 입력 시 경고가 뜸을 확인.

### 7-Y-4. 🟢 "오늘의 트랜짓"에 출생시간 근사치 경고 누락 — 수정

- 별자리 탭에 있던 `astrologyTimeConfidence !== 'exact'` 조건의 출생시간 근사/미상 경고를 "오늘의 트랜짓" 카드 헤더 바로 아래에도 동일하게 추가(문구는 "트랜짓" 맥락에 맞게 일부 수정). Playwright로 기본 폼(12시진 근사) 제출 시 트랜짓 카드에 경고가 뜸을 확인.

### 7-Y-5. 🟢 `GEMINI_API_KEY = 'server-managed'` 죽은 코드 — 주석 보강

- 상수 삭제·분기 제거는 40곳 이상의 호출부를 건드려야 하는 규모라 이번엔 손대지 않고, 대신 정의부 주석을 보강: 이 값이 항상 truthy라 파일 전체의 `if (!GEMINI_API_KEY)` 분기(예: "AI 비활성화" 안내 UI)가 전부 도달 불가능한 죽은 코드라는 점, 서버 쪽 실제 키 누락 여부는 이 상수와 무관하게 `api/gemini.ts`가 매 호출마다 개별 확인(`CONFIG_MISSING`)한다는 점을 명시해 향후 유지보수자의 오인을 방지.

### 7-Y-6. 검증

- `tsc --noEmit` / `npm run build` 클린.
- Playwright로 3가지 확인: (1) 웹에서는 여전히 상대경로만 사용(회귀 없음), (2) 오늘의 트랜짓 카드에 근사치 경고 노출, (3) 궁합 상대방 폼(1935년 입력)에 절기 경고 노출.

---

## 7-Z. 2026-08-06(3차) 🔴 최우선 버그 — 실제 배포·재검증까지 완료해 완전히 해결

> **상태**: ✅ **완전히 해결 — 배포 완료 + 실제 프로덕션 엔드포인트에서 정상 응답(HTTP 200) 확인됨.** 아래 7-Z-1~7-Z-3의 "원인"(Firebase 키 미처리 예외)은 **배포 후 재검증 결과 틀린 가설로 판명됐고**, 실제 원인과 최종 수정은 **7-Z-4**, 배포·최종 검증 결과는 **7-Z-6**을 볼 것. 이 섹션은 틀렸던 가설도 포함해 조사 과정 전체를 그대로 남겨둔 기록.
> **배경**: 사용자가 "오늘의 트랜짓/심화해석/오행 종합 해설" 등 여러 AI 기능에서 실제로 폴백 문구만 뜨고, 트랜짓 버튼은 "API 오류 (500)" 토스트를 보여주는 것을 실제 배포 사이트에서 재현·스크린샷으로 제보함. 이걸 계기로 섹션 8 최상단 🔴 항목("해석" 버튼 API 오류)의 실제 원인을 처음으로 특정하려 시도.

### 7-Z-1. 원인 규명 과정

1. 실제 배포 사이트(`https://mbti-delta-red.vercel.app`)에 `curl`로 Origin 헤더를 정확히 맞춰 `/api/gemini`에 직접 POST 요청을 보내봄 → Gemini 응답도, 우리 코드의 JSON 에러 응답도 아닌 **Vercel 플랫폼 자체의 크래시 페이지**(`FUNCTION_INVOCATION_FAILED`, `Content-Type: text/plain`, JSON 아님)가 매번 반환됨을 확인.
2. 클라이언트가 이 텍스트 응답을 `response.json()`으로 파싱 시도 → 실패 → `.catch(() => ({}))`로 빈 객체가 되어 `err.error.message`가 항상 없음 → 결국 어떤 실패든 뭉뚱그린 "API 오류 (500)"로만 보였던 것(7-X-2에서 고친 에러 모양 통일 문제와는 별개의, 더 근본적인 원인).
3. 코드를 다시 훑어보니 `api/_rateLimit.ts`의 `getAdminDb()`가 `FIREBASE_SERVICE_ACCOUNT_KEY` 환경변수를 `JSON.parse()` → `cert()` → `initializeApp()`에 넘기는 과정을 **try/catch 없이** 실행하고 있었고, `api/gemini.ts`도 `checkAndConsumeRateLimit()` 호출부를 감싸는 try/catch가 없었음 — 즉 이 값이 조금이라도 잘못 저장돼 있으면(Vercel 환경변수 입력창에 여러 줄짜리 JSON의 `private_key` 필드를 붙여넣을 때 개행·이스케이프가 깨지는 건 흔한 실수) 예외가 그대로 위로 던져져 **핸들러 전체가 처리되지 않은 예외로 죽고**, Vercel이 이를 `FUNCTION_INVOCATION_FAILED` 크래시 페이지로 반환함.
4. **로컬 재현으로 확정**: `esbuild`로 `_rateLimit.ts`를 임시 트랜스파일해 일부러 깨진 `FIREBASE_SERVICE_ACCOUNT_KEY`(`'{this is not valid json'`) 환경변수로 직접 호출 — 수정 전 코드(`git show HEAD:api/_rateLimit.ts`)는 그대로 `SyntaxError`를 던지며 크래시, 수정 후 코드는 에러를 잡아 로그로 남기고 `true`(rate limit 없이 통과)를 반환함을 확인. 실제 프로덕션의 `FIREBASE_SERVICE_ACCOUNT_KEY` 값이 정확히 이런 형태로 깨져 있는지까지는 (Vercel 대시보드 접근 권한이 없어) 100% 확정할 수 없지만, 관찰된 증상(모든 AI 기능이 매번 동일하게 실패, 사용자 프롬프트 내용과 무관)과 정확히 일치함.

### 7-Z-2. 수정

- `api/_rateLimit.ts`: `getAdminDb()` 내부(`JSON.parse`/`cert`/`initializeApp`)를 try/catch로 감싸 실패 시 `console.error`로 로그를 남기고 `null` 반환(기존 "키 미설정 시 fail-open"과 동일하게 처리). `checkAndConsumeRateLimit()`의 `db.runTransaction()` 호출도 try/catch로 감싸 Firestore 접근 자체가 실패하는 경우(권한/네트워크 문제 등)도 fail-open 처리.
- `api/gemini.ts`: 핸들러 전체를 최상위 try/catch로 감싸는 안전망 추가 — 이 안에서 예상 못한 예외가 나더라도 Vercel의 불투명한 크래시 페이지 대신 `errorBody()` 형태의 JSON 500 응답 + `console.error` 로그를 남기도록 함(향후 유사한 미처리 예외가 또 생기더라도 같은 증상이 재발하지 않도록 하는 방어선).
- 이 수정으로 **Firebase 관련 설정이 실제로 잘못돼 있어도 AI 기능 자체는 정상 동작**하게 됨(다만 그 기간 동안은 하루 250회 rate limit이 적용되지 않음 — 서비스 중단보다는 낫다는 기존 철학 유지). 근본적으로는 Vercel 대시보드의 `FIREBASE_SERVICE_ACCOUNT_KEY` 값 자체를 재확인/재설정하는 것을 권장하며, 이제는 값이 잘못됐을 때 Vercel 함수 로그에 `[rateLimit] Firebase Admin 초기화 실패 — ...` 로그가 남으므로 다음에 문제가 재발해도 훨씬 빠르게 진단 가능.

### 7-Z-3. 검증 (로컬 한정 — 실제 원인이 아니었음이 이후 밝혀짐)

- `tsc --noEmit` / `npm run build` 클린.
- `esbuild` 임시 트랜스파일로 수정 전/후 동작을 직접 대조 — 수정 전: 크래시(SyntaxError 그대로 전파), 수정 후: 에러 로그 남기고 `true` 반환(정상 진행). **다만 이건 "일부러 깨뜨린 로컬 재현"이 고쳐졌다는 뜻일 뿐, 실제 배포 사이트의 진짜 원인이 이거였다는 뜻은 아니었음** — 사용자가 배포를 요청해 실제로 push한 뒤 재검증한 결과 아래 7-Z-4에서 밝혀짐.

### 7-Z-4. ✅ 실제 원인 발견 (사용자 승인 후 배포·재검증하여 확정)

사용자가 "배포해줘"라고 승인해 위 7-Z-2 수정을 포함한 커밋들을 `git push`함 → Vercel이 자동으로 새 배포를 생성(`vercel project ls`/`vercel inspect`로 배포가 실제로 새 커밋 기준으로 "Ready" 상태임을 확인). 하지만 **배포 후에도 `curl`로 직접 호출해보니 완전히 동일한 `FUNCTION_INVOCATION_FAILED` 크래시가 계속 발생** — 즉 7-Z-2의 수정(try/catch 추가)은 실제 원인이 아니었음.

**`vercel logs`로 실시간 런타임 로그를 직접 스트리밍해서 확인한 진짜 원인**:

```
Warning: Failed to load the ES module: /var/task/api/gemini.js.
Make sure to set "type": "module" in the nearest package.json file or use the .mjs extension.
/var/task/api/gemini.js:14
import { checkAndConsumeRateLimit, extractClientIp } from './_rateLimit';
^^^^^^
SyntaxError: Cannot use import statement outside a module
```

즉 Vercel이 `api/gemini.ts`를 빌드할 때 **ESM `import` 구문을 CommonJS `require`로 변환하지 않은 채 그대로 둔 `.js` 파일을 만들어냈고**, Node.js는 (프로젝트 `package.json`에 `"type": "module"`이 없어) 이 `.js` 파일을 기본값인 CommonJS로 로드하려다 최상단의 `import` 구문 자체에서 문법 오류로 즉시 죽어버리는 것이었음. **이건 Firebase나 rate limit과는 무관하게, `api/gemini.ts`가 로드되는 시점(핸들러 함수가 실행되기도 전) 크래시라서, 7-Z-2에서 추가한 try/catch(핸들러 함수 몸통 안쪽)는 애초에 이 크래시를 잡을 수 있는 위치가 아니었음.**

원인 조사(Vercel 공식 커뮤니티 디스커션 확인, 아래 참고 링크): 루트 `tsconfig.json`이 `"module": "ESNext"`(Vite 빌드용, `include: ["src"]`라 `api/`는 원래 대상이 아님)로 설정돼 있는데, Vercel의 Node 서버리스 함수 빌더가 `api/*.ts`를 컴파일할 때 이 루트 tsconfig.json을 그대로 읽어들여(같은 디렉토리에 별도 tsconfig가 없으면 상위에서 찾아 사용하는 동작으로 추정) `module: ESNext`를 적용해버린 것으로 보임 — 이 프로젝트가 프론트엔드(Vite, ESM)와 서버리스 함수(Node, 기본 CommonJS)를 같은 저장소에서 같이 쓰다 보니 tsconfig 하나가 두 마리 토끼를 잘못 다 잡으려 한 셈.

**참고 자료**:
- [vercel/vercel Discussion #5685 — 502 SyntaxError: Cannot use import statement outside a module](https://github.com/vercel/vercel/discussions/5685)
- [Vercel Knowledge Base — How can I use files in Vercel Functions?](https://vercel.com/kb/guide/how-can-i-use-files-in-serverless-functions)
- [vercel/vercel Discussion #4983 — utility files in /api that shouldn't become functions](https://github.com/vercel/vercel/discussions/4983)

### 7-Z-5. 수정 (커밋은 했으나 **아직 배포 전** — 다음 세션에서 이어서 진행)

- `api/tsconfig.json` 신규 추가: `"module": "commonjs"` (+ `target: ES2020`, `esModuleInterop: true`)로 지정해, Vercel이 `api/` 아래 함수를 컴파일할 때 루트 tsconfig.json 대신 이 설정을 쓰도록 함.
- `npx tsc --noEmit -p api/tsconfig.json`으로 이 설정 기준 `api/*.ts` 전체가 에러 없이 컴파일됨을 확인. 루트 `npx tsc --noEmit`(Vite 앱, `src/` 대상)도 영향 없음을 확인.
- 7-Z-2에서 추가한 `api/_rateLimit.ts`/`api/gemini.ts`의 try/catch 안전망은 **실제 원인은 아니었지만 그 자체로 여전히 유효한 방어적 개선**이라 되돌리지 않고 유지함(Firebase 설정이 훗날 정말로 깨지는 경우를 대비).

**⚠️ 다음 세션 시작 시 가장 먼저 할 일** (2026-08-06 세션은 여기서 사용자 요청으로 종료):
1. `git status`로 `api/tsconfig.json`이 커밋돼 있는지 확인(이번 세션에서 커밋까지만 하고 push는 안 함).
2. `git push`로 배포.
3. 배포 완료 후(`vercel inspect mbti-delta-red.vercel.app`로 새 커밋이 반영됐는지 확인 가능) `curl -X POST "https://mbti-delta-red.vercel.app/api/gemini?model=gemini-3.6-flash" -H "Content-Type: application/json" -H "Origin: https://mbti-delta-red.vercel.app" -d '{"contents":[{"parts":[{"text":"안녕"}]}]}'`로 직접 호출해 이제 `FUNCTION_INVOCATION_FAILED`가 아니라 정상 Gemini JSON 응답(또는 최소한 우리 코드가 만든 JSON 에러)이 오는지 확인.
4. 문제가 남아있다면 `npx vercel logs mbti-delta-red.vercel.app`로 실시간 로그를 다시 확인(이번 세션에서 이 방법으로 진짜 원인을 찾았음 — 앞으로도 이 방법이 제일 빠름).
5. 실제 사이트에서 "오늘의 트랜짓"/"심화해석"/"해석" 등 버튼을 사용자가 직접 눌러 최종 확인.
6. 확인되면 이 섹션(7-Z)과 섹션 8 최상단 항목을 "완전히 해결됨"으로 갱신.

### 7-Z-6. ✅ 배포 및 최종 검증 완료 (2026-08-06, 같은 날 이어서 진행)

사용자가 "다시 이어서 해줘"로 재개 → 위 체크리스트 그대로 실행:

1. `git status`로 `api/tsconfig.json` 커밋 확인(`391004e`) → `git push origin main` 실행(`159ae39..391004e`).
2. `npx vercel ls mbti` / `npx vercel inspect mbti-delta-red.vercel.app`로 새 배포가 실제로 프로덕션 별칭에 반영됐음을 확인(배포 `mbti-gp1h6kyhj-...`, "Ready", "Production").
3. `curl`로 프로덕션 엔드포인트에 직접 두 가지 요청을 보내 확인:
   - 일반 요청(`maxOutputTokens: 50`) → **HTTP 200**, 실제 Gemini `candidates` 응답 반환(더 이상 `FUNCTION_INVOCATION_FAILED` 아님).
   - 실제 앱과 동일한 JSON 모드 요청(`responseMimeType: application/json`, 사주 인트로 프롬프트와 유사한 형태) → **HTTP 200**, 정확히 요청한 JSON 구조(`{"title": ..., "jungianNote": ...}`)가 담긴 정상 응답 확인.
4. `api/gemini.js`가 이제 정상적으로 로드·실행되어 Gemini API까지 도달하고 있음이 실제 프로덕션 트래픽으로 확정됨 — **2026-08-05에 처음 보고된 "해석 버튼 API 오류" 최우선 버그가 완전히 해결됨.**

**남은 것**: 사용자가 실제 앱 화면(브라우저)에서 "해석"/"오늘의 트랜짓"/"심화해석" 등을 직접 눌러 사용자 경험 차원에서도 정상 노출되는지 확인하는 것을 권장(서버는 확실히 고쳐졌지만, 클라이언트 쪽 캐시된 이전 실패 상태나 브라우저 캐시가 남아있을 가능성을 배제하기 위함 — 강력 새로고침 권장).

---

## 7-AA. 2026-08-06(4차) 8-1/8-2 추천 기능·습관화 요소 구현 ("이어서 나머지 부분도 진행해줘")

> **상태**: ✅ 구현 완료, `tsc --noEmit`/`npm run build` 클린, Playwright 4개 시나리오 전부 PASS.
> **배경**: 최우선 🔴 버그 해결 후, 계획안에 "우선순위 낮음, 사용자 요청 시 착수"로 남겨뒀던 8-1(추천 기능 3건)과 "별도 논의 필요"였던 8-2(비주얼/습관화)를 사용자가 이어서 요청. 8-2는 타로 카드 이미지 자산(78장) 확보 여부·습관화 범위에 대해 먼저 `AskUserQuestion`으로 확인 — **"CSS/그라디언트로 카드처럼 꾸미기"**(실제 사진/삽화는 이미지 자산 별도 확보 필요해 보류) + **"연속 방문일수(스트릭) 표시만"**(주간 요약 리포트는 제외)로 방향 확정 후 진행.

### 7-AA-1. 정밀 궁합 "이전에 비교한 상대" 이력

- `App.tsx`에 `PairCompatHistoryEntry` 타입 + `pairCompatHistoryKey()`/`getPairCompatHistory()`/`addPairCompatHistoryEntry()` 헬퍼 신규 추가. 본인(`baseKeyId`) 기준으로 최근 비교한 상대 최대 10명을 `{이름, 생년월일, 성별, 비교시각}`으로 localStorage에 기록(같은 상대 재비교 시 중복 제거 후 최신순 재정렬).
- `handleComparePair()`가 성공/캐시적중 여부와 무관하게 비교 시도 자체를 이력에 기록하도록 수정. 함수 시그니처에 `override` 매개변수를 추가해, 이력에서 상대를 클릭했을 때 폼 state의 React 업데이트 타이밍 문제 없이 곧바로 그 값으로 비교를 실행할 수 있게 함.
- 궁합 탭의 "+ 상대방 입력하고 정밀 궁합 보기" 버튼 아래에 "이전에 비교한 상대" 칩 목록을 추가 — 클릭하면 폼 재입력 없이 바로 비교 실행(캐시가 있으면 API 재호출 없이 즉시 표시).

### 7-AA-2. 매일 알림 시간 변경 UI

- `src/utils/notifications.ts`: `enableDailyNotification()`이 `hour` 매개변수(기본값은 저장된 값 또는 오전 9시)를 받도록 변경. `getNotificationHour()` 신규 export.
- "오늘" 탭 알림 카드에 0~23시 `<select>` 추가. 알림이 이미 켜져 있는 상태에서 시간을 바꾸면 같은 알림 ID로 즉시 재예약(`handleChangeNotificationHour`), 꺼져 있으면 값만 저장해뒀다가 다음에 켤 때 반영.

### 7-AA-3. "오늘의 OO" 3종 실패 시 명시적 재시도 표시

- 나풀이/타로/트랜짓 각각에 `xxxFailed` 상태 추가 — 생성 실패 시 true, 새로 시도하거나 성공하면 false로 리셋.
- 실패 상태일 때 안내 문구를 "⚠️ 생성에 실패했어요. 잠시 후 다시 시도해 주세요."(빨간 톤)로 바꾸고, 버튼 라벨도 최초 생성 문구("~보기"/"~뽑기") 대신 "🔄 다시 시도"로 바뀌도록 함 — 이전에도 같은 버튼을 다시 누르면 재시도는 가능했지만(데이터가 null로 남아 버튼이 그대로 유지되는 구조), 실패했다는 사실과 재시도 가능하다는 것을 명확히 알려주는 문구/버튼이 없었던 것을 보완.

### 7-AA-4. "오늘의 타로" 카드 CSS/그라디언트 비주얼

- `tarotCardTheme(card)` 헬퍼 신규 추가 — 메이저 아르카나는 앱 전체 톤에 맞춘 보라/골드, 마이너는 수트(완드=불/주황, 컵=물/파랑, 소드=공기/은회색, 펜타클=흙/초록)별로 그라디언트+강조색을 반환.
- 기존 "이모지 1개 + 텍스트" 한 줄 표시를, 카드 프레임(둥근 테두리 + 그라디언트 배경 + 은은한 글로우) 안에 큰 이모지·카드명·영문명·정/역방향 배지를 세로로 배치한 형태로 재구성. 역방향이면 이모지 자체가 180도 회전하는 트랜지션 추가.
- 실제 카드 사진/삽화(78장)를 쓰는 방식은 이미지 자산을 별도로 구해야 해서(저작권 자체는 Rider-Waite-Smith 1909년판이 미국 기준 퍼블릭 도메인이라 문제없지만, 78장 전부의 이미지 파일을 확보하는 작업이 이번 세션 범위 밖) 사용자 확인 하에 보류.

### 7-AA-5. 연속 방문일수(스트릭) 표시

- `src/utils/streak.ts` 신규 파일 — `recordTodayVisitAndGetStreak(todayStr)`: 오늘 날짜 기준으로 마지막 방문일과 비교해, 어제 방문했으면 +1, 그 외(첫 방문이거나 하루 이상 건너뜀)면 1로 리셋. 같은 날 여러 번 호출해도 중복 증가 없음.
- 앱이 로드될 때(활성 탭과 무관하게) 한 번 기록하도록 최상위 `useEffect`에 연결. "오늘" 탭 상단에 2일차부터 "🔥 N일 연속 방문 중이에요!" 배지 노출(1일차는 아직 "연속"이라 할 게 없어 숨김).

### 7-AA-6. 검증

- `tsc --noEmit` / `npm run build` 클린.
- Playwright(임시 설치, `--no-save`) 4개 시나리오 전부 PASS: (1) 스트릭 배지 — 어제 방문+4일째 시드 후 오늘 방문 시 "5일 연속 방문 중이에요" 노출, (2) 타로 카드 비주얼 — 그라디언트 프레임 안에 정/역방향 배지 정상 렌더링(스크린샷으로 시각 확인), (3) 재시도 버튼 — 500 오류 모의 후 "생성에 실패했어요" 경고 + "다시 시도" 버튼 노출, (4) 궁합 이력 — 상대 비교 후 새로고침·재제출해도 이력에 남아있고, 클릭 시 캐시에서 바로 결과가 재표시됨을 확인.

---

## 7-AB. 2026-08-06(5차) 로딩 시간 실측·개선 ("로딩시간이 좀 긴것 같아서 한번 점검해봐줄래")

> **상태**: ✅ 원인 실측으로 확인 + 수정 완료(로컬 검증까지, 배포는 사용자 확인 후).
> **배경**: 배포 완료 후 사용자가 로딩이 길게 느껴진다고 피드백. 추측 대신 실제 배포 사이트에 직접 타이밍을 재보는 방식으로 접근.

### 7-AB-1. 실측으로 원인 특정

- 실제 앱의 `generateSajuIntro()` 호출과 동일한 형태(사주 정보 포함 JSON 프롬프트, `maxOutputTokens: 8192`, `responseMimeType: application/json`)로 `gemini-3.6-flash`에 직접 요청해 시간을 측정 — **15.8초** 소요, 응답 텍스트는 671자(405 토큰)뿐인데 `usageMetadata.thoughtsTokenCount`가 **2315**로, 실제 답변 분량보다 훨씬 많은 "숨은 사고(thinking)" 토큰을 먼저 소모하고 있었음.
- 이 앱의 모델 폴백 1순위인 `gemini-3.6-flash`(및 `gemini-2.5-flash`)가 확장 사고(extended thinking) 모델이라, 단순한 페르소나 기반 텍스트 생성 작업에도 매번 이 사고 과정을 거쳐 지연이 발생하는 구조였음 — 사주 인트로/카테고리 해석/오늘의 나풀이/타로/트랜짓 등 앱 전반의 자동/버튼 생성형 콘텐츠가 전부 이 경로를 탐.

### 7-AB-2. 해결 — `thinkingConfig.thinkingBudget` 적용

- Gemini API가 지원하는 `generationConfig.thinkingConfig.thinkingBudget` 파라미터를 직접 실험: `thinkingBudget: 0`은 이 모델에서 400 오류(허용 안 됨), `512`는 짧은 프롬프트(사주 인트로 수준)에서 **15.8초 → 3.6초(약 4.4배)**로 줄이면서 사고 토큰을 0으로 만들고 JSON 품질도 그대로 유지함을 확인. 우리 서버리스 프록시(`api/gemini.ts`)를 거쳐서도 동일하게 동작함을 실제 배포 엔드포인트에 대해 재확인(5.4초).
- 15~20줄 이상을 요구하는 긴 "심화해석"류 프롬프트는 `thinkingBudget: 512`를 줘도 모델이 필요하다고 판단하면 여전히 사고 토큰을 많이 쓰는 것으로 관찰됨(개선 폭이 적음, 17.6초→16.3초) — 즉 이 값은 "상한 힌트"에 가깝고 복잡한 요청엔 강제되지 않는 것으로 보이지만, 짧고 자동으로 실행되는 콘텐츠(로딩 화면 직결)에서는 효과가 뚜렷해 손해 볼 것 없이 전체에 적용.
- `gemini-3.5-flash-lite`/`gemini-2.5-flash`/`gemini-2.5-pro` 나머지 폴백 모델에도 이 파라미터가 오류 없이 받아들여짐을 확인(2.5-pro는 기존에 알려진 무료 할당량 소진으로 인한 429만 발생, `thinkingConfig`와는 무관).
- `src/utils/geminiApi.ts`의 `callGeminiJsonApi`/`callGeminiPlainApi` 두 곳 `generationConfig`에 `thinkingConfig: { thinkingBudget: 512 }` 추가.

### 7-AB-3. 검증 및 배포

- `tsc --noEmit` / `npm run build` 클린.
- 실제 배포된 `api/gemini.ts` 프록시를 통해 `thinkingConfig` 포함 요청이 정상적으로 통과·응답됨을 `curl`로 확인(5.4초, 사고 토큰 0).
- 사용자 승인 후 배포 완료(커밋 `04246ab`).

### 7-AB-4. 후속 — "심화해석"은 왜 덜 빨라졌는지 추가 실측, `DEEP_MODELS` 순서 신설

> 배포 후 사용자가 "심화해석은 로딩시간 좀더 줄일 수 있는 방법 없어?"라고 추가 질문 — 7-AB-2에서 이미 "긴 프롬프트는 개선폭이 작다"고 기록해뒀던 부분을 실제로 더 파고듦.

- `gemini-3.5-flash-lite`에 동일한 심화해석용 긴 프롬프트(15~20줄, 십신 용어 2회 이상, 실생활 예시 2개 이상 요구)를 직접 보내 비교: **5.9초** 만에 완료, 사고 토큰 0, 십신 용어(편관·상관·정관)를 실제로 자연스럽게 녹여 쓰고 구체적 예시도 2개 이상 포함한 풍부한 결과를 냄 — `gemini-3.6-flash`의 16~17초 대비 품질 저하 없이 대폭 빠름을 확인(전문 텍스트 직접 대조).
- 사주 인트로 스타일의 짧은 프롬프트로도 `gemini-3.5-flash-lite` 품질을 재확인(2.8초, 은유적 표현 포함 완성도 높은 JSON) — 다만 이 모델을 전체 기본 순서 1번으로 승격하는 것은 "짧고 자동 생성되는 첫인상 콘텐츠"의 품질 기준을 시스템 전체에 걸쳐 바꾸는 결정이라, 이번엔 사용자가 콕 집어 물어본 "심화해석"에만 국한해 적용(범위를 넘는 결정은 임의로 하지 않음).
- `geminiApi.ts`에 `DEEP_MODELS`(`gemini-3.5-flash-lite` → `gemini-2.5-flash` → `gemini-3.6-flash` → `gemini-2.5-pro`) 배열을 신설하고, `callGeminiPlainApi`에 선택적 `models` 매개변수를 추가(기본값은 기존 `MODELS`). 6개 심화해석 함수(`generateCategoryDeepInterpretation`, `generateFengShuiDeepInterpretation`, `generateFortuneDeepInterpretation`, `generateElementSummaryDeepInterpretation`, `generateCompatibilitySummaryDeepInterpretation`, `generateAstrologyDeepInterpretation`) 전부가 이 `DEEP_MODELS`를 쓰도록 수정. 그 외 자동/버튼 생성형 짧은 콘텐츠는 기존 `MODELS` 순서 그대로 유지.

### 7-AB-5. 검증

- `tsc --noEmit` / `npm run build` 클린.
- 실제 배포된 프록시를 통해 `model=gemini-3.5-flash-lite` 요청도 정상 통과함을 `curl`로 재확인.
- Playwright로 3가지 시나리오 확인: (1) 사주 인트로(짧은 JSON 호출)는 여전히 `gemini-3.6-flash`를 1순위로 요청, (2) 오행 종합 해설(짧은 plain 호출)도 여전히 `gemini-3.6-flash` 1순위, (3) 오행 종합 **심화**해석만 `gemini-3.5-flash-lite`를 1순위로 요청 — 의도한 대로 심화해석에만 정확히 국한되어 적용됨을 확인.
- 번들 크기도 함께 점검: 메인 번들 383KB(gzip 144KB), `cloudSync` 청크 659KB(gzip 165KB)는 로그인 기능 사용 시에만 동적 import되도록 이미 분리돼 있어 초기 로딩을 막지 않음 — 로딩 체감의 지배적 요인은 번들이 아니라 AI 응답 대기 시간이었음을 재확인, 번들 쪽은 추가 조치 불필요.
- 사용자 확인 후 배포 완료(커밋 `b9ad268`), 프로덕션 엔드포인트에서 `gemini-3.5-flash-lite` 요청이 실제로 정상 통과함을 `curl`로 재확인.

---

## 7-AC. 2026-08-06(7차) 신규 기능 3종: AI 후속질문 · 나와 닮은 인물 · PDF 섹션 선택

> **상태**: ✅ 구현 완료, `tsc --noEmit`/`npm run build` 클린, Playwright 4개 시나리오 PASS. 사용자 확인 후 배포 완료(7-AC-4 참고).
> **배경**: 최우선 버그·성능 개선을 마친 뒤 사용자가 "다른 특정한 기능"을 요청. 세 후보(딥링크 공유/AI 후속질문/나와 닮은 인물/라이트 테마/PDF 섹션 선택) 중 딥링크는 보류, 나머지 3개를 진행하기로 함 — `AskUserQuestion`으로 기능당 여러 차례 설계 질문을 주고받아(배치·지속성·범위·인물 풀·PDF 체크 단위 등) 방향을 먼저 확정한 뒤 구현.

### 7-AC-1. AI 후속질문(채팅)

- **확정된 설계**: "AI 해석" 탭 안에 통합(새 탭 아님) / 카테고리와 무관하게 탭 전체에서 대화 하나 공유 / localStorage에 저장해 다음 방문에도 이어보되, 실제 API에 다시 실어 보내는 컨텍스트는 최근 4턴(`CHAT_CONTEXT_TURNS`)까지만, 화면·저장 쪽 표시는 최근 20개 메시지(`CHAT_DISPLAY_LIMIT`, 10턴)까지만 유지해 토큰/저장 공간이 무한정 늘지 않게 함 / "🗑️ 대화 초기화" 버튼 포함(사용자 요청으로 추가).
- **비용 관련 사전 확인**: `gemini-3.5-flash-lite` 입력 $0.30/출력 $2.50(100만 토큰당, [공식 가격표](https://ai.google.dev/gemini-api/docs/pricing)) 기준, 4턴 컨텍스트 유지 시 호출당 입력 약 1,000토큰 안팎 — 사실상 비용 걱정 없는 수준임을 사용자에게 먼저 설명하고 진행.
- `geminiApi.ts`: `generateFollowUpAnswer(apiKey, name, gender, mbti, sajuResult, recentHistory, question)` 신규 추가(`callGeminiPlainApi` 재사용, 2~4문장 존댓말 답변).
- `App.tsx`: `chatMessages`/`chatInput`/`chatLoading` state, `chatCacheKey()`, `handleSendChatMessage()`/`handleClearChat()` 핸들러, "AI 해석" 탭의 5개 카테고리 탭 콘텐츠 아래(탭 전환과 무관하게 항상 노출)에 채팅 버블 리스트 + 입력창 + 전송 버튼 UI 추가.

### 7-AC-2. 나와 닮은 인물 AI 매칭카드

- **확정된 설계**: 역사·신화·고전문학 속 인물만(실존 유명인은 할루시네이션·명예훼손 위험으로 배제) / AI가 자유롭게 고르지 않고 미리 선정한 24명 후보군 안에서만 고르게 함(품질·사실관계 리스크 최소화) / "AI 해석" 탭의 6번째 탭으로 추가 / 한번 생성되면 다른 카테고리처럼 캐시되고 "🔄 다시 뽑기" 버튼으로 재생성.
- `src/data/archetypeFigures.ts` 신규: 동양/서양, 역사/신화/문학을 고루 섞은 24명(소크라테스, 클레오파트라, 다빈치, 잔 다르크, 제갈량, 항우, 유비, 오디세우스, 아킬레우스, 아테나, 헤르메스, 손오공, 헤르미온느, 셜록 홈즈, 돈키호테, 이순신, 세종대왕, 나폴레옹, 마리 퀴리, 간디, 뮬란, 프시케, 마르쿠스 아우렐리우스, 세헤라자데) — 각각 id/이름/출처/한줄 본질/핵심 특성 키워드 보유.
- `geminiApi.ts`: `generateArchetypeMatch()` — 후보 24명 목록을 프롬프트에 전부 포함해 그중 `figureId` 하나만 고르게 하고 `analysis`/`factBomb`은 새로 생성. **안전장치**: AI가 목록 밖 id를 반환하거나 JSON 파싱이 실패해도, 이름+일주 해시 기반 결정론적 폴백으로 반드시 24명 후보 안의 인물이 나오도록 보장(`hashStringToInt`). Playwright로 일부러 목록 밖 id를 응답하게 모의해 실제로 안전하게 대체됨을 확인.
- PDF 리포트의 "AI 해석" 섹션에도 닮은 인물 결과가 포함되도록 `handleDownloadPDF()`에 연동.

### 7-AC-3. PDF 저장 시 섹션 선택

- **확정된 설계**: "PDF 저장" 버튼을 누르면 체크박스 모달이 먼저 뜨고(기본값 전체 체크, 기존 동작과 동일) / 12개 개별 항목이 아니라 8개 큰 단위(AI 해석[성격·커리어·연애·재물·닮은 인물 통합] / 처방전 / 오행 종합 / 궁합 / 풍수 / 대운·세운&운세 해설 / 사주 4기둥 심층 해설 / 별자리)로 묶어서 선택.
- `App.tsx`: `PdfSectionKey` 타입 + `PDF_SECTION_META`(라벨/설명), `pdfModalOpen`/`pdfSections` state. `handleDownloadPDF()`를 섹션별로 게이팅해, 체크 해제한 섹션은 **생성 자체(API 호출)를 건너뛰어** PDF 저장이 더 빨라지도록 함 — 기존에 이미 "값이 있을 때만 렌더링"하던 삼항연산자 구조를 그대로 활용해 HTML 조립부는 손대지 않고 데이터 소스만 게이팅.
- Playwright로 풍수·오행 종합 체크 해제 후 PDF 생성 시 실제로 그 두 섹션에 대한 API 호출이 전혀 발생하지 않음을 네트워크 요청 가로채기로 확인, AI 해석(체크 유지)은 정상 호출됨도 함께 확인.

### 7-AC-4. 검증

- `tsc --noEmit` / `npm run build` 클린(번들 395KB, gzip 149KB — 신규 기능 3종 추가로 약간 증가했으나 미미한 수준).
- Playwright(임시 설치, `--no-save`) 4개 시나리오 전부 PASS: (1) 채팅 응답 표시 + 새로고침 후 대화 유지 + 초기화 버튼 동작, (2) 닮은 인물 매칭 결과 정상 표시, (3) 목록 밖 figureId 응답 시 안전하게 폴백, (4) PDF 섹션 선택이 실제 API 호출 여부를 정확히 좌우함.
- 사용자 확인 후 배포 완료(커밋 `bf4e549`), `vercel inspect`로 새 배포가 프로덕션에 반영됐음을 확인.

---

## 7-AD. 2026-08-06(8차) 실사용 피드백 5건 수정

> **상태**: ✅ 구현 완료, `tsc --noEmit`/`npm run build` 클린, Playwright 스크린샷으로 시각 검증. **2026-08-07 재검토로 배포 확인** — 커밋 `b51e69a`가 `origin/main`에 push되어 있고, 로컬 `npm run build` 결과물이 프로덕션 번들(`index-Ds3ykOW3.js`, 내용 기반 해시 완전 일치)과 동일함을 직접 대조해 실제 배포 완료를 확정함 (아래 7-AD-5 참고).
> **배경**: 7-AC 배포 후 사용자가 실제 화면 스크린샷 4장과 함께 문제 5가지를 한 번에 제보: (1) 오행 심화해석이 "불러올 수 없습니다"로 나옴, (2) AI 채팅 입력이 안 됨, (3) 카테고리 개인화 질문 칩 글자가 안 보임, (4) 닮은 인물 카드가 밋밋함, (5) 타로 카드도 밋밋함.

### 7-AD-1. AI 채팅 입력창이 사실상 입력 불가능했던 CSS 버그

- **원인**: `.btn-primary`가 전역적으로 `width: 100%`인데, 전송 버튼에 `flexShrink: 0`만 주고 너비는 그대로 둬서, flex 컨테이너 안에서 버튼이 "줄어들지도 않으면서 100%를 요구"해 옆의 입력창을 거의 0px로 짓눌렀음. 사용자가 "버튼이 활성화가 안 된다"고 느낀 것도 사실은 입력창이 너무 좁아 글자를 제대로 입력할 수 없었던 게 원인(빈 입력값이라 `disabled` 조건이 계속 참이었음).
- **수정**: 입력창에 `flex: 1, minWidth: 0`, 버튼에 `width: 'auto'`를 명시적으로 추가해 정상적인 "입력창이 늘어나고 버튼은 고정 크기" 레이아웃으로 교정.

### 7-AD-2. 카테고리 개인화 질문 칩 색상 대비 부족

- **원인**: 커리어/연애/재물 탭의 "요즘 내 연애 세포는?" 같은 선택지 버튼이 12시진 선택 버튼과 같은 `.hour-btn` 클래스를 재사용했는데, 원래 패턴은 텍스트를 `<span className="hour-btn-name">`으로 감싸 명시적 색상을 입혀야 하는 구조였음. 이 칩들만 라벨을 감싸지 않고 버튼에 그냥 텍스트를 넣어 색상이 지정되지 않았고, 결과적으로 어두운 배경에 거의 안 보이는 색으로 렌더링되고 있었음.
- **수정**: 선택 여부에 따라 `var(--gold)`/`var(--silver)` 색을 직접 지정.

### 7-AD-3. 🔴 심화해석 실패 시 폴백 문구가 성공한 것처럼 영구 캐시되는 구조적 버그

- 사용자가 겪은 "오행 심화해석 불러올 수 없음"의 근본 원인을 조사하던 중, 그 증상 자체보다 훨씬 심각한 구조적 버그를 발견함. `callGeminiPlainApi`는 완전히 실패해도 예외를 던지지 않고 미리 정해둔 폴백 문구(예: "오행 종합 심화 해설을 지금은 불러올 수 없습니다...")를 정상 반환값처럼 돌려주는데, 심화해석 계열의 폴백 문구는 실제 콘텐츠가 아니라 사실상 에러 메시지임. 호출부(`App.tsx`)는 이걸 성공으로 착각해 `localStorage`에 그대로 캐시해버렸고, 캐시가 있으면 재시도 버튼 없는 "완료된" UI로 전환되는 기존 구조상 **한 번 실패하면 그 사용자는 영구히 이 문구만 보게 되는** 버그였음(재현했던 정확히 그 프롬프트를 다시 보내보니 실제로는 정상 응답이 왔음 — 최초 실패는 일시적이었을 가능성이 높지만, 그 실패가 "성공"으로 영구 박제된 게 진짜 문제).
- **수정**: `callGeminiPlainApi`에 `throwOnFailure` 매개변수 추가(기본 `false`, 기존 동작 유지). 심화해석 6개 함수 + 이번에 추가한 AI 후속질문(`generateFollowUpAnswer`, 이 함수의 폴백 문구도 에러 메시지 성격이라 동일 버그 대상)은 `true`로 호출하도록 변경 — 실패 시 예외를 던져, 원래도 있었던 호출부의 catch(토스트 안내 + state 미설정 + 버튼 유지)가 정상 동작하게 함. 반대로 타로·풍수(짧은 버전) 등 폴백 문구 자체가 그럭저럭 쓸만한 정적 콘텐츠인 함수들은 기존처럼 조용히 대체되도록 그대로 둠.
- **기존에 이미 잘못 캐시된 사용자 데이터 대응**: 위 수정은 "앞으로" 재발을 막을 뿐, 이 버그가 이미 심어놓은 캐시는 그대로 남아있어 `isStaleDeepFallbackText()` 헬퍼를 추가해 캐시를 읽어올 때 알려진 폴백 문구 패턴("...지금은 불러올 수 없습니다. 잠시 후 다시 시도해 주세요."로 끝남)이면 "생성 안 됨" 상태로 취급하도록 6곳의 캐시 로드 지점에 전부 적용 — 기존에 이미 이 문구가 박제된 사용자도 다음 방문 시 자동으로 재시도 버튼이 다시 뜸.

### 7-AD-4. 닮은 인물 · 타로 카드 비주얼 품질 개선

- 기존 카드는 이모지 하나 + 텍스트만 있는 평면적인 그라디언트 박스라 사용자가 "밋밋하다"고 느낌. `App.css`에 `.persona-card`(카드 프레임: 이중 테두리·방사형 글로우·깊은 그림자) + `.persona-card-medallion`(원형 메달리온 배지: 입체적 그라디언트·링 테두리·내부 하이라이트) 공통 스타일을 신설.
- 닮은 인물 카드: 메달리온 안에 인물 이모지, 세리프체로 강조한 이름, 구분선, 출처, 핵심 특성 키워드 배지까지 추가한 "인물 카드"다운 레이아웃으로 재구성(색상은 앱 기본 보라/골드 톤 고정).
- 타로 카드: `tarotCardTheme()`가 이제 강조색뿐 아니라 짙은색·글로우 색까지 반환해 메달리온에 입체감을 주고, 나머지 요소(이름/영문명/정역방향 배지)도 동일한 `.persona-card` 구조로 통일. 역방향이면 메달리온 안 이모지만 180도 회전.
- 실제 카드 사진/삽화(이미지 자산)를 쓰는 방향은 여전히 범위 밖 — 이번 개선은 어디까지나 CSS만으로 낼 수 있는 품질의 상한선을 높인 것.

### 7-AD-5. 검증

- `tsc --noEmit` / `npm run build` 클린.
- Playwright(임시 설치, `--no-save`)로 스크린샷 기반 시각 검증: (1) 개인화 질문 칩 글자가 밝은 회색/골드로 명확히 보임, (2) 채팅 입력창이 정상 너비(700px대)로 늘어나고 실제 타이핑·전송이 됨, (3) 닮은 인물 카드가 메달리온·세리프 이름·구분선·배지를 갖춘 형태로 렌더링, (4) 타로 카드도 동일한 구조로 렌더링.
- **배포 확인 (2026-08-07)**: 당시 "아직 배포 전"으로 기록했으나, `git status` 확인 결과 커밋 `b51e69a`가 이미 `origin/main`에 push되어 있었고, 로컬에서 동일 커밋으로 `npm run build`한 결과물의 메인 번들 파일명(`index-Ds3ykOW3.js`)이 프로덕션과 완전히 일치함을 확인해 실제로 이미 배포되어 있었음을 뒤늦게 확정함. 기록 누락이었을 뿐 실제 배포 자체는 완료돼 있었음.

---

## 7-AE. 2026-08-07 계획안 정합성 재검토 + 우선순위 높은/중간 보완 4건 — 구현 완료

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린(매 단계마다 확인), `vite preview`로 정적 서빙 200 응답까지 확인. **주의**: 이 세션은 Playwright 등 브라우저 자동화 도구를 쓰지 않았음 — 실제 클릭/화면 동작까지의 시각적 검증은 다음 세션에서 한 번 확인 필요.
> **배경**: "계획안 오류·코드 문제를 세밀하게 확인해달라"는 요청으로 문서 전체(1492줄)와 코드를 대조 재검토(7-AE-0). 이어서 "그 외에도 진행해줘" 요청에 따라 검토 중 발견한 보완 항목 4개를 우선순위 낮은 것부터 순서대로 구현.

### 7-AE-0. 문서·코드 정합성 재검토

- 문서 자체의 오기 3건 발견·정정: ① 7-AC/7-AD의 "아직 배포 전" 기재가 실제로는 이미 배포된 상태(위 7-AC-4/7-AD-5 정정 참고)였음, ② 7-AC 섹션 헤더/본문 간 "배포 전" vs "배포 완료" 자기모순, ③ 최상단 요약이 7-AD(같은 날 8차 세션)의 신규 발견을 반영하지 못하고 있었음.
- 최근 10개 이상 "구현 완료" 주장(`api/tsconfig.json`, XSS 방어, JSON 재시도 로직, 십신 계산, `DEEP_MODELS`, 채팅/닮은인물/PDF섹션선택 등)을 실제 소스와 직접 대조 — **실질적 코드 불일치는 없음**을 확인.
- 코드 레벨 신규 발견(문서에 없던 것): localStorage 캐시 만료 로직 부재(즉시 수정, 8번 표 참고), `App.tsx`/`geminiApi.ts` 비대화, 하드코딩 도메인 3곳.

### 7-AE-1. 하드코딩된 배포 도메인 3곳 → 공용 상수로 정리

- **문제**: `mbti-delta-red.vercel.app`이 `App.tsx`(카카오 공유 URL 폴백, 이미지 카드 워터마크) 2곳과 `geminiApi.ts`(네이티브 앱 API 절대경로) 1곳에 각각 하드코딩되어 있어, 도메인 변경 시(7-F-2에서 검토했던 것처럼) 3곳을 일일이 찾아 고쳐야 했음.
- **수정**: `src/deployConfig.ts` 신규 — `DEPLOY_DOMAIN`/`DEPLOY_ORIGIN` 상수 하나로 통일, 3개 지점 전부 이 상수를 참조하도록 교체. 도메인 값 자체는 그대로 유지(7-F-2의 "도메인 변경 보류" 결정은 그대로 존중, 이번엔 유지보수 편의성만 개선).

### 7-AE-2. 개별 결과(오늘의 타로 · 나와 닮은 인물) 이미지 공유 기능 추가

- **배경**: 기존엔 프로필 요약 카드(`handleDownloadImageCard`) 하나만 이미지로 저장·공유할 수 있었음. "오늘의 타로"·"나와 닮은 인물"처럼 개별성이 강한 결과도 각각 공유할 수 있으면 습관화·바이럴 확산에 도움이 될 것으로 판단.
- **구현**: `handleDownloadPersonaCard()` 공용 함수 신규 추가 — 프로필 카드와 동일한 브랜드 크롬(9:16 인스타 스토리 규격, 상단 워터마크·하단 도메인)을 재사용하되, 중앙에는 카드 하나(이모지 메달리온 + 이름 + 부제 + 배지 + 본문, 최대 9줄 안전 절단)만 크게 담는 범용 레이아웃으로 설계. "오늘의 타로" 카드 헤더와 "나와 닮은 인물" 카드 헤더에 각각 "🖼️ 이미지" 버튼 추가.
- `navigator.share`(파일 공유 API) 우선 시도, 미지원 시 PNG 다운로드로 대체 — 기존 프로필 카드와 동일한 패턴.

### 7-AE-3. "오늘" 탭 콘텐츠 구조적 차별화 (8-2번 항목 잔여 과제 해소)

- **문제**: 8-2번에서 이미 지적됐던 것처럼, "오늘의 나풀이"(명리)와 "오늘의 트랜짓"(점성술)이 `{keyword, analysis, factBomb}`로 완전히 동일한 데이터 구조를 갖고 있어 세계관만 다를 뿐 "농담 한 줄로 끝나는 콘텐츠"라는 인상이 겹침.
- **수정**: 트랜짓 쪽 `factBomb`(위트 있는 농담)을 `luckyWindow`(오늘 하루 중 참고할 만한 구체적 행운 시간대 + 이유 + 추천 행동)로 교체. "웃고 넘기는 나풀이" vs "실용적으로 참고하는 하늘 예보"로 두 콘텐츠의 쓸모 자체를 다르게 가져감. `App.css`에 `.lucky-window-box`(파랑 톤, 기존 `.fact-bomb-box`의 빨강/주황 톤과 시각적으로도 구분) 신규 추가.
- 이 필드는 날짜 기준 캐시 키(`transitCacheKey`)라 하루 지나면 자동으로 새 스키마로 갱신되므로 기존 캐시 마이그레이션은 불필요.

### 7-AE-4. `App.tsx` / `geminiApi.ts` 파일 분리 리팩터링

- **배경**: `App.tsx` 4,410줄·`geminiApi.ts` 1,425줄로 계속 비대해지는 중이라는 지적. 기능 변경 없이 구조만 정리.
- **`geminiApi.ts` → `src/utils/gemini/{core,saju,deep,astrology,social}.ts` 5개로 분리**: `core.ts`(모델 목록·JSON/plain 호출 공통 인프라), `saju.ts`(사주 인트로·카테고리·처방전·풍수·운세·오행/궁합 종합·정밀궁합·기둥해설), `deep.ts`(심화해석 5종 + 십신 근거 헬퍼), `astrology.ts`(서양점성술·오늘의 트랜짓·타로), `social.ts`(채팅·닮은인물). 기존 `geminiApi.ts`는 이 5개를 `export *`로 재수출하는 13줄짜리 배럴(barrel)로 축소 — **App.tsx를 포함해 이 모듈을 가져다 쓰던 코드는 단 한 줄도 수정할 필요가 없었음**(import 경로 그대로 유지).
- **`App.tsx`에서 React 컴포넌트/훅과 무관한 모듈 스코프 코드 분리**: `src/appTypes.ts`(FormData/AppResult/Bookmark 등 순수 데이터 타입), `src/appHelpers.ts`(캐시 키 함수 20개·`setCachedItem`·이스케이프/캔버스 유틸·고정 카피 상수), `src/components/NapuliMark.tsx`(로고 SVG 컴포넌트)로 이동. `App.tsx`는 4,410줄 → 4,309줄로 축소.
- **범위 밖으로 남긴 것**: `App.tsx`의 나머지(상태·핸들러·JSX 렌더 트리)는 여전히 하나의 거대한 컴포넌트 함수 안에 있음. 이를 여러 하위 컴포넌트로 쪼개는 작업은 상태를 어디서 어떻게 나눌지에 대한 설계 판단이 필요하고, 이 세션에는 Playwright 등 실제 화면 동작 검증 수단이 없어 회귀 위험을 확인할 방법이 부족해 시도하지 않음 — 향후 별도 세션에서 시각적 검증 도구와 함께 진행 권장.

### 7-AE-5. 검증

- 4개 항목 각각 구현 직후 `npx tsc --noEmit`/`npm run build`로 개별 확인, 마지막에 전체 통합본으로 한 번 더 클린 확인.
- `vite preview`로 빌드 산출물을 로컬에 정적 서빙해 `curl`로 200 응답 확인 — 번들 로드 자체가 깨지지 않았음을 최소한으로 확인.
- **미검증 남은 부분**: 이미지 카드 버튼 클릭 → 실제 PNG 렌더링 결과의 시각적 확인, 트랜짓 `luckyWindow` 필드의 실제 Gemini 응답 확인, 파일 분리 후 전체 사용자 플로우(입력→로딩→결과→탭 전환)의 실제 클릭 테스트는 이번 세션에서 수행하지 못함 — 다음 세션에서 Playwright로 우선 확인 권장.

---

## 7-AF. 2026-08-07 앱스토어 심사 대비 — Sign in with Apple 추가

> **상태**: ⚠️ **코드는 구현 완료, 하지만 대시보드/Xcode 설정 없이는 실제로 동작하지 않음 — 사용자 본인이 해야 하는 수동 설정 3가지가 남아있음(아래 7-AF-2).** `tsc --noEmit`/`npm run build` 클린.
> **배경**: "앱스토어로 낼 건데 매력 요소·리텐션 콘텐츠가 뭐가 있을까" 질문에 코드를 직접 점검해 답한 결과, `src/utils/cloudSync.ts`가 구글 로그인(`GoogleAuthProvider`)만 제공하고 있음을 확인. iOS 앱에 제3자 소셜 로그인이 하나라도 있으면 **Apple App Store 심사 가이드라인 4.8**에 따라 Sign in with Apple을 동등하게 제공해야 하며, 안 지키면 리젝 사유가 됨 — 우선순위 1순위로 먼저 처리.

### 7-AF-1. 구현

- `src/utils/cloudSync.ts`: `signInWithApple()` 신규 추가 — Firebase의 `OAuthProvider('apple.com')`으로 구글 로그인과 동일한 `signInWithPopup` 패턴 재사용(email/name 스코프 요청).
- **부수적으로 발견한 버그 수정**: 기존 `deleteAccount()`가 재인증이 필요할 때(`auth/requires-recent-login`) 무조건 `GoogleAuthProvider`로 재인증을 시도하고 있었음 — 이제 애플 로그인 사용자가 생기면 이 사용자는 계정 삭제 시 재인증에 항상 실패하는 버그가 될 상황이었음. `reauthProviderFor(user)` 헬퍼를 추가해 `user.providerData[0].providerId`를 보고 가입 당시 사용한 제공자(구글/애플)로 정확히 재인증하도록 수정.
- `src/App.tsx`: `handleSignInApple()` 핸들러 추가, 다이어리 화면 로그인 카드에 "구글로 로그인" 버튼 옆에 "Apple로 로그인" 버튼 추가(동일한 `btn-secondary` 스타일로 나란히 배치해 두 로그인 수단이 동등하게 노출되도록 함 — 가이드라인 4.8이 요구하는 "동등한 노출"의 핵심 조건).

### 7-AF-2. 코드만으로는 끝나지 않음 — 사용자가 직접 해야 하는 설정 3가지

1. **Firebase 콘솔**: Authentication → Sign-in method에서 "Apple" 제공자를 활성화하고, Apple 개발자 계정에서 발급받은 Services ID/Team ID/Key ID/개인 키를 등록해야 함.
2. **Apple 개발자 계정**: developer.apple.com에서 이 앱의 App ID에 "Sign in with Apple" capability를 추가하고, Firebase 연동에 필요한 Services ID를 새로 만들어야 함.
3. **Xcode**: `ios/App` 프로젝트를 열어 Signing & Capabilities 탭에서 "Sign in with Apple" capability를 추가해야 함 — 이 프로젝트에는 아직 `.entitlements` 파일 자체가 없어서(코드로 확인함, `find ios -iname "*.entitlements"` 결과 없음) capability를 추가하면 Xcode가 자동으로 생성해 줌. 이 단계는 Xcode GUI 조작이 필요해 코드 편집으로는 대신할 수 없음.

이 3가지가 끝나지 않으면 "Apple로 로그인" 버튼을 눌러도 에러가 남 — 앱스토어 제출 전 반드시 완료 필요.

### 7-AF-3. 검증

- `tsc --noEmit` / `npm run build` 클린.
- 위 대시보드/Xcode 설정이 없어 실제 로그인 동작 자체는 이 세션에서 확인하지 못함 — 설정 완료 후 실기기(또는 웹에서 Apple 로그인 테스트 가능 여부 확인 후)에서 직접 눌러 검증 필요.

---

## 7-AG. 2026-08-07 애널리틱스(GA4) + PWA(설치형 웹앱) 추가

> **상태**: ✅ 코드 구현 완료. `tsc --noEmit`/`npm run build` 클린, `vite preview`로 `/`, `/manifest.json`, `/sw.js`, 아이콘 전부 200 확인. **애널리틱스는 사용자가 GA4 속성을 만들어 측정 ID를 `.env`에 채워야 실제로 데이터가 쌓이기 시작함(아래 7-AG-1 참고) — 이 세션에서 실제 이벤트 수신 확인은 하지 못함.**
> **배경**: "앱스토어 제출 전까지 필요한 과정만 하고, 그 외 보완할 기능이 있다면?" 질문에 코드를 점검해 두 가지를 찾음 — ① 이 앱엔 어떤 사용성 추적 코드도 없어 20개 넘는 AI 기능 중 뭐가 실제로 쓰이는지 전혀 알 수 없었고, ② PWA(설치형 웹앱) 지원이 전무해서 앱스토어 없이 가능한 "홈 화면 설치"·"오프라인 접근"·(향후) 웹 푸시 경로가 막혀 있었음. "애널리틱스 → PWA" 순서로 진행하기로 확정.

### 7-AG-1. GA4 애널리틱스 연동

- `src/utils/analytics.ts` 신규 — `initAnalytics()`(gtag.js 동적 로드, `VITE_GA_MEASUREMENT_ID` 환경변수가 없으면 조용히 비활성화되는 Firebase와 동일한 패턴), `trackEvent(name, params)`(설정 안 돼 있으면 무동작).
- 이 앱은 라우팅 없는 단일 페이지(탭 전환만 있음)라 GA4 자동 page_view는 의미가 없어 꺼두고(`send_page_view: false`), 대신 `activeSection`/`activeSajuTab` 변화를 `screen_view` 이벤트로 직접 추적하는 `useEffect`를 추가.
- **주요 기능 핸들러 20곳 이상에 `content_generate`(feature별로 `personality`/`career`/`romance`/`wealth`/각 `_deep`/`fengshui`/`fortune`/`element_summary`/`compat_summary`/`astrology`/`transit`/`tarot`/`pair_compat`/`daily_fortune`/`pillar_deep`/`archetype`/`chat_message` 등), `share`(카카오·네이티브 공유), `download`(PDF·이미지 카드), `sign_in`(구글/애플 구분), `result_view`(폼 제출→결과 생성 성공, 핵심 전환 지표) 이벤트를 삽입.
- `.env`에 `VITE_GA_MEASUREMENT_ID=`(빈 값) placeholder를 안내 주석과 함께 추가해둠 — **사용자가 analytics.google.com에서 GA4 속성을 만들고 측정 ID(`G-XXXXXXXXXX`)를 발급받아 채워야 실제로 동작**(코드만으로는 데이터가 쌓이지 않음).

### 7-AG-2. PWA(설치형 웹앱) 지원

- `public/manifest.json` 신규 — 앱 이름/아이콘(기존 `public/icons/icon-*.webp` 7종 재사용)/테마 컬러(`#8b5cf6`)/배경색(`#050510`, `--bg-deep`과 동일)/`display: standalone` 설정.
- `public/sw.js` 신규 — 최소 캐싱 서비스워커. Vite가 콘텐츠 해시를 파일명에 넣는 `/assets/*`는 cache-first(안전하게 오래 캐시 가능), `index.html` 등 나머지는 network-first(배포 직후 옛날 페이지에 갇히는 화이트스크린 방지, 오프라인일 때만 캐시로 대체), `/api/*`는 캐시 대상에서 완전히 제외(AI 응답이 캐시되어 재사용되면 안 되므로).
- `index.html`에 `<link rel="manifest">`/`theme-color`/`apple-touch-icon` 메타 추가. `src/main.tsx`에서 프로덕션 빌드 + 웹(네이티브 Capacitor 앱 제외)에서만 서비스워커를 등록.
- **범위 밖으로 남긴 것 — Web Push(실제 알림 발송)**: 이번엔 "설치 가능하게 만들기"까지만 함. 실제로 서버가 사용자에게 푸시를 보내려면 ① VAPID 키 생성, ② 사용자별 푸시 구독 정보를 Firestore에 저장하는 구독 UI, ③ 그 구독으로 실제 알림을 보내는 서버 엔드포인트(예: `api/` 아래 `web-push` 라이브러리 사용) 3가지가 추가로 필요한 별도 작업 — 다음에 진행 여부를 여쭤볼 것.

### 7-AG-3. 검증

- `tsc --noEmit` / `npm run build` 클린.
- `vite preview`로 로컬 정적 서빙 후 `curl`로 `/`, `/manifest.json`, `/sw.js`, `/icons/icon-192.webp` 전부 200 확인, `manifest.json`을 `python -m json.tool`로 유효한 JSON인지 확인.
- **미검증**: 실제 브라우저에서 "홈 화면에 추가" 프롬프트가 뜨는지, GA4 대시보드에 실시간 이벤트가 실제로 찍히는지(측정 ID 미설정)는 이 세션에서 확인하지 못함. `VITE_GA_MEASUREMENT_ID` 설정 후 GA4 실시간 리포트에서 `result_view`/`content_generate` 이벤트가 들어오는지, 모바일 브라우저에서 설치 프롬프트가 정상적으로 뜨는지 다음에 확인 필요.

---

## 7-AH. 2026-08-07 스트릭 보상(마일스톤 배지 + 공유 카드) 추가

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린.
> **배경**: "매력 요소/리텐션 콘텐츠" 논의에서 나온 항목 — 연속 방문일수(스트릭) 표시는 있는데 그걸로 "얻는 게" 없어 습관화 효과가 약하다는 지적(계획안 8-2 초기 원인 분석 중 하나). 이 앱엔 결제/유료 기능이 전혀 없어 "무료 이용권"류 보상은 성립하지 않아서, 대신 **마일스톤 배지 + 자랑할 수 있는 공유 카드**로 방향을 잡음(7-AE-2에서 만든 개별 결과 이미지 카드 인프라를 그대로 재사용).

### 7-AH-1. 구현

- `src/utils/streak.ts`: `STREAK_TIERS`(3일 🌱새싹 / 7일 🔥불꽃 / 14일 ⭐별빛 / 30일 👑나풀이 마스터 / 100일 💎다이아몬드 — 앱의 우주/별자리 톤과 통일된 이름) + `getHighestTier()` 신규. `recordTodayVisitAndGetStreak()`의 반환 타입을 `number` → `{ count, newTier }`로 변경 — 오늘 새로 마일스톤을 넘겼을 때만 `newTier`를 채워 반환(같은 날 재방문·이미 축하한 마일스톤은 `null`). 스트릭이 끊기고 새로 시작되면 "축하 기록"도 초기화해, 다음 스트릭에서 같은 배지를 다시 딸 때도 축하가 재발동되도록 함(한 번 쓰고 버려지는 배지가 아니게).
- `src/App.tsx`: 새 마일스톤 달성 시 축하 토스트("🔥 7일 연속 방문 달성! '불꽃' 배지를 획득했어요") + `trackEvent('streak_milestone', ...)` 발생. "오늘" 탭 상단 스트릭 배지에 현재 티어 이름을 함께 표시하고, 3일차(첫 배지)부터 "🖼️ 배지 카드" 버튼을 노출 — 클릭 시 `handleDownloadPersonaCard()`(기존 오늘의 타로/닮은 인물과 동일한 인스타 스토리 규격 카드 생성 함수)를 `kind: 'streak'`로 재사용해 골드 톤의 스트릭 인증 카드를 즉시 공유/다운로드.

### 7-AH-2. 검증

- `tsc --noEmit` / `npm run build` 클린.
- **미검증**: 실제로 여러 날에 걸쳐 방문해 마일스톤을 실제로 넘겨보는 것은 하루 만에 재현이 어려워(날짜를 인위적으로 바꿔야 함) 이 세션에서 실제 크로스데이 시나리오 테스트는 하지 못함 — `localStorage`에서 `napuli_streak_last_visit`을 어제 날짜로, `napuli_streak_count`를 2로 수동 설정한 뒤 새로고침하는 방식으로 다음에 확인 권장.

---

## 7-AI. 2026-08-07 궁합 초대 링크(바이럴 루프) 추가

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린, 인코딩/URL 왕복 로직은 Node로 직접 시뮬레이션 검증(아래 7-AI-3).
> **배경**: "매력 요소" 논의에서 나온 항목 — 정밀 궁합 기능은 이미 있지만 카카오 공유 링크는 항상 사이트 첫 화면으로만 연결돼(7-A-2에서 "결과별 고유 URL이 없는 SPA라 보류"로 남겼던 부분), 공유해도 상대방이 "그냥 사이트"만 보고 끝나는 문제가 있었음. 전체 결과를 URL에 담는 대신 **정밀 궁합 하나로 범위를 좁혀서** 구현 — 정밀궁합 상대방 입력 폼이 애초에 이름/생년월일/성별 4개 필드만 쓰고 출생시각은 쓰지 않아(대운/시주 없이 일주만 비교), 인코딩할 정보가 가볍고 계산도 별도 AI 호출 없이 클라이언트에서 바로 가능함.

### 7-AI-1. 구현

- `src/appHelpers.ts`: `CompatInvite` 타입 + `encodeCompatInvite()`/`decodeCompatInvite()` — 이름/생년월일/성별을 JSON化 후 `encodeURIComponent`로 UTF-8 안전하게 감싸고 `btoa`로 base64 인코딩(손상된 값이 와도 `decodeCompatInvite`가 `null`을 반환해 앱이 죽지 않도록 방어).
- `src/App.tsx`:
  - 앱 시작 시 `?invite=` 쿼리 파라미터를 확인해 있으면 디코딩 후 상태(`compatInvite`)에 저장하고 즉시 URL에서 제거(`main.tsx`의 `devkey` 처리와 동일한 패턴 — 주소창/방문 기록에 개인정보가 남지 않도록).
  - 입력 화면에 `compatInvite`가 있으면 "OOO님이 궁합을 보자고 초대했어요" 안내 배너 표시.
  - `result`와 `compatInvite`가 둘 다 준비되면(내 정보 입력을 마치고 결과 화면에 도달하면) 자동으로 궁합 탭으로 이동 + 상대방 폼을 초대한 사람 정보로 채우고 `handleComparePair()`를 즉시 호출해 바로 궁합 결과를 보여주는 `useEffect` 추가(한 번 실행 후 `compatInvite`를 `null`로 소모해 재실행 방지).
  - "정밀 궁합" 카드에 "🔗 나와 궁합 볼 초대 링크 보내기" 버튼 추가(`handleShareCompatInvite`) — `navigator.share` 우선 시도, 미지원/취소 시 클립보드 복사로 대체.

### 7-AI-2. 구현 중 발견해 바로 고친 버그

- **base64 인코딩 결과를 URL 쿼리 파라미터에 그대로 이어붙이면 링크가 깨짐**: base64 출력에는 `+`/`/`/`=` 같은 문자가 섞일 수 있는데(직접 실측으로 확인), 특히 `+`는 쿼리스트링(`application/x-www-form-urlencoded`) 규칙상 공백으로 잘못 해석되어 `URLSearchParams`가 값을 그대로 돌려주지 못하고 깨진 문자열을 반환함. `encodeURIComponent(encodeCompatInvite(invite))`로 한 번 더 감싸서 해결 — 이 버그는 겉으로는 "가끔 어떤 이름/생년월일 조합에서만" 링크가 깨지는 것처럼 보였을 것이라 재현이 까다로웠을 텐데, 배포 전 직접 여러 케이스를 시뮬레이션해서 미리 발견함.

### 7-AI-3. 검증

- `tsc --noEmit` / `npm run build` 클린.
- Node로 인코딩→URL 구성→`URLSearchParams` 파싱→디코딩 전 과정을 여러 케이스(특수문자 포함 이름, `=` 패딩이 나오는 생년월일 조합 등)로 직접 시뮬레이션해 전부 원본과 정확히 일치함을 확인(위 7-AI-2 버그도 이 과정에서 발견).
- **미검증**: 실제 브라우저에서 A가 링크를 생성 → B가 그 링크를 열어 자기 정보 입력 → 결과 화면에서 자동으로 궁합이 뜨는 전체 플로우는 두 개의 브라우저 세션이 필요해 이 세션에서 실제로 재현하지 못함 — 다음에 실제로 링크를 만들어 다른 기기/시크릿창에서 열어보는 것을 권장. 개인정보(이름+생년월일) 노출 수준은 base64일 뿐 암호화가 아니므로, 이미 상대방에게 직접 전달하려는 정보 이상은 아니라고 판단했음(계획안 참고).

---

## 7-AJ. 2026-08-07 소셜 공유 미리보기 + 에러 트래킹 + SEO 기본 파일

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린, `vite preview`로 신규 정적 파일 전부 200 + meta 태그가 실제로 서빙된 HTML에 포함됨을 확인.
> **배경**: "추가로 할 수 있는 기능"을 코드 점검으로 찾은 3건, 우선순위 순(소셜 미리보기 → 에러 트래킹 → SEO)으로 전부 진행.

### 7-AJ-1. 소셜 공유 미리보기 이미지 (og:image / twitter:card)

- **문제**: `index.html`에 `og:image`/`twitter:card` 메타 태그가 전혀 없었음. 카카오톡은 자체 SDK(`handleKakaoShare`)로 이미지가 뜨지만, 그 외(문자메시지·디스코드·트위터/X·슬랙 등)로 공유되는 모든 링크 — 특히 7-AI에서 막 만든 **궁합 초대 링크** — 는 이미지 없는 텍스트 링크로만 보였음.
- **구현**: `public/og-image.png`(기존 `src/assets/hero.png`를 그대로 복사) + `index.html`에 `og:type`/`og:url`/`og:image`(+width/height)/`og:locale`/`twitter:card`/`twitter:image`/`canonical` 메타 추가.
- **주의점 2가지**: ① `src/assets/hero.png`는 Vite가 빌드 시 파일명에 해시를 붙이므로(`hero-XXXX.png`) 정적 `<meta>` 태그에서 안정적인 URL로 쓸 수 없어, 해시가 안 붙는 `public/` 루트에 별도로 복사해둠. ② hero 이미지가 343×361(거의 정사각형)이라 트위터 권장 비율(1.91:1, `summary_large_image`)에 안 맞아서, 억지로 `summary_large_image`를 쓰면 크롭이 이상하게 될 수 있어 이미지 실제 크기에 맞는 `summary`(정사각형 썸네일) 카드 타입을 선택.

### 7-AJ-2. 에러 트래킹 (GA4 exception 이벤트)

- **문제**: `ErrorBoundary.tsx`가 렌더링 크래시를 잡아도 `console.error`만 남기고 끝 — 실제 사용자가 크래시를 겪어도 개발자가 알 방법이 전혀 없었음.
- **구현**: `src/utils/analytics.ts`에 `trackException(description, fatal)`(GA4 예약 이벤트명 `exception`, 문자열 파라미터는 GA4 제한에 맞춰 100자로 절단) + `initGlobalErrorTracking()`(`window.onerror`/`unhandledrejection` 전역 리스너, `fatal: false`) 신규 추가. `ErrorBoundary.componentDidCatch()`에서 `trackException(..., fatal: true)` 호출. `main.tsx`에서 앱 시작 시 `initGlobalErrorTracking()` 호출.
- 별도 유료 서비스(Sentry 등) 없이 이미 구축된 GA4 인프라(7-AG)를 재사용 — 스택트레이스 전체 같은 상세 디버깅 정보까지는 아니지만, "크래시가 실제로 몇 번 발생하는지" 최소한의 가시성은 확보됨. `VITE_GA_MEASUREMENT_ID` 설정 전까지는 이 트래킹도 함께 비활성 상태.

### 7-AJ-3. SEO 기본 파일

- `public/robots.txt`(전체 허용 + sitemap 위치 안내), `public/sitemap.xml`(홈/개인정보처리방침/이용약관 3개 URL 등록 — SPA라 그 이상의 개별 라우트가 없음) 신규 추가.

### 7-AJ-4. 검증

- `tsc --noEmit` / `npm run build` 클린.
- `vite preview`로 `/og-image.png`, `/robots.txt`, `/sitemap.xml` 전부 200 확인, 서빙된 `index.html`에 `og:*`/`twitter:*`/`canonical` 메타 태그가 정확한 절대 URL로 포함돼 있음을 `curl`로 직접 확인.
- **미검증**: 실제 카카오톡 외 채널(문자/디스코드/트위터 등)에 링크를 붙여넣었을 때 미리보기 카드가 의도대로 렌더링되는지, GA4에 `exception` 이벤트가 실제로 들어오는지는 이 세션에서 확인하지 못함(각 플랫폼 크롤러 캐시 특성상 실제 공유로 검증 필요, 후자는 측정 ID 설정 후 확인 가능).

---

## 7-AK. 2026-08-07 보안 헤더 + .env.example + Playwright E2E 골든플로우 테스트

> **상태**: ✅ 구현 완료 및 실제 실행 검증까지 완료(다른 7-A* 섹션들과 달리 이번엔 실제로 브라우저 테스트를 돌려봄). `tsc --noEmit`/`npm run build` 클린, **`npm run test:e2e` 4개 테스트 전부 실제 통과(16.5초)**.
> **배경**: "추가로 필요한 부분" 질문에 코드 점검으로 3건을 찾음 — `vercel.json` 자체가 없어 보안 헤더가 전무했고(과거 실제 XSS 취약점 이력 있음, 7-K-1), `.env.example`이 없어 환경변수 재설정 시 참고할 문서가 없었고, 자동화된 테스트가 전무해 지금까지의 버그 대부분이 사람이 수동으로 찾은 것들이었음. 테스트 범위(E2E 몇 개 vs 단위 테스트 vs 둘 다)를 먼저 확인받은 뒤 "골든플로우 E2E"로 확정.

### 7-AK-1. 보안 헤더 (`vercel.json`)

- 신규 `vercel.json` — 모든 경로에 `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy`(카메라/마이크/위치/결제/USB 차단 — 이 앱은 이 중 아무것도 안 씀)를 적용.
- **CSP는 의도적으로 `Content-Security-Policy-Report-Only`로 배포**(강제 차단이 아니라 위반 사항을 콘솔에 로그만 남김). 이유: 이 앱은 Firebase Auth(구글+애플 로그인 팝업, `accounts.google.com`/`appleid.apple.com`/`*.firebaseapp.com` 필요) + 카카오 SDK + Google Fonts + GA4까지 외부 의존이 많아서, 실제 브라우저로 로그인·공유·폰트 렌더링을 살아있는 상태로 확인하지 않고 강제(enforcing) CSP부터 걸면 무언가 조용히 막힐 위험이 있음. 코드를 근거로 필요한 도메인을 최대한 빠짐없이 담았지만(`script-src`/`style-src`/`font-src`/`img-src`/`connect-src`/`frame-src`), **실제 배포 후 로그인/카카오공유/폰트/GA4를 한 번씩 써보면서 브라우저 콘솔에 CSP 위반 로그가 안 뜨는 걸 확인한 뒤에 `-Report-Only`를 떼어내 강제 적용으로 바꾸는 걸 권장**.

### 7-AK-2. `.env.example`

- 신규 — `.env`에 실제로 들어있는 변수 10개(Gemini/카카오/Firebase 6종/Firebase Admin/개발자 우회 키/GA4) 전부를 값 없이, 각각 무슨 용도인지·어디서 발급받는지 주석과 함께 문서화. `.gitignore`에 이미 `.env.*` 제외 규칙과 `!.env.example` 예외가 준비되어 있었어서(이전 세션에서 미리 준비해둔 것으로 보임) 그대로 커밋 가능.

### 7-AK-3. Playwright E2E 골든플로우 테스트 (실제 실행 검증)

- `@playwright/test` devDependency로 정식 설치(이전 세션들의 "임시 설치 `--no-save`" 방식과 달리 이번엔 계속 남겨서 재사용 가능하게 함) + Chromium 브라우저 바이너리 설치.
- `playwright.config.ts` — `npm run build`가 만든 `dist/`를 `vite preview`로 서빙해 테스트(실제 배포 산출물에 가장 가까운 형태). `package.json`에 `test:e2e`(+ `pretest:e2e`가 자동으로 먼저 빌드) 스크립트 추가.
- `e2e/golden-path.spec.ts` — 4개 테스트, **의도적으로 Gemini API 없이도(net-independent) 검증 가능한 범위로 한정**(사주 계산·렌더링은 전부 클라이언트 로직이라 AI 호출이 필요 없음; AI 콘텐츠 생성·PDF·이미지 카드처럼 실제 `/api/gemini` 응답이 필요한 기능은 범위 밖):
  1. 온보딩 → 입력 폼 제출 → 결과 화면(사주원국 4기둥 카드) 도달
  2. 2월 30일처럼 존재하지 않는 날짜 제출 시 결과 화면으로 안 넘어가고 토스트로 안내
  3. "태어난 시간을 모릅니다" 체크 시 사주원국이 3기둥만 표시
  4. 결과 화면의 대분류(오늘/사주/별자리)·서브(운세/해석/궁합/풍수) 탭을 전부 전환해도 예기치 못한 콘솔 에러가 없음(테스트 서버엔 `/api/gemini`가 없어서 나는 404 관련 로그는 제외하고 검사)
- `App.tsx`의 `formData` 초기값(1995-09-27·오시·여성·ENTP·서울)이 이미 유효한 값이라, 테스트에서는 이름만 채우면 바로 제출 가능해 테스트 코드가 짧고 안정적임.
- `.gitignore`에 `test-results`/`playwright-report`/`blob-report`/`playwright/.cache` 추가(테스트 실행 산출물이 git에 안 올라가도록).

### 7-AK-4. 검증

- `tsc --noEmit` / `npm run build` 클린.
- **`npm run test:e2e` 실제 실행 — 4개 테스트 전부 통과, 총 16.5초.** 처음 실행 시 4번째 테스트(탭 전환)가 브라우저의 원시 "Failed to load resource: 404" 로그를 못 걸러내 실패했는데, 예상 에러 패턴에 `Failed to load resource`/`404`/`[GeminiAPI]`를 추가해 재실행 후 통과 — 이 필터링 자체도 실제로 잘못 짜여진 걸 실제 실행으로 잡아낸 사례(코드만 보고 넘어갔다면 놓쳤을 결함).
- 테스트 서버(`/api/gemini` 없는 정적 `vite preview`)에서 AI 인트로 호출이 실패해도, 앱이 설계대로 결과 화면까지 정상 도달함을 실제로 확인(App.tsx 로딩 useEffect의 방어 로직이 실제로 동작함을 재확인한 셈).
- vercel.json은 JSON 유효성만 확인(`python -m json.tool`) — 위 7-AK-1에 적었듯 CSP의 실제 브라우저 동작(특히 로그인 플로우)은 배포 후 확인 필요.

---

## 7-AL. 2026-08-07 PWA 아이콘 파일 확장자/실제 형식 불일치 수정

> **상태**: ✅ 발견 및 수정 완료. `tsc --noEmit`/`npm run build` 클린, `vite preview` + `curl`로 `Content-Type: image/png` 정상 서빙 확인.
> **배경**: "더 찾아봐줘" 요청에 `api/gemini.ts`(모델 화이트리스트·Origin 검사)를 재점검하다 특이사항이 없어, 이번 세션에서 PWA 작업(7-AG-2) 때 새로 참조하게 된 `public/icons/*.webp` 파일들을 `file` 명령으로 직접 열어봄.

### 7-AL-1. 발견한 문제

- `public/icons/icon-{48,72,96,128,192,256,512}.webp` 7개 파일 전부 `file` 명령으로 실제 포맷을 확인한 결과 **파일 내용은 PNG인데 확장자만 `.webp`**였음(과거 세션에서 아이콘을 준비할 때부터 있던 기존 결함, git blame상 `ee9e29f`(앱스토어 출시 준비) 커밋부터 존재).
- 정적 파일 서버는 파일 확장자로 `Content-Type`을 결정하므로, 실제로는 `image/webp` 헤더로 PNG 바이트를 서빙하고 있었음. 이번 세션에서 이 파일들을 `manifest.json`(`"type": "image/webp"` 명시)과 `index.html`의 `apple-touch-icon`에 새로 연결했는데(7-AG-2), 선언된 MIME 타입과 실제 리소스가 어긋나 있어 브라우저의 PWA 설치 가능성(installability) 검사나 iOS의 홈 화면 아이콘 파싱에서 조용히 걸릴 위험이 있었음.

### 7-AL-2. 수정

- `git mv`로 7개 파일 전부 `.webp` → `.png` 확장자로 정정(내용은 그대로, 실제 포맷과 확장자만 일치시킴).
- `public/manifest.json`의 `icons` 배열 7개 항목의 `src`/`type`을 `image/png`로 갱신, `index.html`의 `apple-touch-icon` 경로도 `.png`로 갱신.

### 7-AL-3. 검증

- `tsc --noEmit` / `npm run build` 클린.
- `vite preview` + `curl -D -`로 `/icons/icon-192.png`가 `Content-Type: image/png`로 정확히 서빙됨을 직접 확인(수정 전에는 `.webp` 경로로 같은 파일이 `image/webp`로 잘못 서빙되고 있었음).
- `python -m json.tool`로 `manifest.json` 유효성 재확인.

---

## 7-AM. 2026-08-07 실사용 중 발견된 버그 2건 — 구글 로그인 배포 도메인 미승인 + Firestore 클라이언트 DB ID 누락

> **상태**: ✅ 둘 다 해결 완료, **사용자가 실제 프로덕션에서 구글 로그인 → 다이어리 클라우드 동기화까지 직접 눌러보고 "이상 없음" 확인**(이 문서의 다른 "미검증" 항목들과 달리 실제 라이브 검증까지 끝난 몇 안 되는 사례).
> **배경**: 사용자가 배포된 사이트에서 구글 로그인을 처음 직접 눌러봄 → `auth/unauthorized-domain` 에러 스크린샷 제보.

### 7-AM-1. `auth/unauthorized-domain` (Firebase 콘솔 설정, 코드 변경 없음)

- **원인**: `mbti-delta-red.vercel.app`이 Firebase 콘솔의 "Authentication → Settings → 승인된 도메인" 목록에 등록되어 있지 않았음. 이 문서 어디에도 이 에러가 기록된 적이 없어서, 구글 로그인을 실제 배포 도메인에서 눌러본 게 이번이 처음이었을 가능성이 높음(그동안의 "검증"은 전부 코드 리뷰/자동화 범위였고, 계획안 7-N-4 자체도 "OAuth 플로우는 헤드리스 자동화로 검증 어려움 — 사용자가 직접 확인" 이라고 한계를 이미 명시해뒀었음).
- **조치**: 코드 변경 사항 없음 — 사용자가 Firebase 콘솔에서 직접 도메인 추가.

### 7-AM-2. 🔴 클라이언트 Firestore 접근 전체 실패 (코드 버그, 수정 완료)

- **원인**: `src/firebase.ts`의 `getFirestore(firebaseApp)` 호출에 데이터베이스 ID가 빠져 있었음. 이 프로젝트의 Firestore는 "Enterprise 에디션"으로 생성되어 데이터베이스 ID가 관용적인 특수값 `(default)`가 아니라 문자 그대로 `default`임 — 서버 쪽(Firebase Admin SDK, `api/_rateLimit.ts`)은 이 문제를 예전에(7-R-3) 이미 겪고 `getFirestore(app, 'default')`로 고쳐뒀는데, 클라이언트 SDK 쪽(로그인 후 다이어리 클라우드 동기화)에는 같은 수정이 반영되어 있지 않았음. Auth(로그인)는 Firestore와 무관해 정상 동작했고, 그다음 단계인 Firestore 읽기/쓰기(`fetchCloudBookmarks`/`pushBookmarksToCloud`)만 조용히 실패해 "로그인은 되는데 클라우드 동기화만 실패" 증상으로 나타남.
- **수정**: `getFirestore(firebaseApp, 'default')`로 통일.
- **함께 배포한 것**: 구글 폰트를 `App.css`의 CSS `@import`(렌더링 차단 + 순차 로드)에서 `index.html`의 `<link>`+`preconnect`로 이동하는 성능 개선도 같은 커밋에 포함.
- **검증**: `tsc --noEmit`/`npm run build` 클린, 배포 후 프로덕션 번들 해시 일치로 배포 확인. **사용자가 실제로 구글 로그인 → 클라우드 동기화까지 다시 시도해 정상 동작을 직접 확인함.**

---

## 7-AN. 2026-08-07 스트릭 보상 구체화 — 티어별 카드 디자인 + 영구 배지 컬렉션

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린, `npm run test:e2e` 4개 통과, 배지 영속성 로직은 Node 시뮬레이션 6개 시나리오로 별도 검증.
> **배경**: 스트릭 배지를 프로필로 옮긴(7-AM 이전 세션) 뒤, "좀 더 구체적이면 좋겠다"는 요청으로 "질문을 1개씩" 방식 진행 — ① 보상 성격(코스메틱 vs 기능 언락) → 코스메틱 확정, ② 지금의 한계(복수 선택) → "티어별 카드 디자인이 다 똑같음" + "지난달에 따던 배지가 사라짐" 확정, ③ 카드 디자인 차별화 축 → 색상+화려함 둘 다, ④ 사라진 배지 문제 해법 → 다이어리 화면에 전용 "내 배지" 섹션으로 확정.

### 7-AN-1. 근본 원인 — "현재 스트릭"과 "배지"가 같은 값이었음

- 기존엔 `getHighestTier(streakCount)`로만 배지를 계산해서, 스트릭이 끊겨 `streakCount`가 1로 리셋되면 이미 땄던 배지 자체가 화면에서 사라졌음(축하 여부만 기록하던 `CELEBRATED_KEY`는 애초에 "배지 보유 여부"를 위한 값이 아니었음).
- **해법**: `src/utils/streak.ts`에 `EARNED_TIERS_KEY`(영구 기록, `{days, earnedAt}[]`) 신설. `recordTodayVisitAndGetStreak()`가 방문마다 현재 `streakCount`로 도달 가능한 모든 티어를 이 영구 기록에 추가(이미 있으면 건너뜀, 최초 획득일 유지) — 스트릭 리셋 시 `CELEBRATED_KEY`(축하 재발동용)만 지우고 `EARNED_TIERS_KEY`는 절대 건드리지 않음. 이 기능 출시 전부터 이미 스트릭이 쌓여있던 사용자도 다음 방문 시 자동으로 소급 반영됨(매번 "도달 가능한 모든 티어"를 추가하는 방식이라 별도 마이그레이션 코드 불필요).

### 7-AN-2. 티어별 카드 디자인 차별화

- `StreakTier`에 `accent`/`accentDark`/`glow`/`sparkle`(0~4) 필드 추가 — "게임 레어도"처럼 새싹(초록) → 불꽃(주황) → 별빛(파랑) → 나풀이 마스터(보라) → 다이아몬드(시안) 순으로 색과 장식이 단계적으로 진해지도록 구성(사용자 확정 방향).
- `handleDownloadPersonaCard()`에 `sparkle?: number` 옵션 추가 — 값이 클수록 메달리온 주변에 링(1~2겹)과 별 장식(sparkle×3개)을 더 그림. 다른 카드(타로/닮은인물)는 이 옵션을 안 넘기므로 기존 디자인 그대로 유지(회귀 없음).

### 7-AN-3. 다이어리 "내 배지" 섹션

- 다이어리 화면(로그인 카드 아래)에 5개 티어 전부를 격자로 표시하는 "🏅 내 배지 (N/5)" 섹션 신규 추가 — 획득한 배지는 티어 색상 그라디언트 + 획득일 + "🖼️ 카드" 저장 버튼, 미획득 배지는 회색조 🔒 아이콘으로 표시(성취감 있는 "업적" 화면 스타일, 사용자가 직접 고른 옵션).
- 각 획득 배지마다 개별적으로 카드를 다시 저장/공유할 수 있어, 예전에 딴 배지도 원할 때 다시 자랑할 수 있음.

### 7-AN-4. 검증

- `tsc --noEmit` / `npm run build` 클린, `npm run test:e2e` 4개 전부 통과(기존 골든플로우에 회귀 없음).
- Node 스크립트로 배지 영속성 로직 6개 시나리오 시뮬레이션: 연속 방문 시 정확한 시점에 배지 획득, 스트릭 리셋 후에도 기존 배지 유지, 리셋 후 재도전 시 축하 재발동, 같은 배지 재획득 시 중복 기록 없이 최초 획득일 유지 — 전부 통과.
- **미검증**: 실제 브라우저에서 다이어리 화면의 배지 그리드 레이아웃(특히 모바일 좁은 화면에서의 줄바꿈)과 카드 저장 버튼 클릭 시 실제 렌더링 결과는 이 세션에서 시각적으로 확인하지 못함 — 배포 후 직접 확인 권장.

---

## 7-AO. 2026-08-07 타로 78장 실제 카드 이미지 연동

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린, `npm run test:e2e` 4개 통과, 이미지 78장 전부 직접 열어 매칭 정확도 표본 검증(아래 7-AO-3).
> **배경**: 사용자가 위키미디어 등에서 1909년 라이더-웨이트-스미스(RWS) 덱 스캔본을 직접 모아 `타로카드/` 폴더에 준비 — 저작권 만료(1930년 이전 출판물, 미국 기준 확실한 퍼블릭 도메인) 확인 후 진행. 처음엔 74장(4장 누락 오인, 실제론 The Hermit 중복 파일 때문에 73장+5장 누락)이었다가, 두 차례에 걸쳐 빠진 카드(Five/Knight of Wands, Four of Cups, Eight of Swords, Knight of Pentacles)를 채워 78장 완성 — 이 과정에서 Knight of Pentacles 최초본이 완전히 다른 덱(스위스 1JJ/Troccas)이었던 것도 실제로 이미지를 열어봐서 발견·교체함.

### 7-AO-1. 이미지 처리 파이프라인

- **문제**: 이 환경엔 ImageMagick이 따로 없고(Git Bash의 `convert`는 Windows 자체 NTFS 변환 도구라 오작동), 그래픽 처리 도구가 사실상 없었음.
- **해결**: `sharp`+`tsx`를 `--no-save`로 임시 설치(계획안.md의 Playwright 임시 설치와 동일한 패턴 — `package.json`에 흔적 안 남음)하고, `scripts/build-tarot-images.ts` 신규 작성:
  - 폴더의 원본 파일명(불규칙한 여러 출처 — Wikimedia 표준명/예일대 소스/번호식 SVG/숫자 약칭 파일명 등 뒤섞여 있었음)을 `TarotCard.nameEn`과 매칭되는 표준 키로 정규화(접미사 제거, 별칭 5개 처리, "Two"→"2" 등 숫자 단어→숫자 변환).
  - 매칭된 78장을 각각 500×866 이내로 리사이즈 + webp 82% 품질로 변환해 `public/tarot/{card.id}.webp`로 출력(카드 `id`를 파일명으로 써서 조회를 단순한 문자열 조합 하나로 끝냄).
  - 매칭 안 되는 파일이 있으면 어떤 카드가 왜 안 됐는지 정확히 출력하도록 설계 — 실제로 1차 실행에서 핍 카드(2~10) 37장이 전부 실패해서(파일명은 "Two_of_Wands" 스타일인데 데이터는 "2 of Wands" 숫자 표기) 이 리포트 덕분에 바로 원인을 파악하고 고침.
  - 결과: 78/78 매칭, 원본 205MB → `public/tarot/` 9.2MB(장당 평균 118KB)로 축소.

### 7-AO-2. UI/공유 카드 연동

- `src/App.css`: `.persona-card-image`(132×228px, 카드 비율에 맞는 둥근 사각 프레임, 테마 색상 글로우) 신규 — 기존 원형 이모지 메달리온(`.persona-card-medallion`)과 별개 스타일이라 닮은 인물·스트릭 배지 카드는 기존 그대로 영향 없음.
- `src/App.tsx`: "오늘의 타로" 카드 표시를 이모지 원형 배지에서 실제 이미지(`/tarot/{card.id}.webp`, 역방향이면 180도 회전)로 교체.
- `handleDownloadPersonaCard()`에 `imageUrl?` 옵션 추가 — 공유용 이미지 카드(다운로드/카톡 등 공유되는 PNG)에도 실제 카드 아트를 캔버스에 그려 넣도록 확장(이미지 로드 실패 시 `.catch(() => null)`로 조용히 기존 이모지 방식으로 대체 — 카드 이미지 하나 문제로 공유 기능 전체가 죽지 않게 함). 닮은 인물·스트릭 카드는 `imageUrl`을 안 넘기므로 기존 방식 그대로 유지.
- `src/appHelpers.ts`: `loadCanvasImage()` 헬퍼 신규(Promise 기반 `<img>` 로드).

### 7-AO-3. 검증

- `tsc --noEmit` / `npm run build` 클린, `npm run test:e2e` 4개 전부 통과.
- `vite preview` + `curl`로 `/tarot/0.webp`, `/tarot/22.webp`, `/tarot/47.webp`, `/tarot/77.webp` 전부 200 + `Content-Type: image/webp` 확인.
- 변환된 이미지를 표본으로 직접 열어 매칭이 정확한지 재확인(예: id=75 → Knight of Pentacles 정확히 일치, id=26 → Five of Wands 정확히 일치) — 매칭 스크립트의 별칭/숫자변환 로직이 실제로 옳게 작동함을 이미지 내용으로 재확인.
- **범위 밖으로 남긴 것**: 원본 스캔본이 담긴 `타로카드/` 폴더(205MB)는 git에 커밋하지 않음(용량 문제, 그리고 `public/tarot/`가 실제 사용되는 결과물이라 원본은 필요 없음) — 로컬에 그대로 남아있으니 필요 없으면 사용자가 직접 정리해도 됨.
- **미검증**: 실제 결과 화면에서 "오늘의 타로 뽑기" 버튼을 눌러 카드가 화면에 정상 렌더링되는지는, 이 환경엔 로컬에서 실제 Gemini API 키로 AI 텍스트 생성까지 되는 경로가 없어(테스트 서버엔 `/api/gemini`가 없음) 끝까지 확인하지 못함 — 배포 후 실제로 카드를 뽑아 이미지가 정상 표시되는지, 공유 카드 다운로드 시 이미지가 잘 그려지는지 직접 확인 필요.

## 7-AP. 2026-08-07 타로 카드 이미지 크기 확대 (앱 화면 + 공유 카드)

> **상태**: ✅ 구현 완료·배포됨 (커밋 `b3cfd77`).
> **배경**: 사용자가 78장 이미지 연동(7-AO) 직후 "이미지가 너무 작다"고 피드백. "너가 납득이 될때까지 질문을 1개씩 해줘" 방식으로 확인한 결과 (1) 앱 화면 속 카드와 공유용 PNG 카드 둘 다 작다는 지적이었고, (2) 반응형(퍼센트 기반)보다 고정 크기 확대를 사용자가 선택.

- `src/App.css`: `.persona-card-image` 132×228px → **220×380px**로 확대(`max-width:100%` 안전장치 유지, 작은 화면에서 컨테이너 넘침 방지).
- `src/App.tsx`의 `handleDownloadPersonaCard()`: 공유 카드 캔버스에서 카드 이미지 크기 230×398 → **310×537**로 확대. 이미지 카드 전용 `layout` 좌표 객체(`kickerY`/`nameY`/`badgeCy`/`resultNameY`/`subtitleY`/`badgeBoxY`/`badgeTextY`/`bodyBoxTop`)를 새 카드 크기에 맞춰 전체 재계산 — 이미지가 없는 카드(닮은 인물/스트릭 배지, 기존 원형 메달리온 방식)는 별도 좌표 세트를 그대로 사용해 영향 없음.
- 검증: `tsc --noEmit`/`npm run build` 클린. 실기기·실브라우저에서 확대된 카드가 레이아웃을 깨뜨리지 않는지는 배포 후 사용자 확인 필요(미검증 상태로 배포됨 — 이후 별도 재보고 없어 문제 없었던 것으로 판단).

---

## 7-AQ. 2026-08-07 별자리 행성/하우스 클릭 시 AI 심층 해설 추가

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build` 클린, `npm run test:e2e` 4개 통과(그중 탭 전환 테스트가 별자리 화면을 실제로 방문하므로 회귀 없음을 추가로 확인).
> **배경**: 사용자가 "하우스나 행성도 클릭하면 해석이 나오면 좋겠다"고 제안, "너가 납득이 될때까지 질문을 1개씩 해줘" 방식으로 두 차례 확인:
> 1. 범위 — **행성 7개 + 하우스 12개 둘 다**.
> 2. 상호작용 방식 — 기존 "사주 4기둥 클릭" 패턴과 동일하게, **정적 설명 즉시 표시 + "AI 심화해설 생성하기" 버튼**.

### 구현
- `src/utils/gemini/astrology.ts`: `generatePlanetPlacementInterpretation()`, `generateHousePlacementInterpretation()` 신규 — 각각 행성×별자리×하우스(+품위), 하우스×별자리(+그 하우스에 있는 행성 목록)를 프롬프트에 반영해 4~6줄 분량의 짧은 해설 생성(기존 "심화"/8192토큰급이 아니라 4기둥 해설과 동일한 짧은 톤 유지).
- `src/appHelpers.ts`: `astroPlacementCacheKey(f, city, placementKey)` 추가 — `placementKey`는 `planet_sun`/`house_4`처럼 구분되는 값, 기존 `astrologyCacheKey()`에 이어붙이는 방식이라 이름+생년월일+시간+출생도시 전부 캐시 키에 반영됨(섹션 7-K-2의 `baseKeyId()` 원칙 그대로 계승).
- `src/App.tsx`:
  - 신규 상태 `astroModal`(열린 모달의 대상 정보) / `astroPlacementAiData`(캐시된 해설 텍스트 맵) / `astroPlacementAiLoading`.
  - 결과 로드 시 행성 7개+하우스 12개 전체의 캐시를 한 번에 조회해 `astroPlacementAiData`에 미리 채워두는 `useEffect` 추가.
  - `handleAstroPlanetClick()`/`handleAstroHouseClick()`: 클릭 즉시 정적 설명(행성의 좋을 때/나쁠 때/상징 인물/품위, 또는 하우스의 의미/강도/길흉)을 모달로 표시.
  - `handleGenerateAstroPlacementAi()`: "AI 심화해설 생성하기" 버튼 클릭 시 위 두 프롬프트 함수를 호출하고 결과를 캐시에 저장, `trackEvent('content_generate', { feature: 'astro_placement', kind })`로 애널리틱스도 반영.
  - 기존 "🌟 행성 배치"/"🏠 하우스 배치(홀사인)" 렌더 목록에 `onClick`/`cursor:pointer`/안내 문구("💡 각 행성을/각 하우스를 클릭하면...") 추가 — 4기둥 클릭 UI와 동일한 시각적 힌트 패턴.
  - 새 모달 JSX는 기존 4기둥 모달과 동일 구조(정적 설명 → 있으면 AI 텍스트+다이어리 저장 버튼, 없으면 생성 버튼)로 작성해 UI 일관성 유지.

### 검증
- `tsc --noEmit` 클린, `npm run build` 성공(`index-CD8tqcxa.js` 412.51 kB).
- `npm run test:e2e` 4개 전부 통과 — 특히 "결과 화면의 대분류/서브 탭을 전환해도 콘솔 에러 없이 정상 렌더링된다" 테스트가 별자리 탭을 실제로 열어보므로, 새로 추가된 클릭 핸들러/모달 코드가 있어도 콘솔 에러나 렌더링 문제가 없음을 확인.
- **미검증**: 이 환경엔 로컬에서 실제 Gemini API 키로 AI 텍스트까지 생성해보는 경로가 없어(`/api/gemini` 부재), 실제로 행성/하우스를 클릭했을 때 AI가 생성하는 해설 문구의 품질·정확성은 배포 후 직접 확인 필요.

## 7-AR. 2026-08-09 모바일 초기 로딩 지연 진단 및 수정

> **상태**: ✅ 원인 2건 확인·수정 완료. `tsc --noEmit`/`npm run build` 클린, `npm run test:e2e` 4개 통과. Playwright로 iPhone 12 에뮬레이션 + 네트워크 스로틀링(Fast 3G/Slow 4G)을 실제 프로덕션과 로컬 빌드에 적용해 수정 전/후를 직접 측정해 개선을 확인.
> **배경**: 사용자가 "모바일로 접속할 때 로딩이 오래 걸리고 제대로 안 뜨는 부분이 있다"고 보고. 코드 정적 분석 + 실측(네트워크 워터폴/콘솔 에러/스크린샷)으로 원인을 좁힘.

### 7-AR-1. 진단 방법

- Playwright `chromium` + `devices['iPhone 12']`로 모바일 뷰포트 에뮬레이션, CDP `Network.emulateNetworkConditions`로 Fast 3G(1.5Mbps)/Slow 4G(4Mbps) 스로틀링, 일부는 `Emulation.setCPUThrottlingRate(4)`로 저가형 안드로이드폰 CPU까지 재현.
- 프로덕션(`mbti-delta-red.vercel.app`)에 접속해 리소스 로드 순서(워터폴)와 `#root`에 실제 콘텐츠가 렌더링되는 시점을 측정.
- 실제 폼 제출 → 결과 화면 도달까지 전체 플로우를 모바일 뷰포트로 실행해 콘솔 에러·레이아웃 깨짐 여부 확인 — **결과: 콘솔 에러 없음, 레이아웃 정상, 실제 AI 응답 포함 6.5초 만에 결과 화면 도달**(정상 범위). 즉 "제대로 안 뜨는" 원인은 앱 내부 렌더링 버그가 아니라 **최초 페이지 접속 단계**에 있었음.

### 7-AR-2. 발견 1 — 카카오 SDK `<script>` 태그가 HTML 파싱을 차단

- `index.html` `<head>`에 `<script src="https://t1.kakaocdn.net/kakao_js_sdk/2.7.2/kakao.min.js">`가 `async`/`defer` 없이 있어, 브라우저가 이 외부 스크립트(84KB)를 전부 내려받고 실행할 때까지 이후 HTML 파싱(및 `<body>`의 `main.tsx` 모듈 스크립트 발견)이 전부 멈춰 있었음. 실측 결과 Fast 3G에서 메인 앱 번들 요청 시작이 이 때문에 ~2.2초까지 밀림.
- 이미 `App.tsx`의 `handleKakaoShare()`에 카카오 SDK가 없을 때 동적으로 `<script>`를 주입해 로드하는 견고한 폴백 로직(로드/에러 이벤트 + 5초 폴링 백업)이 존재 — 즉 `index.html`의 사전 로드는 애초에 불필요한 중복이었음.
- **수정**: `index.html`에서 해당 `<script>` 태그 완전히 제거. 카카오 공유는 이제 사용자가 실제로 "카톡 공유" 버튼을 처음 누를 때만 기존 동적 로더가 SDK를 받아온다(동작 자체는 무변경, 최초 공유 클릭 시 0.1~0.8초 정도의 로드 지연만 새로 생김 — 이미 로딩 상태 UI로 처리되고 있음).

### 7-AR-3. 발견 2 — Firebase(클라우드 동기화) 660KB 청크가 모든 사용자에게 즉시 다운로드됨

- `App.tsx`의 클라우드 동기화 `useEffect(() => {...}, [])`가 로그인 여부와 무관하게 **앱 마운트 즉시** `loadCloudSync()`(동적 import이지만 즉시 호출)를 실행 — 이 청크(`cloudSync-*.js`, 660KB, Firebase Auth+Firestore SDK)와 뒤이어 Google 로그인 헬퍼(`apis.google.com/js/api.js`)까지, 로그인/클라우드 동기화를 전혀 쓰지 않는 절대다수 사용자도 첫 화면이 뜨는 바로 그 순간에 함께 받고 있었음. 저속 모바일망에서는 이 대용량 청크가 폰트·본문 콘텐츠와 대역폭을 다퉈 체감 로딩을 늦추는 요인.
- **수정**: `main.tsx`가 서비스워커 등록에 이미 쓰고 있던 것과 동일한 패턴 — `window`의 `load` 이벤트(핵심 리소스 로드 완료 시점) 이후로 `loadCloudSync()` 호출을 지연. 로그인 상태 구독 자체는 여전히 자동으로 이루어지므로 기존 사용자 경험(재방문 시 자동 로그인 유지)에는 변화 없음, 다만 그 시작 시점이 초기 렌더링 경쟁 구간 밖으로 밀려남.

### 7-AR-4. 수정 전/후 실측 비교 (동일 로컬 빌드, CPU 스로틀링 없이 네트워크만)

| 지표 | 수정 전 | 수정 후 |
|---|---|---|
| Fast 3G — 앱 콘텐츠 렌더링 시점 | 2863ms | 2583ms |
| Slow 4G — 앱 콘텐츠 렌더링 시점 | 1311ms | 1161ms |
| 초기 리소스 워터폴에 kakao.min.js/cloudSync 청크/apis.google.com 포함 여부 | 포함(대역폭 경쟁) | **완전히 사라짐**(핵심 경로 이탈 확인) |

### 7-AR-5. 검증 및 남은 과제

- `tsc --noEmit`/`npm run build` 클린, `npm run test:e2e` 4개 통과(회귀 없음).
- **범위 밖으로 남긴 것**: 구글 폰트(2개 서체군 × 5개 굵기, 총 10개 woff2)도 렌더 차단 리소스이나, 실측상 영향이 카카오/Firebase보다 작고(`preconnect` 이미 적용됨, `display=swap`으로 텍스트 자체는 폰트 로드 전에도 보임) 실사용 굵기 재조정은 이번 범위에서 보류.
- **미검증**: 사용자가 보고한 "제대로 안 뜨는" 증상이 정확히 이 두 리소스로 인한 지연 때문이었는지(vs. 실제 기기의 카카오 CDN 접근 장애 등 네트워크 환경 문제)는 코드 레벨에서 재현하지 못했으므로 단정할 수 없음 — 다만 두 원인 모두 확실한 안티패턴(불필요한 렌더 차단 리소스, 전원 대상 무조건 즉시 로드)이었고 실측으로 개선을 확인했으므로 수정 근거는 충분함. 배포 후 실제 기기에서 체감 개선 여부 확인 필요.

## 7-AS. 2026-08-09 유닛 테스트 도입 + 번들 코드 스플리팅 + npm audit 검토

> **상태**: ✅ 완료. `tsc --noEmit`/`npm run build`/`npm run test:e2e`(4개)/`npm run test:unit`(30개) 전부 통과.
> **배경**: "그 외에 보완할 점"을 다시 검토해 우선순위 3건(계산 엔진 유닛 테스트 부재, 단일 거대 JS 번들, npm audit 중간 취약점 8건)을 제안했고 사용자가 전부 진행을 요청.

### 7-AS-1. 유닛 테스트 도입 (vitest)

- E2E(Playwright) 4개가 유일한 안전망이었는데, 정작 과거 실제로 발생했던 버그(대운 순행/역행이 성별 무관하게 계산되던 문제, 야자시 일주 이월 누락 등, 계획안.md 7-K/8-1 참고)는 전부 사주/점성술 "계산 엔진" 내부 로직 문제였음 — E2E는 화면이 뜨는지만 보지, 간지가 실제로 맞는지까지는 검증하지 못함.
- `vite.config.ts`에 `vitest/config`의 `defineConfig`로 교체 + `test: { environment: 'node', include: ['src/**/*.test.ts'] }` 추가(계산 함수는 순수 함수라 DOM 불필요, jsdom보다 빠른 node 환경 사용). `npm run test:unit` 스크립트 추가.
  - vitest 최신(4.x)은 vite 7/8을 요구해 프로젝트의 기존 vite@5.4와 별도로 두 번째 vite 사본이 설치되는 문제가 있어, vite@5.x와 호환되는 **vitest@3.2.7**로 명시 설치(`npm ls vite`로 단일 버전 dedupe 확인).
- `src/utils/sajuCalculator.test.ts`(13개) — 실제 프로덕션 화면에서 Playwright로 직접 확인한 값(1995-09-27/오시/여성 → 연주 乙亥·월주 乙酉·일주 辛酉·시주 甲午)을 회귀 기준값으로 사용. 대운 순행/역행 방향(계획안.md 7-K에서 실제로 수정했던 버그)을 성별별로 재검증, 연주 입춘 경계(코드에 명시된 baseYear=1984=갑자 공식으로 직접 손 계산), 야자시/정확한 시각 입력 시 일주 이월, 60갑자 주기성 등을 검증.
- `src/utils/astrologyCalculator.test.ts`(17개) — 홀사인 하우스 구조적 불변식(1하우스=어센던트 별자리, 12하우스가 12별자리를 정확히 한 번씩 포함 등), 위도 변화가 실제로 어센던트에 반영되는지, 서머타임 보정 경계일(1987/1988년 시작·종료일), 본질적 품위 테이블, 트랜짓 계산의 자기일관성(네이탈과 동일 시각을 트랜짓에 넣으면 태양·달 모두 컨정션/orb≈0이 나와야 함)을 검증.
- 작성한 30개 테스트는 전부 **첫 실행에 통과** — 직접 손으로 유도한 기준값(연주/월주/일주/시주, 대운 방향, 품위 테이블)이 실제 구현과 정확히 일치함을 별도로 재확인한 셈이라, 회귀 방지 테스트로서의 신뢰도가 높음.

### 7-AS-2. 번들 코드 스플리팅 — astronomy-engine을 초기 로딩 경로에서 제외

- `rollup-plugin-visualizer`로 일회성 번들 구성 분석(작업 후 devDependency 제거) 결과, 메인 청크(412KB) 안에서 `astronomy-engine`이 **119KB**(App.tsx 다음으로 큰 단일 기여자)를 차지하고 있었음. 문제는 이 라이브러리가 실제로 필요한 시점(폼 제출 후 계산 시)이 아니라, `astrologyCalculator.ts`를 `import`하는 순간 무조건 초기 번들에 포함되고 있었다는 것 — 온보딩 화면만 보는 사용자도 이 무게를 다운로드하고 있었음.
- `astrologyCalculator.ts`를 둘로 분리:
  - `src/utils/astrologyData.ts`(신규) — 별자리/행성/하우스/도시 좌표 등 순수 데이터·타입, `astronomy-engine` 의존성 전혀 없음. 입력 폼의 도시 선택 드롭다운 등 계산 전에도 필요한 것들이라 그대로 정적 import 유지.
  - `astrologyCalculator.ts`(기존 파일 유지, 축소) — `astronomy-engine`을 실제로 쓰는 `calculateAstrology`/`calculateTodayTransits`만 남기고, `astrologyData.ts`를 `export *`로 재노출(테스트 파일 등 기존 소비처 하위호환).
- `App.tsx`: 정적 import를 `astrologyData`로 바꾸고, `calculateAstrology`(폼 제출 시)와 `calculateTodayTransits`(트랜짓 갱신 시) 딱 두 호출부만 `await import('./utils/astrologyCalculator')` 동적 import로 전환. `src/utils/gemini/astrology.ts`도 타입/데이터만 쓰므로 import 경로를 `astrologyData`로 변경(그렇지 않으면 이 파일을 거쳐 astronomy-engine이 다시 메인 번들에 딸려 들어옴 — 실제로 처음엔 이 누락을 놓쳤다가 재확인 과정에서 발견해 수정).
- **결과**: 메인 청크 412.66KB → **362.39KB**(gzip 154.85KB → 131.07KB, 약 15% 감소), `astrologyCalculator-*.js`(50.87KB, gzip 22.31KB) 별도 청크로 분리— 폼을 제출하거나 트랜짓을 갱신할 때만 받아짐. 온보딩/입력 화면의 초기 로딩 무게가 그만큼 가벼워짐(7-AR에서 고친 카카오/Firebase 지연 로딩과 같은 방향의 개선).
- `tsc --noEmit`/`npm run build`/`npm run test:unit`(30개)/`npm run test:e2e`(4개, 폼 제출·탭 전환 실사용 경로 포함) 전부 통과 — 동적 import 전환이 실제 제출 플로우에서 문제없이 동작함을 확인.

### 7-AS-3. npm audit 검토 — 강제 수정하지 않기로 판단한 근거

- `npm audit`(전체) 기준 **모더레이트 9건 + 하이 1건**. 전부 자동 수정(`npm audit fix --force`)이 존재하지만, 다음 이유로 **강제 수정하지 않음**:
  - 모더레이트 8건(uuid 관련, `firebase-admin`/`@capacitor/cli` 경유)의 유일한 해결책은 `@capacitor/cli`를 8.5.0→8.4.2로 **다운그레이드**하는 것(npm이 "breaking change"로 명시) — 네이티브 앱 빌드에 쓰는 도구를 실제 취약점 리스크보다 더 큰 리스크(빌드 파이프라인 회귀, 이 환경엔 Xcode/Android Studio가 없어 재검증 불가)를 감수하며 낮출 이유가 없음. 해당 CVE(uuid의 버퍼 바운드 체크 누락)도 이 앱에서는 attacker가 제어하는 버퍼가 `uuid.parse()` 등에 전달되는 경로 자체가 없어(firebase-admin/xcode가 내부적으로 자체 ID 생성용으로만 사용) 실질 위험이 낮음.
  - 나머지 모더레이트 1건 + 하이 1건(`esbuild`/`vite`)은 **devDependency 전용**(vitest 설치 이전부터 이미 존재 — `npm audit --omit=dev`로 확인하면 이 두 건은 아예 안 뜸)이자 **Vite 개발 서버 전용** 취약점(경로 순회 등, 프로덕션 빌드 결과물에는 영향 없음, 이 프로젝트의 개발 서버는 로컬에서만 실행하고 외부에 노출한 적 없음). 유일한 수정은 vite@5→8 메이저 업그레이드인데, Capacitor의 `base: './'` 상대경로 로딩 등과의 호환성을 이 환경에서 전부 재검증할 방법이 없어 별도 세션에서 다뤄야 할 작업으로 보류.
- vitest는 vite@5와 호환되는 3.2.7로 설치해 **새로운 취약점을 추가하지 않음**(위 하이 1건은 vitest 설치 전부터 이미 있던 것, `--omit=dev` 비교로 확인).
- 결론: 8+1(하이)건 전부 **의식적으로 보류**, 강제 수정보다 현상 유지가 더 안전하다고 판단. 계획안.md 8절 "알려진 이슈"에도 등재.

---

## 7-AT. 2026-08-10 💡 기능 가이드 팝업 신설 (하루 1회 자동 표시 + 실제 화면 스크린샷)

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(30개)/`npm run test:e2e`(4개) 전부 통과.
> **배경**: 사용자가 "가이드라인이 맨 처음(팝업 형식)에 있으면 좋겠다, 하루 한 번씩 뜨게 하고 상단에 온오프 기능을 넣어서, 구체적으로 어떤 기능이 있는지 사진과 함께 설명하면 좋겠다"고 요청. 두 차례 질문으로 확정: (1) 이미지는 아이콘/이모지 카드가 아니라 **실제 앱 화면 스크린샷**, (2) 앱 기능이 많아 **핵심 5개**(사주×MBTI 융합해석/서양 점성술·타로/궁합/다이어리 저장/PDF·카톡 공유)만 간결하게 소개.

### 7-AT-1. 실제 화면 스크린샷 확보

- Playwright로 프로덕션(`mbti-delta-red.vercel.app`)에 아이폰12 뷰포트로 접속해, 실제 폼 제출(예시 이름 "김서연")→결과 화면에서 5개 기능 화면을 요소 단위로 캡처(`.glass-card`/`.profile-actions` 등 텍스트로 대상 카드를 특정해 그 카드만 스크린샷 — 전체 페이지가 아니라 딱 필요한 카드만 깔끔하게 잘라냄).
- 임시로 `sharp`를 `--no-save`로 설치(기존 타로 이미지 파이프라인과 동일한 패턴)해 각 스크린샷을 700px 너비로 리사이즈 + webp 85% 품질로 변환, `public/guide/{saju-mbti,astrology,compat,diary,share}.webp`로 저장(5장 합계 약 98KB).
- 첫 시도에서 예시 이름을 세션 내내 써온 "테스트유저"로 캡처했다가, 사주×MBTI 카드에 "테스트유저 님의..." 문구가 그대로 박혀 있으면 버그처럼 보일 수 있어 "김서연"으로 다시 캡처 — 영구적으로 앱에 남는 자산이라 예시 데이터 품질을 한 번 더 점검함.

### 7-AT-2. 구현

- `src/appTypes.ts`: `GUIDE_LAST_SHOWN_KEY`(마지막 자동 표시 날짜)/`GUIDE_DAILY_ENABLED_KEY`(토글 on/off 여부) localStorage 키와, `GUIDE_FEATURES`(이미지 경로·이모지·제목·설명 5개 항목) 데이터 배열 신규.
- `src/App.tsx`:
  - `guideModalOpen`/`guideDailyEnabled` 상태 추가. 마운트 시 `useEffect`로 "오늘 이미 자동으로 뜬 적 없고 + 토글이 꺼져있지 않으면" 자동으로 모달을 열고 오늘 날짜를 기록 — 온보딩(`ONBOARDING_SEEN_KEY`, 최초 1회뿐)과는 완전히 독립적인 별도 로직이라, 처음 쓰는 사람도 재방문자도 하루에 한 번은 보게 됨.
  - `handleOpenGuide()`: 헤더의 새 "💡" 버튼(다이어리 버튼 왼쪽)으로 언제든 수동으로 다시 열 수 있음 — 사용자가 토글을 꺼서 자동 표시를 막았더라도, 나중에 다시 보고 싶으면 이 버튼으로 열고 그 안에서 토글을 다시 켤 수 있어 "한 번 끄면 영영 못 본다"는 문제가 없음.
  - `handleToggleGuideDaily()`: 모달 상단의 스위치 — 끄면 `GUIDE_DAILY_ENABLED_KEY='false'`로 저장, 이후 자동 표시 안 함(수동 재열람은 계속 가능).
  - 모달 JSX는 기존 `.modal-overlay`/`.modal-box` 패턴 그대로 재사용(일관성), 상단에 토글 스위치 → 5개 기능 카드(스크린샷 이미지 + 이모지 제목 + 설명)를 세로로 나열.
- `src/App.css`: `.guide-toggle-row`/`.guide-switch`(커스텀 토글 스위치, 기존 프로젝트에 토글 UI가 없어 신규 제작)/`.guide-feature-card` 등 신규 스타일.

### 7-AT-3. 검증

- `tsc --noEmit`/`npm run build` 클린(번들 크기 변화 미미).
- Playwright로 프로덕션 빌드를 직접 띄워 확인: 첫 방문 시 자동으로 뜸 → 5개 이미지 전부 정상 로드(`naturalWidth=700`, 깨짐 없음) → "확인했어요"로 닫은 뒤 새로고침해도 같은 날엔 다시 안 뜸 → 헤더 "💡" 버튼으로 수동 재오픈 정상 → 토글 클릭 시 상태 전환 및 `localStorage` 저장 정상, 콘솔 에러 없음.
- **회귀 발견 및 수정**: 이 팝업이 프레시 세션(로컬스토리지 빈 상태)에서 자동으로 뜨면서 기존 E2E 4개가 전부 실패함(모달 오버레이가 "시작하기"/제출 버튼 클릭을 가로챔). `e2e/golden-path.spec.ts`에 `skipGuidePopup()` 헬퍼(오늘 날짜를 `GUIDE_LAST_SHOWN_KEY`에 미리 기록해 자동 표시를 건너뜀, 기존 `skipOnboarding()`과 동일한 `addInitScript` 패턴)를 추가해 4개 테스트 전부에 적용 — 재실행 결과 4개 전부 통과.
- `npm run test:unit`(30개)도 무관한 파일이라 영향 없이 통과 확인.
- 배포 후 실제로 하루 뒤 재방문 시 다시 뜨는지(날짜가 바뀌는 실제 시나리오)는 이 세션 안에서 시간을 앞당겨 확인할 방법이 없어 로직 검토로만 확인(코드상 `todayDateStr()` 비교이므로 자정을 넘기면 정상적으로 다시 뜸).

---

## 7-AU. 2026-08-10 모바일 전반 재점검 — PDF 팝업 차단 · iOS 자동 확대 · 로딩 중 검은 화면

> **상태**: ✅ 6건 수정 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(30개)/`npm run test:e2e`(4개) 전부 통과.
> **배경**: "모바일 환경에서 대부분 쓰다 보니 문제가 없는지 전체적으로 확인해달라"는 요청으로 1차 점검 3건(PDF 팝업 차단·iOS 입력창 자동 확대·터치 영역)을 발견, 사용자가 진행을 요청하며 추가로 "로딩 중 간혹 화면이 새까맣게 되거나 중간에 끊겨서 내용이 안 나올 때가 있다, 지하철 등 네트워크 문제인지 모바일 환경 자체가 불안정한 건지 확인해달라"고 요청 — 조사 결과 세 번째 원인(청크 로딩 실패)까지 총 6건을 발견·수정.

### 7-AU-1. 🔴 PDF 저장 팝업이 모바일에서 차단될 가능성이 높았음

- `handleDownloadPDF()`가 여러 AI 콘텐츠를 순차로 `await` 생성한 **다음에** `window.open('', '_blank')`을 호출하고 있었음. 모바일 브라우저(특히 iOS Safari)는 사용자의 클릭과 "직접 연결된" 팝업만 허용하는데, 중간에 비동기 대기가 여러 번 끼어들면 그 연결이 끊겨 팝업이 차단되거나 빈 창만 뜬다 — 아직 캐시 안 된 섹션이 있어 생성 시간이 길수록 더 잘 터지는 구조였음.
- **수정**: `window.open()`을 함수의 맨 처음(클릭 이벤트와 동기적으로 실행되는 지점)으로 이동, 반환된 창 참조를 그대로 들고 있다가 기존과 동일하게 맨 마지막에 완성된 HTML을 그 창에 써넣도록 변경 — 실제 생성 로직/결과물은 전혀 안 바뀜, 오직 "창을 언제 여는지"만 바뀜. 생성 도중 실패하면 미리 열어둔 빈 창이 방치되지 않도록 `catch` 블록에서 `printWindow.close()` 추가.

### 7-AU-2. 🟠 입력창에 손대면 화면이 확 커지는 문제 (iOS Safari)

- 앱 전체 입력창(`.form-input`/`.form-select`)이 `font-size: 14px`였는데, iOS Safari는 16px 미만 입력창에 포커스가 가면 페이지 전체를 자동으로 확대한다(업계에서 가장 잘 알려진 모바일 웹 버그 중 하나). 이름/생년월일/시:분/도시/MBTI/궁합 상대방 정보/AI 챗봇 입력까지 전부 이 두 클래스를 공유해서 앱 전체 입력 지점에 영향.
- **수정**: 두 클래스 모두 `font-size: 16px`로 상향. 이 환경엔 실 아이폰이 없어 확대 애니메이션 자체는 재현 못 했지만(Playwright의 WebKit 엔진은 이 OS 레벨 동작을 재현하지 않음), 원인 조건은 명확해 조치.

### 7-AU-3. 🟡 헤더 "가이드" 버튼 터치 영역 미달

- 지난 세션(7-AT)에 추가한 `.btn-guide-header`가 34×34px로 애플 HIG 권장 최소 터치 영역(44×44pt) 미달 — 44×44로 확대.

### 7-AU-4. 🔴 "로딩 중 검은 화면 / 중간에 끊김" — 원인은 지하철뿐 아니라 코드 스플리팅 자체의 구조적 리스크

- 조사 결과 사주/AI 생성 로직 자체는 이미 견고함(모델 4개 폴백 × 재시도 3회 × 타임아웃 20초, AI 실패해도 사주원국은 항상 표시됨 — `run()` 함수에 예외 경로가 사실상 하나뿐임). 문제의 핵심은 그 하나의 예외 경로와, **이번 세션(7-AS)에서 도입한 코드 스플리팅(별자리 계산을 동적 import로 분리)** 이 두 가지가 겹치는 지점이었음:
  - 지하철 등에서 네트워크가 **순간적으로 끊긴 경우** — `import('./utils/astrologyCalculator')` 같은 동적 import fetch가 실패.
  - **네트워크와 무관하게**, 사용자가 탭을 오래 열어둔 사이 이 프로젝트가 새 버전을 배포하면(이 세션에서만 벌써 여러 번 배포함), 브라우저가 이미 메모리에 들고 있는 이전 버전의 JS는 "그 시점에 존재하던 파일 해시"를 그대로 참조하는데, 그 해시의 청크 파일이 새 배포로 서버에서 사라져 동적 import가 **네트워크가 멀쩡해도** 실패함(SPA 배포의 가장 흔한 함정 중 하나) — 이 프로젝트처럼 배포가 잦을수록 자주 겪게 됨. 즉 "모바일 환경 자체가 불안정해서"가 아니라 **어떤 환경에서든 앱을 오래 열어둔 채 새 배포를 만나면** 발생할 수 있는 문제였고, 마침 이번 세션에서 이 실패 지점(동적 import)을 새로 만든 게 겹쳐서 최근 들어 더 잘 보이게 된 것으로 추정됨.
  - 위 두 경우 다 실제로 걸리면: (a) 기존 catch 블록이 "생년월일 입력값이 올바르지 않습니다"라는 **엉뚱한 안내**를 띄우며 입력 화면으로 돌려보냈고(사용자 입장에선 "제대로 입력했는데 왜 튕기지" 싶은 경험), (b) 만약 이 실패가 React가 앱을 마운트하기도 전(최초 진입 시 메인 스크립트 자체가 이 문제로 실패)에 발생하면 `index.html`의 `#root`가 완전히 빈 채로 남아 **검은 배경만 남는 화면**으로 보임(React의 ErrorBoundary는 React가 이미 켜진 뒤의 렌더링 오류만 잡을 수 있어, 이 경우는 애초에 손댈 수가 없는 영역).
- **수정 (총 4곳)**:
  1. `src/main.tsx`: Vite가 청크 로딩 실패 시 쏘는 `vite:preloadError` 이벤트를 받아 **세션당 1회** 자동 새로고침 — 최신 배포의 HTML/청크를 다시 받아오면서 대부분의 "탭을 오래 열어둔 사이 새 배포" 케이스가 사용자가 눈치채기도 전에 조용히 복구됨. 무한 새로고침을 막기 위해 `sessionStorage` 플래그로 1회 제한(진짜 네트워크 두절 상태면 새로고침도 실패하는 게 당연하므로 반복 시도는 의미 없음).
  2. `src/appHelpers.ts`: `isChunkLoadError()` 신규 — 브라우저가 이런 실패에 공통으로 쓰는 문구(`dynamically imported module`/`Failed to fetch`/`module script` 등)를 판별. `App.tsx`의 두 동적 import 호출부(별자리 계산·오늘의 트랜짓) catch 블록에서 이걸로 분기해, 진짜 네트워크/배포 문제일 땐 "네트워크 연결이 불안정합니다"로 정확하게 안내(엉뚱하게 생년월일을 의심하게 만들지 않음).
  3. `index.html`: `<div id="root">` 안에 정적 안내문(🔮 이모지 + "나풀이를 불러오는 중이에요. 화면이 계속 이 상태라면 새로고침해 주세요")을 미리 넣어둠 — React가 정상 마운트되면 즉시 덮어써지지만, 최악의 경우(스크립트 자체가 로드 실패)에도 완전히 빈 검은 화면 대신 최소한의 안내는 보이도록. 이 안내문의 배경/글자색은 외부 CSS 파일(App.css, 해시 붙은 별도 파일)에도 의존하지 않도록 `<head>`에 별도 인라인 `<style>`로 지정.
  4. `public/sw.js`: `/assets/` 캐시 우선 전략에 `.catch()`가 없어 네트워크 실패 시 처리 안 된 rejection으로 남던 것을 캐시 재확인 폴백으로 보완(HTML 브랜치는 원래부터 이런 폴백이 있었는데 assets 브랜치만 빠져있던 비대칭을 바로잡음).

### 7-AU-5. 검증

- `tsc --noEmit`/`npm run build`/`npm run test:unit`(30개)/`npm run test:e2e`(4개) 전부 통과.
- **미검증**: `vite:preloadError` 자동 새로고침과 `index.html` 정적 폴백 둘 다 "청크/스크립트 로딩이 실제로 실패하는 상황"을 인위적으로 재현하기 어려워(로컬에서 네트워크를 정확한 타이밍에 끊거나, 실제 배포 사이에 사용자가 탭을 열어두는 상황을 재현하기 곤란) 이 세션에서 직접 눈으로 확인하지는 못함 — 코드 검토와 Vite/브라우저의 표준 동작 방식에 근거해 구현. 배포 후 며칠 지나 다음 배포가 나갈 때, 그 사이 앱을 열어둔 사용자가 있다면 자동 복구가 실제로 작동하는지 체감 여부로 간접 확인 가능.
- PDF 팝업 수정은 로컬 환경엔 `/api/gemini`가 없어 AI 콘텐츠가 없으면 PDF 버튼 자체가 안 보이는 구조(프로필 액션 버튼 행이 `result.aiIntro` 존재 여부로 조건부 렌더링됨)라 로컬에서 끝까지 재현하지 못함 — 배포 후 프로덕션에서 실제 팝업 오픈 및 내용 채워짐을 확인 필요.

---

## 7-AV. 2026-08-18 24절기 데이터 전면 재검증 + 표준시(UTC+8:30·서머타임) 보정

> **상태**: ✅ 전부 구현 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(48개) 전부 통과. 서머타임 보정(7-AV-4)은 최초 발견 시점엔 근거 미검증이라 보류했으나, 같은 세션에서 이어서 조사·구현까지 완료.
> **배경**: 사용자가 경쟁 앱 분석 문서(`사주앱_경쟁분석.md`)를 리뷰해달라고 요청. 그 문서에 적힌 "1930~1939년/2031년 이후 절기 데이터 부재" 이슈를 검증하던 중, 처음엔 "기존 1940~2030 테이블은 이미 검증된 데이터이니 건드리지 말고 범위 밖만 계산으로 보완하자"는 하이브리드 방향으로 논의했으나, 후보 라이브러리(`lunar-typescript`)를 실제로 검증하는 과정에서 **기존 테이블 자체가 광범위하게 틀려 있다는 사실이 먼저 발견**되어 방향이 전면 재생성으로 바뀜.

### 7-AV-1. 발견 — 기존 solarTerms.ts(1940~2030) 전수 검증 결과 82% 오차

- `lunar-typescript`로 기존 테이블을 검증하려다 그 라이브러리 자체가 한국천문연구원(KASI) 공식 발표치와 최대 30시간 어긋남을 먼저 발견. 라이브러리 문제인지 검증 방법 문제인지 가리기 위해 `astronomia`(VSOP87 태양 겉보기 황경 계산)로 별도 구현 후 (1) 2020년 공개적으로 알려진 춘분/하지/추분/동지 시각, (2) KASI가 실제 발표한 2024년 경칩·2028년 소한 등 여러 건과 대조해 오차 25초~1분 이내임을 먼저 확인 — 계산 방식 자체는 신뢰할 수 있음을 검증.
- 이 검증된 계산으로 **기존 `SOLAR_TERMS`(1940~2030, 1092건) 전수를 재검증**한 결과: 60분 이상 오차 899건(82%), 600분(10시간) 이상 오차 23건, 최대 오차 1830분(약 30.5시간). 특히 청명·입하·망종·소서는 91개 연도 전부에서 오차, 경칩은 윤년마다 반나절 이상 오차 — 무작위 노이즈가 아니라 구조적 문제로 판단.
- KASI가 실시간 조회 가능한 2021~2028년 자료로 5개 항목(2024 경칩, 2028 소한/경칩/백로, 2025 백로)을 직접 대조 — 전부 1분 이내로 일치, 기존 테이블은 같은 항목에서 6~13시간씩 어긋남을 재확인.

### 7-AV-2. 수정 — astronomy-engine으로 테이블 재생성 (1900~2100)

- 프로젝트에 이미 프로덕션 의존성으로 있던 `astronomy-engine`(별자리 기능에서 지연 로딩 중, 섹션 7-AS-2 참고)의 `SearchSunLongitude()`가 KASI 공식치와 초 단위로 일치함을 재확인 — 새 라이브러리를 추가하지 않고 기존 의존성만으로 해결 가능해, 번들 크기 영향 없이 처리.
- `scripts/generateSolarTerms.ts` 신규 — Node 전용 생성 스크립트(`npx tsx scripts/generateSolarTerms.ts`)로 `src/data/solarTerms.ts`의 `SOLAR_TERMS` 테이블을 1900~2100년(201개 연도) 범위로 전면 재생성. 데이터만 담긴 파일이라 `astronomy-engine`은 여전히 앱 번들에 안 들어감(빌드 후 메인 청크 크기 변화 없음, 파일 자체 크기만 13.5KB→37KB).
- `App.tsx`: 생년 입력 `min/max`를 1930/2030 → **1900/2100**으로 확장, 이제 부정확해진 "1940년 이전 근사값" 경고 문구 2곳(본인 폼·궁합 상대방 폼)을 제거.
- `src/data/solarTerms.test.ts` 신규 — 위에서 대조한 KASI 공식치 5건을 회귀 테스트로 고정(향후 재생성 스크립트나 데이터가 다시 깨지면 이 테스트가 잡아냄), 데이터 범위(1900/2100/1930/2031 연도 존재) 확인 테스트 포함.

### 7-AV-3. 추가 발견 — 1954~1961년 한국 표준시는 실제로 UTC+9가 아니었음

- 위 작업을 마무리하며 "혹시 또 놓친 전제가 있는지" 점검하던 중, **1954년 3월 21일(대통령령 제876호, 이승만 정부)부터 1961년 8월 10일(박정희 정부) 전날까지 한국이 독자 표준자오선(동경 127.5도=UTC+8:30)을 썼다**는 사실을 웹 검색으로 확인(나무위키·머니투데이 등 복수 출처 일치). 그 기간 출생자가 기억/기록으로 갖고 있는 시:분은 실제로는 UTC+8:30 시계 값인데, 앱은 이를 무조건 UTC+9(KST)로 가정해 절기 경계 판정에 최대 30분의 절대시각 오차가 생길 수 있었음.
- `sajuCalculator.ts`에 `isInHistoricalUTC830Period()` 추가, `calculateSaju()` 진입부에서 이 기간 출생자의 입력 시:분에 **+30분** 보정을 적용한 뒤 연주·월주·일주·시주·대운 계산에 일괄 반영(시간을 모르면 보정할 실제 시계값이 없으므로 건너뜀). 실제로 판정이 뒤집히는 경계 사례(1955-02-04 22:55생: 보정 전 갑오년/보정 후 을미년)를 포함해 회귀 테스트 5개로 검증.

### 7-AV-4. ✅ 서머타임(일광절약시간제) 보정 — 조사 후 구현 완료

- 위 UTC+8:30 보정 직후, 사용자가 "이제 문제 없냐"고 재확인하는 과정에서 **한국이 1950년대에 서머타임도 시행했다는 사실이 추가로 걸려** 처음엔 근거 미검증이라 다음 세션으로 미뤘으나, 사용자가 바로 이어서 진행을 요청해 같은 세션에서 조사·구현까지 마침.
- 웹 검색(나무위키 "서머타임" 문서, 국가기록원 "서머타임제" 자료, 2026-08-18 확인)으로 시행 연도·정확한 시작/종료일을 확인: **1948~1951년**(6/1~9/13, 4/3~9/11, 4/1~9/10, 5/6~9/9)과 **1955~1960년**(5/5~9/9, 5/20~9/30, 5/5~9/22, 5/4~9/21, 5/3~9/20, 5/1~9/18) — 국가기록원은 "1949~1961년 서머타임 실시"로 뭉뚱그려 서술하지만, 나무위키 표(2회 별도 조회로 동일 값 재확인, 1952~1954년 공백은 6·25 전쟁 시기와 겹쳐 정황상 타당)가 연도별 정확한 일자를 제공. **다만 이 날짜들은 대통령령 원문 같은 1차 사료까지는 대조하지 못해, 위 UTC+8:30 전환일(대통령령 제876호로 확인)보다는 확신도가 한 단계 낮음** — 정정 필요 시 참고.
- 1987~1988년(5/10~10/11, 5/8~10/9)은 이미 `astrologyData.ts`의 `KNOWN_DST_PERIODS`로 검증·구현돼 있던 값과 동일(별자리 기능에 한해서만 적용 중이었고, 사주 계산에는 지금까지 미반영 상태였음).
- **핵심 함정**: 1955~1960년 서머타임은 1954~1961년 UTC+8:30 시기와 겹친다. 그 여름 동안 실제 표준시는 UTC+8:30에 서머타임 1시간이 더해진 **UTC+9:30**이라, KST(UTC+9) 대비 보정값은 +30분이 아니라 **-30분**이어야 한다. 반대로 1948~1951·1987~1988년(원래 UTC+9 시기)의 서머타임은 UTC+10이라 보정값이 **-60분**이다.
- `sajuCalculator.ts`에 `HISTORICAL_DST_PERIODS`(12개 구간) + `isHistoricalDstDate()` + `getHistoricalUtcOffsetMinutes()`(그 날짜의 실제 민간 표준시 오프셋을 UTC+8:30 여부와 서머타임 여부를 함께 반영해 산출) + `historicalMinuteCorrection()`(KST 대비 보정 분수)를 추가. 기존 "1954~1961이면 무조건 +30분"이던 로직을 이 통합 함수로 교체 — 평시 0분, UTC+8:30만 +30분, UTC+8:30+서머타임 -30분, UTC+9+서머타임 -60분이 자동으로 계산됨.
- 회귀 테스트 8개 추가: 오프셋 계산 자체(5개, 각 케이스별 보정값 직접 검증) + 실제 파이프라인 반영 확인(2개, 1987년 서머타임 -60분 보정으로 야자시 이월 여부가 바뀌는 경계 사례 — 이 케이스는 지금까지 사주 계산에서 전혀 보정되지 않고 있었으므로 이번에 새로 고쳐진 것).

### 7-AV-5. 검증

- `npm run test:unit`: 48개 전부 통과(기존 30개 + 절기 데이터 회귀 5개 + UTC+8:30 보정 5개 + 서머타임 보정 8개).
- `tsc && vite build` 클린, 번들 크기 변화 없음(`astronomy-engine`은 여전히 `astrologyCalculator-*.js` 청크에만 존재, 메인 청크는 데이터 증가분만큼만 소폭 증가).

---

## 7-AW. 2026-08-18(2차) 심화해석 6종 크레딧제 결제 시스템 — 1단계 코드 구현 완료

> **상태**: ✅ 코드 구현 완료(1단계). `tsc --noEmit`/`npm run build`/`npm run test:unit`(48개) 전부 통과. **실제 결제(2단계)는 사용자가 외부 콘솔에서 계정·상품·키를 등록해야만 동작** — 이 환경엔 실기기·RevenueCat/App Store Connect/Google Play Console/토스페이먼츠 계정이 없어 종단 검증은 못 함(과거 Sign in with Apple/앱스토어 심사 때와 동일한 제약).
> **배경**: 경쟁 앱 분석에서 "나풀이만 확인된 유료 화면이 없다"는 공백이 지적됨. 수익화 모델 여러 개(크레딧제/리포트단건/구독형/무료체험+구독 하이브리드/단계적 접근)를 축별로 비교한 끝에, **"핵심 무료 유지 + 실제 코드에서 확인한 8192토큰급 '심화'(Deep) 레이어 6개 함수만 크레딧제로, 우선 1단계로 출시 후 데이터 보고 2단계(구독 등) 검토"** 방향으로 확정. 세부값도 순차 확정: 크레딧 소모 대상은 6개 심화함수만(채팅 제외), IAP는 RevenueCat(영수증 검증 직접 구현 리스크 회피), 웹은 토스페이먼츠, 무료체험 3크레딧, **로그인 필수**(재설치 무한 리필 악용 방지 — 단 이 게이트는 심화해석 6개에만 적용, 나머지 기능은 계속 비로그인 무료).

### 7-AW-1. 설계 교정 — 크레딧 차감을 프록시 엔드포인트가 아니라 별도 "차감 전용" 호출로 분리

- 최초 설계는 `api/gemini.ts`를 감싸는 `api/gemini-deep.ts`를 만들어 그 안에서 크레딧을 차감하려 했으나, `callGeminiPlainApi`가 모델 4개×재시도 3회까지 내부적으로 여러 번 HTTP 요청을 보낸다는 걸 재확인하면서 **버튼 한 번 클릭에 최대 12번 중복 차감될 뻔한 설계 결함**을 구현 중 발견. `api/gemini-deep.ts`/`api/_geminiProxy.ts`를 삭제하고, 대신 클라이언트가 실제 생성 요청 **전에 딱 한 번** `POST /api/credits`(크레딧 차감 전용)를 호출하고, 그 다음 생성 요청은 기존 `api/gemini.ts`를 그대로(무변경) 쓰는 구조로 교정.

### 7-AW-2. 서버 (Vercel `api/`, 전부 신규)

- `api/_firebaseAdmin.ts` — `api/_rateLimit.ts`에 있던 Admin SDK 초기화(`getAdminDb`)를 공용 모듈로 추출 + `getAdminAuth()` 신규 추가. `_rateLimit.ts`는 이 모듈을 쓰도록 리팩터링(동작 변화 없음).
- `api/_credits.ts` — `verifyIdToken(req)`(Authorization 헤더의 Firebase ID 토큰 검증), `checkAndConsumeCredit(uid)`(트랜잭션: 최초 호출 시 무료체험 3개 지급 후 그중 1개 차감, 이후는 잔액에서 1개씩 차감), `getCreditBalance(uid)`, `grantCredits(uid, amount)`(결제 확정 시 지급). **`_rateLimit.ts`와 반대로 fail-closed** — Firestore/설정 오류 시 차단(유료 콘텐츠가 인프라 오류로 새 나가는 것을 막기 위해, 레이트리밋의 "서비스 중단보다 낫다"는 논리가 여기선 거꾸로 적용됨).
- `api/credits.ts` — GET(잔액 조회)/POST(1개 차감) 엔드포인트. 로그인 필수.
- `api/revenuecat-webhook.ts` — RevenueCat 구매완료 웹훅. `Authorization` 헤더를 `REVENUECAT_WEBHOOK_SECRET`과 대조, `app_user_id`(=Firebase uid, 클라이언트가 RevenueCat 초기화 시 지정)로 대상 특정, 이벤트 ID 기준 멱등성 보장(중복 웹훅 방지) 후 상품별 크레딧 지급.
- `api/toss-confirm.ts` — 토스페이먼츠 결제 승인. 클라이언트가 보낸 금액이 아니라 **토스 승인 응답의 실제 금액**으로 크레딧을 매칭(위조 방지), `orderId` 기준 멱등성 보장.
- `api/_creditProducts.ts` — 크레딧 상품 카탈로그. RevenueCat 상품 ID↔크레딧, 토스 결제금액↔크레딧 두 테이블이 있고 **반드시 서로 일치해야 함**(주석으로 명시, 자동 검증은 아직 없음 — 향후 가격 조정 시 실수하기 쉬운 지점이라 유의). 가격은 사주도령 참고 초안으로 확정(2026-08-18, 사용자 확인): **1개 990원 / 5개 2,900원(개당 580원) / 15개 6,900원(개당 460원) / 30개 9,900원(개당 330원)** — 무료체험 3개를 다 쓴 뒤 "딱 하나만 더" 필요한 경우를 위해 낱개(1개) 옵션을 추가로 확인받아 넣음.
- `api/_errorBody.ts` — 기존 `api/gemini.ts`의 에러 응답 형태를 여러 엔드포인트가 공유하도록 추출.

### 7-AW-3. 클라이언트

- `src/utils/credits.ts` — `fetchCreditBalance`/`consumeCredit` (Firebase ID 토큰을 실어 위 서버 엔드포인트 호출).
- `src/utils/purchases.ts` — RevenueCat 네이티브 래퍼. `src/utils/appReview.ts`와 동일 패턴(`isNativePlatform()` 가드 + 지연 import)으로 웹 번들에서 완전히 제외. `initPurchases(uid)`가 RevenueCat의 `appUserID`를 Firebase uid로 맞춰, 웹훅의 `app_user_id`가 곧 우리 uid가 되도록 함.
- `src/utils/tossPayments.ts` — 토스 "결제창"(API 개별 연동 키) 방식. 결제 후 `successUrl`로 돌아오면 URL의 `paymentKey`/`orderId`/`amount`를 읽어 서버에 승인 요청.
- `src/components/PaywallOptions.tsx`(신규) — 네이티브면 RevenueCat 상품 목록, 웹이면 토스 정가 옵션을 보여줌. 2단계(콘솔 설정)가 아직 안 끝났으면 "결제 준비 중" 안내로 우아하게 저하.
- `src/App.tsx`:
  - `creditBalance`/`showLoginGateModal`/`showPaywallModal`/`paywallLoading` 상태 추가.
  - `ensureCreditForDeepGeneration()` — 비로그인이면 로그인 모달, 크레딧 0이면 결제 모달을 띄우고 `false` 반환. 심화해석 6개 핸들러(`handleGenerateCategoryDeep` 등) 맨 앞에 `if (!(await ensureCreditForDeepGeneration())) return null;` 한 줄씩 추가 — 기존 "GEMINI_API_KEY 없으면 이른 반환" 관례와 동일한 자리에 이어붙임.
  - 로그인 상태 구독(`subscribeToAuthState`) 콜백에 크레딧 잔액 조회 + `initPurchases` 호출 추가.
  - 토스 결제 후 돌아왔을 때(URL 쿼리 감지) 자동으로 `api/toss-confirm` 호출하는 `useEffect` 신규.
  - 로그인 유도 모달 + 결제 모달 JSX 신규(기존 `handleSignIn`/`handleSignInApple` 재사용, 기존 모달들과 동일한 `role="dialog"` 접근성 패턴 유지).
- `package.json`: `@revenuecat/purchases-capacitor`(npm 존재 확인 후 추가), `@tosspayments/tosspayments-sdk`(동일하게 확인 후 추가).

### 7-AW-4. 검증

- `npm run test:unit`: 48개 전부 통과(신규 추가 없음 — 크레딧 차감 로직은 Firebase Admin SDK 의존이라 이 프로젝트의 순수 로직 전용 테스트 인프라(node 환경, jsdom 없음)로는 단위 테스트하기 어려워 보류. 결제 상품 가격 테이블 두 곳(`api/_creditProducts.ts` ↔ `src/utils/tossPayments.ts`)의 수동 대조로 일치 확인).
- `tsc --noEmit` 클린. `npm run build` 클린 — RevenueCat SDK가 `index-g1PlE-cL.js`(7.24KB)/`web-B76C0SUY.js`(10.04KB) 별도 청크로 정상 분리됐음을 빌드 산출물에서 직접 확인(메인 번들에 문자열 검색해 안 들어갔음도 확인). 토스 SDK는 런타임에 CDN에서 로드하는 방식이라 빌드 시점 청크 자체가 거의 없음. 메인 청크는 382KB→389KB로 신규 UI 코드만큼만 소폭 증가.
- **미검증(2단계 완료 전 불가능)**: 실제 인앱결제·토스 결제·웹훅 수신·크레딧 반영까지의 종단 흐름.

### 7-AW-5. 2단계 — 사용자가 직접 해야 하는 일 (코드로 대신할 수 없음)

| 항목 | 내용 |
|---|---|
| RevenueCat | 계정 생성 → iOS/Android 앱 등록 → 상품 4종(`napuli_credits_1`/`5`/`15`/`30`, `api/_creditProducts.ts`와 이름 일치) 등록 → 웹훅 URL(`api/revenuecat-webhook.ts`)·시크릿 설정 → 공개 SDK 키 발급 |
| App Store Connect | 위와 동일한 상품 ID로 인앱결제(소모성) 등록, 가격 설정, 심사 제출 |
| Google Play Console | 위와 동일한 인앱상품 등록 |
| 토스페이먼츠 | 가맹점 가입·심사 → 클라이언트 키/시크릿 키 발급 |
| 환경변수 등록 | `VITE_REVENUECAT_IOS_KEY`, `VITE_REVENUECAT_ANDROID_KEY`, `REVENUECAT_WEBHOOK_SECRET`, `VITE_TOSS_CLIENT_KEY`, `TOSS_SECRET_KEY` (Vercel 환경변수 + 로컬 `.env`) |
| Firestore 보안 규칙 | 규칙 파일을 `firestore.rules`(신규, 저장소 루트)에 작성해뒀음 — Firebase 콘솔(Firestore Database → 규칙)에 그 내용을 그대로 붙여넣고 게시. 상세: 섹션 7-AW-6 |
| 실기기 검증 | 위 설정이 끝난 뒤 TestFlight/내부 테스트 트랙에서 실제 구매·복원·웹훅 반영까지 확인 |

### 7-AW-6. 🔴 배포된 Firestore 규칙이 크레딧 필드를 막고 있지 않던 구멍 발견 — 규칙 파일 작성 완료(게시는 사용자 조치 필요)

- 섹션 7-N-4에서 게시했던 기존 규칙은 `request.auth.uid == userId`면 `users/{uid}` 문서 전체를 자유롭게 읽고 쓸 수 있게 열려 있었음 — 다이어리 동기화(`bookmarks`)만 염두에 둔 규칙이라, 이번에 새로 생긴 `credits`/`freeTrialGranted` 필드도 똑같이 클라이언트가 직접 쓸 수 있는 상태였음. 즉 로그인한 사용자가 브라우저 개발자도구에서 Firestore 클라이언트 SDK로 직접 `credits: 99999`를 써넣으면 서버의 차감 로직을 완전히 우회할 수 있었던 셈 — 크레딧제 자체를 무의미하게 만들 수 있는 구멍.
- `firestore.rules`(신규, 저장소 루트) 작성 — `bookmarks`/`updatedAt` 등 기존 필드는 그대로 클라이언트 쓰기 허용(회귀 없음), `credits`/`freeTrialGranted`만 `request.resource.data.diff(resource.data).affectedKeys().hasAny([...])`로 걸러 클라이언트 쓰기에서 명시적으로 제외 — Admin SDK(서버)는 규칙 자체를 우회하는 권한이라 영향 없음.
- **아직 게시 전** — 코드로 파일만 만들어뒀고, 실제 반영은 사용자가 Firebase 콘솔(Firestore Database → 규칙)에 이 파일 내용을 붙여넣고 게시해야 함(7-N-4 때와 동일한 방식). 게시 전까지는 이 구멍이 여전히 열려 있는 상태.

### 7-AW-7. 크레딧 가격 확정 + 낱개(1개) 옵션 추가

- 가격 초안(5/15/30개)에 대해 "부담 없는 가격대"를 상의한 뒤, 무료체험 3개를 다 쓴 직후 "딱 하나만 더" 필요한 상황을 위해 **1개 990원** 낱개 옵션을 추가로 확정. 최종: 1개 990원 / 5개 2,900원(개당 580원) / 15개 6,900원(개당 460원) / 30개 9,900원(개당 330원).
- `api/_creditProducts.ts`(`napuli_credits_1` 추가), `src/utils/tossPayments.ts`(`CREDIT_PRICE_OPTIONS`에 990원 옵션 추가) 반영. RevenueCat 쪽 상품도 4종으로 늘어나 7-AW-5 체크리스트도 갱신.

### 7-AW-8. 배포 킬스위치 추가 — "구현은 완료, 실제 전환은 앱 출시 시점에"

- 사용자 지시: "유료버전은 구현만 해놓고 배포는 앱 버전까지 다 완료될 때 하자, 지금 당장 쓰는 건 막지 말고." 그런데 지금 코드를 그대로 배포하면 `ensureCreditForDeepGeneration()`이 곧바로 작동해 **비로그인 사용자가 심화해석 버튼을 누르는 즉시 로그인 모달에 막히는** 회귀가 생김 — 결제 키가 없어도 게이트 자체는 이미 살아있기 때문.
- `src/utils/credits.ts`에 `CREDIT_GATE_ENABLED`(환경변수 `VITE_CREDIT_GATE_ENABLED === 'true'`, 미설정 시 기본 `false`) 킬스위치 추가. `ensureCreditForDeepGeneration()` 맨 앞에서 이 값이 꺼져 있으면 즉시 `true`를 반환해 기존처럼 완전 무료·비로그인으로 동작(회귀 없음). 로그인 시 크레딧 잔액 조회·RevenueCat 초기화, 토스 결제 복귀 처리도 전부 같은 스위치로 묶어 꺼진 동안은 불필요한 네트워크 호출 자체가 안 나가게 함.
- `.env.example`에 `VITE_CREDIT_GATE_ENABLED`와 RevenueCat/토스 관련 키 전부 문서화(실제 값 없음). **네이티브 앱 출시 + 2단계(계정·상품·키) 설정이 다 끝난 뒤 이 값을 `true`로 바꾸는 게 유일한 활성화 스위치.**
- `tsc --noEmit`/`npm run test:unit`(48개) 통과 확인.

## 7-AX. 2026-08-18(3차) 온보딩 진입점 과다 완화 — 가벼운 안내 강조

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(48개) 전부 통과.
> **배경**: 경쟁 분석에서 지적된 "사주+MBTI+점성술+타로+궁합+풍수가 동시에 노출돼 뭐부터 봐야 할지 방향성이 약하다"는 공백을 다룸. 실제 구조를 보니 결과 화면 대분류는 오늘/사주/별자리 3개뿐이고 기본값도 "사주 › 운세"로 고정돼 있어 완전히 방향 없이 떠 있는 상태는 아니었음 — 다만 사주 서브탭 4개(운세/해석/궁합/풍수), 해석 안 카테고리 6개까지 합치면 한 화면에 보이는 선택지가 많아 "일단 하나만 밀어주는" 느낌은 약했음.
> **범위 결정 과정**: 처음 제안한 "결과 화면 탭 구조 자체 재설계"(대분류 유지, 서브탭 4→2개 통합 + 해석 6개 카테고리를 탭 대신 세로 스크롤로 전환)를 실제 목업으로 비교해봤으나, 기존 탭 구조가 이미 표준적인 패턴이고 실제 이탈 데이터(GA4는 이미 연동됨) 없이 구조를 바꾸는 리스크가 커서 보류 — 대신 가벼운 안내 강조만 추가하기로 결정. 이 논의 중 "탭을 합치면 카테고리별 AI 채팅 기능이 빠지는 것 아니냐"는 우려가 나왔는데, 실제 코드 확인 결과 채팅은 애초에 카테고리 무관하게 "해석" 탭 전체에서 공유되는 구조였어서(`generateFollowUpAnswer`가 `activeTab`이 아니라 `result.sajuResult` 전체를 받음) 이 우려는 해당 없음으로 확인됨.

### 7-AX-1. 구현

- `src/appTypes.ts`: `RESULT_HINT_SEEN_KEY` 신규.
- `src/App.tsx`:
  - 결과 화면에 처음 도달했을 때 1회만 뜨는 안내 배너 — "사주원국과 🌌 운세부터 먼저 보시면 돼요, 나머지는 궁금할 때 둘러보세요" 문구를 대분류 탭 바로 위에 배치, `showResultHint`/`handleDismissResultHint`로 닫으면 `localStorage`에 기록해 다시 안 뜸.
  - 기존 "💡 기능 가이드" 팝업(`GUIDE_FEATURES`)의 첫 번째 카드(사주×MBTI 융합 해석)에 "먼저 보세요" 뱃지 추가(기존 `.hero-badge` 스타일 재사용, 신규 CSS 없음).
- 온보딩 화면 자체(4개 기능 소개 카드)는 손대지 않음 — 결과 화면과 가이드 팝업 두 지점만 조정.

### 7-AX-2. 검증

- `tsc --noEmit`/`npm run build`(번들 크기 변화 미미, 신규 CSS 없이 기존 클래스 재사용)/`npm run test:unit`(48개, 회귀 없음) 전부 통과.

---

## 7-AY. 2026-08-18(4차) 온보딩 무료→유료 브릿지 — "오늘의 나풀이" → 심화해석 CTA

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(48개) 전부 통과.
> **배경**: 8절에 새로 등록했던 "온보딩 무료→유료 브릿지 부재" 항목 착수. 구현 방식 후보 3가지(①심화해석 버튼 옆 티저 문구, ②정적 미리보기 스니펫, ③특정 무료 기능을 명시적 브릿지로 지정) 중 사용자 요청으로 추천안 제시 — 네임드사주의 "무료 사떡궁합 → 유료 카테고리" 패턴의 핵심이 "버튼 문구 개선"이 아니라 "이미 인기 있는 별개 무료 기능이 통로 역할을 한다"는 점이라고 판단해 **③(명시적 브릿지)**을 추천, 채택됨.

### 7-AY-1. 구현

- 매일 자동 생성되고 스트릭/배지로 재방문이 이미 잦은 "🌅 오늘의 나풀이" 카드 하단에, 결과가 보이는 시점에만 CTA 카드 신규 추가: "오늘의 기운 말고, {이름}님의 성격 자체를 더 깊게 알고 싶다면? → 심화해석" — 클릭하면 `activeSection`을 `'saju'`, `activeSajuTab`을 `'ai'`, `activeTab`을 `'personality'`로 바꿔 심화해석이 있는 화면으로 이동(기존 "해석" 탭의 실제 생성 버튼은 그대로 두고 이동만 시킴 — 자동 생성/크레딧 소모는 하지 않음).
- 크레딧제 킬스위치(`CREDIT_GATE_ENABLED`, 섹션 7-AW-8)와는 **별개로 항상 노출**하기로 함 — 이 CTA는 "돈 내라"는 광고가 아니라 이미 존재하는 무료 기능(심화해석은 지금도 무료)을 사용자가 발견하도록 돕는 내비게이션이라, 결제 여부와 무관하게 가치가 있고 문구도 가격을 언급하지 않아 나중에 킬스위치가 켜져도 그대로 유효함.

### 7-AY-2. 검증

- `tsc --noEmit`/`npm run build`(번들 크기 변화 미미)/`npm run test:unit`(48개, 회귀 없음) 전부 통과.

## 7-AZ. 2026-08-18(5차) 귀인지도 — 오행 상생상극 관계를 방사형 지도로 시각화

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(52개) 전부 통과.
> **배경**: 8절 "소셜 그래프 기능(귀인지도류) 부재" 항목 착수. 실제 코드를 먼저 확인한 결과 **이론 자체(오행 상생상극 → 관계 유형 매핑)는 `pairCompatibility.ts`의 `StemRelationType`(6종: 비화/내가생함/상대가생함/내가극함/상대가극함/무관계)으로 이미 완전히 구현돼 있었고**, 최근 비교한 상대 이력(`getPairCompatHistory`, 최대 10명)도 이미 저장되고 있었음 — 새로 만들 건 "학술적 라벨을 게임처럼 재포장"하는 것과 "여러 명을 한 화면에 보여주는 시각화"뿐이었음. 시각화 수준은 카드 그리드(가벼움) vs 방사형 네트워크(더 멋지지만 공수 큼) 중 방사형 네트워크로 확정.

### 7-AZ-1. 구현

- `src/utils/pairCompatibility.ts`: `GWIIN_TYPE_META`(신규) — 기존 `STEM_RELATION_LABEL`(학술적 설명)과 같은 6종 분류를 게임화 라벨로 재포장. 오행 판정 로직 자체는 손대지 않음. 최종 매핑: 상대가 나를 생함="🌟 귀인"(금색), 내가 상대를 생함="🌱 내 편"(초록), 내가 상대를 극함="⚡ 라이벌"(보라), 상대가 나를 극함="🔥 자극제"(주황), 비화="🤝 동료"(파랑), 무관계="◯ 무난한 사이"(회색).
- `src/components/GwiinMap.tsx`(신규) — 순수 시각화 컴포넌트. 나를 중심(금색 그라디언트 원)에 두고, 상대들을 방사형(각도 = 360°/인원수)으로 배치, 중심과 선으로 연결(관계 색상 반영), 각 노드는 이모지+이름, 클릭 시 콜백. SVG라 별도 차트 라이브러리 불필요.
- `src/App.tsx`: `gwiinNodes`를 `useMemo`로 계산 — 이력의 각 상대마다 `calculateSaju()`(순수 계산, API 호출 없음)로 사주를 재산출하고 `comparePillars()`로 관계를 판정(기존 `handleComparePair`와 동일하게 "시간 모름"으로 계산해 일관성 유지). 기존 "이전에 비교한 상대" 단순 버튼 목록을 이 지도로 교체, 노드 클릭 시 기존 `handleReopenPairCompatHistory()`를 그대로 재사용해 재조회. 하단에 6개 관계 유형 범례 추가.

### 7-AZ-2. 검증

- `src/utils/pairCompatibility.test.ts`(신규, 4개) — `GWIIN_TYPE_META`가 6종 전부를 다루는지, 실제 사주 조합(1995-09-27=금 vs 2000-01-01=토, 기존 테스트에서 이미 검증된 기준값 재사용)이 "토생금이므로 상대가 나를 생함=귀인"으로 정확히 판정되는지, 동일인 비교 시 "동료"로 판정되는지 확인 — 첫 실행에 전부 통과(손으로 미리 계산한 예측과 정확히 일치).
- `tsc --noEmit`/`npm run build`(SVG 컴포넌트라 번들 크기 영향 미미)/`npm run test:unit`(52개, 회귀 없음) 전부 통과.

## 7-BA. 2026-08-19 "손없는날" 무료 SEO 페이지 — 신규 음력 계산 엔진 구현

> **상태**: ✅ 구현 완료. `tsc --noEmit`/`npm run build`/`npm run test:unit`(58개) 전부 통과. 정적 페이지 실제 200 응답 + 메타 태그 확인.
> **배경**: `사주앱_경쟁분석.md` 재검토(2026-08-18)에서 발견한 "로그인 없이 보는 SEO 공용 콘텐츠 부재" 공백 착수. 후보 비교(십이건제신 vs 손없는날) 끝에 검색량이 큰 **손없는날**(음력 날짜 끝자리 9·0)로 확정 — 시간 여유가 있다는 사용자 확인 하에 신규 음력 계산 엔진을 제대로 구현하기로 함.

### 7-BA-1. 설계 — 계산 범위를 의도적으로 좁힘

- 손없는날 판정에는 음력 "몇 월"인지(윤달 배정, 월 번호 부여— 전통 음력 계산에서 가장 복잡한 부분)가 불필요하다는 점을 먼저 확인 — **"가장 최근 신월(삭)로부터 며칠째인지"만 있으면 충분**해, 전통 음력 계산의 절반 이상을 차지하는 윤달 로직을 통째로 생략할 수 있었음.
- `astronomy-engine`(이미 절기 계산에서 검증된 라이브러리)의 `SearchMoonPhase(0, ...)`로 신월 시각만 순차 탐색.

### 7-BA-2. 구현

- `scripts/generateNewMoons.ts`(신규, `generateSolarTerms.ts`와 동일 컨벤션) → `src/data/newMoons.ts` 생성 — 1900~2100년 범위 신월 날짜 2,500개.
- `src/utils/lunarCalendar.ts`(신규) — `getLunarDayOfMonth()`(이분 탐색으로 가장 최근 신월 대비 며칠째인지 계산), `isSohnEobsNeunNal()`(끝자리 9·0 판정).
- `scripts/buildLunarPage.ts`(신규) — `src/data/newMoons.ts`를 읽어 완전 정적인 `public/sohn-eobsneun-nal.html`을 생성. **React SPA에 통합하지 않고 `public/privacy.html`/`terms.html`과 동일한 독립 정적 페이지 패턴**으로 감 — SPA는 클라이언트 렌더링이라 네이버 등 크롤러가 콘텐츠를 못 읽을 위험이 있어, SEO가 목적인 이 페이지엔 안 맞는다고 판단. 신월 데이터를 페이지에 인라인 `<script>`로 통째로 임베드해, 매달 재배포 없이도 "오늘 기준" 손없는날이 클라이언트에서 항상 최신으로 계산됨.
- `public/sitemap.xml`에 URL 추가(`changefreq: daily`).
- `src/App.tsx`: 풍수 탭 하단에 이 페이지로 가는 링크 카드 추가(내부 발견성).

### 7-BA-3. 검증

- **신월 계산 정확도**: 2020~2025년 설날(음력 1월 1일) 6개 연도 전부와 정확히 일치(2020-01-25/2021-02-12/2022-02-01/2023-01-22/2024-02-10/2025-01-29) — 전부 실제 공휴일 날짜와 대조. KASI 음양력 변환 페이지는 JS 기반 폼이라 직접 GET 대조는 못 해, 이 방법(잘 알려진 공휴일 다수 대조)으로 대체함 — 절기 때의 KASI 직접 대조보다는 한 단계 낮은 확신도라는 점은 남겨둠.
- `src/utils/lunarCalendar.test.ts`(신규, 6개) — 위 설날 6개 + 추석(음력 8월 15일) 2개 연도로 교차검증, 신월 당일부터 날짜가 정확히 1씩 증가하는지, 손없는날 경계(음력 9·10일)가 정확히 판정되는지 확인 — 전부 통과.
- `npm run test:unit`(58개, 전체 회귀 없음)/`tsc --noEmit`/`npm run build` 클린.
- 빌드 산출물의 `sohn-eobsneun-nal.html`을 `vite preview`로 직접 서빙해 HTTP 200, `<title>`/`<meta description>` 정상 포함, 오늘 날짜 기준 손없는날 목록이 실제로 정확히 계산돼 표시되는지(DOM 스텁으로 스크립트 로직 직접 실행) 확인.
- **이 세션에서 확인 못 하는 것**: 실제 네이버/구글 색인 여부와 검색 노출 — 배포 후 시간이 지나야 확인 가능.

## 7-BB. 2026-08-19(2차) E2E + 모바일 실뷰포트 재검증, "궁합 비교 직후 폼이 안 닫히는" 버그 발견·수정

> **상태**: ✅ 검증 완료 + 버그 1건 수정. `npm run test:e2e`(4개)/`npm run test:unit`(58개) 전부 통과, 모바일(iPhone 12) 뷰포트로 신규 기능 2종(귀인지도·손없는날 페이지) 스크린샷 직접 확인.
> **배경**: 사용자가 "앱 등록·유료화 말고 그 외에 할 일"을 물어, 이번 세션에 App.tsx를 많이 건드렸는데 정작 `test:e2e`(Playwright)를 한 번도 안 돌려봤다는 점을 스스로 발견해 먼저 실행. 이어서 신규 기능(귀인지도·손없는날)이 모바일에서 실제로 어떻게 보이는지 확인.

### 7-BB-1. E2E 회귀 확인

- `npm run test:e2e`: 4개 전부 통과, 콘솔 에러 없음(대분류/서브탭 전환 테스트가 궁합·풍수 탭까지 실제로 열어봄 — 이번 세션 변경 사항이 렌더링을 깨뜨리지 않음을 확인).

### 7-BB-2. 🔴 발견·수정 — "정밀 궁합" 비교 시 AI 텍스트 생성이 실패하면 입력 폼이 영구히 안 닫히던 버그

- `@playwright/test`를 직접 스크립팅해(스캐치패드, 프로젝트 내부로 임시 복사 후 실행 — `require` 해석을 위해 프로젝트 node_modules가 필요했음) iPhone 12 뷰포트로 귀인지도를 확인하는 과정에서, `handleComparePair()`가 **성공/실패 어느 쪽이든 `partnerFormOpen`을 다시 `false`로 되돌리는 코드가 전혀 없었다는** 걸 발견 — grep으로 전체 파일에서 `setPartnerFormOpen` 호출 4곳을 확인했는데 전부 `true`로만 설정. AI 텍스트 생성이 예외를 던지는 경우(네트워크 문제 등), 이력 기록 자체는 이미 끝났는데도 폼이 그대로 남아 사용자가 새로고침 없이는 빠져나갈 방법이 없었음 — 귀인지도 이전의 "이전에 비교한 상대" 버튼 목록(7-AA-1)도 처음부터 같은 결함을 안고 있었던 것으로 보임(이번에 우연히 발견).
- `handleComparePair()`의 `catch` 블록에 `setPartnerFormOpen(false)` 추가 — 비교(일주 산출·이력 기록)는 AI 텍스트 호출 전에 이미 끝나 있으므로, AI 텍스트만 실패해도 폼을 닫아 귀인지도/이전 이력으로 자연스럽게 돌아가게 함.
- **참고**: 실제로는 `generatePairCompatibilityInterpretation()`이 실패 시에도 예외를 던지지 않고 무난한 대체 문구를 반환하는 구조라(다른 "일반" 생성 함수들과 동일 패턴, 심화해석류만 `throwOnFailure`), 로컬처럼 `/api/gemini`가 아예 없는 환경에서도 이 경로를 타는 경우는 드묾 — 그래도 진짜 예외가 나는 경우(네트워크 완전 단절 등)를 위한 방어적 수정으로 유지.

### 7-BB-3. 신규 기능 모바일 렌더링 확인 (스크린샷 직접 확인)

- **귀인지도**: 같은 세션에서 비교 직후에는 뜨지 않고(비교 결과 화면이 먼저 보이는 게 의도된 동작), **새로고침 후 재방문 시**(`pairCompatHistory`는 `localStorage`에 남고 화면 상태만 초기화되는 구조) 정상적으로 뜬다는 걸 확인 — 중심 "나" 원, 상대 노드, 관계색 연결선, 이모지·이름 전부 깨짐 없이 표시됨.
- **손없는날 페이지**: iPhone 12 뷰포트에서 전체 페이지 스크린샷 확인 — 날짜 칩이 줄바꿈되며 정상 배치, 타이포그래피·색상 전부 정상, 레이아웃 깨짐 없음.
- 스크린샷 확인 중 타이틀 문구 오타("이사·이사 날짜 확인" 중복) 하나를 추가로 발견해 "이사·혼례 날짜 확인"으로 수정.

### 7-BB-4. 검증

- `npm run test:unit`(58개)/`npm run test:e2e`(4개)/`tsc --noEmit`/`npm run build` 전부 클린.

---

## 7-BC. 2026-08-19(3차) 귀인지도 v2 — 시각 품질 개선 + 관계 설명 노출 강화

> **상태**: ✅ 구현 + 모바일 실뷰포트 검증 완료.
> **배경**: 배포 후 사용자가 "귀인지도는 안뜨는데"라고 보고 — 조사 결과 실제로는 "정밀 궁합 비교 직후가 아니라 재방문 시에만 뜨는" 설계(7-BB-3)에 대한 발견성/UX 혼동에 가까웠음(사용자의 후속 질문 "정밀궁합이 귀인지도 파트야?"로 미루어 순수 버그는 아니었던 것으로 판단). 이를 계기로 사용자가 "지도가 좀더 퀄리티가 있고 잘 보여지면 좋을것 같아, 그리고 관계도에 추가가 될 때 설명이 좀더 잘 보여지면 좋을것 같은데"라고 요청.

### 7-BC-1. `GwiinMap.tsx` 시각 개선

- 중심("나")·상대 노드 크기 확대, 방사형 그라디언트 글로우(각 노드마다 관계색 기준 `radialGradient`) 추가.
- 노드 등장 시 페이드인+스케일 애니메이션(`gwiinFadeIn`, 노드별 80ms씩 지연 — 순차적으로 나타나는 효과).
- **탭 상호작용 변경**: 기존엔 노드를 탭하면 곧장 재비교(`onNodeClick`)로 넘어가, 관계 설명은 SVG `<title>`(호버 전용 — 모바일에서 사실상 노출 안 됨)에만 있어 확인할 방법이 없었음. 탭 시 먼저 설명 팝업(이모지·이름·관계 라벨·상세 설명, 기존 `.modal-overlay`/`.modal-box` 재사용)이 뜨고, 그 안의 "🔄 다시 비교하기" 버튼을 눌러야 실제 재비교가 실행되도록 변경(사용자 확인: "설명 팝업이 먼저, 그 안에 다시비교 버튼").

### 7-BC-2. "귀인지도에 추가됐어요" 강조 카드 (`App.tsx`)

- 정밀 궁합 비교 결과가 나오는 즉시(같은 세션, 재방문 불필요) 관계 유형을 바로 보여주는 카드를 일주 비교 표 위에 추가 — 관계색 기준 배경/테두리, 이모지, "{상대이름}님은 나에게 "{관계라벨}"", 관계 설명 한 줄. 비교 → 귀인지도 개념을 즉시 연결해 발견성 문제를 근본적으로 완화(재방문해야만 지도가 보이는 기존 구조는 유지하되, "지금 막 지도에 뭔가 추가됐다"는 걸 그 자리에서 알려줌).

### 7-BC-3. 모바일 실뷰포트 검증

- Playwright(iPhone 12) 로 (a) 정밀 궁합 비교 직후 강조 카드 노출 여부, (b) 재방문 후 지도의 확대된 노드·글로우·연결선 렌더링, (c) 노드 탭 → 설명 팝업(단일 "다시 비교하기" 버튼) 노출을 각각 스크린샷으로 확인 — 전부 정상. (풀페이지 스크린샷은 `position:fixed` 모달을 스크롤 위치에 이어붙이는 특성상 육안 확인이 어려워, 뷰포트 단독 스크린샷 + `scrollIntoViewIfNeeded()`로 재확인.)
- `tsc --noEmit`/`npm run test:unit`(58개)/`npm run build` 전부 클린.

---

## 7-BD. 2026-08-20 귀인지도 궁합 점수 추가 + 관계 라벨 게임 아이템 컨셉으로 재작명

> **상태**: ✅ 구현 + 모바일 실뷰포트 검증 완료.
> **배경**: 사용자가 "귀인지도 점수도 표시가 되면 좋을것 같고, 귀인/내편/라이벌/자극제/동료/무난한 사이라고 하는것보다 좀더 재밌게 만들어주면 좋을것 같아"라고 요청. 라벨 톤은 3가지 후보(게임 아이템/유행어/기존유지+부제)를 프리뷰로 제시했고, 사용자가 추천을 요청해 "게임 아이템 컨셉"(지도·노드라는 기존 UI 컨셉과 잘 어울리고, 점수와도 자연스럽게 결합)을 추천해 채택.

### 7-BD-1. `GWIIN_TYPE_META` 라벨 재작명 (`pairCompatibility.ts`)

- 귀인→**치트키**🎁, 내 편→**성장캐**🌱, 라이벌→**라이벌전**⚔️, 자극제→**카페인**☕, 동료→**평행이론**🪞, 무난한 사이→**잔잔케미**💧. 판정 로직(오행 상생상극)과 색상은 그대로 두고 표현만 교체.

### 7-BD-2. 궁합 점수(`getGwiinScore`) 신설

- 일간 관계(`STEM_RELATION_BASE_SCORE`: 상대가 나를 생 88 > 내가 상대를 생 82 > 비화 72 > 무관 65 > 내가 극 60 > 상대가 나를 극 55)를 기본값으로 하고, 일지 관계(`BRANCH_RELATION_SCORE_DELTA`: 육합 +12, 삼합 +8, 파 -7, 해 -6, 형 -14, 충 -18)로 가감해 5~99 범위로 clamp한 정수 점수. 이미 계산돼 있던 두 판정(오행 상생상극 + 합충형파해)을 재사용해 새 계산 로직 없이 숫자 하나로 요약 — "학술적 정밀도"가 아니라 재미용 지표임을 전제로 설계.
- `pairCompatibility.test.ts`에 회귀 테스트 추가: 전체 조합이 5~99 범위 정수인지, "상대가 나를 생함+육합"(최고 조합)이 "상대가 나를 극함+충"(최악 조합)보다 확실히 높은지, 실제 사주 조합(1995-09-27 vs 2000-01-01)으로 계산되는지.

### 7-BD-3. UI 노출 (`GwiinMap.tsx`, `App.tsx`)

- 지도 노드: 각 노드 우상단에 작은 원형 배지로 점수 표시(이름·이모지 레이아웃과 겹치지 않게 위치 재조정).
- 노드 탭 시 뜨는 설명 팝업: 관계 라벨 아래 점수를 알약(pill) 형태로 강조 표시.
- "🌟 귀인지도에 추가됐어요" 카드(비교 직후): 카드 오른쪽에 큰 숫자로 점수 표시.

### 7-BD-4. 모바일 실뷰포트 검증

- Playwright(iPhone 12)로 (a) 비교 직후 강조 카드의 점수, (b) 지도 노드의 점수 배지, (c) 노드 탭 설명 팝업의 점수 알약을 각각 확인 — 새 라벨("평행이론" 등)과 점수(예: 84점) 모두 잘림·겹침 없이 정상 렌더링.
- `tsc --noEmit`/`npm run test:unit`(61개, +3)/`npm run build` 전부 클린.

---

## 7-BE. 2026-08-20(2차) 시그니처 캐릭터 "나풀이" 디자인 착수 — AI 이미지 생성 파이프라인

> **상태**: 🟡 진행 중(코드 변경 없음, 순수 에셋 제작 단계). 상세 프롬프트·리뷰 기록은 별도 파일 `나풀이_캐릭터_디자인_브리프.md`에 있음(이 섹션은 요약만).
> **배경**: 사용자가 가져온 캐릭터 기획서(`나풀이_사주_MBTI_시그니처_캐릭터_기획.md`, 이후 포스터형 연출로 리뉴얼)를 바탕으로, 미드저니로 "나풀이" 도깨비 마스코트를 실제 제작. 코드가 아니라 미드저니 프롬프트 설계·이미지 리뷰·품질 검증이 이번 작업의 본체라, 세션 대부분이 AI 이미지 생성 반복 작업으로 진행됨.

### 7-BE-1. 베이스 캐릭터 확정 + Oref 파이프라인 확립

- 4개 초기 시안 중 3번을 베이스로 확정(도깨비 정체성이 가장 뚜렷함 — 1/4번은 용·고양이 등 다른 트로프로 새어나감).
- 미드저니 `--cref`(구버전)/**Oref**(옴니 레퍼런스, v7~8.2부터 UI화)로 캐릭터 정체성을 고정한 뒤 텍스트로 변형을 주는 워크플로 확립. 시행착오로 배운 것: **참고 이미지가 Images+Oref 두 슬롯에 동시에 걸리면 신호가 섞여 텍스트 지시가 안 먹힘**(로브 색이 하나도 안 바뀌던 원인) — Oref 슬롯 하나만 쓰는 게 핵심.
- 색상처럼 면적이 큰 변경은 Oref 강도를 낮춰야(65~70) 반영되고, 정체성(얼굴·비율) 유지가 중요할 땐 강도를 높여야(85~100) 함 — 트레이드오프 존재.

### 7-BE-2. 완성된 에셋 (2026-08-20 기준)

| 카테고리 | 수량 | 상태 |
|---------|------|------|
| 기본 캐릭터(용도별 3포즈) | 3장 | ✅ 완료 |
| 오행 5종 변형 | 5장(원소당 1장 최종 선택) | ✅ 완료 |
| MBTI 16종 변형(표정·포즈만, 색 유지) | 16장(원소당 아니고 유형당 1장 선택 예정) | ✅ 완료 |
| 귀인지도 궁합 상호작용 6종(`GWIIN_TYPE_META`와 동일 분류 재사용) | 6장 | ✅ 완료 |
| 성장 단계 4종(Lv.1~4) | 4장 | ✅ 완료 |
| 탄생 포스터(오행별 융합 연출, MBTI는 표정으로 이미 구분되니 제외해 80콤보 방지) | 20장(원소당 4, 1장씩 추천 선정) | ✅ 완료 |
| 나풀이의 방(오행별 다이오라마 배경, 캐릭터 없이 빈 공간으로 제작해 재사용성 확보) | 20장(원소당 4) | ✅ 완료 |

### 7-BE-3. 프로덕션 준비 — 배경 제거 + 워터마크 제거

- ImageMagick 없어 Python(3.14.4) + Pillow(12.3.0)로 자체 스크립트 작성.
- 배경 제거: 모서리 색 샘플링 후 flood-fill로 배경 연결 픽셀만 투명화. 128장(오행+MBTI+궁합+성장) 일괄 처리 완료 → `캐릭터_투명배경/`.
- 워터마크 제거: "이미지 전체가 워터마크 있음/없음" 자동 판별은 반짝이 효과와 혼동돼 신뢰 불가로 폐기. 대신 이미지별 연결 요소 분석 → 캐릭터 본체(최대 덩어리)는 항상 보존, 그 외 하단의 비금색 작은 덩어리만 투명화하는 방식으로 128장 처리 → **`캐릭터_최종소스/`(실사용 최종 폴더)**.
- 한계: 워터마크가 캐릭터 그림자와 픽셀 단위로 붙은 극소수 케이스는 완전히는 안 지워짐(실사용엔 거의 안 보이는 수준의 잔여물).
- **탄생 포스터·나풀이의 방(40장)은 배경 자체가 작품의 일부라 배경 제거 대상 아님** — 워터마크만 제거 예정.

### 7-BE-4. 탄생 포스터·나풀이의 방 워터마크 제거 — 자동화 시도했으나 실패, 의도적으로 보류

- 캐릭터 스프라이트에 썼던 "연결 요소 분석" 방식은 투명 배경 전제라 이 40장(불투명 배경)엔 안 맞음. 대안으로 국소 분산(local variance) 기반 탐지를 시도했으나, 에너지 이펙트·반짝이 같은 실제 작품 디테일을 워터마크로 오인해 하얗게 뭉개버리는 오작동 확인(예: 탄생 포스터 화3) — 작품 훼손 위험 커서 폐기.
- 이 40장이 앱에서 정확히 어디·어떤 크기로 쓰일지 아직 안 정해진 상태라 **지금은 워터마크를 그대로 두기로 결정**(작은 카드/썸네일에선 거의 안 보임). 나중에 큰 배너 등 눈에 띄는 용도로 채택될 때만 그 이미지를 개별적으로 수동 처리하는 쪽으로 방침 확정.

### 7-BE-5. 다음 세션에서 이어서 할 일

- [x] 128장 중 MBTI 16종 유형당 1장 최종 선택 완료(2026-08-21) — 상세: `나풀이_캐릭터_디자인_브리프.md` 8-5.
- [ ] 실제 앱 코드에 캐릭터 에셋 연결(예: `GwiinMap.tsx` SVG 노드를 캐릭터 이미지로 교체, 결과 화면 히어로 이미지 등) — 아직 전혀 미착수, 지금까지는 순수 에셋 제작만 진행
- [ ] 탄생 포스터·나풀이의 방 워터마크 — 특정 이미지가 고해상도/큰 화면 용도로 최종 채택될 때 그때그때 수동 처리(위 7-BE-4 참고)

---

## 7-BF. 2026-08-21 캐릭터 에셋 첫 앱 통합 — 귀인지도 노드를 나풀이 캐릭터 이미지로 교체

> **상태**: ✅ 완료(코드 변경 + 검증). 지금까지 만든 캐릭터 에셋(128장, `캐릭터_최종소스/`)이 실제 앱 화면에 처음으로 연결됨.
> **배경**: 캐릭터 제작이 일단락된 후 "어디부터 실제 코드에 연결할지" 3가지 선택지(귀인지도 노드/결과 화면 히어로/오늘의 나풀이 카드) 중 귀인지도 노드를 우선 선택 — 범위가 작고(파일 하나), 이미 있는 궁합 상호작용 6종 일러스트가 `GWIIN_TYPE_META` 6분류와 정확히 1:1로 맞아떨어져 통합이 자연스러움.

### 7-BF-1. 에셋 최적화

- 원본(`캐릭터_최종소스/`)은 1024×1024 PNG, 장당 약 900KB~1MB — 지도의 작은 원형 노드(지름 64px 표시)에 쓰기엔 과함.
- Python + Pillow로 256×256 리사이즈 + WebP 변환(quality 85) → 장당 13~20KB로 축소. `public/gwiin/`에 7장(중심 캐릭터 `na.webp` + 관계 유형 6종) 배치.

### 7-BF-2. 코드 변경

- `pairCompatibility.ts`: `GwiinTypeMeta`에 `image` 필드 추가, `GWIIN_TYPE_META` 6개 항목에 각각 대응 이미지 경로(`/gwiin/*.webp`) 채움.
- `GwiinMap.tsx`: SVG `<circle>`+이모지로 표현하던 노드를 `<image>`(원형 `clipPath` 적용)로 교체. 중심 "나" 노드도 동일 방식으로 실제 캐릭터 이미지 표시. 이름표는 원 안에서 원 아래로 이동(이미지가 원 전체를 채우므로), 지도 캔버스를 살짝 키워(size 320→340, orbitRadius 118→108) 이름표가 잘리지 않게 조정. 노드 탭 시 뜨는 설명 팝업에도 140px 캐릭터 이미지 추가.

### 7-BF-3. 발견·수정한 버그 — 중앙 캐릭터 이미지가 안 보이던 clip-path 좌표 버그

- 상대방 노드는 `<g transform="translate(x,y)">`로 감싸져 있어 `clipPath`(`userSpaceOnUse`, 기본값)의 로컬 원점이 노드 위치와 일치했지만, 중심 "나" 이미지는 그룹 트랜스폼 없이 절대좌표로만 배치돼 있어서 clip 원(로컬 (0,0) 기준)이 캔버스 좌상단 구석에 그려짐 — 실제 이미지 위치와 전혀 안 겹쳐서 통째로 잘려 안 보였음.
- 중심 이미지도 `<g transform="translate(center, center)">`로 감싸 상대방 노드와 동일한 좌표 구조로 통일해 해결. 모바일 스크린샷으로 수정 전/후 직접 비교 확인.

### 7-BF-4. 검증

- `tsc --noEmit`/`npm run test:unit`(61개)/`npm run build` 전부 클린.
- Playwright(iPhone 12)로 재방문 후 귀인지도 확인 — 중심 캐릭터·상대방 노드(관계 일러스트)·점수 배지·이름표 전부 잘림 없이 정상 렌더링, 노드 탭 시 설명 팝업의 캐릭터 이미지도 정상 표시.

---

## 7-BG. 2026-08-21(2차) 결과 화면에 탄생 포스터 히어로 이미지 추가

> **상태**: ✅ 완료(코드 변경 + 검증). 두 번째 캐릭터 에셋 통합 지점.
> **배경**: 귀인지도 다음으로 "결과 화면 히어로 이미지"를 선택 — 탄생 포스터(섹션 11) 5장이 정확히 이 용도로 만들어둔 에셋이라 자연스러운 다음 단계.

### 7-BG-1. 에셋 최적화

- `캐릭터/mbti/탄생/`의 오행별 추천 픽(화1·목4·토4·금3·수1, 928×1232 JPEG 원본)을 폭 700px로 리사이즈 + WebP 변환(quality 82) → 장당 57~77KB. `public/gwiin/poster/{wood,fire,earth,metal,water}.webp`로 배치(파일명이 `ELEMENT_LABELS`/`dayStemElement` 키와 그대로 일치하도록 맞춤 — 별도 매핑 테이블 불필요).

### 7-BG-2. 코드 변경

- `App.tsx`: 결과 화면(`step === 'result'`) 최상단, 기존 프로필 배너 바로 위에 `result.sajuResult.dayStemElement`에 맞는 포스터 이미지 + 캡션("🔮 {오행} 기운의 나풀이가 찾아왔어요") 삽입. 둥근 모서리 + 그림자로 카드형 UI 톤에 맞춤.

### 7-BG-3. 검증

- `tsc --noEmit`/`npm run test:unit`(61개)/`npm run build` 전부 클린.
- Playwright(iPhone 12)로 결과 화면 진입 직후 스크린샷 확인 — 테스트 계정(일간 금)의 탄생 포스터가 정확히 표시되고, 캡션·프로필 배너·이후 콘텐츠 모두 잘림/겹침 없이 정상 이어짐.

---

## 7-BH. 2026-08-21(3차) 오늘의 나풀이 카드에 캐릭터 아바타 추가 — 3개 통합 지점 완료

> **상태**: ✅ 완료(코드 변경 + 검증). 캐릭터 에셋 통합 3단계(귀인지도/결과 히어로/오늘의 나풀이) 전부 마무리.
> **배경**: 마지막 통합 지점. 기획서 17장 "캐릭터가 결과 페이지에만 있는 게 아니라 앱의 안내자 역할을 함"을 반영 — 결과 화면 히어로(7-BG)에서 본 것과 **같은 오행 캐릭터**를 여기서도 재사용해 "같은 나풀이가 매일 말을 걸어준다"는 일관성을 줌.

### 7-BH-1. 에셋 최적화

- 오행 최종 픽(목4·화4·토4·금4·수1, `캐릭터_최종소스/`의 배경 제거+워터마크 제거 완료본)을 220×220 WebP로 축소(장당 10~16KB) → `public/gwiin/element/{wood,fire,earth,metal,water}.webp`.

### 7-BH-2. 코드 변경

- `App.tsx`: "오늘" 탭의 "오늘의 나풀이" 카드(`glass-card-gold`) 타이틀 왼쪽에 `result.sajuResult.dayStemElement` 기준 캐릭터 아바타(40px 원형, 골드 테두리) 추가.

### 7-BH-3. 검증

- `tsc --noEmit`/`npm run test:unit`(61개)/`npm run test:e2e`(4개)/`npm run build` 전부 클린.
- Playwright(iPhone 12)로 "오늘" 탭 진입 후 카드 확인 — 아바타가 타이틀 옆에 잘림·겹침 없이 정상 표시.

### 7-BH-4. 캐릭터 에셋 통합 현황 정리 (7-BI/7-BJ 반영 후 최신화)

| 통합 지점 | 상태 | 사용 에셋 |
|-----------|------|----------|
| 귀인지도 노드(`GwiinMap.tsx`) | ✅ | 궁합 상호작용 6종 |
| 결과 화면 히어로(`App.tsx` 결과 최상단) | ✅ | 탄생 포스터 5종(오행별) |
| 오늘의 나풀이 카드(`App.tsx` 오늘 탭) | ✅ | 오행 캐릭터 5종 |
| 배지 컬렉션(`App.tsx` 다이어리 "내 배지") | ✅ | 성장 4단계 |
| SNS 공유 이미지 카드(`handleDownloadImageCard`) | ✅ | MBTI 16종 |
| 나풀이의 방 | ⬜ 미착수 | 새 화면/탭 신설이 필요해 별도 기획 필요 |

---

## 7-BI. 2026-08-21(4차) 배지 컬렉션에 성장 단계 캐릭터 연결

> **상태**: ✅ 완료. 기존 "연속 방문 스트릭" 시스템(5티어: 새싹3일·불꽃7일·별빛14일·나풀이마스터30일·다이아몬드100일)에 성장 4단계 캐릭터를 매핑 — 새 시스템을 만들지 않고 기존 습관화 기능을 캐릭터로 강화.

### 7-BI-1. 매핑

5티어 vs 4단계라 상위 2티어(나풀이마스터·다이아몬드)는 도사(Lv.4) 이미지를 공유: 새싹→Lv.1(아기), 불꽃→Lv.2(새싹1), 별빛→Lv.3(빛나1, "별빛"↔"빛나는" 네이밍이 우연히 잘 맞음), 나풀이마스터/다이아몬드→Lv.4(도사1).

### 7-BI-2. 코드 변경

- `streak.ts`: `StreakTier`에 `growthImage` 필드 추가, 5개 티어에 각각 매핑.
- `App.tsx`: "내 배지" 컬렉션 그리드에서 획득한 티어는 기존 이모지 대신 56px 성장 캐릭터 이미지로 표시(미획득 티어는 기존 🔒 유지).
- 이미지: 220×220 WebP(장당 9~17KB), `public/gwiin/growth/lv1~4.webp`.

### 7-BI-3. 검증

- `tsc --noEmit`/`npm run build` 클린. Playwright로 `localStorage`에 티어 3개(새싹·불꽃·별빛) 획득 상태를 시뮬레이션해 배지 컬렉션 카드에 캐릭터 이미지가 정상 표시되는지 확인.

---

## 7-BJ. 2026-08-21(5차) SNS 공유 이미지 카드에 MBTI 캐릭터 연결

> **상태**: ✅ 완료(그리기 로직 검증까지, 실제 다운로드 흐름은 로컬 환경 한계로 미검증).

### 7-BJ-1. 코드 변경

- `App.tsx`의 `handleDownloadImageCard`(메인 프로필 공유 카드, 캔버스로 직접 그림) 안에서 기존 "MBTI 이모지 원형 배지"를 사용자 MBTI에 맞는 나풀이 캐릭터 이미지로 교체.
- 기존 `loadCanvasImage()` 헬퍼(오늘의 타로 카드에서 이미 쓰던 패턴) 재사용 — 이미지 로드 실패 시 기존 이모지 배지로 조용히 폴백.
- MBTI 최종 픽 16장을 260×260 WebP로 최적화(장당 11~44KB) → `public/gwiin/mbti/{mbti}.webp`.

### 7-BJ-2. 검증 — 로컬 환경 한계

- 이 카드는 `result.aiIntro`(AI 생성 텍스트)가 있어야 버튼이 나타나는데, 로컬 프리뷰 환경엔 Gemini API 키가 없어 `aiIntro`가 안 채워짐 → 실제 다운로드 흐름 전체는 이 세션에서 못 돌려봄(기존에도 있던 환경 제약, 이번에 새로 생긴 문제 아님).
- 대신 **동일한 원형 클리핑+drawImage 로직만 별도 HTML 캔버스로 분리 검증** — 실제 코드와 같은 좌표·반지름 값으로 ENTP 캐릭터 이미지를 그려봐서 원형 배지 안에 정확히 잘리는지 확인 완료. 나머지(카드 전체 레이아웃, 텍스트 배치 등)는 기존 코드 그대로라 변경 없음.
- `tsc --noEmit`/`npm run build` 클린.

---

## 7-BK. 2026-08-21(6차) "나풀이의 방" 신규 화면 — 캐릭터 에셋 통합 마지막 단계

> **상태**: ✅ 완료(코드 변경 + 검증). Plan Mode로 범위를 먼저 정하고 승인받은 뒤 구현(계획 파일: `distributed-wishing-piglet.md`).
> **배경**: 기획서 18장 "나풀이의 방"이 유일하게 새 화면이 필요한 통합 지점이었음. 원안의 7개 구성요소(나의 나풀이·오행 아이템·MBTI 아이템·오늘의 운세·획득 배지·궁합 캐릭터·분석 기록)를 전부 구현하는 대신, **새 상태/계산을 전혀 추가하지 않고 이미 앱에 있는 값들을 한 화면에 모아 보여주는 정적 요약 화면**으로 범위를 좁힘 — 이번 세션 내내 써온 "최소 버전부터" 원칙의 연장.

### 7-BK-1. 설계

- 배경 오행 → `result.sajuResult.dayStemElement`(히어로 포스터·오늘의 나풀이 아바타와 동일 소스), 캐릭터 → 이미 배치한 `/gwiin/element/*.webp` 재사용, 통계 4종(오늘의 기운·획득 배지·다이어리 기록·귀인지도 상대 수) → `dailyFortuneData`/`earnedTiers`+`STREAK_TIERS`/`bookmarks`/`pairCompatHistory` 전부 기존 상태 재사용. 새 Firestore/localStorage 스키마 없음.
- 아이템 드래그앤드롭 배치, 궁합 캐릭터 개별 카드화는 이번 범위에서 제외(숫자 요약만).

### 7-BK-2. 코드 변경

- `appTypes.ts`: `Step` 유니언에 `'room'` 추가.
- `App.tsx`: "오늘" 탭 최상단(오늘의 나풀이 카드보다 먼저)에 방 미리보기 썸네일 + 진입 카드 추가. 헤더에는 진입점을 안 둠 — 이미 버튼 3개(가이드/다이어리/다시하기)로 375px 모바일 화면이 꽉 차 있어 4번째를 추가하면 줄바꿈 위험이 있었음.
- `step === 'room'` 블록 신설(`step === 'bookmarks'`와 동일한 헤더+"돌아가기" 구조) — 방 배경 이미지 위에 캐릭터를 절대 위치로 겹쳐 배치, 아래 2×2 통계 카드 그리드.
- 방 배경 오행별 추천 1장씩을 700×700 WebP로 최적화(장당 42~62KB) → `public/gwiin/room/{element}.webp`.

### 7-BK-3. 검증

- `tsc --noEmit`/`npm run test:unit`(61개)/`npm run build` 클린.
- Playwright(iPhone 12)로 결과 화면 → "오늘" 탭 → 진입 카드 클릭 → 방 화면(배경·캐릭터·통계 4종 모두 정상 표시, 신규 사용자 기준 0 값들도 깨짐 없이 표시) → "돌아가기"로 결과 화면 복귀까지 전체 흐름 확인.
- `npm run test:e2e` 1차 실행에서 2개 실패했으나(방금 종료한 프리뷰 서버 포트와 겹친 것으로 추정되는 일시적 플레이키 현상), 재실행 및 개별 실행에서 4개 전부 통과 — 실제 회귀 아님으로 확인.

### 7-BK-4. 캐릭터 에셋 통합 — 전체 완료

이걸로 기획서에서 계획했던 캐릭터 에셋 활용 지점 6곳(귀인지도·결과 히어로·오늘의 나풀이·배지 컬렉션·SNS 공유 카드·나풀이의 방) 전부 앱에 연결 완료. 아직 push는 안 됨(사용자 요청으로 "작업 다 마무리하고 일괄 배포" 대기 중).

---

## 7-BL. 2026-08-21(7차) 앱 전체 톤 재검토 — 캐릭터 스타일에 맞춘 UI 요소 교체

> **상태**: ✅ 완료(코드 변경 + 검증). "질문을 1개씩" 방식으로 범위를 좁혀가며 진행(사용자 요청).
> **배경**: 캐릭터 에셋 통합이 끝난 뒤, 기존 UI(짙은 네이비+골드 "정통 계산기" 톤)와 새로 들어온 캐릭터 비주얼이 어울리는지 재검토 요청. Q&A로 범위를 단계적으로 확정: ① 앱 전체 톤 재검토(캐릭터 관련 화면만이 아니라) → ② 색상/배경은 유지, UI "요소"만 캐릭터 스타일로 → ③ 자주 보이는 것부터(이모지 아이콘류 최우선, 파비콘은 앱스토어 규격 문제로 다음으로 미룸).

### 7-BL-1. 새 이미지 없이 처리한 것

- **헤더 로고**: 추상적 SVG 마크(`NapuliMark`, 크리스탈볼+별)를 확정 캐릭터 이미지(`/gwiin/na.webp`)로 교체.
- **로딩 화면**: `.orb-center`의 "🌌" 이모지를 캐릭터 이미지로 교체(회전하는 링 애니메이션은 그대로 유지).
- **카드/버튼 모서리**: `.glass-card`/`.glass-card-gold` 24px→28px, `.btn-primary` 16px→20px, `.btn-secondary` 12px→16px — 캐릭터 일러스트의 둥근 느낌에 맞춰 전반적으로 더 라운드하게.

### 7-BL-2. 새 이미지 제작 — 탭 아이콘 7종

- **1차 시도 실패 → 원인 파악**: Style Reference에 기존 캐릭터 이미지를 걸고 "크리스탈볼 아이콘(캐릭터 없이 사물만)"을 요청했더니, Style Reference가 캐릭터의 "귀여운 얼굴" 특징까지 끌고 와서 눈코입 있는 미니 캐릭터가 나옴. Style Reference에는 강도 조절 슬라이더가 없다는 것도 이번에 확인.
- **해결**: Style Reference를 아예 빼고, 프롬프트 텍스트만으로 그림체(굵은 선·셀셰이딩·색상)를 지정 + "no face, no eyes, not a creature" 등 강한 네거티브 프롬프트 → 얼굴 없는 깔끔한 사물 아이콘으로 성공.
- **메인 탭 3종**(`public/gwiin/tabicon/`): 사주=크리스탈볼(`saju.webp`), 오늘=해돋이(`today.webp`, 기존 "🌅 오늘의 나풀이"와 모티프 통일), 별자리=망원경+별자리(`astrology.webp`, 기존 🪐 대신 — 신비로운 톤과 더 잘 맞음).
- **서브 탭 4종**(사주 대분류 안): 운세=금빛 화살선이 그려진 두루마리(`unse.webp`), 해석=별이 반짝이는 돋보기(`interpret.webp`), 궁합=붉은 실로 이어진 두 하트(`compat.webp`, 인연의 실 모티프), 풍수=한옥(`fengshui.webp`).
- 전부 160×160 WebP로 배경 제거 후 최적화(장당 4~7KB).

### 7-BL-3. 방 꾸미기 캐러셀 — 하우징 모드 이전 단계

- 사용자가 나풀이의 방을 보다가 "하우징 모드(가구 배치)도 가능한가" 질문 → 지금 에셋(배경+가구가 한 장에 합쳐진 완성 그림)으로는 개별 배치가 불가능함을 설명하고, 대신 **이미 오행당 4장씩 만들어둔 방 중 원하는 걸 고르는 캐러셀**을 절충안으로 제안해 채택.
- 오행 5종 × 4배리언트를 전부 700×700 WebP로 최적화(`public/gwiin/room/{element}-{1~4}.webp`, 기존 오행당 1장 파일명에서 인덱스 방식으로 전환).
- `App.tsx`에 `roomVariant` 상태 추가, 사람별로 선택 저장(`appHelpers.ts`에 `roomVariantCacheKey` 신설 — 기존 `baseKeyId` 컨벤션 재사용, 향후 진짜 하우징 모드로 갈 때도 이 저장 패턴을 그대로 쓸 수 있도록 미리 맞춤). 방 화면에 ‹ › 버튼 + 점 4개 인디케이터 추가.

### 7-BL-4. 검증

- `tsc --noEmit`/`npm run test:unit`(61개)/`npm run build` 클린.
- Playwright(iPhone 12)로 헤더 로고·로딩 화면·카드 모서리·방 캐러셀(1→2 전환)·메인 탭 3종·서브 탭 4종 전부 스크린샷 확인, 정상 렌더링.
- `npm run test:e2e`에서 1개 실패 발견 — **실제 회귀**: 탭 라벨에서 이모지를 빼고 이미지로 바꿨는데, 테스트가 여전히 이모지 포함 텍스트(`/🔮 사주/` 등)로 탭을 찾고 있었음. `e2e/golden-path.spec.ts`의 셀렉터를 이모지 없는 새 라벨로 갱신해 해결, 재실행 후 4개 전부 통과.

---

## 7-BM. 2026-08-21(8차) 나풀이의 방 진입 방식 변경 — "오늘" 탭 카드 → 프로필 배너 아이콘 버튼 + 팝업

> **상태**: ✅ 완료(코드 변경 + 검증). "질문을 1개씩" 방식으로 진행(사용자 요청).
> **배경**: 사용자가 "오늘 탭에 있으면 발견성이 낮을 것 같다"고 지적 — 실제로 확인해보니 결과 화면의 **기본 활성 탭이 "사주"**(`activeSection` 초기값 `'saju'`, `'today'` 아님)라, 나풀이의 방 진입 카드가 "오늘" 탭 안에 있으면 사용자가 일부러 탭을 옮기지 않는 한 아예 안 보이는 실제 문제였음.

### 7-BM-1. Q&A로 확정한 방향

① 위치: 프로필 배너 근처(탭 선택과 무관하게 항상 보임, 헤더는 이미 버튼 3개로 꽉 차 있어 제외) ② 형태: 작은 아이콘 버튼(방 썸네일 이미지, 텍스트 라벨 없음) ③ 진입 방식: 팝업(모달) — 별도 화면 이동 대신 현재 화면 위에 오버레이로 즉시 표시.

### 7-BM-2. 코드 변경

- `appTypes.ts`: 더 이상 안 쓰는 `Step`의 `'room'` 값 제거.
- `App.tsx`: `showRoomModal` 상태 신설. "오늘" 탭의 기존 진입 카드는 삭제(중복 진입점 정리). 프로필 배너의 이름 이니셜 아바타 옆에 56px 아이콘 버튼(오행+선택된 방 배리언트 썸네일) 추가 — 클릭 시 `setShowRoomModal(true)`.
- 기존 `step === 'room'` 전체 화면 블록을 기존 `.modal-overlay`/`.modal-box`(GwiinMap 팝업 등에서 이미 쓰던 패턴) 기반 모달로 전환 — 내부 콘텐츠(방 배경+캐릭터+캐러셀+통계 4종)는 그대로 재사용, "← 돌아가기" 버튼만 `.modal-close` "✕" 버튼으로 교체.

### 7-BM-3. 검증

- `tsc --noEmit`/`npm run test:unit`(61개)/`npm run test:e2e`(4개)/`npm run build` 전부 클린.
- Playwright(iPhone 12)로 결과 화면 진입 직후(기본 탭인 "사주"에서, 탭 이동 없이) 프로필 배너 버튼이 바로 보이는지 확인 — 확인됨. 버튼 클릭 → 모달 정상 오픈(배경·캐릭터·캐러셀·통계 전부 스크롤 가능한 모달 안에 깨짐 없이 표시) 확인.

---

## 7-BN. 2026-08-21(9차) 격국·신살 8종·십신 분포·지장간 추가 — 경쟁 사주 계산기(사주도령 등) 대비 격차 해소

> **상태**: ✅ 완료(코드 변경 + 검증). "일단 제대로 잡고 가자"는 사용자 요청으로 Plan Mode 승인 후 진행.
> **배경**: 경쟁 분석 결과 나풀이엔 없지만 흔히 제공되는 전통 명리학 심화 요소 4가지를 추가 요청받음. 조사 결과 **십신은 이미 계산 로직이 있었으나(`sajuCalculator.ts`, AI 프롬프트용 내부 참고자료로만 사용) 화면 표시 UI가 없었고**, 나머지 3개(지장간·신살·격국)는 계산 로직 자체가 없었음.

### 7-BN-1. 신규 데이터 모듈 3개

- `src/data/jijanggan.ts`: 12지지 → 여기(餘氣)/중기(中氣)/정기(正氣) 고정 테이블(명리학 표준, 이견 없음). 자/묘/유는 중기 없이 2단계.
- `src/data/sinsal.ts`: 신살 8종(도화살·역마살·화개살·양인살·괴강살·백호살·원진살·귀문관살) 판별 로직. **신뢰도 참고**: 도화/역마/화개(삼합 기반 고전 공식)와 양인살은 문헌 간 이견이 거의 없는 표준. 반면 **괴강살 정확한 4개 60갑자, 백호살 정확한 7개 60갑자, 원진살/귀문관살의 정확한 지지 쌍은 유파·문헌마다 미세한 차이가 있음** — 가장 흔히 인용되는 표준 조합을 채택(절기 재검증 때처럼 1차 사료 대조는 어려운 영역).
- `src/data/gyeokguk.ts`: 월지 기준 내격 8정격(식신격·상관격·편재격·정재격·편관격·정관격·편인격·정인격) + 특수 내격 2개(건록격·양인격). **범위를 의도적으로 좁힘**: 일간 강약을 별도로 따져야 하는 "외격"(종격·화격 등, 유파마다 이견이 큰 고급 판별법)은 제외.

### 7-BN-2. 계산 로직 (`sajuCalculator.ts`)

- `calcGyeokguk()`: 월지가 왕지(자오묘유)면 정기를 그대로 격의 기준으로, 생지/고지(인신사해·진술축미)면 지장간 중 실제 투출한 천간이 있는지 확인 후 없으면 정기를 기본값으로 사용. 기준 천간을 일간과 비교해 기존 `resolveSipsin()`으로 십신 판별 → 격 이름 매핑.
- `SajuResult`에 `sinsal: SinsalType[]`, `gyeokguk: GyeokgukResult` 필드 추가.
- 순환 참조 회피: `gyeokguk.ts`가 `sajuCalculator.ts`의 `SipsinType`을 쓰는데 반대로 `sajuCalculator.ts`도 `gyeokguk.ts`의 값을 쓰는 구조라 `import type`으로 완전 소거해 런타임 순환을 끊음.

### 7-BN-3. UI 반영 (`App.tsx`)

- **십신 분포**: "운세" 서브탭, 기존 "오행 분포" 카드 바로 아래에 5열 그리드(10종 × 개수) + 보유한 십신만 필터링한 한 줄 설명.
- **신살**: 같은 위치에 배지 리스트(보유한 것만, 없으면 "해당 없음" 문구).
- **지장간**: 기둥 클릭 시 뜨는 기존 팝업(`pillarModal`)에 여기/중기/정기 3단계 표시 추가.
- **격국**: 사주원국 그리드 카드의 "일간 설명" 박스 바로 아래에 "당신은 건록격입니다" 식 헤드라인 + 한 줄 설명 — MBTI 유형처럼 정체성 급으로 다룸.

### 7-BN-4. 검증

- `tsc --noEmit`/`npm run test:unit`(67개, 신규 6개 포함)/`npm run test:e2e`(4개)/`npm run build` 전부 클린.
- `sajuCalculator.test.ts`에 회귀 테스트 6개 추가 — 실측 기준 케이스(1995-09-27 오시 여성)로 격국(건록격), 신살(도화살·역마살), 십신 분포 합계(7), 지장간(酉=경/신, 子=임/계, 辰=을/계/무) 검증. 손으로 직접 원리 대조(월지 酉는 왕지 → 지장간 정기가 일간 辛과 동일 → 비견 → 건록격; 일지 酉가 속한 사유축 그룹 기준 도화=午·역마=亥가 사주 안에 있어 해당)해 계산 정확성 확인.
- Playwright(iPhone 12)로 사주원국 카드(격국 헤드라인), 기둥 클릭 팝업(지장간), 운세 탭(십신 분포·신살) 3곳 모두 잘림·깨짐 없이 정상 렌더링 확인.
- 외부 만세력 사이트와의 교차 검증은 진행하지 않음(범위 밖 표준 문헌 데이터 + 코드 내 신뢰도 주석으로 대체) — 위 7-BN-1의 신뢰도 참고 사항 재확인.

---

## 8. 알려진 이슈 / 향후 과제 (검증됨, 우선순위 순)

| 우선순위 | 항목 | 설명 |
|---------|------|------|
| 🟡 | 온보딩 화면 히어로에 캐릭터 이미지 미적용 (2026-08-21) | `step === 'input'` 히어로에는 `/gwiin/na.webp` 캐릭터 이미지를 이미 추가했으나(섹션 7-BG 계열), `step === 'onboarding'` 히어로는 아직 예전 `<NapuliMark size={56} />` 아이콘 그대로임. `App.tsx`에서 온보딩 히어로 블록을 찾아 input 화면과 동일한 패턴(`<img src="/gwiin/na.webp" ... />`)으로 교체하면 됨 — 코드 변경만 필요, 리서치는 끝난 상태. |
| 🟡 | 24종 "닮은 인물" 아키타입 초상화 — 프롬프트 전달 완료, 생성/리뷰 대기 (2026-08-21) | "OO 님과 닮은 인물 → 이순신" 같은 카드(`ARCHETYPE_FIGURES`, `src/data/archetypeFigures.ts`)에 쓸 24개 인물 초상화용 Midjourney 프롬프트는 이미 작성해 전달함. 사용자가 이미지를 생성해 공유하면, 그다음 기존 캐릭터 에셋 파이프라인과 동일하게 리뷰 → 배경/워터마크 정리(Python/Pillow) → WebP 최적화 → 앱 통합(4단계)을 진행하면 됨 — 아직 사용자가 결과물을 만들지 않아 착수 전. |
| 🟢 | 궁합 조합표 상세화 | 관계(합/충/형/파/해) 태그는 표시되나, 관계별 구체적 설명 텍스트는 이제 "궁합 종합 AI 해설"(섹션 7-B-2)로 보완됨 — 정적 텍스트 추가 여지는 여전히 있으나 우선순위 낮음 |
| 🟡 | npm audit 취약점 9건(모더레이트 8+1) + 하이 1건 (2026-08-09 검토, 의식적 보류) | 전부 `npm audit fix --force`로만 고쳐지는데, 모더레이트 8건은 `@capacitor/cli` 다운그레이드(빌드 도구 회귀 위험), 나머지 2건(모더레이트1+하이1)은 vite 개발서버 전용(프로덕션 빌드 무관) + vite@5→8 메이저 업그레이드가 유일한 수정이라 보류. 근거 상세: 섹션 7-AS-3. |
| 🟢 | 1948~1951·1955~1960년 서머타임 날짜의 출처 신뢰도 (2026-08-18 구현, 근거는 2차 출처) | 시행 자체는 국가기록원(정부 공식)으로 확인되나, 연도별 정확한 시작/종료일은 나무위키 표가 유일한 출처라 대통령령 원문 같은 1차 사료 대조는 못 함. 실사용 중 특정 날짜가 이상해 보이면 이 표부터 의심할 것 — 상세: 섹션 7-AV-4. |
| 🔴 | 심화해석 6종 크레딧제 — 2단계(외부 콘솔 설정) + 킬스위치 미완료라 실결제 불가 (2026-08-18 1단계 코드 구현 완료, 킬스위치는 의도적으로 꺼둔 상태) | RevenueCat/App Store Connect/Google Play Console/토스페이먼츠 계정·상품·API 키 등록과 Firestore 보안 규칙 반영이 전부 사용자가 직접 해야 하는 외부 작업. 또한 네이티브 앱 출시 전까지는 `CREDIT_GATE_ENABLED` 킬스위치를 의도적으로 꺼둬(섹션 7-AW-8) 지금 배포해도 기존처럼 완전 무료로 동작 — 위 외부 설정이 끝나고 앱 출시 시점이 되면 이 스위치를 켜는 게 마지막 단계. 할 일 목록: 섹션 7-AW-5. |
| 🟢 | 심화해석 크레딧제 2단계(구독 등) 전환 판단 기준 미정의 (2026-08-18) | "①(크레딧제)로 먼저 출시 후 데이터 보고 2단계 검토"로 결정했으나, 어떤 실사용 지표(예: 크레딧 소진 사용자 비율 등)가 얼마나 나오면 2단계 착수를 판단할지 기준을 아직 안 정함 — 기준 없이는 "데이터를 더 보자"는 판단이 계속 미뤄질 수 있음(1단계 논의 중 직접 지적된 리스크). |

### 해결됨 (2026-08-18)

| 항목 | 해결 내용 |
|------|----------|
| 🔴 `solarTerms.ts`(1940~2030) 60분 이상 오차 899건(82%), 최대 30.5시간 오차 (기존엔 "검증된 데이터"로 알려져 있었으나 실제로는 미발견 상태였음) | astronomy-engine(`SearchSunLongitude`)으로 재구현한 계산이 KASI 공식 발표치와 초 단위로 일치함을 확인한 뒤, 이 계산으로 `scripts/generateSolarTerms.ts`를 새로 만들어 `SOLAR_TERMS` 테이블을 1900~2100년(201개년)으로 전면 재생성. 상세: 섹션 7-AV-1, 7-AV-2. |
| 🟡 1930~1939년 절기 데이터 부재 | 위 재생성으로 1900년부터 정밀 데이터 확보 — 근사치 경고 문구(App.tsx 2곳) 제거, 입력 범위 1900~2100으로 확장. 상세: 섹션 7-AV-2. |
| 🟡 2031년 이후 절기 데이터 부재 | 위와 동일 재생성으로 2100년까지 정밀 데이터 확보. 상세: 섹션 7-AV-2. |
| 🟡 1954~1961년 UTC+8:30 표준시 미반영 (재검토 중 신규 발견) | 그 기간 한국이 실제로 UTC+8:30을 썼다는 사실을 확인(대통령령 제876호 등), `calculateSaju()`에 해당 기간 출생자 입력 시:분에 +30분 보정을 적용하도록 수정. 상세: 섹션 7-AV-3. |
| 🟡 서머타임(1948~1951·1955~1960·1987~1988년) 사주 계산 미반영 (1987~88년은 별자리 기능엔 이미 있었으나 사주엔 없었음) | `HISTORICAL_DST_PERIODS` 12개 구간 + UTC+8:30 시기와 겹치는 여름(1955~1960)은 -30분, 그 외 서머타임은 -60분으로 정확히 분기하는 `historicalMinuteCorrection()` 구현. 상세: 섹션 7-AV-4. |
| 🟢 소셜 그래프 기능(귀인지도류) 부재 | 오행 상생상극 이론(`StemRelationType` 6종)은 이미 구현돼 있던 걸 확인, 게임화 라벨(`GWIIN_TYPE_META`)과 방사형 시각화(`GwiinMap.tsx`)만 새로 만들어 "정밀 궁합" 탭의 상대 이력을 귀인지도로 표시. 상세: 섹션 7-AZ. |
| 🟡 온보딩 무료→유료 브릿지 부재 | "오늘의 나풀이"(매일 자동 생성, 재방문 잦음) 결과 하단에 심화해석(성격)으로 이동하는 CTA 카드 추가. 크레딧제 킬스위치와 무관하게 항상 노출(가격 언급 없는 순수 발견 유도 문구). 상세: 섹션 7-AY. |

### 8-1. 추천 기능 아이디어 — ✅ 2026-08-06(4차)에 사용자 요청으로 전부 구현 완료

관찰된 구현 격차에 근거한 추천이었음(우선순위 낮음, 사용자 요청 시 착수 조건이었고 실제로 요청받아 착수):

- ~~정밀 궁합의 "이전에 비교한 상대" 이력 UI 부재~~ → 구현 완료 (섹션 7-AA-1)
- ~~매일 알림 시간이 오전 9시 고정~~ → 구현 완료 (섹션 7-AA-2)
- ~~"오늘의 OO" 3종 실패 시 재시도 버튼 부재~~ → 구현 완료 (섹션 7-AA-3)

### 8-2. "오늘" 탭 매력도 부족 피드백 — 원인 3축 (2026-08-05)

> 사용자가 "오늘" 탭 3개(나풀이/타로/트랜짓)를 다 클릭해봐도 지루하고 비슷한 느낌이라고 피드백. "안 끌린다"는 느낌이 아래 3가지 축 **전부**에 해당한다고 확인함(질문으로 좁혀본 결과):

1. ~~**콘텐츠 깊이**: `analysis`+`factBomb` 2단 JSON 구조 자체가 나풀이·트랜짓 둘 다 동일~~ → ✅ 2026-08-07에 구조 자체를 다르게 바꿔 해소 완료 — 상세: 섹션 7-AE-3.
2. ~~**비주얼/디자인 톤**: 타로 카드가 실제 카드 이미지 없이 이모지 1개 + 텍스트로만 표시됨~~ → ✅ 2026-08-06(4차)에 CSS/그라디언트 카드 프레임으로 구현 완료 (섹션 7-AA-4). 실제 타로 카드 사진/삽화(78장)는 이미지 자산 별도 확보가 필요해 이번 범위에서는 보류(사용자가 CSS 방식을 선택).
3. ~~**습관화 요소**: 연속 방문일수(스트릭) 표시나 뱃지 없음~~ → ✅ 2026-08-06(4차)에 스트릭 표시 구현 완료 (섹션 7-AA-5). "주간 요약" 리포트는 사용자가 이번 범위에서 제외하기로 함.

**1~3번 전부 구현 완료.**

### 해결됨 (2026-08-01)

| 항목 | 해결 내용 |
|------|----------|
| 🔴 잘못된 날짜 입력 시 로딩 화면 무한 멈춤 | 로딩 `useEffect`에서 `calculateSaju()` 호출을 try/catch로 감싸, 예외 발생 시 에러 토스트를 띄우고 입력 화면(`step: 'input'`)으로 복귀하도록 수정. Playwright로 월=13 입력 시 무한 멈춤 없이 정상 복귀됨을 확인. |
| 🔴 대운 순행/역행이 성별과 무관하게 항상 "여성" 기준으로 계산됨 (만세력 로직 재검토로 신규 발견) | `calculateSaju()`는 `gender` 매개변수(기본값 `'female'`)를 받아 대운 순행/역행 판별에 쓰지만, 유일한 호출부인 `App.tsx`의 로딩 `useEffect`가 `calculateSaju(year, month, day, formData.birthBranch)`처럼 `gender` 인자를 넘기지 않아 실제 선택과 무관하게 항상 `'female'`로 계산되고 있었음. 대운 순행/역행 공식(양남음녀 순행·음남양녀 역행) 자체는 정확했으나, 이 결함으로 남성 사용자 전원의 대운 순서가 실제와 반대로 나오고 있었음(대운표 UI·대운세운 AI 해설·PDF 리포트 전체에 전파). `App.tsx`에서 `calculateSaju(year, month, day, formData.birthBranch, formData.gender)`로 다섯 번째 인자를 추가해 수정. Playwright로 동일 생년월일(1995-09-27, 을해년)에 대해 남성 선택 시 역행(갑신→계미→임오…), 여성 선택 시 순행(병술→정해→무자…)으로 서로 다르게 나오는 것을 확인해 수정을 검증함. |
| 🟡 카카오 공유 SDK 로드 대기 로직의 잠재적 재발 위험 | `(existing as any).readyState`(레거시 IE 전용 속성, 항상 미실행) 죽은 분기를 제거하고, `load`/`error` 이벤트 리스너와 별개로 100ms 간격 폴링(최대 5초)을 백업으로 추가해 이벤트가 이미 지나간 경우에도 무한 대기하지 않도록 수정. |
| 🟢 카카오 공유 디버그 로그 잔재 | `console.log('KAKAO_APP_KEY=', ...)` 제거. |

> [!NOTE]
> 2026-08-01 세션에서 "계획안 토대로 전부 작업 시작" 지시에 따라 섹션 7/7-A/7-B/7-C에 문서화되어 있던 사양을 모두 구현하고, 이 표에 있던 🔴/🟡/🟢 이슈 3건도 함께 해결. 나머지 항목은 우선순위가 낮아 보류.

### 해결됨 (2026-08-03)

| 항목 | 해결 내용 |
|------|----------|
| 🔴 PDF 생성 시 저장형 XSS 취약점 | `escapeHtml()`/`escapeHtmlBreaks()` 헬퍼 추가, `handleDownloadPDF()`의 `document.write()` 템플릿 내 이름·MBTI·생년월일·AI 생성 텍스트 전 지점에 적용. `<script>`/`<img onerror>`/`</title>` 브레이크아웃 등 5개 페이로드로 스크립트 미실행을 Playwright로 검증. 상세: 섹션 7-K-1. |
| 🔴 AI JSON 응답 파싱 실패 시 무재시도 폴백 (사고형 모델 토큰 소진) | `callGeminiJsonApi()`가 파싱 실패를 HTTP 오류와 동일하게 재시도하도록 재구성, 사주 인트로/카테고리 해석/처방전/오늘의 나풀이 4개 호출의 `maxOutputTokens`를 8192로, 타임아웃을 45초로 상향. 상세: 섹션 7-K-1. |
| 🟡 캐시 키에 출생시간(시주) 미포함 | `baseKeyId()` 헬퍼로 (2026-08-03 도입 당시) 12개 캐시 키 함수 전부에 `birthBranch`/`hourUnknown` 반영. 시간만 바꿔 재제출해도 캐시가 섞이지 않음. 상세: 섹션 7-K-2. (2026-08-07 재확인: 이후 세션에서 채팅/닮은인물/별자리/정밀궁합 등 기능이 추가되며 `baseKeyId()` 사용처가 19개로 늘어남 — 신규 캐시 키도 전부 이 헬퍼 기반이라 문제는 없으나, 함께 지적된 "만료/정리 로직 부재"는 여전히 미해결. 아래 신규 항목 참고.) |
| ✅ localStorage 캐시 만료/정리 로직 부재 (2026-08-07 발견·수정) | `App.tsx`에 `setCachedItem()` 헬퍼 추가 — 콘텐츠 캐시 키(15개 prefix, `PRUNABLE_CACHE_PREFIXES`) 저장이 `QuotaExceededError`로 실패하면 해당 prefix의 캐시를 최대 50개까지 비우고 한 번 재시도, 그래도 실패하면 캐싱만 조용히 포기(생성된 텍스트 자체는 정상 표시). 사전 예방적 나이 기반(TTL) 정리는 아니고, 실제로 저장공간이 찼을 때만 개입하는 방어적 방식 — 각 콘텐츠 키에 생성 시각이 저장돼 있지 않아 "오래된 것부터" 지우는 진짜 LRU는 아님(발견 순서로 비움). 22개 저장 지점(카테고리/심화해석/처방전/오행/궁합/풍수/운세/별자리/트랜짓/타로/정밀궁합/채팅/닮은인물/기둥해설/인트로) 전부 `localStorage.setItem` → `setCachedItem`으로 교체. 북마크·다이어리 이력(최근 10명 자체 상한 있음)·온보딩 플래그는 사용자 데이터라 정리 대상에서 제외. `tsc --noEmit`/`npm run build` 클린(번들 크기 변화 미미, +0.15KB). |
| 🟡 `solarTerms.ts` 데이터 범위 주석 불일치 | 상단 주석을 실제 데이터 범위(1940~2030)로 정정. (2031년 이후 데이터 확장 자체는 별도 과제로 위 표에 유지) 상세: 섹션 7-K-2. |
| 🟢 궁합 탭 인해(寅亥) 육합·파 동시 표시로 인한 모순적 인상 | 육합 파트너와 파 파트너가 같을 때(인·해 조합) 설명 문구를 조건부로 추가. 1990-02-15(일지=亥)로 Playwright 검증. 상세: 섹션 7-K-2. |
| 🟢 `App.css` 클래스 중복 정의 | `.lucky-item-box`/`.prescription-card`의 죽은 첫 번째 정의 삭제, 실제 적용 중이던 `transition`은 살아남은 정의로 이관해 시각적 회귀 없이 정리. 상세: 섹션 7-K-2. |
| 🟡 날짜 유효성 검증 (2월 30일 등) | `handleSubmit()`에서 `new Date()` 결과를 역검증해 존재하지 않는 날짜는 토스트로 안내하고 제출을 막음. Playwright로 2월 30일 입력 시 결과 화면으로 넘어가지 않음을 확인. 상세: 섹션 7-K-2. |
| 🟢 자시(子時) 2구간 미분리 + 야자시 보정 로직이 도달 불가능한 죽은 코드 | `HOUR_BRANCHES`에 `야자시`(23:00~24:00) 항목 신설, `calculateSaju()`가 이를 인식해 `calcHour=23`을 실제로 사용하도록 수정. 부수적으로 발견된 "이월된 날짜를 연주/월주 절기 판정에도 잘못 넘기던" 결함도 함께 수정(일주만 이월, 연주/월주는 원래 시각 기준 유지). Playwright로 자시=辛酉/야자시=壬戌(60갑자 순서상 정확히 다음 날)로 서로 다르게 계산됨을 확인. 상세: 섹션 7-K-2. |

### 해결됨 (2026-08-03, 2차 — "다른것도 작업 다 해줘")

| 항목 | 해결 내용 |
|------|----------|
| 🟡 시·분 정밀 출생시각 입력 부재 | `useExactTime`/`exactHour`/`exactMinute` 필드와 UI 추가, `calculateSaju()`가 정확한 시각이 주어지면 12시진 버튼 선택과 무관하게 직접 시진 지지를 산출하도록 수정. Playwright로 23:30 입력 시 야자시 규칙(일주 壬戌 이월)이 정확히 반영됨을 확인. 상세: 섹션 7-K-4. |
| 🟢 factBomb 등 AI 응답에 프롬프트 지침 문구 누출 | `cleanField()` 헬퍼로 알려진 JSON 예시 placeholder가 응답 앞부분에 그대로 남아있으면 제거. `generateSajuIntro`/`generateCategoryInterpretation`/`generateDailyFortune` 전체 적용. 상세: 섹션 7-K-4. |
| 🟢 전역 ErrorBoundary 부재 | `src/ErrorBoundary.tsx` 신설, `main.tsx`에서 `<App />`을 감싸 렌더링 오류 시 복구 UI(새로고침 버튼) 표시. 상세: 섹션 7-K-4. |
| 🟢 접근성(ARIA) 속성 전무 | 모달 3곳에 `role="dialog"`/`aria-modal`/`aria-label`, 토스트에 `role="status"`/`aria-live="polite"` 추가. Playwright로 모달 오픈 시 `role="dialog"` 존재 확인. 상세: 섹션 7-K-4. |
| 🟢 로딩 `useEffect` 의존성 배열 불완전 | 실제로 읽는 `formData`를 의존성 배열에 명시(`[step, formData]`). 로딩 중 재실행 없음(참조 불변). 상세: 섹션 7-K-4. |
| 🟢 다이어리 재방문 시 aiIntro 매번 재생성 | `aiIntroCacheKey()` 추가해 이름+생년월일+시간+MBTI 기준 캐싱. Playwright로 캐시 키 저장 및 재사용 확인. 상세: 섹션 7-K-4. |

### 해결됨 (2026-08-06 — 전면 재검토)

| 항목 | 해결 내용 |
|------|----------|
| 🔴 `api/gemini.ts` 자체 에러 응답을 클라이언트가 읽지 못하던 버그 | 자체 반환 에러(403/429/500/400)를 업스트림과 동일한 `{ error: { message } }` 모양으로 통일(`errorBody()` 헬퍼). Playwright로 429 모의 시 실제 안내 문구가 노출됨을 확인. 상세: 섹션 7-X-2. |
| 🔴 "재시도 불가" 오류가 실제로는 12회 재시도되던 구조적 버그 | `NonRetryableApiError` 마커로 catch에서 즉시 재던지도록 수정. Playwright로 설정 오류 시나리오가 1회 요청으로 끝남을 확인. 상세: 섹션 7-X-2. |
| 🟡 `keyword` 필드 없는 구버전 캐시 시 "오늘의 기운: undefined" 노출 가능성 (섹션 8 표에서 이동) | 나풀이/트랜짓 키워드 칩을 타로 칩과 동일하게 `keyword` 값 자체로 가드하도록 수정. 실제 증상은 리터럴 "undefined" 문자열이 아니라 빈 칩이었음(React가 undefined를 빈 값으로 렌더링) — 문서 오기 정정. 상세: 섹션 7-X-3. |
| 🟢 `api/gemini.ts` model 화이트리스트 검증 순서 | 잘못된 model 파라미터 요청이 rate limit을 먼저 소비하던 순서를 model 검증 우선으로 변경. 상세: 섹션 7-X-2. |

### 해결됨 (2026-08-06, 2차 — "나머지도 작업해줘")

| 항목 | 해결 내용 |
|------|----------|
| 🔴 네이티브 앱(iOS/Android)에서 AI 기능 전체 실패 가능성 | `geminiApi.ts`에 `API_BASE`(네이티브일 때만 배포 도메인 절대경로) 분기 추가 + `api/gemini.ts` Origin 검사에 `capacitor://localhost` 등 허용리스트 추가. Playwright로 웹에서는 여전히 상대경로만 쓰는 회귀 없음을 확인(실기기 검증은 미완 — Xcode/Android Studio 빌드 시 확인 권장). 상세: 섹션 7-Y-1. |
| 🟡 `X-Forwarded-For` rate limit 우회 우려 | Vercel 공식 문서 확인 결과 클라이언트가 이 헤더를 조작해도 Vercel이 자체 값으로 덮어써 우회 불가능함을 확인 — 근거 없는 우려였음. 코드는 그대로 두고 주석으로 근거 남김. 상세: 섹션 7-Y-2. |
| 🟢 정밀 궁합 상대방 폼에 절기 근사치 경고 문구 누락 | 본인 폼과 동일한 경고 블록을 상대방 생년월일 입력 폼에도 추가. 상세: 섹션 7-Y-3. |
| 🟢 "오늘의 트랜짓"에 출생시간 근사치 경고 누락 | 별자리 탭과 동일한 경고를 트랜짓 카드에도 추가. 상세: 섹션 7-Y-4. |
| 🟢 `GEMINI_API_KEY = 'server-managed'` 죽은 코드 | 40여 곳의 호출부를 건드리는 전면 제거 대신, 정의부 주석을 보강해 향후 오인 방지. 상세: 섹션 7-Y-5. |

### 해결됨 (2026-08-06, 3차 — 최우선 🔴 버그 완전 해결)

| 항목 | 해결 내용 |
|------|----------|
| 🔴 **"해석" 버튼 등 AI 기능 전반이 API 오류로 실패 (2026-08-05 최초 보고 → 2026-08-06 완전 해결)** | 진짜 원인은 `api/gemini.ts`가 Vercel 빌드 시 루트 `tsconfig.json`(Vite용, `module: ESNext`)을 잘못 물려받아 ESM `import` 구문이 남은 `.js`로 컴파일되고, Node가 이를 CommonJS로 로드하려다 즉시 크래시(`FUNCTION_INVOCATION_FAILED`)하던 것 — `vercel logs`로 런타임 로그를 직접 확인해 확정(1차 가설이었던 Firebase 키 미처리 예외는 배포·재검증 결과 틀렸음이 밝혀짐). `api/tsconfig.json`(module: commonjs) 신규 추가로 해결. 배포 후 `curl`로 프로덕션 엔드포인트에 실제 앱과 동일한 JSON 모드 요청을 보내 HTTP 200 + 정상 Gemini 응답을 직접 확인. 상세: 섹션 7-Z(특히 7-Z-4~7-Z-6). |

---

## 9. 프로젝트 파일 구조 (실측)

```
사주해석모바일버전/
├── .env                        # 환경변수 (VITE_GEMINI_API_KEY, VITE_KAKAO_JS_KEY)
├── .gitignore
├── index.html                  # Vite 진입점 + 카카오 SDK 스크립트 태그
├── package.json                # 의존성 (Electron 관련 설정 제거됨, PDF는 별도 라이브러리 없이 window.print() 사용)
├── tsconfig.json
├── vite.config.ts
├── 계획안.md                    # 본 문서
├── public/                     # 정적 자산 (favicon.svg, icons.svg)
├── dist/                       # Vite 프로덕션 빌드 산출물
├── scripts/
│   ├── generateSolarTerms.ts   # solarTerms.ts 재생성 스크립트 (astronomy-engine, 2026-08-18 신규)
│   └── build-tarot-images.ts   # 타로카드 원본 → public/tarot/ webp 변환 일회성 스크립트
├── src/
│   ├── App.tsx                 # 메인 앱 (UI + 로직 + PDF + 카카오 공유)
│   ├── App.css                 # 전체 스타일 (다크테마 + 모바일 반응형)
│   ├── main.tsx                # React 진입점
│   ├── vite-env.d.ts           # Vite 환경 타입 (2026-07-31 신규 추가)
│   ├── assets/
│   │   └── hero.png            # 카카오 공유 카드 이미지로도 사용
│   ├── data/
│   │   ├── solarTerms.ts       # 1900~2100 24절기 정밀 데이터 (201개년, astronomy-engine 계산 기반 — scripts/generateSolarTerms.ts로 재생성 가능)
│   │   ├── mbtiTypes.ts        # MBTI 16유형 정적 데이터 (2026-07-31 신규)
│   │   ├── compatibility.ts    # 일지 기준 궁합 룩업 테이블 (2026-07-31 신규)
│   │   ├── elementTypes.ts     # 오행 5종 해석 데이터 (2026-07-31 신규)
│   │   └── categoryQuestions.ts # 커리어/연애/재물 개인화 질문 데이터 (2026-08-01 신규)
│   └── utils/
│       ├── sajuCalculator.ts   # 사주 계산 엔진 (4기둥 + 대운 + 세운)
│       └── geminiApi.ts        # Gemini AI API (4단계 폴백 + 풍수 프롬프트)
```
