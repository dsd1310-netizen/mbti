# 🌌 사주 × MBTI 복합 해석 앱 — 개발 및 결과 보고서

> **작성일**: 2026년 7월 23일  
> **상태**: 개발 완료 및 Windows 실행 프로그램(.exe) 패키징 완료  
> **참고 파일**: `mbti.pdf`, `동추원만세력.pdf`, `sa_ju_mbti_interactive_app.tsx`

---

## 1. 프로젝트 개요

생년월일시와 MBTI를 입력하면,  
**정밀 만세력 기반 사주 계산** + **Google Gemini AI 실시간 해석**을 결합하여 나만의 사주 × MBTI 분석 결과를 제공하는 데스크톱 및 웹 통합 응용 프로그램.

---

## 2. 완료된 최종 기술 스택 & 환경

| 항목 | 사용 기술 / 설정 |
|------|-----------------|
| 프레임워크 | React 18 + Vite 5 |
| 언어 | TypeScript (.tsx, .ts) — Strict Type Verification 통과 |
| 스타일 | Vanilla CSS (글라스모피즘, 우주/별자리 다크 테마 디자인 시스템) |
| 데스크톱 패키징 | Electron + electron-packager (Windows 64bit 독립 실행형 빌드) |
| AI 해석 엔진 | Google Gemini API (`gemini-2.5-flash`, `gemini-1.5-flash`, `gemini-1.5-pro` 릴레이) |
| 데이터베이스 | 1940년 ~ 2030년 24절기(節氣) 정밀 시각 데이터 내장 (`solarTerms.ts`) |
| 실행 경로 | 1) 로컬 개발 서버: `npm run dev` (http://localhost:5173)<br>2) 데스크톱 실행 파일: `dist-exe/SajuMBTI-win32-x64/SajuMBTI.exe`<br>3) 배포용 압축: `dist-exe/SajuMBTI-win64.zip` |

---

## 3. 주요 구현 및 문제 해결 내역 (완료 사항)

### ✅ 1) 정밀 절기 만세력 산출 엔진 구축
- 기존의 단순 양력 월 계산 문제를 해결하기 위해 한국천문연구원 및 `동추원만세력` 기반의 24절기 입절 시각(일/시/분) 데이터 구축.
- 입력된 출생 시각의 절기 전후를 대조하여 연주(年柱) 및 월주(月柱)를 산출하도록 구현. 현재 UI는 2시간 단위 시진만 입력받으므로, 절기 경계 시각의 정확도 보완이 필요함.

### ✅ 2) Gemini AI 호출 방어 및 트래픽 분산 시스템 구현
- **모델 자동 릴레이 (Model Fallback)**: `gemini-2.5-flash` 과부하 발생 시 `gemini-1.5-flash` ➔ `gemini-1.5-pro`로 자동 전환.
- **지수 백오프 자동 재시도 (Exponential Backoff Retry)**: HTTP 429(High Demand/Too Many Requests) 및 503/500 에러 발생 시 1.5초, 3초, 4.5초 지연 대기 후 최대 3회 자동 재시도.
- **마크다운 정규식 제거 & 안전 JSON 파싱**: AI 응답 텍스트에 포함된 ` ```json `, ` ``` ` 등 백틱 기호를 정규식으로 완벽히 제거하고 최외곽 `{ ... }` 범위를 슬라이싱하여 파싱 오류 전면 차단 (`responseMimeType: 'application/json'` 적용).

### ✅ 3) 우주/별자리 감성 UI/UX 및 기능 완성
- 글라스모피즘(Glassmorphism) 카드, 별빛 파티클 애니메이션(`stars-bg`), 성운 그라데이션 적용.
- 사주 4기둥(연/월/일/시주) 및 오행(목/화/토/금/수) 과부족 비주얼 차트 제공.
- AI 해석 항목 (성격/커리어/연애/재물/팩폭) 보관함 북마크 저장 및 삭제 기능 구현 (`localStorage` 연동).

### ✅ 4) Windows 전용 데스크톱 실행 프로그램(.exe) 패키징
- Vite 프로덕션 빌드 후 Electron 기반의 **[SajuMBTI.exe](file:///C:/test/AI데이터바이브/사주%20해석/saju-mbti-app/dist-exe/SajuMBTI-win32-x64/SajuMBTI.exe)** 단독 실행 파일 및 [SajuMBTI-win64.zip](file:///C:/test/AI데이터바이브/사주%20해석/saju-mbti-app/dist-exe/SajuMBTI-win64.zip) 압축 패키지 생성 완료.

---

## 4. 최종 개발 순서 진행 결과

1. [x] **Vite + React + TypeScript 프로젝트 세팅** (완료)
2. [x] **절기 데이터 내장 + 정밀 사주 계산 엔진 구현** (완료)
3. [x] **Google Gemini API 연동 모듈 및 예외 방어 구현** (완료)
4. [x] **UI 컴포넌트 제작 및 다크 테마 디자인 시스템 완성** (완료)
5. [x] **입력 → 로딩 → 결과 → 북마크 보관함 전체 플로우 연결** (완료)
6. [x] **TypeScript 타입 검사 & 빌드 오류 0건 검증** (완료)
7. [x] **Electron 데스크톱 .exe 프로그램 패키징** (완료)

---

## 5. API 키 발급 및 실행 가이드

1. **Google AI Studio 접속**: https://aistudio.google.com
2. 구글 계정 로그인 후 **"Get API Key"** 버튼 클릭 ➔ 무료 API 키 발급.
3. 앱 첫 화면의 **`🔑 Google Gemini API 키`** 입력란에 키를 붙여넣은 뒤 실행. (최초 1회 입력 시 자동 저장 유지)

---

## 6. 2026-07-23 코드 검토 및 검증 기록

### 검증 완료

- `npm.cmd run build` 실행 성공: TypeScript 타입 검사와 Vite 프로덕션 빌드가 오류 없이 완료됨.
- 검증 과정에서 `saju-mbti-app/dist` 정적 산출물만 재생성함. `src` 소스 코드는 수정하지 않음.
- Git 명령이 현재 환경에 설치되어 있지 않아 변경 이력 비교는 수행하지 못함.
- Gemini API 키가 제공되지 않아 실제 AI API 호출과 EXE GUI 수동 동작은 검증 범위에서 제외함.

### 확인된 보완 과제

1. **사주 계산 정확도**: 실제 출생 시각(시·분)을 입력받지 않고 시진의 고정 시각으로 계산하므로, 절기 경계 구간에서 월주·연주가 달라질 수 있음. 자시의 날짜 변경 기준도 정책으로 명확히 해야 함.
2. **입력 검증**: 2월 30일 등 유효하지 않은 날짜가 JavaScript 날짜 보정으로 다른 날짜에 계산될 수 있음. 빈 날짜값도 기본값으로 대체하지 말고 오류로 안내해야 함.
3. **데이터 범위**: 절기 데이터는 1940년~2030년이며, 입력 UI는 1930년부터 허용함. 데이터 범위 밖 입력을 막거나 근사 계산임을 명시해야 함.
4. **API 키 보안**: Gemini API 키가 `localStorage`에 평문으로 영구 저장됨. 기본은 세션 전용으로 하고, 저장 기능이 필요하면 Electron 보안 저장소로 이전해야 함.
5. **Gemini 모델 및 재시도**: 현재 fallback의 `gemini-1.5-*` 모델 정책을 최신 지원 모델 기준으로 갱신해야 함. 요청 타임아웃, 재시도 대상 상태 코드, 출력 토큰 제한도 조정이 필요함.
6. **패키징 재현성**: 기존 `dist-exe`는 `electron-packager` 방식이고 `npm run dist`는 `electron-builder` 방식임. 도구와 출력 경로를 하나로 통일하고, 배포물에 소스·의존성·중첩 산출물이 불필요하게 포함되지 않도록 정리해야 함.

### 현재 상태

- 사용자 요청으로 개선 작업은 코드 검토 단계에서 중지함.
- 권장 재개 순서: 실제 시·분 입력 및 날짜 검증 → 패키징 단일화 → API 키 보안 및 Gemini 모델 갱신.
- 2026-07-24 2차 심층 코드 검토로 이어짐 (아래 섹션 7 참조).

---

## 7. 2026-07-24 심층 코드 검토 결과

> **검토 범위**: 계획안.md, 전체 소스 코드 (`App.tsx`, `sajuCalculator.ts`, `geminiApi.ts`, `solarTerms.ts`, `electron-main.js`, `package.json`, `vite.config.ts`), EXE 패키지 구조 (`dist-exe/SajuMBTI-win32-x64/`)  
> **검토 방식**: 소스 코드 정적 분석 + 계획안 대비 구현 대조 + 패키징 산출물 구조 점검

### 7-1. 전체 평가 요약

| 분류 | 평가 |
|------|------|
| 계획안 완성도 | ⭐⭐⭐⭐ — 체계적이고 기술 스택 및 문제 해결 내역이 잘 정리됨 |
| 사주 계산 엔진 | ⭐⭐⭐ — 기본 로직은 정확하나 **경계 조건 처리에 버그** 존재 |
| Gemini API 연동 | ⭐⭐⭐⭐ — 방어 로직이 견고하나 일부 개선 필요 |
| UI/UX | ⭐⭐⭐⭐ — 디자인 시스템이 잘 구성되어 있음 (App.css 1094줄) |
| EXE 패키징 | ⭐⭐ — **심각한 문제 존재** (불필요 파일 포함, 패키징 도구 충돌) |

### 7-2. 발견된 이슈 목록 (심각도별)

#### 🔴 Critical (심각) — 3건

**C-1. EXE 패키지에 전체 소스코드 + node_modules 포함**
- **위치**: `dist-exe/SajuMBTI-win32-x64/resources/app/`
- 패키징된 EXE 내부에 `src/` (TypeScript 소스 전체), `node_modules/` (npm 의존성 전체), `dist-electron/`, `.gitignore`, `tsconfig.json`, `vite.config.ts` 등이 모두 포함됨.
- 📦 배포 ZIP 용량이 **282MB**에 달하며, 실제 필요한 것은 `dist/` 폴더와 `electron-main.js`뿐.
- 🔒 소스코드 및 API 호출 관련 코드가 그대로 노출되는 보안 위험.
- **원인**: `electron-packager`가 `.gitignore` 패턴 기반 필터링을 하지만, `src/` 등 소스 디렉토리 제외 규칙이 없음.

**C-2. 패키징 도구 이중 설치 (electron-packager + electron-builder)**
- **위치**: `package.json` devDependencies
- `electron-builder` (^26.15.3) → `npm run dist` 스크립트에서 사용, 출력 경로: `dist-electron/`
- `electron-packager` (^17.1.2) → 실제 EXE 생성에 사용, 출력 경로: `dist-exe/`
- 두 도구가 혼재하여 "공식" 빌드가 무엇인지 혼란 발생. 하나로 통일 필요.

**C-3. 절기 데이터의 비정상 시/분 값**
- **위치**: `src/data/solarTerms.ts`
- 다음 항목에서 분(minute) 값이 60 이상인 비정상 데이터 확인:
  - `2024`: `{ 0:[6,10,60] }` — 소한 10시 **60분** (→ 11시 00분이어야 함)
  - `2016`: `{ 9:[8,5,62] }` — 한로 5시 **62분** (→ 비정상)
  - `2007`: `{ 8:[8,14,60], 9:[9,0,60] }` — 백로 14시 **60분**, 한로 0시 **60분**
- 시간 비교 로직(`day * 1440 + hour * 60 + minute`)에서 1~2분 오차가 발생하며, 데이터 정합성 문제.

#### 🟡 Warning (주의) — 4건

**W-1. 자시(子時) 날짜 변경 미처리**
- **위치**: `src/utils/sajuCalculator.ts` 229~231행
- 자시(23:00~01:00) 선택 시 `calcHour = 0`으로 일괄 처리됨.
- 23:00~24:00(야자시)에 태어난 사람은 **다음날 일주**를 써야 할 수 있음 (조자시/야자시 학파 논쟁).
- 시진 선택만 가능하므로 야자시/조자시 구분 자체가 불가능하며, **일주가 하루 차이로 바뀌면 시주도 함께 변경** → 전체 4기둥 중 2기둥이 달라질 수 있음.

**W-2. 입력 날짜 유효성 검증 부재**
- **위치**: `src/App.tsx` 130~132행
- `parseInt(formData.birthYear) || 1995` 형태로, 빈 값이면 `1995-09-27`로 **묵시적 대체**.
- `2월 30일`, `4월 31일` 같은 존재하지 않는 날짜가 입력 가능.
- `1930~1939년` 입력 가능하지만 절기 데이터는 `1940년`부터만 존재 (UI의 min="1930"과 불일치).

**W-3. 1월 소한 이전 월주 경계 처리 미흡**
- **위치**: `src/data/solarTerms.ts` 183~186행
- 1월(termIdx=0)에 소한 이전 출생이면 `prevTermIdx = 11`(대설) → `SOLAR_TERMS_BRANCH_IDX[11] = 0`(자월)을 반환.
- 지지 인덱스 자체는 맞지만, **전년도 12월 대설 절기 데이터를 참조하지 않고** 당해 연도의 소한 날짜만으로 판단하므로 정밀도 한계가 있음.

**W-4. 미사용 파일 잔여**
- **위치**: `src/counter.ts` (274 bytes), `src/style.css` (5,060 bytes)
- `counter.ts` — Vite 초기 카운터 데모 코드. 프로젝트에서 미참조.
- `style.css` — Vite 기본 템플릿 CSS. `main.tsx`에서 `App.css`만 import하므로 미사용.
- `tsconfig.json`의 `"noUnusedLocals": true`는 파일 단위 미참조는 검출하지 않음.

#### 🟢 Info (참고) — 4건

**I-1. API 키 localStorage 평문 저장**
- **위치**: `src/App.tsx` 82행
- Electron 환경에서 `localStorage`는 파일 시스템에 평문 저장됨. `safeStorage` API 또는 `keytar` 모듈로 암호화 저장 권장.

**I-2. Gemini 1.5 계열 모델 활성 상태 확인 필요**
- **위치**: `src/utils/geminiApi.ts` 9~13행
- 2026년 7월 현재 `gemini-1.5-flash`, `gemini-1.5-pro` 모델이 아직 활성 상태인지 확인 필요. 비활성화된 모델로 fallback하면 재시도가 모두 실패할 수 있음.

**I-3. React useEffect 의존성 배열 불완전**
- **위치**: `src/App.tsx` 169행 `}, [step]);`
- `step`만 의존성에 포함되어 있지만, 내부에서 `formData`, `apiKey`를 캡처. React Strict Mode에서 stale closure 문제 발생 가능.

**I-4. 오행 매핑 '신' 글자 중복**
- **위치**: `src/utils/sajuCalculator.ts` 62행
- `ELEMENT_MAP`에서 `'신'`이 `metal`로 매핑됨. 천간 신(辛)=금, 지지 신(申)=금으로 결과적으로는 동일하여 **실제 오류로 이어지지는 않음**. 다만 `calcElementCounts()`에서 stem/branch를 분리 카운트하므로 간접적으로 안전.

**I-5. Gemini API 요청 타임아웃 미설정**
- **위치**: `src/utils/geminiApi.ts` 186행 `fetch()` 호출
- `fetch()`에 타임아웃이 설정되어 있지 않아, Gemini 서버가 응답을 보내지 않으면 **무한 대기** → 로딩 화면이 영원히 멈춤.
- 이전 요청(응답 끊김/로딩 멈춤 현상)의 `maxOutputTokens` 확대 및 안전 JSON 파싱은 반영 완료되었으나, **네트워크 수준 타임아웃**은 아직 미적용 상태.
- `AbortController` + `setTimeout` 조합으로 15~20초 타임아웃 추가 필요.

**I-6. 모델 재시도 중 사용자 피드백 UI 부재**
- **위치**: `src/App.tsx` 로딩 화면 / `src/utils/geminiApi.ts` 재시도 루프
- 3개 모델 × 3회 재시도 = 최대 **9번 호출** 시 약 40초 이상 로딩이 걸릴 수 있으나, 현재 UI에는 "재시도 중" 또는 "다른 모델로 전환 중" 등의 상태 피드백이 없음.
- 사용자 입장에서 앱이 먹통인지 정상 동작 중인지 구분 불가.

### 7-3. 계획안 vs 실제 구현 대조표

| 계획안 기재 사항 | 실제 코드 상태 | 일치 |
|-----------------|---------------|------|
| React 18 + Vite 5 + TypeScript | ✅ 일치 | ✅ |
| Vanilla CSS 글라스모피즘 디자인 | ✅ 일치 (App.css 1094줄, 글라스모피즘·성운·별빛 파티클) | ✅ |
| Electron + electron-packager 패키징 | ⚠️ electron-builder도 함께 설치됨 | ⚠️ |
| 1940~2030년 절기 데이터 | ✅ 일치 (solarTerms.ts, 91개 연도분) | ✅ |
| Gemini 모델 릴레이 (2.5/1.5-flash/1.5-pro) | ✅ 일치 | ✅ |
| 지수 백오프 재시도 (1.5초 × attempt, 최대 3회) | ✅ 일치 | ✅ |
| localStorage 북마크 저장/삭제 | ✅ 일치 | ✅ |
| 마크다운 정규식 제거 + 안전 JSON 파싱 | ✅ 일치 (extractJson 함수, regex fallback) | ✅ |
| `base: './'` Electron 상대 경로 | ✅ 일치 (vite.config.ts) | ✅ |
| 사주원국 4기둥 + 오행 차트 UI | ✅ 일치 | ✅ |
| AI 해석 5개 항목 (성격/커리어/연애/재물/팩폭) | ✅ 일치 | ✅ |

### 7-4. 권장 개선 순서 (우선순위 로드맵)

| 순서 | 심각도 | 작업 항목 | 대상 파일 |
|------|--------|----------|----------|
| 1 | 🔴 | EXE 패키징 정리 — `--ignore` 옵션 또는 electron-builder 통일 | `package.json` |
| 2 | 🔴 | 절기 데이터 이상값(60분, 62분) 수정 | `solarTerms.ts` |
| 3 | 🟡 | 입력 날짜 유효성 검증 추가 — 존재하지 않는 날짜 차단, 범위(1940~2030) 밖 경고 | `App.tsx` |
| 4 | 🟡 | 자시(子時) 정책 명확화 — 야자시/조자시 구분 UI 또는 정책 고정 | `sajuCalculator.ts`, `App.tsx` |
| 5 | 🟡 | 미사용 파일 정리 | `counter.ts`, `style.css` |
| 6 | 🟢 | React useEffect deps 배열 수정 — formData, apiKey를 의존성 추가 또는 ref 전환 | `App.tsx` |
| 7 | 🟢 | Gemini fallback 모델 목록 최신화 | `geminiApi.ts` |
| 8 | 🟢 | API 키 암호화 저장 (Electron safeStorage) | `App.tsx`, `electron-main.js` |
| 9 | 🟢 | Gemini API 요청 타임아웃 추가 (AbortController) | `geminiApi.ts` |
| 10 | 🟢 | 모델 재시도 중 사용자 피드백 UI 추가 | `App.tsx`, `geminiApi.ts` |

---

## 8. 2026-07-24 신규 요구사항: 매콤한 사주 × MBTI 팩폭 진단 & 심층 보고서 기능 고도화

사용자 요구사항에 따라 각 해석 항목(성격/커리어/연애/재물)의 흥미도, 전문성, 가독성을 극대화하기 위한 **심층 보고서형 팩폭 진단 시스템** 고도화 계획.

### 8-1. 톤앤매너 & AI 프롬프트 정책 (AI Deep Prompting)
- **방식**: **솔직 매운맛 팩폭 🌶️ + 사주xMBTI 원리 융합 분석**
- **비유기법**: 사주 오행(목/화/토/금/수)과 MBTI 요소가 만나 작용하는 원리를 "용광로", "스포츠카", "스펀지" 등 비전공자도 한눈에 이해할 수 있는 **쉬운 비유**로 풀어서 설명.
- **말투**: 예의 바르면서 반전 있게 뼈를 짚는 **존댓말 매운맛 팩폭 톤** 적용.
- **깊이 & 분량**: 각 항목별 짧은 요약이 아닌, 5~7줄 이상의 **풍부하고 긴 심층 보고서형** 텍스트 제공.

### 8-2. 분야별 심층 분석 구성
- **성격**: 사주 오행 과부족과 MBTI 4가지 지표가 만나 발현되는 독특한 성격 원리 팩폭 분석.
- **일 & 재물**: 직장에서 홧병 나는 이유, 당장 피해야 할 직무, 돈이 새어나가는 멍청한 지출 구멍 분석.
- **연애 & 인간관계**: 찰떡 궁합과 반대로 만나면 피 터지는 상극 유형, 연애할 때 고쳐야 할 팩폭 포인트.
- **풍성한 추가 요소**:
  1. 🔥 사주 × MBTI 뼈 때리는 팩폭 한줄평
  2. 🍀 럭키 아이템 & ⚠️ 피해야 할 상극 유형
  3. 🎯 맞춤형 3가지 현실 실천 처방전 (행동 지침)

### 8-3. UI/UX 레이아웃 (Tab Navigation & Visual System)
- **가독성 탭 UI**: 긴 내용을 답답함 없이 읽을 수 있도록 **[성격] [커리어/재물] [연애/궁합] [종합 처방전] 탭(Tab) 구조**로 개편.
- **비주얼 효과**: 팩폭 및 처방전 카드에 **주황/빨간 불꽃 그라데이션 테두리** 및 **🔥 🌶️ 🎯 🍀** 아이콘 적용.

### 8-4. 북마크(보관함) 완벽 연동
- 심층 분석 결과 전체(럭키 아이템, 상극 유형, 3가지 처방전 포함)를 `localStorage`에 **100% 누락 없이 완전 저장**.

---

## 9. 작업 진행 계획 (Next Steps)
1. [x] `계획안.md` 인터뷰 결과 반영 및 업데이트 완료
2. [ ] `geminiApi.ts` 프롬프트 & 데이터 구조 개편 (매운맛 팩폭 + 오행xMBTI 쉬운 원리 비유 + 상세 분량)
3. [ ] `App.tsx` & `App.css` UI 개편 (탭 전환 인터페이스, 불꽃 테두리 디자인, 완전 북마크 기능)
4. [ ] `npm run dist` (`electron-builder`)를 통한 실행 파일 (`SajuMBTI 1.0.0.exe`) 최종 패키징 및 검증

