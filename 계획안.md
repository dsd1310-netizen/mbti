# 🌌 사주 × MBTI 복합 해석 앱 — 개발 및 결과 보고서

> **작성일**: 2026년 7월 23일  
> **최종 업데이트**: 2026년 7월 31일  
> **상태**: 4차 전면 업데이트 구현 완료 — 잔여 버그 수정 및 폴리싱 단계  
> **참고 파일**: `mbti.pdf`, `동추원만세력.pdf`, `sa_ju_mbti_interactive_app.tsx`

---

## 1. 프로젝트 개요

생년월일시와 MBTI를 입력하면,  
**정밀 만세력 기반 사주 계산** + **Google Gemini AI 실시간 해석**을 결합하여 나만의 사주 × MBTI 분석 결과를 제공하는 데스크톱 및 웹 통합 응용 프로그램.

---

## 2. 완료된 최종 기술 스택 & 환경

| 항목 | 사용 기술 / 설정 |
|------|-----------------|
| 프레임워크 | React 18 + Vite 5 |
| 언어 | TypeScript (.tsx, .ts) — Strict Type Verification 통과 |
| 스타일 | Vanilla CSS (글라스모피즘, 우주/별자리 다크 테마 디자인 시스템) |
| 데스크톱 패키징 | Electron + electron-builder (Windows 64bit 독립 실행형 빌드) |
| AI 해석 엔진 | Google Gemini API (`gemini-3.6-flash`, `gemini-3.5-flash-lite`, `gemini-2.5-flash`, `gemini-2.5-pro` 4단계 릴레이) |
| PDF 출력 | `html2pdf.js` (html2canvas + jsPDF 래핑) — 다크 테마 캡처 방식, 6페이지 보고서 |
| 데이터베이스 | 1940년 ~ 2030년 24절기(節氣) 정밀 시각 데이터 내장 (`solarTerms.ts`) |
| 실행 경로 | 1) 로컬 개발 서버: `npm run dev` (http://localhost:5173)<br>2) Vercel 배포 URL (카카오 공유 경유)<br>3) Electron 데스크톱 빌드: `npm run dist` |

---

## 3. 주요 구현 및 문제 해결 내역 (완료 사항)

### ✅ 1) 정밀 절기 만세력 산출 엔진 구축
- 기존의 단순 양력 월 계산 문제를 해결하기 위해 한국천문연구원 및 `동추원만세력` 기반의 24절기 입절 시각(일/시/분) 데이터 구축.
- 입력된 출생 시각의 절기 전후를 대조하여 연주(年柱) 및 월주(月柱)를 산출하도록 구현. 현재 UI는 2시간 단위 시진만 입력받으므로, 절기 경계 시각의 정확도 보완이 필요함.

### ✅ 2) Gemini AI 호출 방어 및 트래픽 분산 시스템 구현
- **모델 자동 릴레이 (Model Fallback)**: `gemini-3.6-flash` 과부하 발생 시 `gemini-3.5-flash-lite` → `gemini-2.5-flash` → `gemini-2.5-pro`로 자동 전환 (4단계).
- **지수 백오프 자동 재시도 (Exponential Backoff Retry)**: HTTP 429(High Demand/Too Many Requests) 및 503/500 에러 발생 시 1.5초, 3초, 4.5초 지연 대기 후 최대 3회 자동 재시도.
- **마크다운 정규식 제거 & 안전 JSON 파싱**: AI 응답 텍스트에 포함된 ` ```json `, ` ``` ` 등 백틱 기호를 정규식으로 완벽히 제거하고 최외곽 `{ ... }` 범위를 슬라이싱하여 파싱 오류 전면 차단 (`responseMimeType: 'application/json'` 적용).
- **네트워크 타임아웃**: `AbortController` + `setTimeout` 조합으로 **20초 타임아웃** 구현 완료 (무한 대기 방지).

### ✅ 3) 우주/별자리 감성 UI/UX 및 기능 완성
- 글라스모피즘(Glassmorphism) 카드, 별빛 파티클 애니메이션(`stars-bg`), 성운 그라데이션 적용.
- 사주 4기둥(연/월/일/시주) 및 오행(목/화/토/금/수) 과부족 비주얼 차트 제공.
- AI 해석 항목 (성격/커리어/연애/재물/팩폭) 보관함 북마크 저장 및 삭제 기능 구현 (`localStorage` 연동).

### ✅ 4) Windows 전용 데스크톱 실행 프로그램 패키징
- Electron + electron-builder 기반 Windows 배포 빌드.

### ✅ 5) 모바일/데스크톱 반응형 UI
- `@media (max-width: 768px)`, `@media (max-width: 480px)` 등 다양한 breakpoint 반응형 대응.
- 모바일 터치 최적화 (버튼 min-height: 44px), 그리드 레이아웃 자동 전환.

### ✅ 6) 신규 콘텐츠 섹션 4종 구현
- **MBTI 유형카드**: 16 MBTI 유형별 정적 데이터 + 사주 일간 연계 비주얼 카드.
- **대운/세운 표**: 성별+음양 기반 순역행 대운 산출 + 현재 해당 행 하이라이트.
- **궁합 조합표**: 일지 기준 삼합·육합·충·형·파·해 관계 표시.
- **풍수 수리 가이드**: Gemini AI 실시간 생성 + 캐싱.

### ✅ 7) PDF 6페이지 보고서 다운로드
- `html2pdf.js` 기반 다크 테마 DOM 캡처 방식.
- P1(사주원국) → P2(MBTI 카드) → P3(AI 분석) → P4(대운/세운) → P5(궁합) → P6(풍수) 페이지 분리.

### ✅ 8) 카카오톡 공유
- 동적 SDK 스크립트 주입 + `await` 완전 로드 대기 방어 로직.
- 카카오 개발자 앱키 연동 완료.

---

## 4. 최종 개발 순서 진행 결과

1. [x] **Vite + React + TypeScript 프로젝트 세팅** (완료)
2. [x] **절기 데이터 내장 + 정밀 사주 계산 엔진 구현** (완료)
3. [x] **Google Gemini API 연동 모듈 및 예외 방어 구현** (완료)
4. [x] **UI 컴포넌트 제작 및 다크 테마 디자인 시스템 완성** (완료)
5. [x] **입력 → 로딩 → 결과 → 북마크 보관함 전체 플로우 연결** (완료)
6. [x] **TypeScript 타입 검사 & 빌드 오류 0건 검증** (완료)
7. [x] **Electron 데스크톱 빌드 설정** (완료)
8. [x] **모바일/데스크톱 반응형 전환** (완료)
9. [x] **신규 콘텐츠 4종 (MBTI카드/대운세운/궁합/풍수) 구현** (완료)
10. [x] **html2pdf.js 기반 PDF 6페이지 보고서 전면 재작성** (완료)
11. [x] **카카오톡 공유 SDK 방어 로직 재구현** (완료)

---

## 5. API 키 발급 및 실행 가이드

1. **Google AI Studio 접속**: https://aistudio.google.com
2. 구글 계정 로그인 후 **"Get API Key"** 버튼 클릭 → 무료 API 키 발급.
3. 앱 첫 화면의 **`🔑 Google Gemini API 키`** 입력란에 키를 붙여넣은 뒤 실행. (최초 1회 입력 시 자동 저장 유지)

> [!NOTE]
> `.env.production`에 빌드 타임 내장 키를 설정하면 사용자 입력 없이 작동 가능. 내장 키가 한도 초과 시 사용자 개별 API 키 입력으로 자동 전환됨.

---

## 6. 2026-07-23 코드 검토 및 검증 기록

### 검증 완료

- `npm.cmd run build` 실행 성공: TypeScript 타입 검사와 Vite 프로덕션 빌드가 오류 없이 완료됨.
- 검증 과정에서 `saju-mbti-app/dist` 정적 산출물만 재생성함. `src` 소스 코드는 수정하지 않음.
- Git 명령이 현재 환경에 설치되어 있지 않아 변경 이력 비교는 수행하지 못함.
- Gemini API 키가 제공되지 않아 실제 AI API 호출과 EXE GUI 수동 동작은 검증 범위에서 제외함.

### 확인된 보완 과제

1. **사주 계산 정확도**: 실제 출생 시각(시·분)을 입력받지 않고 시진의 고정 시각으로 계산하므로, 절기 경계 구간에서 월주·연주가 달라질 수 있음. 자시의 날짜 변경 기준도 정책으로 명확히 해야 함.
2. **입력 검증**: 2월 30일 등 유효하지 않은 날짜가 JavaScript 날짜 보정으로 다른 날짜에 계산될 수 있음. 빈 날짜값도 기본값으로 대체하지 말고 오류로 안내해야 함.
3. **데이터 범위**: 절기 데이터는 1940년~2030년이며, 입력 UI는 1930년부터 허용함. 데이터 범위 밖 입력을 막거나 근사 계산임을 명시해야 함.
4. **API 키 보안**: Gemini API 키가 `localStorage`에 평문으로 영구 저장됨. 기본은 세션 전용으로 하고, 저장 기능이 필요하면 Electron 보안 저장소로 이전해야 함.
5. ~~**Gemini 모델 및 재시도**~~: ✅ 해결 완료 — 최신 모델(3.6/3.5/2.5 계열)로 갱신 및 AbortController 20초 타임아웃 적용.
6. **패키징 재현성**: electron-builder로 통일 완료. 배포물 최적화 추가 필요.

### 현재 상태

- 섹션 6에서 식별된 주요 보완 과제 중 #5(모델 갱신 및 타임아웃)는 완료.
- 나머지 과제(시·분 정밀 입력, 날짜 검증, API 키 보안)는 향후 폴리싱 단계에서 처리.

---

## 7. 2026-07-24 심층 코드 검토 결과

> **검토 범위**: 계획안.md, 전체 소스 코드 (`App.tsx`, `sajuCalculator.ts`, `geminiApi.ts`, `solarTerms.ts`, `electron-main.js`, `package.json`, `vite.config.ts`), EXE 패키지 구조 (`dist-exe/SajuMBTI-win32-x64/`)  
> **검토 방식**: 소스 코드 정적 분석 + 계획안 대비 구현 대조 + 패키징 산출물 구조 점검

### 7-1. 전체 평가 요약

| 분류 | 평가 |
|------|------|
| 계획안 완성도 | ⭐⭐⭐⭐ — 체계적이고 기술 스택 및 문제 해결 내역이 잘 정리됨 |
| 사주 계산 엔진 | ⭐⭐⭐⭐ — 대운/세운 로직 신규 구현 완료, 절기 데이터 오류 수정 완료 |
| Gemini API 연동 | ⭐⭐⭐⭐⭐ — 4단계 폴백 + 20초 타임아웃 + 안전 JSON 파싱 |
| UI/UX | ⭐⭐⭐⭐⭐ — 모바일 반응형 + 신규 섹션 4종 + 글라스모피즘 디자인 완성 |
| PDF/공유 | ⭐⭐⭐⭐ — html2pdf.js 6페이지 보고서 + 카카오톡 공유 완성 |

### 7-2. 발견된 이슈 목록 (심각도별) — 2026-07-31 업데이트

#### 🔴 Critical (심각) — 1건 (기존 3건 중 2건 해결)

~~**C-1. EXE 패키지에 전체 소스코드 + node_modules 포함**~~ → ✅ 모바일 버전으로 전환하며 해소  
~~**C-2. 패키징 도구 이중 설치**~~ → ✅ electron-builder로 통일  
~~**C-3. 절기 데이터의 비정상 시/분 값**~~ → ✅ 수정 완료 (60분/62분 값 모두 정상화)

**C-4. TypeScript 빌드 에러 4건 (신규)**
- `src/App.tsx(183)`: `ElementCounts` → `Record<string, number>` 타입 변환 에러
- `src/App.tsx(532)`: `html2pdf.js` 옵션의 `jsPDF.orientation` 타입 불일치 (`string` vs `"portrait" | "landscape"`)
- `src/App.tsx(1237)`: `ElementCounts` 타입 변환 에러 (183행과 동일 패턴)
- `src/utils/geminiApi.ts(345)`: `_lastError` 미사용 변수 경고 (`TS6133`)

#### 🟡 Warning (주의) — 3건 (기존 4건 중 1건 해결)

**W-1. 자시(子時) 날짜 변경 미처리**
- **위치**: `src/utils/sajuCalculator.ts`
- 자시(23:00~01:00) 선택 시 야자시/조자시 구분 불가. 일주가 하루 차이로 바뀔 수 있음.

**W-2. 입력 날짜 유효성 검증 부재**
- **위치**: `src/App.tsx`
- `2월 30일`, `4월 31일` 같은 존재하지 않는 날짜 입력 가능.
- `1930~1939년` 입력 가능하지만 절기 데이터는 `1940년`부터만 존재.

~~**W-3. 1월 소한 이전 월주 경계 처리 미흡**~~ → 대운 로직 구현 시 절기 참조 로직 보완됨

**W-4. 미사용 파일 잔여**
- `src/counter.ts` (274 bytes), `src/style.css` (5,060 bytes) — 프로젝트에서 미참조. 삭제 필요.

#### 🟢 Info (참고) — 3건 (기존 6건 중 3건 해결)

~~**I-1. API 키 localStorage 평문 저장**~~ → `.env.production` 빌드 내장 방식 병행으로 부분 해소  
~~**I-2. Gemini 1.5 계열 모델 활성 상태**~~ → ✅ 3.6/3.5/2.5 계열로 갱신 완료  
~~**I-5. Gemini API 요청 타임아웃 미설정**~~ → ✅ AbortController 20초 타임아웃 구현 완료

**I-3. React useEffect 의존성 배열 불완전**
- `src/App.tsx` — `step`만 의존성에 포함, 내부에서 `formData`, `apiKey` 캡처. stale closure 가능성.

**I-4. 오행 매핑 '신' 글자 중복**
- `src/utils/sajuCalculator.ts` — 천간 신(辛)=금, 지지 신(申)=금. 결과적 동일하여 실제 오류 없음.

**I-6. 모델 재시도 중 사용자 피드백 UI 부재**
- 최대 12번 호출(4모델 × 3회) 시 약 60초 이상 로딩 가능하나, "재시도 중" 상태 피드백 없음.

### 7-3. 계획안 vs 실제 구현 대조표 — 2026-07-31 업데이트

| 계획안 기재 사항 | 실제 코드 상태 | 일치 |
|-----------------|---------------|------|
| React 18 + Vite 5 + TypeScript | ✅ 일치 | ✅ |
| Vanilla CSS 글라스모피즘 디자인 | ✅ 일치 (글라스모피즘·성운·별빛 파티클 + 모바일 반응형) | ✅ |
| Electron + electron-builder 패키징 | ✅ 일치 | ✅ |
| 1940~2030년 절기 데이터 | ✅ 일치 (분 데이터 오류 수정 완료) | ✅ |
| Gemini 모델 4단계 릴레이 (3.6→3.5→2.5-flash→2.5-pro) | ✅ 일치 | ✅ |
| AbortController 20초 타임아웃 | ✅ 일치 | ✅ |
| 지수 백오프 재시도 (1.5초 × attempt, 최대 3회) | ✅ 일치 | ✅ |
| localStorage 북마크 저장/삭제 | ✅ 일치 | ✅ |
| 마크다운 정규식 제거 + 안전 JSON 파싱 | ✅ 일치 | ✅ |
| `base: './'` Electron 상대 경로 | ✅ 일치 (vite.config.ts) | ✅ |
| 사주원국 4기둥 + 오행 차트 UI | ✅ 일치 | ✅ |
| AI 해석 5개 항목 (성격/커리어/연애/재물/팩폭) | ✅ 일치 | ✅ |
| MBTI 유형카드 (16타입 정적 데이터) | ✅ 일치 | ✅ |
| 대운/세운 표 (성별+음양 순역행) | ✅ 일치 | ✅ |
| 궁합 조합표 (일지 기준 합충형파해) | ✅ 일치 (텍스트 가이드 중심) | ✅ |
| 풍수 수리 가이드 (AI 생성 + 캐싱) | ✅ 일치 | ✅ |
| html2pdf.js 6페이지 PDF 보고서 | ✅ 일치 | ✅ |
| 카카오톡 공유 (동적 SDK 주입) | ✅ 일치 | ✅ |
| 모바일/데스크톱 반응형 | ✅ 일치 (768px/480px breakpoint) | ✅ |

### 7-4. 권장 개선 순서 (우선순위 로드맵) — 2026-07-31 업데이트

| 순서 | 심각도 | 작업 항목 | 대상 파일 | 상태 |
|------|--------|----------|----------|------|
| 1 | 🔴 | TypeScript 빌드 에러 4건 수정 | `App.tsx`, `geminiApi.ts` | ⏳ 대기 |
| 2 | 🟡 | 입력 날짜 유효성 검증 추가 | `App.tsx` | ⏳ 대기 |
| 3 | 🟡 | 자시(子時) 정책 명확화 | `sajuCalculator.ts`, `App.tsx` | ⏳ 대기 |
| 4 | 🟡 | 미사용 파일 정리 | `counter.ts`, `style.css` | ⏳ 대기 |
| 5 | 🟢 | React useEffect deps 배열 수정 | `App.tsx` | ⏳ 대기 |
| 6 | 🟢 | 모델 재시도 중 사용자 피드백 UI | `App.tsx`, `geminiApi.ts` | ⏳ 대기 |
| ~~7~~ | ~~🟢~~ | ~~Gemini fallback 모델 목록 최신화~~ | ~~`geminiApi.ts`~~ | ✅ 완료 |
| ~~8~~ | ~~🟢~~ | ~~Gemini API 요청 타임아웃 추가~~ | ~~`geminiApi.ts`~~ | ✅ 완료 |
| ~~9~~ | ~~🔴~~ | ~~절기 데이터 이상값(60분/62분) 수정~~ | ~~`solarTerms.ts`~~ | ✅ 완료 |
| ~~10~~ | ~~🔴~~ | ~~EXE 패키징 정리~~ | ~~`package.json`~~ | ✅ 완료 |

---

## 8. 2026-07-24 신규 요구사항: 매콤한 사주 × MBTI 팩폭 진단 & 심층 보고서 기능 고도화

> **상태**: ✅ 구현 완료

사용자 요구사항에 따라 각 해석 항목(성격/커리어/연애/재물)의 흥미도, 전문성, 가독성을 극대화하기 위한 **심층 보고서형 팩폭 진단 시스템** 고도화 계획.

### 8-1. 톤앤매너 & AI 프롬프트 정책 (AI Deep Prompting)
- **방식**: **솔직 매운맛 팩폭 🌶️ + 사주xMBTI 원리 융합 분석**
- **비유기법**: 사주 오행(목/화/토/금/수)과 MBTI 요소가 만나 작용하는 원리를 "용광로", "스포츠카", "스펀지" 등 비전공자도 한눈에 이해할 수 있는 **쉬운 비유**로 풀어서 설명.
- **말투**: 예의 바르면서 반전 있게 뼈를 짚는 **존댓말 매운맛 팩폭 톤** 적용.
- **깊이 & 분량**: 각 항목별 짧은 요약이 아닌, 5~7줄 이상의 **풍부하고 긴 심층 보고서형** 텍스트 제공.

### 8-2. 분야별 심층 분석 구성
- **성격**: 사주 오행 과부족과 MBTI 4가지 지표가 만나 발현되는 독특한 성격 원리 팩폭 분석.
- **일 & 재물**: 직장에서 홧병 나는 이유, 당장 피해야 할 직무, 돈이 새어나가는 멍청한 지출 구멍 분석.
- **연애 & 인간관계**: 찰떡 궁합과 반대로 만나면 피 터지는 상극 유형, 연애할 때 고쳐야 할 팩폭 포인트.
- **풍성한 추가 요소**:
  1. 🔥 사주 × MBTI 뼈 때리는 팩폭 한줄평
  2. 🍀 럭키 아이템 & ⚠️ 피해야 할 상극 유형
  3. 🎯 맞춤형 3가지 현실 실천 처방전 (행동 지침)

### 8-3. UI/UX 레이아웃 (Tab Navigation & Visual System)
- **가독성 탭 UI**: 긴 내용을 답답함 없이 읽을 수 있도록 **[성격] [커리어/재물] [연애/궁합] [종합 처방전] 탭(Tab) 구조**로 개편.
- **비주얼 효과**: 팩폭 및 처방전 카드에 **주황/빨간 불꽃 그라데이션 테두리** 및 **🔥 🌶️ 🎯 🍀** 아이콘 적용.

### 8-4. 북마크(보관함) 완벽 연동
- 심층 분석 결과 전체(럭키 아이템, 상극 유형, 3가지 처방전 포함)를 `localStorage`에 **100% 누락 없이 완전 저장**.

---

## 10. 2026-07-24 신규 요구사항: 사주 4기둥 Interactive AI 심층 해석 & 오행 종합 해설 & 계산 엔진 정밀화

> **상태**: ✅ 핵심 기능 구현 완료 (시·분 정밀 입력 미반영)

사용자와의 질문/답변 인터뷰를 통해 확정된 2차 고도화 사양.

### 10-1. 사주 4기둥(연주/월주/일주/시주) Interactive AI 심층 해석 (클릭 연동)
- ✅ **구현 완료**: 각 기둥 카드 클릭 시 Gemini AI 실시간 심층 해석 생성.
- ✅ 기둥별 AI 해석 메모리 캐싱 구현.
- ✅ 기둥별 AI 심층 해석 결과 북마크 저장 가능.

### 10-2. 오행(목/화/토/금/수) 종합 해설 & 밸런스 보고서
- ✅ **구현 완료**: 오행 비율 차트 + AI 오행 종합 분석 요약 카드 + 상세 보고서.

### 10-3. 사주 계산 엔진 & 입력 UX 정밀화 (보완 과제 반영)
- ⏳ **시·분(HH:mm) 정밀 입력 지원**: 미구현 — 기존 2시간 단위 시진 선택만 제공.
- ✅ **절기 입절 시각 정밀 대조**: 절기 데이터 오류 수정 완료.
- ⚠️ **날짜 입력 유효성 검증**: 부분 구현 — HTML5 min/max만 적용, 2월 30일 등 논리적 검증 미비.

---

## 12. 2026-07-29 신규 요구사항: API 키 내부 삽입, 사주원국 대중 해설, 카톡 공유 및 PDF 다운로드

> **상태**: ✅ 구현 완료

### 12-1. API 키 내부 삽입 & 보안 관리 (지인 배포형 최적화)
- ✅ `.env.production` 환경변수 빌드 타임 내장 완료.
- ✅ 사용자 개별 API 키 입력 및 덮어쓰기 기능 유지.

### 12-2. 전문가가 아닌 사람들을 위한 쉽고 재미있는 사주원국 AI 해설
- ✅ 자연과 사물에 빗댄 시각적 비유 사용 프롬프트 구현.
- ✅ 한자 용어 최소화, 역할 중심 쉬운 한글 풀이 병행.

### 12-3. 보고서형 PDF 파일 다운로드 기능
- ✅ `html2pdf.js` 기반 6페이지 다크 테마 보고서 PDF 구현.

### 12-4. 무료 카카오톡 결과 공유 기능
- ✅ 카카오 개발자 API 연동 완료 (동적 SDK 주입 방어 로직 포함).

---

## 13. 2026-07-30 카카오톡 공유 기능 오류 해결 기록

> **상태**: ✅ 해결 완료 (2026-07-31 추가 버그 수정)

* **이슈**: Vercel 배포 및 도메인 연동 상태에서 공유 버튼 클릭 시 `"카카오톡 SDK가 로드되지 않았습니다."` 에러 발생.
* **원인**: CDN 스크립트 로드 시점과 React의 컴포넌트 마운트 및 클릭 시점 간의 비동기적 타이밍 이슈.
* **조치**: `handleKakaoShare` 실행 시점에 `window.Kakao` 객체가 없는 경우, 동적으로 SDK 스크립트를 생성하여 `head`에 주입하고 비동기로 완벽히 로드될 때까지 `await` 대기한 후 초기화 및 공유를 수행하도록 방어 로직 설계. (타입 에러 검수 완료)

### 2026-07-31 추가 버그 수정 (본 세션)

| # | 문제 | 수정 내용 |
|---|------|----------|
| 1 | `useEffect(() => {` 시작부 누락 → 빌드 에러 TS1128 | `useEffect` 선언 복구 |
| 2 | `aiError` 상태 변수 선언 누락 | `useState<string\|null>(null)` 추가 |
| 3 | `selectedModal` 상태 변수 선언 누락 → TS2304 에러 12건 | `useState<{...}\|null>(null)` 추가 |
| 4 | 카카오 공유 `imageUrl` Unsplash 리다이렉트 URL → 카카오 크롤러 접근 불가 | 안정적인 퍼블릭 이미지 URL로 교체 |
| 5 | SDK 중복 삽입 방지 로직 미비 (index.html에도 로드, 클릭 시 또 삽입) | `querySelector` 체크 후 중복 삽입 방지 |
| 6 | `Pillar.fullText` 존재하지 않는 속성 참조 → TS2339 에러 | `Pillar.text`로 수정 |
| 7 | `localhost` 환경에서 카카오 공유 URL 사용 불가 | `shareUrl` 변수로 배포 URL 폴백 처리 |
| 8 | `Kakao.init()` 실패 시 에러 무시하고 공유 실행 | 초기화 실패 시 `return` 처리 추가 |
| 9 | 공유 실행 중 에러 무시 | `try-catch` 감싸고 `showToast`로 사용자 피드백 |

> [!IMPORTANT]
> **카카오 공유 localhost 한계**: `localhost` 환경에서는 카카오 공유가 동작하지 않습니다. 카카오 개발자 콘솔(developers.kakao.com)에 등록된 배포 도메인(예: Vercel URL)에서만 정상 동작합니다. `src/App.tsx`의 `shareUrl` 변수에 실제 Vercel 배포 URL을 입력해야 합니다.

---

## 14. 2026-07-30 신규 요구사항: PDF 저장 고도화 및 앱 신규 섹션 추가

> **상태**: ✅ 구현 완료

사용자 인터뷰(2026-07-30)를 통해 확정된 4차 고도화 기획 사양.  
PDF 저장 기능의 전면 재설계와 함께, PDF에 담길 신규 콘텐츠를 **앱 화면에도 동일하게 노출**하는 것이 핵심 방향.

---

### 14-1. PDF 출력 방식 전면 전환 — ✅ 완료

| 항목 | 기존 | 변경 후 |
|------|------|--------|
| 출력 방식 | `window.open()` 팝업 → 브라우저 인쇄 다이얼로그 | ✅ `html2pdf.js` → **팝업 없이 즉시 PDF 다운로드** |
| 스타일 | 흑백 인쇄 최적화 CSS | ✅ **앱 다크 테마(우주/글라스모피즘) 색상 그대로 반영** |
| 콘텐츠 | 사주원국 표 + AI 분석 텍스트 | ✅ **신규 섹션 전체 포함 (6페이지)** |
| 레이아웃 | 단일 스크롤 페이지 | ✅ **섹션별 페이지 분리** (P1~P6) |

---

### 14-2. PDF 페이지 구성 (섹션별 1페이지) — ✅ 완료

| 페이지 | 섹션 | 콘텐츠 | 상태 |
|--------|------|--------|------|
| P1 | 📋 표지 / 사주원국 | 이름·생년월일·MBTI 헤더 + 사주 4기둥 표 + 오행 분포 요약 | ✅ |
| P2 | 🃏 사주 MBTI 유형카드 | MBTI 타입 바이오 카드 (아이콘·성격 키워드·코어 특성 비주얼) | ✅ |
| P3 | 🤖 AI 심층 분석 | 성격·커리어·연애·재물 분석 + 팩폭 + 럭키 아이템 | ✅ |
| P4 | 📅 대운 / 세운 표 | 대운 10개(10년 단위) + 세운 10년 테이블 | ✅ |
| P5 | 💑 궁합 조합표 | 일지 기준 삼합·육합·충·형·파·해 관계 표 | ✅ |
| P6 | 🏡 풍수 수리 가이드 | AI가 생성하는 방위·색상·숫자 기반 풍수 가이드 텍스트 | ✅ |

---

### 14-3. 앱 화면 신규 섹션 추가 (PDF와 동일 콘텐츠) — ✅ 완료

#### ① 사주 MBTI 유형카드 (앱 내 표시) — ✅ 완료
- 16 MBTI 유형별 고정 데이터 (아이콘, 부제목, 성격 핵심 문장 3줄).
- 우주 그라데이션 배경 + 글라스모피즘 카드.

#### ② 대운 / 세운 표 (앱 내 표시) — ✅ 완료
- **대운 10개**: 10년 단위 간지 + 시작 나이 + 오행 색상 표시.
- **세운 10~11년**: 현재 기준 ±5년, 연도별 간지 테이블.
- 현재 해당하는 대운/세운 행 **하이라이트** 처리.
- 계산 방식: `calculateDaeun()` 및 `calculateSeun()` 신규 구현 완료.

#### ③ 궁합 조합표 (앱 내 표시) — ✅ 완료
- 본인 일지(日支) 기준 삼합·육합·충·형·파·해 관계 표시.
- 컬러 코딩: 합(초록), 충(빨강), 형(주황), 해(보라) 등.

#### ④ 풍수 수리 가이드 (앱 내 표시) — ✅ 완료
- Gemini AI 실시간 생성 + 캐싱 (재클릭 시 재생성 없이 재사용).
- 결과 화면 하단 카드에 동시 표시.

---

### 14-4. AI 호출 전략 (PDF 저장 시) — ✅ 완료

- PDF 저장 버튼 클릭 → 로딩 오버레이 표시.
- 풍수 수리 가이드 AI 생성 (미생성 시) → html2pdf.js 렌더링 → PDF 다운로드.

---

### 14-5. 구현 범위 및 영향 파일 — ✅ 전체 완료

| 파일 | 작업 내용 | 상태 |
|------|----------|------|
| `src/utils/sajuCalculator.ts` | 대운/세운 산출 로직 신규 구현 | ✅ |
| `src/utils/geminiApi.ts` | 풍수 수리 가이드 프롬프트 신규 추가 | ✅ |
| `src/App.tsx` | 신규 섹션 4종 + PDF 전면 재작성 + 카카오 공유 재정비 | ✅ |
| `src/App.css` | 신규 섹션 스타일 + 모바일 반응형 미디어쿼리 | ✅ |
| `package.json` | `html2pdf.js` 의존성 추가 | ✅ |

---

## 15. 2026-07-31 4차 전면 업데이트 계획 (인터뷰 확정 사양)

> **작성일**: 2026년 7월 31일  
> **상태**: ✅ 핵심 구현 완료 — 잔여 버그 수정 단계  
> **목표**: 모바일/데스크톱 반응형 전환 + 섹션 14 신규 기능 전체 구현

### 15-0. 인터뷰 확정 사항 요약

| 항목 | 확정 내용 |
|------|-----------|
| 업데이트 범위 | 모바일/데스크톱 반응형 전환 + 신규 기능 4종 동시 구현 |
| 사용 환경 | 모바일 / 데스크톱 동등 지원 (모바일 우선 카카오 공유 경유) |
| PDF 기능 현황 | ~~기존 `window.open()` 팝업 방식 존재~~ → ✅ `html2pdf.js`로 전면 교체 완료 |
| 신규 기능 4종 | MBTI 유형카드, 대운/세운 표, 궁합 조합표, 풍수 수리 가이드 — ✅ 모두 구현 완료 |
| 성별 입력 | 기존 앱에 이미 존재 (대운 계산에 활용 중) |
| 이름 입력 | 기존 앱에 이미 존재 (풍수 수리 가이드에 활용 중) |
| 카카오 공유 | ✅ 앱키 연동 완료, 동적 SDK 방어 로직 구현 |

---

### 15-1. 모바일/데스크톱 반응형 전환 (`App.css`) — ✅ 완료

- **breakpoint 기준**: `max-width: 768px` (모바일), `max-width: 480px` (소형 모바일)
- **주요 변경 사항**:
  - `pillar-grid`: 데스크톱 4열 → 모바일 2×2 그리드
  - `element-grid`: 5열 → 반응형 전환
  - `hour-grid`: 3열 → 2열
  - 버튼 크기 모바일 터치 최적화 (min-height: 44px)
  - 탭 네비게이션 스크롤 가능
  - 폰트 크기 모바일 최적화

---

### 15-2. 신규 기능: MBTI 유형카드 — ✅ 완료

- **위치**: 결과 화면 프로필 배너 아래
- **구성**: 16 MBTI 유형별 정적 데이터 (아이콘, 부제목, 성격 핵심 문장 3줄)
- **구현 방식**: 정적 데이터 (AI 호출 불필요)
- **PDF 연동**: PDF P2 페이지 포함 ✅

---

### 15-3. 신규 기능: 대운/세운 표 — ✅ 완료

#### 대운 계산 로직 (`sajuCalculator.ts`) — ✅ 구현 완료
- 양남·음녀: 출생일 → 다음 절기까지 일수 ÷ 3 = 대운 진입 나이 (순행)
- 음남·양녀: 출생일 → 이전 절기까지 일수 ÷ 3 = 대운 진입 나이 (역행)
- 월주 기준 간지를 10개 대운으로 순행/역행 전개
- `solarTerms.ts` 절기 데이터 활용

#### 세운 계산 로직 — ✅ 구현 완료
- 현재 연도 기준 ±5년 (총 10~11개 연도)
- 각 연도의 연주 간지 계산

#### UI 구성 (`App.tsx`) — ✅ 구현 완료
- 대운 테이블: 10개 행 (간지, 시작 나이, 오행 색상 배지)
- 세운 테이블: 연도별 간지
- 현재 해당 대운/세운 행 하이라이트
- PDF P4 페이지 포함 ✅

---

### 15-4. 신규 기능: 궁합 조합표 — ✅ 완료

- **기준**: 본인 일지(日支) 기준 고정 명리학 규칙 (상대방 입력 불필요)
- **표시 관계**: 삼합(三合), 육합(六合), 충(沖), 형(刑), 파(破), 해(害)
- **컬러 코딩**: 합(초록), 충(빨강), 형(주황), 해(보라) 등
- **구현 방식**: 정적 룩업 테이블
- **PDF 연동**: PDF P5 페이지 포함 ✅

---

### 15-5. 신규 기능: 풍수 수리 가이드 — ✅ 완료

- **트리거**: PDF 저장 버튼 클릭 또는 "실시간 생성" 버튼
- **AI 생성**: Gemini AI 전용 프롬프트 (`generateFengShuiInterpretation()`)
- **캐싱**: 한 번 생성된 풍수 가이드는 재생성 없이 재사용
- **PDF 연동**: PDF P6 페이지 포함 ✅

---

### 15-6. PDF 전면 재설계 — ✅ 완료

| 항목 | 변경 내용 | 상태 |
|------|-----------|------|
| 라이브러리 | `html2pdf.js` (npm 설치 완료) | ✅ |
| 스타일 | 앱 다크 테마 색상 그대로 캡처 | ✅ |
| 레이아웃 | 섹션별 페이지 분리 (P1~P6) | ✅ |
| 트리거 | PDF 버튼 → 로딩 → 풍수 AI 생성 → PDF 다운로드 | ✅ |

---

### 15-7. 카카오톡 공유 재구현 — ✅ 완료

- **SDK 방어 로직**: 동적 스크립트 주입 + `await` 완전 로드 대기
- **초기화**: `Kakao.isInitialized()` 체크 후 중복 초기화 방지
- **공유 템플릿**: feed 형식, 앱키 연동 완료

---

### 15-8. 영향 파일 및 작업 범위 — ✅ 전체 완료

| 파일 | 작업 내용 | 상태 |
|------|-----------|------|
| `package.json` | `html2pdf.js` 의존성 추가 | ✅ |
| `src/utils/sajuCalculator.ts` | 대운/세운 산출 로직 신규 구현, `SajuResult` 타입 확장 | ✅ |
| `src/utils/geminiApi.ts` | 풍수 수리 가이드 프롬프트 함수 신규 추가 + 타임아웃 | ✅ |
| `src/App.tsx` | MBTI 카드, 대운/세운 표, 궁합표, 풍수카드 + PDF + 카카오 | ✅ |
| `src/App.css` | 신규 섹션 스타일 + 모바일 반응형 미디어쿼리 | ✅ |

---

### 15-9. 구현 진행 상황 — 최종 업데이트 (2026-07-31)

| 순서 | 작업 | 상태 |
|------|------|------|
| 1 | 계획안.md 업데이트 | ✅ 완료 (2026-07-31 최종 갱신) |
| 2 | `html2pdf.js` 설치 | ✅ 완료 |
| 3 | `sajuCalculator.ts` 대운/세운 로직 구현 | ✅ 완료 |
| 4 | `geminiApi.ts` 풍수 수리 프롬프트 추가 + 타임아웃 | ✅ 완료 |
| 5 | `App.tsx` 신규 섹션 4종 구현 | ✅ 완료 |
| 6 | `App.tsx` PDF 전면 재작성 | ✅ 완료 |
| 7 | `App.css` 모바일 반응형 + 신규 스타일 | ✅ 완료 |
| 8 | TypeScript 타입 검증 및 빌드 확인 | 🔴 에러 4건 잔존 (섹션 16 참조) |

---

## 16. 잔여 이슈 및 향후 과제 (2026-07-31 기준)

> [!WARNING]
> 아래 항목들은 핵심 기능 구현이 완료된 후 폴리싱 단계에서 처리해야 할 사항입니다.

### 🔴 즉시 수정 필요 — TypeScript 빌드 에러 4건

| # | 파일 | 라인 | 에러 | 설명 |
|---|------|------|------|------|
| 1 | `App.tsx` | 183 | TS2352 | `ElementCounts` → `Record<string, number>` 타입 변환. `as unknown as Record<string, number>` 또는 인터페이스에 인덱스 시그니처 추가 필요. |
| 2 | `App.tsx` | 532 | TS2345 | `html2pdf.js` jsPDF 옵션의 `orientation` 타입 불일치. `orientation: 'portrait' as const` 로 수정 필요. |
| 3 | `App.tsx` | 1237 | TS2352 | #1과 동일 패턴. |
| 4 | `geminiApi.ts` | 345 | TS6133 | `_lastError` 미사용 변수. 변수 제거 또는 `_` prefix 유지하고 `// @ts-ignore` 처리. |

### 🟡 기능 보완 과제

| # | 과제 | 설명 |
|---|------|------|
| 1 | **시·분(HH:mm) 정밀 입력** | 현재 12시진 선택만 가능. 절기 경계 정밀 판정을 위해 시·분 직접 입력 UI 추가 필요. |
| 2 | **날짜 유효성 검증** | 2월 30일, 4월 31일 등 존재하지 않는 날짜 차단 로직 미비. 1940~2030 범위 밖 경고도 필요. |
| 3 | **자시(子時) 정책** | 야자시(23:00~24:00) / 조자시(00:00~01:00) 구분 불가. 일주·시주 2기둥이 달라질 수 있음. |
| 4 | **미사용 파일 정리** | `src/counter.ts`, `src/style.css` 삭제 필요. |

### 🟢 개선 권장 사항

| # | 과제 | 설명 |
|---|------|------|
| 1 | **React useEffect deps** | `step`만 의존성에 포함. `formData`, `apiKey` 캡처로 stale closure 가능성. |
| 2 | **재시도 중 사용자 피드백 UI** | 4모델 × 3회 = 최대 12번 재시도 시 상태 표시 없음. "재시도 중..." 텍스트 추가 권장. |
| 3 | **궁합 조합표 상세화** | 현재 텍스트 가이드 중심. 12지신별 구체적 상대 동물 매칭 데이터 보강 가능. |
| 4 | **API 키 보안 강화** | Electron 환경에서 `safeStorage` API 또는 `keytar` 모듈로 암호화 저장 권장. |

---

## 부록: 전체 프로젝트 파일 구조

```
사주해석모바일버전/
├── .env                      # 환경변수 (API 키)
├── .gitignore
├── index.html                # Vite 진입점
├── package.json              # 의존성 (html2pdf.js, react, vite, electron 등)
├── tsconfig.json
├── vite.config.ts
├── 계획안.md                  # 본 문서
├── public/                   # 정적 자산
├── dist/                     # Vite 프로덕션 빌드 산출물
├── src/
│   ├── App.tsx               # 메인 앱 (UI + 로직 + PDF + 카카오 공유)
│   ├── App.css               # 전체 스타일 (다크테마 + 모바일 반응형)
│   ├── main.tsx              # React 진입점
│   ├── vite-env.d.ts         # Vite 환경 타입
│   ├── counter.ts            # ⚠️ 미사용 (Vite 템플릿 잔여)
│   ├── style.css             # ⚠️ 미사용 (Vite 템플릿 잔여)
│   ├── data/
│   │   └── solarTerms.ts     # 1940~2030 24절기 정밀 데이터
│   └── utils/
│       ├── sajuCalculator.ts # 사주 계산 엔진 (4기둥 + 대운 + 세운)
│       └── geminiApi.ts      # Gemini AI API (4단계 폴백 + 풍수 프롬프트)
```
